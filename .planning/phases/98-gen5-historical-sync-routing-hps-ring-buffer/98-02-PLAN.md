---
phase: 98-gen5-historical-sync-routing-hps-ring-buffer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/CoreBluetoothBLETransport+Parsing.swift
autonomous: true
requirements:
  - SYNC-10
must_haves:
  truths:
    - "ring_capacity, current_page, and read_pointer are extracted from GET_DATA_RANGE response body when ring buffer fields are present"
    - "ring_wrapped = (current_page < read_pointer) correctly detects ring wrap-around"
    - "pages_behind_corrected uses ring-aware formula when fields present; falls back to existing pagesBehind when absent"
    - "historical_sync.get_data_range.ring log event fires when ring fields are present (ring_capacity_present == true)"
    - "When ring fields are absent, existing historical_sync.command.response and historical_sync.range.rejected log events still fire unchanged"
    - "GitHub issue #160 is closed"
  artifacts:
    - path: "GooseSwift/CoreBluetoothBLETransport+Parsing.swift"
      provides: "Extended historicalRangePageState with optional ring buffer fields and emitHistoricalRangeTelemetry with ring log"
      contains: "ring_capacity_present"
  key_links:
    - from: "CoreBluetoothBLETransport+Parsing.swift:historicalRangePageState(fromRangeBody:)"
      to: "CoreBluetoothBLETransport+Parsing.swift:emitHistoricalRangeTelemetry"
      via: "pageState returned from historicalRangePageState used in emitHistoricalRangeTelemetry to emit ring log"
      pattern: "emitHistoricalRangeTelemetry"
---

<objective>
SYNC-10: Extend the Swift GET_DATA_RANGE response parser to extract ring buffer fields (ring_capacity, current_page, read_pointer) from higher-offset words in the response body, apply the D-06 wrap-around formula, and log the result via ble.record() with title "historical_sync.get_data_range.ring". All parsing lives in Swift (CoreBluetoothBLETransport+Parsing.swift) — the CONTEXT.md D-05 Rust reference does not match the actual architecture; GET_DATA_RANGE response parsing has always been entirely in Swift.

Purpose: Correct pages_behind computation for Gen5 devices where the ring buffer has wrapped. Without ring awareness, pages_behind is underestimated, sync may terminate early thinking it is caught up when it has actually missed a full ring cycle worth of data.

Output: Updated CoreBluetoothBLETransport+Parsing.swift with ring buffer field extraction, wrap detection, and log event; issue #160 closed.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GooseSwift/CoreBluetoothBLETransport+Parsing.swift
@GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend historicalRangePageState to parse ring buffer fields with ring_capacity_present guard</name>
  <files>GooseSwift/CoreBluetoothBLETransport+Parsing.swift</files>
  <action>
Read CoreBluetoothBLETransport+Parsing.swift lines 657–681 to understand the current parse. The existing `historicalRangePageState(fromRangeBody:)` static function reads 6 UInt32 LE words starting at body offset 1 (words[0..5]), requiring body.count >= 25. It extracts pageCurrent=words[2], pageOldest=words[3], pageEnd=words[5] and constructs a HistoricalRangePageState.

Also read the HistoricalRangePageState struct definition (search for "struct HistoricalRangePageState" in CoreBluetoothBLETransport.swift or Parsing.swift) to understand its current fields before extending it.

Extend as follows:

1. Add three optional fields to HistoricalRangePageState (or a companion struct — match the existing pattern for the struct definition):
   - `ringCapacity: UInt32?`
   - `ringCurrentPage: UInt32?`
   - `ringReadPointer: UInt32?`

   Add a computed Bool property:
   - `ringCapacityPresent: Bool { ringCapacity != nil }`

2. Extend `historicalRangePageState(fromRangeBody:)` to attempt to read additional words beyond the existing 6 (words[6..8] at body offsets 25, 29, 33 — i.e., body.count >= 37). These are the candidate offsets for ring buffer fields based on the established pattern (words increment by 4 bytes from offset 1). If body.count >= 37, read words[6], words[7], words[8] and assign:
   - `ringCapacity = words[6]`
   - `ringCurrentPage = words[7]`
   - `ringReadPointer = words[8]`

   If body.count < 37, leave all three as nil (fallback per D-08).

   IMPORTANT: The exact byte offsets of ring_capacity/current_page/read_pointer in firmware responses are not confirmed from static analysis alone (noted as "Claude's Discretion" in CONTEXT.md). The indices words[6]/[7]/[8] are the best-effort assumption from the established word-stride pattern. The ring_capacity_present guard (D-08) means an incorrect parse will simply produce unreliable ring log output but will NOT regress the existing pageCurrent/pageOldest/pageEnd parse path. If ring fields prove incorrect at runtime, fixing the offsets is a follow-up with zero risk to existing functionality.

3. Add a computed property `pagesBehindCorrected: Int64` that applies the D-06 ring-aware formula when ring fields are present:
   ```
   // When ring fields are present:
   //   ring_wrapped = ringCurrentPage < ringReadPointer
   //   pages_behind_corrected = ring_wrapped
   //     ? (ringCapacity - ringReadPointer) + ringCurrentPage
   //     : ringCurrentPage - ringReadPointer
   // When absent, fall back to existing pagesBehind (uses pageCurrent/pageOldest/pageEnd)
   ```

   Implement this as a computed property on HistoricalRangePageState named `pagesBehindCorrected: Int64` that returns the ring-corrected value if ringCapacity != nil, otherwise returns `pagesBehind` (the existing computed property).

Do not alter the existing `pagesBehind` computed property (lines 502–506 of CoreBluetoothBLETransport.swift) — it must remain unchanged for backward compatibility. `pagesBehindCorrected` is additive.

Code style: 2-space indentation, opening braces same line, match the Swift style of the surrounding file. No inline fenced code in the file.
  </action>
  <verify>
    <automated>grep -n "ringCapacityPresent\|ringCapacity\|ringCurrentPage\|ringReadPointer\|pagesBehindCorrected" GooseSwift/CoreBluetoothBLETransport+Parsing.swift</automated>
  </verify>
  <done>grep finds ringCapacityPresent, ringCapacity, ringCurrentPage, ringReadPointer, and pagesBehindCorrected defined in Parsing.swift; existing pagesBehind property in CoreBluetoothBLETransport.swift is untouched</done>
</task>

<task type="auto">
  <name>Task 2: Emit ring log in emitHistoricalRangeTelemetry + build clean + close #160</name>
  <files>GooseSwift/CoreBluetoothBLETransport+Parsing.swift</files>
  <action>
Read `emitHistoricalRangeTelemetry` in CoreBluetoothBLETransport+Parsing.swift (lines 715–777) to find the right insertion point for the ring log event. The function calls `Self.historicalRangePageState(fromRangeBody: body)` at line 734 and extracts pageCurrent/pageOldest/pageEnd/pagesBehind from the result. Add the ring log immediately after the existing `historicalSyncPagesTotal` seeding block (after line 755) and before the `onHistoricalRangeTelemetry?()` call (line 757).

Add the ring log block as follows (adapt to actual pageState variable name found in the function):

```
// Only emit ring buffer log when response includes ring fields (Gen5 firmware with
// HPS ring capacity reporting). Falls back silently when fields are absent (D-08).
if let pageState, pageState.ringCapacityPresent,
   let ringCapacity = pageState.ringCapacity,
   let ringCurrentPage = pageState.ringCurrentPage,
   let ringReadPointer = pageState.ringReadPointer {
  let ringWrapped = ringCurrentPage < ringReadPointer
  let pagesBehindRaw: Int64 = pageState.pagesBehind
  let pagesBehindCorrected: Int64 = pageState.pagesBehindCorrected
  record(
    source: "ble.sync",
    title: "historical_sync.get_data_range.ring",
    body: "ring_capacity=\(ringCapacity) current_page=\(ringCurrentPage) read_pointer=\(ringReadPointer) ring_wrapped=\(ringWrapped) pages_behind_raw=\(pagesBehindRaw) pages_behind_corrected=\(pagesBehindCorrected)"
  )
}
```

IMPORTANT: The `record()` call uses the default level (info) — do not specify level: .debug; the D-07 log format uses info level. The source is "ble.sync" consistent with all other historical sync log events in the file.

After adding the ring log, confirm that the existing log events still use their original titles unchanged:
- `historical_sync.command.response` (info, line ~556 of HistoricalHandlers.swift)
- `historical_sync.range.rejected` (warn, line ~512)
- `historical_sync.range.invalid_body` (warn, line ~537)

These are in HistoricalHandlers.swift, NOT in Parsing.swift — do not modify them.

Then build for iOS simulator to confirm no new warnings or errors. Use XcodeBuildMCP or xcodebuild:
  `xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build`

The build must complete with BUILD SUCCEEDED and 0 new warnings.

Then confirm existing GET_DATA_RANGE log events still fire by grepping that their titles are still present and unchanged:
  `grep -n "historical_sync.command.response\|historical_sync.range.rejected\|historical_sync.range.invalid_body" GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift`

Then close GitHub issue #160:
1. `gh issue comment 160 --body "Ring buffer fields (ring_capacity, current_page, read_pointer) are now parsed from GET_DATA_RANGE response body in historicalRangePageState(fromRangeBody:) in CoreBluetoothBLETransport+Parsing.swift. Wrap-around detection uses ring_wrapped = current_page < read_pointer. Results are logged via ble.record() with title historical_sync.get_data_range.ring. When ring fields are absent from the response (older firmware), the parser falls back to the existing pagesBehind calculation — no regression."`
2. `gh issue close 160 --reason completed`
  </action>
  <verify>
    <automated>grep -n "historical_sync.get_data_range.ring" GooseSwift/CoreBluetoothBLETransport+Parsing.swift</automated>
  </verify>
  <done>grep finds the ring log title in Parsing.swift; iOS build passes with BUILD SUCCEEDED; gh issue view 160 shows CLOSED; grep confirms the three pre-existing GET_DATA_RANGE log titles still present in HistoricalHandlers.swift</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE response bytes → Swift parse | Raw bytes from WHOOP GET_DATA_RANGE response are read as UInt32 LE words |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-10 | Tampering | Ring buffer byte parse | accept | Reads are bounds-checked (body.count >= 37 guard before accessing words[6..8]); out-of-range reads produce nil fields, triggering fallback path — no crash or incorrect routing |
| T-98-11 | Information Disclosure | Ring log via ble.record() | accept | Log contains only page count integers from WHOOP firmware — no PII, no user-identifiable data beyond device sync state |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — Swift-only code edit |
</threat_model>

<verification>
After both tasks complete:
1. `grep -n "ringCapacityPresent\|pagesBehindCorrected" GooseSwift/CoreBluetoothBLETransport+Parsing.swift` returns matches
2. `grep -n "historical_sync.get_data_range.ring" GooseSwift/CoreBluetoothBLETransport+Parsing.swift` returns exactly one match
3. `grep -n "historical_sync.command.response\|historical_sync.range.rejected" GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift` — both titles still present (unchanged)
4. iOS simulator build: BUILD SUCCEEDED, 0 new warnings
5. `gh issue view 160 --json state -q .state` returns CLOSED
</verification>

<success_criteria>
1. historicalRangePageState(fromRangeBody:) attempts to read words[6..8] at body offsets 25/29/33 when body.count >= 37; sets ringCapacity/ringCurrentPage/ringReadPointer optionals
2. pagesBehindCorrected uses ring-aware D-06 formula when ringCapacity is non-nil; falls back to pagesBehind otherwise
3. emitHistoricalRangeTelemetry emits historical_sync.get_data_range.ring log event when ring fields present; silent when absent
4. iOS simulator build passes clean (0 errors, 0 new warnings)
5. Existing GET_DATA_RANGE log event titles (historical_sync.command.response, historical_sync.range.rejected, historical_sync.range.invalid_body) unchanged
6. GitHub issue #160 closed with explanation comment
</success_criteria>

<output>
Create `.planning/phases/98-gen5-historical-sync-routing-hps-ring-buffer/98-02-SUMMARY.md` when done.
</output>
