---
phase: 98-gen5-historical-sync-routing-hps-ring-buffer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift
autonomous: true
requirements:
  - SYNC-08
must_haves:
  truths:
    - "historicalData (type 47) and historicalIMUDataStream (type 52) are routed to handleHistoricalSyncValue when isHistoricalSyncing is true"
    - "historicalPacketsReceivedThisSync increments during an active sync when packet types 47/52 arrive"
    - "SAFETY threading comment is present at the isHistoricalSyncing read inside shouldDispatchNotificationSideEffectsToMain"
    - "Packets with types 47/52 during live capture (isHistoricalSyncing == false) continue to fall through to the skip-counter path — no performance regression"
    - "GitHub issue #24 is closed"
  artifacts:
    - path: "GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift"
      provides: "shouldDispatchNotificationSideEffectsToMain with historicalData/IMUStream guard and SAFETY comment"
      contains: "SAFETY: isHistoricalSyncing"
  key_links:
    - from: "CoreBluetoothBLETransport+PeripheralDelegate.swift:shouldDispatchNotificationSideEffectsToMain"
      to: "CoreBluetoothBLETransport+HistoricalHandlers.swift:handleHistoricalSyncValue"
      via: "DispatchQueue.main.async → handlePeripheralValueUpdate → handleHistoricalSyncValue"
      pattern: "handleHistoricalSyncValue"
---

<objective>
SYNC-08: The dispatch guard that routes historicalData (V5PacketType 47) and historicalIMUDataStream (V5PacketType 52) BLE notifications to the main sync handler when isHistoricalSyncing is true is already committed to the codebase at CoreBluetoothBLETransport+PeripheralDelegate.swift lines 163–173. This plan verifies the guard is correct per D-01/D-04, adds the mandatory SAFETY threading comment per D-03 (currently absent — prose comment exists but no // SAFETY: prefix), traces the full dispatch chain to confirm historicalPacketsReceivedThisSync increments, builds clean, and closes GitHub issue #24.

Purpose: Without this guard, historical body packets during Gen5 sync are dropped (stay off-main), historicalPacketsReceivedThisSync never increments, the idle timer fires, and sync fails with "no packet47 bodies." The fix is already in place; this plan confirms correctness and closes the issue.

Output: Updated CoreBluetoothBLETransport+PeripheralDelegate.swift with // SAFETY comment; issue #24 closed.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift
@GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
@GooseSwift/GooseBLEHistoricalManager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SAFETY threading comment to isHistoricalSyncing read (per D-03)</name>
  <files>GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift</files>
  <action>
Read CoreBluetoothBLETransport+PeripheralDelegate.swift lines 163–175 to confirm the current state of the guard. You will find the case arm for V5PacketType.historicalData and V5PacketType.historicalIMUDataStream already present with a prose comment block (lines 165–169) and the bare `if isHistoricalSyncing { return true }` check (line 170). The prose comment explains the guard purpose but is missing a // SAFETY: threading note per D-03.

Add the SAFETY comment immediately above the `if isHistoricalSyncing {` line, inside the case arm, after the existing prose comment block. The exact text to add (one line):

    // SAFETY: isHistoricalSyncing set+read on same CB notification queue — no lock needed.

Placement: immediately before `if isHistoricalSyncing {` at line 170. Do not alter any other text in the function; do not remove the existing prose comment (lines 165–169) — the SAFETY comment supplements it.

After editing, verify the guard arm reads (in order):
1. The case labels: `case V5PacketType.historicalData, V5PacketType.historicalIMUDataStream:`
2. The existing prose comment block explaining the guard
3. The new `// SAFETY: isHistoricalSyncing set+read on same CB notification queue — no lock needed.` line
4. `if isHistoricalSyncing { return true }`
5. `continue`

Note on `isHistoricalSyncing` resolution: the function uses the bare computed property `isHistoricalSyncing` which is defined in CoreBluetoothBLETransport.swift as `var isHistoricalSyncing: Bool { historicalManager.isHistoricalSyncing }` — this is correct and consistent with the rest of the file. Do NOT change it to `historicalManager.isHistoricalSyncing`.

Note on threading: the setter for isHistoricalSyncing runs on main thread (via DispatchQueue.main.async in the historical command handling path). The reader runs on the CoreBluetooth notification queue. This is a benign race — identical to other isHistoricalSyncing reads in the CB queue path — and the SAFETY comment is the only mitigation per D-03.
  </action>
  <verify>
    <automated>grep -n "SAFETY: isHistoricalSyncing" GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift</automated>
  </verify>
  <done>grep finds exactly one line containing "SAFETY: isHistoricalSyncing set+read on same CB notification queue" in PeripheralDelegate.swift</done>
</task>

<task type="auto">
  <name>Task 2: Trace dispatch chain + build iOS simulator to confirm correctness</name>
  <files>GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift</files>
  <action>
Confirm the full dispatch chain is intact by reading the relevant sections. Do not modify any code in this task — it is verification only.

Dispatch chain to verify (all in PeripheralDelegate.swift unless noted):
1. `peripheral(_:didUpdateValueFor:)` at line 66 — entry point
2. Guard `shouldFanOutNotificationBeforeMain(characteristic)` — returns true for notify characteristics
3. Guard `shouldDispatchNotificationSideEffectsToMain(value, characteristic:)` — the gate we modified; returns true when packet types 47/52 arrive and isHistoricalSyncing is true
4. `DispatchQueue.main.async { self?.handlePeripheralValueUpdate(value, characteristic:, fanOutNotifications: false) }`
5. In `handlePeripheralValueUpdate` (PeripheralDelegate.swift line ~288): `handleHistoricalSyncValue(value, characteristic:)` is called
6. In `handleHistoricalSyncValue` (HistoricalHandlers.swift line 7): guard `isHistoricalSyncing else { return }` — second gate
7. `handleHistoricalSyncFrame(frame, characteristic:)` → `case V5PacketType.historicalData, V5PacketType.historicalIMUDataStream:` → `historicalManager.historicalPacketsReceivedThisSync &+= 1` at line 26

Confirm: `notificationSideEffectSkipCount` is only incremented inside `recordSkippedNotificationSideEffect` (line ~186), which is only called when `shouldDispatchNotificationSideEffectsToMain` returns false. For packet types 47/52 when isHistoricalSyncing is true, the gate returns true early — skip counter is never incremented. This confirms D-02 (gate inserted before skip-counter path).

Confirm: Gen4 safety — the `handleHistoricalSyncFrame` switch uses `V5PacketType` named constants. Gen4 packets arriving on the Gen4 characteristic (61080005) carry different raw values; even if they pass the dispatch gate (which is characteristic-agnostic for notify chars), they will fall through to `default: break` in `handleHistoricalSyncFrame`. No behavioral regression for Gen4.

Then build for iOS simulator to confirm no new warnings. Use XcodeBuildMCP or the Xcode build system — build the GooseSwift scheme for an iPhone simulator (arm64 simulator or x86_64 simulator). The build must complete with 0 errors and 0 new warnings compared to baseline.
  </action>
  <verify>
    <automated>xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>iOS simulator build completes with BUILD SUCCEEDED; grep confirms the dispatch chain reads (shouldDispatchNotificationSideEffectsToMain → handlePeripheralValueUpdate → handleHistoricalSyncValue → historicalPacketsReceivedThisSync &+= 1) are all present in source; 0 new warnings introduced</done>
</task>

<task type="auto">
  <name>Task 3: Close GitHub issue #24 with fix reference</name>
  <files></files>
  <action>
Close GitHub issue #24 with a comment that links the commit where the SYNC-08 guard was applied. Use the gh CLI.

Steps:
1. Get the short hash of the commit that introduced the historicalData/historicalIMUDataStream dispatch gate in CoreBluetoothBLETransport+PeripheralDelegate.swift: `git log --oneline --follow GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift | head -5` — identify the relevant commit.
2. Post a comment on issue #24 explaining the fix: the dispatch gate `shouldDispatchNotificationSideEffectsToMain` now returns true for packet types 47 (historicalData) and 52 (historicalIMUDataStream) when `isHistoricalSyncing == true`, routing them to `handleHistoricalSyncValue` on main so `historicalPacketsReceivedThisSync` increments correctly. Reference the commit hash. Mention that a SAFETY threading comment has been added per D-03.
3. Close the issue: `gh issue close 24 --reason completed`

Comment body (adapt to actual commit hash found in step 1):

  Fixed in CoreBluetoothBLETransport+PeripheralDelegate.swift.
  The dispatch gate `shouldDispatchNotificationSideEffectsToMain` now routes packet types 47 (historicalData) and 52 (historicalIMUDataStream) to the main handler when `isHistoricalSyncing == true`. This ensures `historicalPacketsReceivedThisSync` increments during active Gen5 sync, preventing premature idle-timer firing and the "no packet47 bodies" failure mode.
  A `// SAFETY:` threading comment has been added at the `isHistoricalSyncing` read site to document the queue-affinity invariant.

Do not include RE references, Ghidra, or APK analysis in the comment per project conventions.
  </action>
  <verify>
    <automated>gh issue view 24 --json state,stateReason -q '.state + " " + .stateReason'</automated>
  </verify>
  <done>gh issue view 24 shows state=CLOSED and stateReason=COMPLETED</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE notification queue → main thread | Raw BLE bytes from WHOOP device cross into main-thread processing via DispatchQueue.main.async |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Spoofing | isHistoricalSyncing flag | accept | Flag is set by sync start/stop on main thread, read on CB queue — benign race documented by SAFETY comment; no external party can forge the flag |
| T-98-02 | Tampering | BLE packet routing gate | accept | Gate is guarded by packet type constants (V5PacketType enum); raw UInt8 values not from the enum fall through to `default: continue` |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — Swift-only code edit |
</threat_model>

<verification>
After both tasks complete:
1. `grep -n "SAFETY: isHistoricalSyncing" GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift` returns exactly one match
2. iOS simulator build succeeds with 0 errors
3. `gh issue view 24 --json state -q .state` returns `CLOSED`
4. `grep -n "historicalPacketsReceivedThisSync" GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift` confirms the increment at line 26 is reachable via the dispatch chain
</verification>

<success_criteria>
1. SAFETY comment present at isHistoricalSyncing read site in shouldDispatchNotificationSideEffectsToMain
2. Full dispatch chain confirmed: shouldDispatchNotificationSideEffectsToMain → handlePeripheralValueUpdate → handleHistoricalSyncValue → historicalPacketsReceivedThisSync &+= 1
3. iOS simulator build passes clean (0 errors, 0 new warnings)
4. GitHub issue #24 closed with fix reference comment
</success_criteria>

<output>
Create `.planning/phases/98-gen5-historical-sync-routing-hps-ring-buffer/98-01-SUMMARY.md` when done.
</output>
