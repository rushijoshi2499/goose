---
phase: "05"
plan: "05-01"
title: "Configure Upstream Remote + Integrate Low-Risk PRs (#3, #6)"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - ".github/workflows/"
  - "Rust/core/src/bridge.rs"
requirements:
  - FORK-01
  - FORK-03
  - FORK-06
---

# Plan 05-01: Configure Upstream Remote + Integrate Low-Risk PRs (#3, #6)

<objective>
Configure the upstream remote `b-nnett/goose` in the fork, then integrate the two lowest-risk upstream PRs: PR #3 (FFI safety contract documentation — docs only, zero behaviour change) and PR #6 (Rust core CI GitHub Actions workflow — CI config only, no source changes). After each merge, verify that fork-specific files (`server/`, `GooseSwift/GooseAppModel+Upload.swift`, `GooseSwift/MoreRemoteServerViews.swift`) are untouched.
</objective>

<context>
**Decisions from CONTEXT.md:**
- D-01: Merge order step 1: PR #3, step 2: PR #6 (lowest risk first)
- D-02: `git merge --no-ff` for every PR (not cherry-pick)
- D-03: Remote `upstream` (b-nnett/goose) configured before any merge (FORK-01)
- D-04: `git fetch upstream` + merge via `git merge upstream/pr-branch --no-ff` or `gh pr checkout` + merge
- D-05: `cargo test` after any PR touching Rust/
- D-06: After each merge, verify fork-specific files were not altered
- D-09: Resolve conflicts inline, priority: preserve fork-specific functionality

**PR details:**
- PR #3: "Document FFI safety contracts for bridge entry points" — Rust doc comments only, no logic change
- PR #6: "Add Rust core CI GitHub Actions workflow" — `.github/workflows/` new file only

**User requirement (from CONTEXT.md specifics):** Explain what each PR does before merging.
</context>

<tasks>

## Task 1: Configure Upstream Remote (FORK-01)

<task id="1">
<type>execute</type>
<title>Add upstream remote and fetch branches</title>

<read_first>
- Run `git remote -v` to see current remotes before adding
- `.planning/phases/05-upstream-pr-integration/05-CONTEXT.md` — D-03 (FORK-01 requirement)
</read_first>

<action>
1. Add upstream remote: `git remote add upstream https://github.com/b-nnett/goose`
   (If already exists: `git remote set-url upstream https://github.com/b-nnett/goose`)
2. Fetch upstream branches and PR refs: `git fetch upstream` (D-04)
3. Confirm connectivity: `git branch -r | grep upstream`
</action>

<acceptance_criteria>
- `git remote -v` shows `upstream  https://github.com/b-nnett/goose (fetch)` and `(push)`
- `git fetch upstream` completes without error
- `git branch -r | grep upstream` lists at least one upstream branch
</acceptance_criteria>
</task>

## Task 2: Review and Merge PR #3 (FFI safety contract docs)

<task id="2">
<type>execute</type>
<title>Explain PR #3 changes, then merge via --no-ff</title>

<read_first>
- `Rust/core/src/bridge.rs` — current FFI bridge before reviewing PR #3 diff
- `.planning/phases/05-upstream-pr-integration/05-CONTEXT.md` — D-02 (--no-ff), D-06 (protect fork files)
- `.github/workflows/` — list existing workflows for conflict detection
</read_first>

<action>
1. Fetch PR branch: `git fetch upstream pull/3/head:upstream-pr-3`
   (Fallback if gh CLI authenticated: `gh pr checkout 3 --repo b-nnett/goose -b upstream-pr-3`)
2. Show and explain the diff: `git diff main...upstream-pr-3` — present to user:
   - What PR #3 adds: doc comments (/// or //!) on bridge entry points explaining memory ownership, threading safety contracts, and FFI invariants
   - Confirm zero logic changes (no new `fn`, `impl`, `use` blocks; no Swift or server changes)
3. Merge: `git checkout main && git merge --no-ff upstream-pr-3 -m "merge: upstream PR #3 — Document FFI safety contracts for bridge entry points"`
4. Resolve conflicts if any (unlikely for docs-only PR): keep both upstream docs + fork's existing comments
5. Verify fork files unchanged: `git diff HEAD~1 -- GooseSwift/ server/ .planning/`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows merge commit containing "PR #3"
- `git show HEAD --stat` shows only `Rust/core/src/` `.rs` files (doc-only changes)
- `git diff HEAD~1 -- GooseSwift/` is empty
- `git diff HEAD~1 -- server/` is empty
- `git diff HEAD~1 -- .planning/` is empty
</acceptance_criteria>
</task>

## Task 3: Review and Merge PR #6 (Rust CI GitHub Actions)

<task id="3">
<type>execute</type>
<title>Explain PR #6 changes, then merge via --no-ff</title>

<read_first>
- `.github/workflows/` — list existing workflows to detect naming collision with PR #6
- `.planning/phases/05-upstream-pr-integration/05-CONTEXT.md` — D-02 (--no-ff), D-09 (conflict resolution)
</read_first>

<action>
1. Fetch PR branch: `git fetch upstream pull/6/head:upstream-pr-6`
2. Show and explain the diff: `git diff main...upstream-pr-6` — present to user:
   - What PR #6 adds: a GitHub Actions workflow file under `.github/workflows/` that runs `cargo test` on push/PR
   - Confirm no Swift source or server files are touched
   - Check for naming collision with any existing fork workflows
3. If naming collision: note the conflict (resolve by keeping both — rename PR #6's file if needed, e.g., `rust-upstream-ci.yml`)
4. Merge: `git checkout main && git merge --no-ff upstream-pr-6 -m "merge: upstream PR #6 — Add Rust core CI GitHub Actions workflow"`
5. Verify fork files unchanged: `git diff HEAD~1 -- GooseSwift/ server/ Rust/core/src/`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows merge commit containing "PR #6"
- `git show HEAD --stat` shows only `.github/workflows/` files changed
- `git diff HEAD~1 -- GooseSwift/` is empty (no SwiftUI changes)
- `git diff HEAD~1 -- server/` is empty
- `git diff HEAD~1 -- Rust/core/src/` is empty (CI config only — no Rust source changes)
- No existing fork workflow was overwritten
</acceptance_criteria>
</task>

## Task 4: Run cargo test and verify Wave 1 state

<task id="4">
<type>execute</type>
<title>Verify tests pass and all Wave 1 requirements satisfied</title>

<read_first>
- `Rust/core/Cargo.toml` — verify test targets exist
- `.planning/REQUIREMENTS.md` — FORK-01, FORK-03, FORK-06 definitions
</read_first>

<action>
1. Run tests (D-05 gate): `cd Rust/core && cargo test 2>&1`
   - PR #3 adds doc comments only; PR #6 is CI config only — tests should pass without modification
   - If tests fail: document whether the failure pre-existed on main before these PRs
2. Verify all Wave 1 merge commits present: `git log --oneline | grep -E "PR #(3|6)"`
3. Verify upstream remote configured: `git remote -v | grep upstream`
4. Check fork-specific files not altered by any Wave 1 merge:
   `git log --oneline -- GooseSwift/GooseAppModel+Upload.swift GooseSwift/MoreRemoteServerViews.swift server/ | head -5`
   (Should show no commits from today's upstream merges touching these files)
</action>

<acceptance_criteria>
- `cargo test` in `Rust/core/` exits 0
- `git log --oneline | grep -E "PR #(3|6)"` shows 2 merge commits
- `git remote -v` shows `upstream https://github.com/b-nnett/goose`
- FORK-01, FORK-03, FORK-06 requirements satisfied
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `git remote -v | grep upstream` shows `https://github.com/b-nnett/goose` — FORK-01 satisfied
2. `git log --oneline | grep "PR #3"` — merge commit present — FORK-03 satisfied
3. `git log --oneline | grep "PR #6"` — merge commit present — FORK-06 satisfied
4. `cargo test` in `Rust/core/` passes after Wave 1 merges
5. Fork-specific files (`server/`, upload client, server settings UI) unchanged
</verification>

<must_haves>
  <truths>
    - T-05-01: Remote `upstream` points to `https://github.com/b-nnett/goose` (FORK-01)
    - T-05-02: PR #3 merged via `--no-ff` with merge commit — FFI docs integrated (FORK-03)
    - T-05-03: PR #6 merged via `--no-ff` with merge commit — CI workflow added (FORK-06)
    - T-05-04: `cargo test` passes in `Rust/core/` after Wave 1 merges
    - T-05-05: `server/`, `GooseAppModel+Upload.swift`, `MoreRemoteServerViews.swift` not altered by Wave 1 merges
  </truths>
  <goal>Remote upstream configured and two lowest-risk PRs (#3, #6) integrated without regressions</goal>
</must_haves>

<threat_model>
- **Merge conflict on docs PR (#3):** Low probability. Mitigation: accept both sides of doc comments; keep fork's existing bridge.rs comments.
- **Workflow filename collision on PR #6:** Medium probability if fork has existing CI. Mitigation: rename incoming file to avoid clobbering fork CI.
- **cargo test pre-existing failures on main:** Check main branch test state before attributing failures to these PRs. Document if pre-existing.
- **gh CLI not authenticated for b-nnett/goose:** Fallback to `git fetch upstream pull/N/head:upstream-pr-N` without gh CLI dependency.
</threat_model>
