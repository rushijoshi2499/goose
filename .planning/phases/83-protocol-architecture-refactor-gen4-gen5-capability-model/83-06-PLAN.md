---
phase: 83
plan: 06
type: execute
wave: 4
depends_on:
  - 83-02
  - 83-05
files_modified: []
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03

must_haves:
  truths:
    - "cargo test --locked passes with all new tests green and no regressions"
    - "iOS build compiles without new warnings"
    - "Zero occurrences of rustDeviceType in GooseSwift/"
    - "Zero occurrences of activeDeviceGeneration in GooseSwift/"
    - "capture.import_frame_batch regression tests pass: zero MAVERICK/PUFFIN rows persisted"
    - "No production Swift write path uses MAVERICK or PUFFIN string literals"
    - "CURRENT_SCHEMA_VERSION is 22 in store.rs"
    - "device.capabilities is in BRIDGE_METHODS"
    - "WireProtocol enum exists in protocol.rs"
    - "DeviceCapabilities struct exists in capabilities.rs"
  artifacts:
    - path: "Rust/core/src/capabilities.rs"
      provides: "DeviceCapabilities verified by cargo test"
      contains: "DeviceKind"
    - path: "Rust/core/src/protocol.rs"
      provides: "WireProtocol verified by cargo test"
      contains: "WireProtocol"
  key_links:
    - from: "cargo test --locked"
      to: "all unit tests green"
      via: "Rust test runner"
      pattern: "test result: ok"
---

<objective>
Phase gate verification: confirm all Rust unit tests pass, the iOS Xcode build succeeds,
and all structural invariants from Plans 01-05 are verifiable with grep and cargo test.
This plan produces no file changes — it is a verification-only checkpoint.

Purpose: Per D-18, cargo test --locked must pass clean and the iOS build must compile
without new warnings. This plan enforces that gate before the phase is marked complete.
It also confirms all success criteria from the ROADMAP phase entry are met.

Output: Verified phase state documented in 83-06-SUMMARY.md. No source files changed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-CONTEXT.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cargo test --locked and verify all structural invariants</name>
  <files></files>
  <read_first>
    - .planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-RESEARCH.md
      Validation Architecture section — full test map and per-wave commands
  </read_first>
  <action>
Run the following checks in sequence. Record output for SUMMARY.md. If any check fails,
do NOT proceed — diagnose and fix the failing plan (01-05) first, then re-run this plan.

1. Rust full test suite:
   cd Rust/core && cargo test --locked 2>&1 | tail -20
   Expected: "test result: ok. N passed; 0 failed"

2. Targeted test filters (confirm new tests are present and passing):
   cargo test --locked -p goose-core wire_protocol_tests 2>&1 | tail -5
   cargo test --locked -p goose-core capabilities_tests 2>&1 | tail -5
   cargo test --locked -p goose-core migration_step_22 2>&1 | tail -5
   cargo test --locked -p goose-core parse_device_type 2>&1 | tail -5
   cargo test --locked -p goose-core bridge_methods_constant_matches_dispatcher 2>&1 | tail -5
   cargo test --locked -p goose-core device_capabilities_bridge 2>&1 | tail -5
   cargo test --locked -p goose-core import_frame_batch_rejects_legacy 2>&1 | tail -5

3. Structural grep checks (all must return 0 or the expected value):
   grep -c "pub enum WireProtocol" Rust/core/src/protocol.rs     → must be 1
   grep -c "pub struct DeviceCapabilities" Rust/core/src/capabilities.rs   → must be 1
   grep -c "pub mod capabilities" Rust/core/src/lib.rs            → must be 1
   grep -c "CURRENT_SCHEMA_VERSION: i64 = 22" Rust/core/src/store.rs  → must be 1
   grep -c "device.capabilities" Rust/core/src/bridge.rs          → must be ≥ 2 (BRIDGE_METHODS + arm)
   grep -rn "rustDeviceType" GooseSwift/ 2>/dev/null | grep -v "^Binary" | wc -l  → must be 0
   grep -rn "activeDeviceGeneration" GooseSwift/ 2>/dev/null | grep -v "^Binary" | wc -l  → must be 0
   grep -rn '"MAVERICK"\|"PUFFIN"' GooseSwift/ Rust/core/src/ 2>/dev/null | grep -v "^Binary" | grep -v "device_type_name\|store.rs\|protocol.rs\|capabilities.rs\|openwhoop_reference" | wc -l  → must be 0 (no production write paths use legacy strings)

4. iOS build (Xcode build, no run needed):
   Use XcodeBuildMCP build_run_sim or xcodebuild directly:
   xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "error:|warning:|BUILD SUCCEEDED|BUILD FAILED" | tail -20
   Expected: BUILD SUCCEEDED, zero new warnings (pre-existing warnings from other modules are acceptable — only Phase 83 files must be clean)

Record pass/fail for each check in 83-06-SUMMARY.md.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | grep -E "^test result|FAILED|error\[" | tail -5</automated>
  </verify>
  <done>
    - cargo test --locked: test result: ok, 0 failed
    - All 7 targeted test filters pass individually (includes import_frame_batch_rejects_legacy)
    - All 8 structural grep checks pass (includes MAVERICK production-path check)
    - iOS build: BUILD SUCCEEDED with no new warnings from Phase 83 files
    - 83-06-SUMMARY.md records pass/fail for each check
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase gate | cargo test --locked is the enforcement mechanism; iOS build is secondary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-10 | Tampering | Skipping phase gate | accept | This is a verification-only plan; skipping it hides regressions but does not introduce new security risk |
| T-83-SC | Tampering | cargo installs | accept | No new packages throughout Phase 83 |
</threat_model>

<verification>
cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | grep "t
grep -rn "rustDeviceType\|activeDeviceGeneration" /Users/francisco/Documents/goose/GooseSwift/ 2>/dev/null | grep -v "^Binary" | wc -l
</verification>

<success_criteria>
- cargo test --locked: 0 failures
- All targeted test filters green (7 total, including import_frame_batch_rejects_legacy)
- Zero rustDeviceType occurrences in GooseSwift/
- Zero activeDeviceGeneration occurrences in GooseSwift/
- Zero production write paths using MAVERICK/PUFFIN string literals
- CURRENT_SCHEMA_VERSION = 22
- "device.capabilities" in BRIDGE_METHODS
- WireProtocol enum in protocol.rs
- DeviceCapabilities struct in capabilities.rs
- iOS BUILD SUCCEEDED
</success_criteria>

## Artifacts This Phase Produces

None — verification only. See Plans 01-05 for all new symbols.

<output>
Create `.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-06-SUMMARY.md` when done
</output>
