---
phase: 83
plan: 05
type: execute
wave: 3
depends_on:
  - 83-04
files_modified:
  - GooseSwift/GooseBLEClient+HistoricalHandlers.swift
  - GooseSwift/GooseBLEClient+HistoricalCommands.swift
  - GooseSwift/GooseBLEClient+Parsing.swift
  - GooseSwift/GooseBLEClient+DebugAndSync.swift
  - GooseSwift/GooseAppModel+NotificationPipeline.swift
  - GooseSwift/GooseAppModel+Upload.swift
  - GooseSwift/OvernightRawNotificationSpool.swift
  - GooseSwift/OvernightSQLiteMirrorQueue.swift
  - GooseSwift/MovementPacketSamples.swift
autonomous: true
requirements:
  - PROTO-01
  - PROTO-03

must_haves:
  truths:
    - "Zero occurrences of activeDeviceGeneration across all Swift files"
    - "Zero occurrences of rustDeviceType across all Swift files"
    - "HistoricalHandlers line 80 switch default writes connectedCapabilities?.wireProtocol.bridgeString ?? "GOOSE" — never MAVERICK"
    - "8 guard sites in HistoricalHandlers/HistoricalCommands/DebugAndSync use connectedCapabilities?.historicalSync == .pageSequence"
    - "GooseBLEClient+Parsing.swift reset site uses connectedCapabilities = nil"
    - "GooseAppModel+NotificationPipeline.swift reassembly uses event.wireProtocol enum checks"
    - "HR_MONITOR string comparison at NotificationPipeline line 720 replaced by event.wireProtocol == .hrMonitor"
    - "Bridge arg device_type sites in Upload/Spool/MirrorQueue/MovementPacketSamples use event.wireProtocol.bridgeString"
    - "iOS build compiles without new warnings"
  artifacts:
    - path: "GooseSwift/GooseBLEClient+HistoricalHandlers.swift"
      provides: "6 guard sites replaced with capabilities checks"
      contains: "connectedCapabilities?.historicalSync"
    - path: "GooseSwift/GooseAppModel+NotificationPipeline.swift"
      provides: "Reassembly string comparisons replaced with wireProtocol enum checks"
      contains: "event.wireProtocol"
  key_links:
    - from: "GooseAppModel+NotificationPipeline.swift"
      to: "GooseBLETypes.swift WireProtocol"
      via: "event.wireProtocol == .gen4 in frame reassembly"
      pattern: "wireProtocol == .gen4"
    - from: "GooseBLEClient+Parsing.swift"
      to: "GooseBLEClient.connectedCapabilities"
      via: "connectedCapabilities = nil in resetBLEState()"
      pattern: "connectedCapabilities = nil"
---

<objective>
Replace all remaining activeDeviceGeneration guard sites and rustDeviceType string
consumers across 9 Swift files. After this plan, no Swift file contains either identifier.

Purpose: Completes D-08 (activeDeviceGeneration replaced by connectedCapabilities guards)
and D-02 (rustDeviceType string comparisons replaced by wireProtocol enum checks). This
plan depends on Plan 04 having defined the connectedCapabilities property on GooseBLEClient
and the wireProtocol/bridgeString helpers on GooseNotificationEvent.

Output:
- 6 guard sites in HistoricalHandlers replaced with connectedCapabilities?.historicalSync checks
- 2 guard sites + 1 frame-building in HistoricalCommands replaced
- 5 sites in Parsing.swift (1 reset + 2 guards + 2 switches) replaced
- 3 sites in DebugAndSync replaced
- 6 sites in NotificationPipeline replaced (2 reassembly + 1 HR_MONITOR + 1 cache key + 2 bridge args)
- 1 site each in Upload/Spool/MirrorQueue/MovementPacketSamples replaced
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-CONTEXT.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-RESEARCH.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-PATTERNS.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace guard sites in GooseBLEClient extensions (HistoricalHandlers, HistoricalCommands, Parsing, DebugAndSync)</name>
  <files>GooseSwift/GooseBLEClient+HistoricalHandlers.swift, GooseSwift/GooseBLEClient+HistoricalCommands.swift, GooseSwift/GooseBLEClient+Parsing.swift, GooseSwift/GooseBLEClient+DebugAndSync.swift</files>
  <read_first>
    - GooseSwift/GooseBLEClient+HistoricalHandlers.swift lines 75-90 — switch at line 80,
      guard at line 80 block; read surrounding code to understand context before replacing
    - GooseSwift/GooseBLEClient+HistoricalHandlers.swift lines 450-460, 563-575, 588-598, 668-680
      — four boolean guards (lines 453, 567, 593, 672)
    - GooseSwift/GooseBLEClient+HistoricalCommands.swift lines 63-75, 103-115 — two guards
      at lines 68 and 108; frame-building call at line 114
    - GooseSwift/GooseBLEClient+Parsing.swift lines 544-552 — reset at line 548
    - GooseSwift/GooseBLEClient+Parsing.swift lines 733-752 — two guards at lines 737 and 748
    - GooseSwift/GooseBLEClient+Parsing.swift lines 1010-1025 — switches at lines 1015 and 1022
    - GooseSwift/GooseBLEClient+DebugAndSync.swift lines 395-445 — three sites at lines 399, 419, 440
    - .planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-PATTERNS.md
      all four extension sections — exact before/after patterns for each site
    - .planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-RESEARCH.md
      Pitfall 2 (frame building), Pitfall 5 (nil connectedCapabilities)
  </read_first>
  <action>
GooseBLEClient+HistoricalHandlers.swift (6 sites):

Line 80 — switch statement:
  Before: switch activeDeviceGeneration { case .gen4: ... case .gen5: ... }
  After:  switch connectedCapabilities?.historicalSync {
            case .pageSequence: deviceType = "GEN4"
            default: deviceType = connectedCapabilities?.wireProtocol.bridgeString ?? "GOOSE"
          }

  CRITICAL (HIGH-1): Do NOT write "MAVERICK" here. The default case must use
  connectedCapabilities?.wireProtocol.bridgeString ?? "GOOSE" so that Gen5 frames
  sent to capture.import_frame_batch carry "GOOSE", not "MAVERICK". After migration
  (Plan 02), parse_device_type() rejects "MAVERICK" (Plan 03), so writing MAVERICK
  here would cause bridge errors at runtime. Using bridgeString ensures the value is
  always one of {"GEN4", "GOOSE", "HR_MONITOR"} — all accepted by parse_device_type().

Lines 453, 567, 672 — positive gen4 guards:
  Before: if activeDeviceGeneration == .gen4 {
  After:  if connectedCapabilities?.historicalSync == .pageSequence {

Line 593 — negative gen4 guard:
  Before: if activeDeviceGeneration != .gen4 {
  After:  if connectedCapabilities?.historicalSync != .pageSequence {

GooseBLEClient+HistoricalCommands.swift (2 guards + 1 frame building):

Lines 68, 108 — positive gen4 guards:
  Before: if activeDeviceGeneration == .gen4 {
  After:  if connectedCapabilities?.historicalSync == .pageSequence {

Line 114 — frame building:
  Before: activeDeviceGeneration.buildCommandFrame(...)
  After:  whoopGenerationFromCapabilities().buildCommandFrame(...)

GooseBLEClient+Parsing.swift (5 sites):

Line 548 — reset:
  Before: activeDeviceGeneration = .gen5
  After:  connectedCapabilities = nil

Lines 737, 748 — negative gen4 guards using wireProtocol (Parsing checks wire protocol,
not historical sync — these are byte-level frame header decisions):
  Before: if activeDeviceGeneration != .gen4 {
  After:  if connectedCapabilities?.wireProtocol != .gen4 {

Lines 1015, 1022 — switch statements (these switch on wire protocol for byte-level parsing):
  Before: switch activeDeviceGeneration { case .gen4: ... case .gen5: ... }
  After:  switch connectedCapabilities?.wireProtocol {
            case .gen4: /* same body as .gen4 case */
            default: /* same body as .gen5 case */
          }

GooseBLEClient+DebugAndSync.swift (3 sites):

Line 399 — description string (use bridgeString from wireProtocol for a canonical string
  that matches what the bridge understands):
  Before: activeDeviceGeneration.description
  After:  connectedCapabilities.map { $0.wireProtocol == .gen4 ? "gen4" : "gen5" } ?? "unknown"

Line 419 — positive gen4 guard:
  Before: if activeDeviceGeneration == .gen4 {
  After:  if connectedCapabilities?.historicalSync == .pageSequence {

Line 440 — positive gen4 guard:
  Before: if activeDeviceGeneration == .gen4 {
  After:  if connectedCapabilities?.historicalSync == .pageSequence {

After all changes, run:
  grep -rn "activeDeviceGeneration" GooseSwift/GooseBLEClient+HistoricalHandlers.swift GooseSwift/GooseBLEClient+HistoricalCommands.swift GooseSwift/GooseBLEClient+Parsing.swift GooseSwift/GooseBLEClient+DebugAndSync.swift
to confirm zero occurrences.
  </action>
  <verify>
    <automated>grep -rn "activeDeviceGeneration" /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+HistoricalHandlers.swift /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+HistoricalCommands.swift /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+Parsing.swift /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+DebugAndSync.swift 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    - Zero activeDeviceGeneration references in all 4 files
    - HistoricalHandlers: 6 sites replaced with connectedCapabilities?.historicalSync checks
    - HistoricalCommands: 2 guards + 1 frame-building replaced
    - Parsing: reset = nil; 2 wireProtocol guards; 2 wireProtocol switches
    - DebugAndSync: description string + 2 guards replaced
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace rustDeviceType consumers in pipeline and ancillary files</name>
  <files>GooseSwift/GooseAppModel+NotificationPipeline.swift, GooseSwift/GooseAppModel+Upload.swift, GooseSwift/OvernightRawNotificationSpool.swift, GooseSwift/OvernightSQLiteMirrorQueue.swift, GooseSwift/MovementPacketSamples.swift</files>
  <read_first>
    - GooseSwift/GooseAppModel+NotificationPipeline.swift lines 519-530 — bridge arg at line 524
    - GooseSwift/GooseAppModel+NotificationPipeline.swift lines 695-705 — bridge arg at line 700
    - GooseSwift/GooseAppModel+NotificationPipeline.swift lines 715-725 — HR_MONITOR bypass at line 720
    - GooseSwift/GooseAppModel+NotificationPipeline.swift lines 824-845 — two reassembly checks
      at lines 829 and 841 (primary PROTO-01 targets)
    - GooseSwift/GooseAppModel+NotificationPipeline.swift lines 877-885 — cache key at line 881
    - GooseSwift/GooseAppModel+Upload.swift line 125 — bridge arg
    - GooseSwift/OvernightRawNotificationSpool.swift line 369 — bridge arg
    - GooseSwift/OvernightSQLiteMirrorQueue.swift line 100 — bridge arg
    - GooseSwift/MovementPacketSamples.swift lines 226-244 — two logging sites at lines 229, 240
    - .planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-PATTERNS.md
      GooseAppModel+NotificationPipeline.swift section — exact replacement patterns for all sites
  </read_first>
  <action>
GooseAppModel+NotificationPipeline.swift:

Line 524 — bridge arg (device_type passed to frame parsing):
  Before: "device_type": event.rustDeviceType
  After:  "device_type": event.wireProtocol.bridgeString

Line 700 — same pattern (second frame parsing call):
  Before: "device_type": event.rustDeviceType
  After:  "device_type": event.wireProtocol.bridgeString

Line 720 — HR_MONITOR bypass (primary string comparison #3):
  Before: if event.rustDeviceType == "HR_MONITOR" {
  After:  if event.wireProtocol == .hrMonitor {

Lines 829 and 841 — frame reassembly (primary PROTO-01 string comparisons):
  Before: let headerLength = event.rustDeviceType == "GEN4" ? 4 : 8
  After:  let headerLength = event.wireProtocol == .gen4 ? 4 : 8

  Before: if event.rustDeviceType == "GEN4" {
  After:  if event.wireProtocol == .gen4 {

Line 881 — cache key:
  Before: let key = "\(event.deviceID)-\(event.rustDeviceType)"
  After:  let key = "\(event.deviceID)-\(event.wireProtocol.bridgeString)"

GooseAppModel+Upload.swift line 125:
  Before: deviceType: deviceEvent.rustDeviceType
  After:  deviceType: deviceEvent.wireProtocol.bridgeString
  (parameter name stays as deviceType; only the value expression changes)

OvernightRawNotificationSpool.swift line 369:
  Before: "device_type": event.rustDeviceType
  After:  "device_type": event.wireProtocol.bridgeString

OvernightSQLiteMirrorQueue.swift line 100:
  Before: "device_type": event.rustDeviceType
  After:  "device_type": event.wireProtocol.bridgeString

MovementPacketSamples.swift lines 229 and 240 — logging strings:
  Before: event.rustDeviceType  (used inside string interpolation for logs)
  After:  event.wireProtocol.bridgeString

After all changes, verify:
  grep -rn "rustDeviceType" GooseSwift/ | grep -v "^Binary" | wc -l   → must be 0
  grep -rn "activeDeviceGeneration" GooseSwift/ | grep -v "^Binary" | wc -l  → must be 0
  </action>
  <verify>
    <automated>grep -rn "rustDeviceType\|activeDeviceGeneration" /Users/francisco/Documents/goose/GooseSwift/ 2>/dev/null | grep -v "^Binary" | wc -l</automated>
  </verify>
  <done>
    - Zero rustDeviceType occurrences across all GooseSwift/ files
    - Zero activeDeviceGeneration occurrences across all GooseSwift/ files
    - NotificationPipeline: 2 reassembly checks use wireProtocol enum; HR_MONITOR uses .hrMonitor;
      bridge args use bridgeString; cache key uses bridgeString
    - Upload/Spool/MirrorQueue/MovementPacketSamples: all use wireProtocol.bridgeString
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GooseNotificationEvent → frame reassembly | wireProtocol enum replaces string comparison; exhaustive at compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-08 | Tampering | nil connectedCapabilities during historical sync | accept | optional chaining evaluates to false/nil; Gen5 path taken silently — safe default documented in Pitfall 5 |
| T-83-09 | Tampering | bridgeString producing wrong device_type for Rust | mitigate | bridgeString tested: gen4→"GEN4", gen5→"GOOSE", hrMonitor→"HR_MONITOR" — all accepted by parse_device_type() |
| T-83-SC | Tampering | cargo/npm installs | accept | No new packages |
</threat_model>

<verification>
grep -rn "rustDeviceType" /Users/francisco/Documents/goose/GooseSwift/ 2>/dev/null | grep -v "^Binary" | wc -l
grep -rn "activeDeviceGeneration" /Users/francisco/Documents/goose/GooseSwift/ 2>/dev/null | grep -v "^Binary" | wc -l
grep -n "wireProtocol == .gen4" /Users/francisco/Documents/goose/GooseSwift/GooseAppModel+NotificationPipeline.swift
grep -n "wireProtocol == .hrMonitor" /Users/francisco/Documents/goose/GooseSwift/GooseAppModel+NotificationPipeline.swift
grep -n "connectedCapabilities = nil" /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+Parsing.swift
</verification>

<success_criteria>
- Zero occurrences of rustDeviceType in all GooseSwift/ files
- Zero occurrences of activeDeviceGeneration in all GooseSwift/ files
- NotificationPipeline reassembly uses wireProtocol enum checks
- All bridge arg pass-throughs use wireProtocol.bridgeString
- Parsing reset uses connectedCapabilities = nil
- HistoricalHandlers line 80 switch default uses connectedCapabilities?.wireProtocol.bridgeString ?? "GOOSE" (HIGH-1: never MAVERICK)
</success_criteria>

## Artifacts This Phase Produces

No new symbols — this plan replaces existing identifiers with the typed equivalents
produced by Plans 01-04. The final state is zero string comparisons on device type
in all consumer files.

<output>
Create `.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-05-SUMMARY.md` when done
</output>
