---
phase: 83
plan: 04
type: execute
wave: 3
depends_on:
  - 83-03
files_modified:
  - GooseSwift/GooseBLETypes.swift
  - GooseSwift/GooseBLEClient.swift
  - GooseSwift/GooseBLEClient+Commands.swift
  - GooseSwift/GooseBLEClient+Haptics.swift
  - GooseSwift/GooseBLEClient+UserActions.swift
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02

must_haves:
  truths:
    - "WireProtocol Swift enum with gen4/gen5/hrMonitor cases exists in GooseBLETypes.swift"
    - "HistoricalSyncKind Swift enum with pageSequence/stream exists in GooseBLETypes.swift"
    - "DeviceCapabilities Swift struct (Decodable) exists in GooseBLETypes.swift"
    - "WireProtocol.bridgeString computed property exists"
    - "GooseNotificationEvent.wireProtocol computed property replaces rustDeviceType"
    - "GooseBLEClient.connectedCapabilities: DeviceCapabilities? declared (replaces activeDeviceGeneration)"
    - "processDiscoveredCharacteristics calls device.capabilities and sets connectedCapabilities"
    - "whoopGenerationFromCapabilities() internal helper exists in GooseBLEClient+Commands.swift with nil guard + OSLog .error"
    - "GooseBLEClient+Haptics.swift and GooseBLEClient+UserActions.swift use whoopGenerationFromCapabilities()"
    - "iOS build compiles without new warnings"
  artifacts:
    - path: "GooseSwift/GooseBLETypes.swift"
      provides: "WireProtocol, HistoricalSyncKind, DeviceCapabilities Swift types + wireProtocol computed property"
      contains: "enum WireProtocol"
    - path: "GooseSwift/GooseBLEClient.swift"
      provides: "connectedCapabilities declaration replacing activeDeviceGeneration"
      contains: "connectedCapabilities"
    - path: "GooseSwift/GooseBLEClient+Commands.swift"
      provides: "device.capabilities bridge call at GATT discovery + whoopGenerationFromCapabilities helper"
      contains: "device.capabilities"
  key_links:
    - from: "GooseBLEClient+Commands.swift processDiscoveredCharacteristics"
      to: "bridge.rs device.capabilities arm"
      via: "historicalDirectWriteBridge.request(method: \"device.capabilities\")"
      pattern: "device.capabilities"
    - from: "GooseAppModel+NotificationPipeline.swift"
      to: "GooseBLETypes.swift WireProtocol"
      via: "event.wireProtocol"
      pattern: "wireProtocol"
---

<objective>
Add the Swift-side type definitions for Phase 83 and replace the rustDeviceType computed
property and activeDeviceGeneration state variable with typed equivalents. This plan covers
GooseBLETypes.swift (new types), GooseBLEClient.swift (property declaration),
GooseBLEClient+Commands.swift (GATT discovery + helper), and the two frame-building files
(Haptics + UserActions) that use whoopGenerationFromCapabilities().

Purpose: After this plan, GooseNotificationEvent carries a typed wireProtocol property
and GooseBLEClient holds connectedCapabilities instead of the defaulted activeDeviceGeneration.
Plans 05 will replace the remaining guard sites and pipeline consumers.

Output (per D-02, D-07, D-08):
- WireProtocol, HistoricalSyncKind, DeviceCapabilities Swift types + wireProtocol property
  on GooseNotificationEvent + bridgeString helper on WireProtocol (GooseBLETypes.swift)
- connectedCapabilities: DeviceCapabilities? replaces activeDeviceGeneration (GooseBLEClient.swift)
- device.capabilities call + whoopGenerationFromCapabilities() (GooseBLEClient+Commands.swift)
- Frame-building calls updated in Haptics + UserActions
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-CONTEXT.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-RESEARCH.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-PATTERNS.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Swift type definitions to GooseBLETypes.swift</name>
  <files>GooseSwift/GooseBLETypes.swift</files>
  <read_first>
    - GooseSwift/GooseBLETypes.swift lines 70-90 — rustDeviceType computed property
      (lines 75-84) on GooseNotificationEvent; characteristicUUID-based UUID detection
    - GooseSwift/GooseBLETypes.swift lines 200-295 — WhoopGeneration enum structure;
      MARK comment style, enum declaration, case list pattern to copy for WireProtocol
    - .planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-PATTERNS.md
      GooseBLETypes.swift section — exact replacement pattern for wireProtocol property
      and DeviceCapabilities struct shape
  </read_first>
  <action>
1. Replace the rustDeviceType computed property on GooseNotificationEvent (lines 75-84)
   with wireProtocol: WireProtocol computed property using identical UUID detection logic:
   - characteristicUUID.lowercased().hasPrefix("610800") → return .gen4
   - normalizedUUID == "2a37" || normalizedUUID.hasPrefix("00002a37") → return .hrMonitor
   - default → return .gen5

   IMPORTANT: rustDeviceType is also used in OvernightRawNotificationSpool.swift,
   OvernightSQLiteMirrorQueue.swift, MovementPacketSamples.swift, GooseAppModel+Upload.swift,
   and GooseAppModel+NotificationPipeline.swift. Those files are updated in Plan 05.
   This task only removes the rustDeviceType property from GooseBLETypes.swift and adds
   wireProtocol. The build will not compile until Plan 05 also updates all callers.

2. After the WhoopGeneration enum (after line ~291), add with // MARK: - WireProtocol header:
   - WireProtocol enum with cases: gen4, gen5, hrMonitor (per planning note in RESEARCH.md
     Open Questions #1 — add hrMonitor to eliminate the last string comparison)
   - HistoricalSyncKind enum with cases: pageSequence, stream
   - WireProtocol.bridgeString computed property:
     gen4 → "GEN4", gen5 → "GOOSE", hrMonitor → "HR_MONITOR"
     (per PATTERNS.md GooseAppModel+NotificationPipeline.swift section)

3. After HistoricalSyncKind, add DeviceCapabilities struct (Decodable, no Encodable needed):
   struct DeviceCapabilities: Decodable with CodingKeys mapping snake_case JSON to camelCase:
     wireProtocol: WireProtocol  (key: "wire_protocol")
     historicalSync: HistoricalSyncKind  (key: "historical_sync")
     batteryViaR22: Bool  (key: "battery_via_r22")
     batteryViaEvent48: Bool  (key: "battery_via_event48")
     batteryViaCMD26: Bool  (key: "battery_via_cmd26")
     r22Realtime: Bool  (key: "r22_realtime")
   WireProtocol and HistoricalSyncKind must also be Decodable with matching raw string values
   matching the snake_case JSON from Rust (gen4, gen5, hr_monitor, page_sequence, stream).
   Use a custom init(from:) on DeviceCapabilities or add RawRepresentable conformance
   to WireProtocol and HistoricalSyncKind — follow the pattern that is least invasive
   given the existing codebase style. If enums do not use rawValue elsewhere in the file,
   use CodingKey-based decoding on DeviceCapabilities.

Note on WhoopGeneration: The WhoopGeneration enum is NOT deleted — it is still used for
buildCommandFrame() and helloFrame. Only the rustDeviceType property is replaced.
  </action>
  <verify>
    <automated>grep -n "wireProtocol\|WireProtocol\|DeviceCapabilities\|HistoricalSyncKind" /Users/francisco/Documents/goose/GooseSwift/GooseBLETypes.swift | head -20</automated>
  </verify>
  <done>
    - rustDeviceType computed property removed from GooseNotificationEvent
    - wireProtocol: WireProtocol computed property added with same UUID detection logic
    - WireProtocol enum with gen4/gen5/hrMonitor exists (per D-02 + RESEARCH Open Questions #1)
    - HistoricalSyncKind enum with pageSequence/stream exists
    - WireProtocol.bridgeString returns "GEN4", "GOOSE", "HR_MONITOR"
    - DeviceCapabilities struct (Decodable) exists with all 6 fields
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace activeDeviceGeneration + add GATT discovery bridge call + update frame-building files</name>
  <files>GooseSwift/GooseBLEClient.swift, GooseSwift/GooseBLEClient+Commands.swift, GooseSwift/GooseBLEClient+Haptics.swift, GooseSwift/GooseBLEClient+UserActions.swift</files>
  <read_first>
    - GooseSwift/GooseBLEClient.swift lines 268-300 — property block; activeDeviceGeneration
      declaration at line 275 with the threading comment above it
    - GooseSwift/GooseBLEClient+Commands.swift lines 250-270 — first buildCommandFrame call
      using activeDeviceGeneration (line 262)
    - GooseSwift/GooseBLEClient+Commands.swift lines 355-365 — second buildCommandFrame (line 361)
    - GooseSwift/GooseBLEClient+Commands.swift lines 490-500 — third buildCommandFrame (line 496)
    - GooseSwift/GooseBLEClient+Commands.swift lines 980-1040 — processDiscoveredCharacteristics
      (line 986 sets activeDeviceGeneration; this is where device.capabilities is added)
    - GooseSwift/GooseBLEClient+Haptics.swift lines 15-25 — frame building call at line 19
    - GooseSwift/GooseBLEClient+UserActions.swift lines 78-90 — buildCommandFrame at line 82
    - GooseSwift/GooseBLEClient+UserActions.swift lines 200-210 — helloFrame at line 204
    - .planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-PATTERNS.md
      GooseBLEClient.swift and GooseBLEClient+Commands.swift sections — exact replacement
      patterns including whoopGenerationFromCapabilities() helper definition
  </read_first>
  <action>
GooseBLEClient.swift:
1. At line 275, replace:
   var activeDeviceGeneration: WhoopGeneration = .gen5
   with:
   // connectedCapabilities is nil when disconnected; set by processDiscoveredCharacteristics
   // after a successful device.capabilities bridge call.
   var connectedCapabilities: DeviceCapabilities?
   Keep the threading comment that was above activeDeviceGeneration — it applies equally
   to connectedCapabilities.

GooseBLEClient+Commands.swift:
2. Add helper method on GooseBLEClient (place near top of the extension or at end).
   Use internal visibility (Swift default — no access modifier keyword) so it is
   accessible from GooseBLEClient+Haptics.swift, GooseBLEClient+UserActions.swift,
   and GooseBLEClient+HistoricalCommands.swift (HIGH-2: private is file-scoped in Swift
   and would cause a compile error when called from sibling extension files):
   func whoopGenerationFromCapabilities() -> WhoopGeneration {
     guard let caps = connectedCapabilities else {
       // connectedCapabilities is nil when disconnected or if the device.capabilities
       // bridge call failed. Log the nil access and return .gen5 as a conservative
       // fallback; generation-specific commands should not be sent in this state.
       os_log(.error, log: .default, "[BLE] whoopGenerationFromCapabilities called with nil connectedCapabilities — generation unknown, defaulting to gen5")
       return .gen5
     }
     return caps.wireProtocol == .gen4 ? .gen4 : .gen5
   }

3. Replace all three activeDeviceGeneration.buildCommandFrame(...) calls at lines 262, 361, 496
   with whoopGenerationFromCapabilities().buildCommandFrame(...). The method arguments and
   surrounding code are unchanged — only the receiver changes.

4. In processDiscoveredCharacteristics at line 986, the existing line:
   activeDeviceGeneration = WhoopGeneration.detect(from: characteristic)
   becomes two things: keep WhoopGeneration.detect() as a local let (needed for device_kind
   determination), then add the device.capabilities bridge call:
   a. Replace assignment with: let detectedGeneration = WhoopGeneration.detect(from: characteristic)
   b. Immediately after (before activeDescriptor assignment), add:
      let deviceKindString = detectedGeneration == .gen4 ? "WHOOP4" : "WHOOP5"
      if let result = try? historicalDirectWriteBridge.request(
            method: "device.capabilities",
            args: ["device_kind": deviceKindString]),
         let capData = try? JSONSerialization.data(withJSONObject: result),
         let caps = try? JSONDecoder().decode(DeviceCapabilities.self, from: capData) {
        connectedCapabilities = caps
      }
   The historicalDirectWriteBridge already exists on GooseBLEClient (line 296 in client file).
   Do NOT create a new GooseRustBridge instance. Use try? throughout. If the bridge call
   fails, connectedCapabilities stays nil — generation-specific BLE commands (buildCommandFrame,
   helloFrame) will be guarded by whoopGenerationFromCapabilities(), which logs an OSLog
   .error and returns .gen5 as a safe fallback. This matches the HIGH-3 mitigation: nil
   is not silently ignored; it is surfaced in the log so developers can diagnose bridge
   failures during GATT discovery.

   IMPORTANT: The guard in whoopGenerationFromCapabilities() already handles the nil case
   with an OSLog .error. Do NOT add a separate os_log call at the bridge call site — the
   try? pattern is sufficient here; the guard in the helper is the enforcement point.

GooseBLEClient+Haptics.swift:
5. Replace activeDeviceGeneration.buildCommandFrame(...) at line 19 with:
   whoopGenerationFromCapabilities().buildCommandFrame(...)
   (whoopGenerationFromCapabilities is defined in the Commands extension above)

GooseBLEClient+UserActions.swift:
6. Replace activeDeviceGeneration.buildCommandFrame(...) at line 82 with:
   whoopGenerationFromCapabilities().buildCommandFrame(...)
7. Replace activeDeviceGeneration.helloFrame at line 204 with:
   whoopGenerationFromCapabilities().helloFrame
  </action>
  <verify>
    <automated>grep -rn "activeDeviceGeneration" /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient.swift /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+Commands.swift /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+Haptics.swift /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+UserActions.swift 2>/dev/null | grep -v "^Binary"</automated>
  </verify>
  <done>
    - GooseBLEClient.swift: connectedCapabilities: DeviceCapabilities? declared; no activeDeviceGeneration
    - GooseBLEClient+Commands.swift: whoopGenerationFromCapabilities() helper exists with
      internal visibility (no private keyword) and OSLog .error on nil connectedCapabilities;
      processDiscoveredCharacteristics calls device.capabilities and sets connectedCapabilities;
      3 buildCommandFrame calls updated
    - GooseBLEClient+Haptics.swift: 1 buildCommandFrame call updated
    - GooseBLEClient+UserActions.swift: 2 frame-building calls updated
    - grep finds zero occurrences of activeDeviceGeneration in these 4 files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge response → DeviceCapabilities | JSON from Rust decoded by Swift JSONDecoder; malformed response leaves connectedCapabilities nil |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-06 | Tampering | Malformed device.capabilities response | mitigate | try? + optional binding — nil connectedCapabilities is safe; no force-unwrap |
| T-83-07 | Denial of Service | Bridge call in processDiscoveredCharacteristics blocking BLE queue | accept | historicalDirectWriteBridge is synchronous but capabilities call is cheap (pure computation in Rust); no I/O path |
| T-83-SC | Tampering | cargo/npm installs | accept | No new packages |
</threat_model>

<verification>
grep -n "wireProtocol\|WireProtocol\|DeviceCapabilities" /Users/francisco/Documents/goose/GooseSwift/GooseBLETypes.swift | wc -l
grep -n "connectedCapabilities" /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient.swift
grep -n "device.capabilities\|whoopGenerationFromCapabilities" /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+Commands.swift
grep -rn "activeDeviceGeneration" /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient.swift /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+Commands.swift /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+Haptics.swift /Users/francisco/Documents/goose/GooseSwift/GooseBLEClient+UserActions.swift 2>/dev/null
</verification>

<success_criteria>
- WireProtocol/HistoricalSyncKind/DeviceCapabilities types exist in GooseBLETypes.swift
- GooseNotificationEvent.wireProtocol replaces rustDeviceType
- connectedCapabilities: DeviceCapabilities? declared in GooseBLEClient.swift
- processDiscoveredCharacteristics sets connectedCapabilities via device.capabilities call
- whoopGenerationFromCapabilities() helper exists with internal visibility + nil guard with OSLog .error; Haptics + UserActions use it
- Zero activeDeviceGeneration references in the 4 modified files
</success_criteria>

## Artifacts This Phase Produces

| Symbol | File | Kind |
|--------|------|------|
| `WireProtocol` | `GooseSwift/GooseBLETypes.swift` | `enum` |
| `HistoricalSyncKind` | `GooseSwift/GooseBLETypes.swift` | `enum` |
| `DeviceCapabilities` | `GooseSwift/GooseBLETypes.swift` | `struct: Decodable` |
| `GooseNotificationEvent.wireProtocol` | `GooseSwift/GooseBLETypes.swift` | computed property |
| `WireProtocol.bridgeString` | `GooseSwift/GooseBLETypes.swift` | computed property |
| `GooseBLEClient.connectedCapabilities` | `GooseSwift/GooseBLEClient.swift` | `var: DeviceCapabilities?` |
| `whoopGenerationFromCapabilities()` | `GooseSwift/GooseBLEClient+Commands.swift` | `func` (internal) |

<output>
Create `.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-04-SUMMARY.md` when done
</output>
