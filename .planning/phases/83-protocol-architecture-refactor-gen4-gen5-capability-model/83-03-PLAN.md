---
phase: 83
plan: 03
type: execute
wave: 2
depends_on:
  - 83-01
files_modified:
  - Rust/core/src/bridge.rs
  - Rust/core/tests/bridge_tests.rs
autonomous: true
requirements:
  - PROTO-02
  - PROTO-03

must_haves:
  truths:
    - "device.capabilities is in BRIDGE_METHODS sorted between debug.start_session and diagnostics.perf_budget"
    - "device.capabilities match arm exists in the dispatcher"
    - "parse_device_type rejects MAVERICK and PUFFIN with GooseError"
    - "parse_device_type still accepts GEN4, GEN_4, GOOSE, HR_MONITOR"
    - "bridge_methods_constant_matches_dispatcher test passes"
    - "capture.import_frame_batch regression tests pass: zero MAVERICK/PUFFIN rows persisted after import"
    - "cargo test --locked passes clean"
  artifacts:
    - path: "Rust/core/src/bridge.rs"
      provides: "device.capabilities bridge method + parse_device_type rejection"
      contains: "device.capabilities"
  key_links:
    - from: "GooseBLEClient+Commands.swift"
      to: "bridge.rs device.capabilities arm"
      via: "bridge.request(method: \"device.capabilities\", args: [\"device_kind\": ...])"
      pattern: "device.capabilities"
---

<objective>
Wire the device.capabilities bridge method in bridge.rs and reject MAVERICK/PUFFIN in
parse_device_type(). This plan depends on Plan 01 (capabilities.rs must exist with
DeviceKind and DeviceCapabilities before this can compile).

Purpose: After this plan, Swift can call bridge.request("device.capabilities",
device_kind: "WHOOP4"|"WHOOP5"|"HR_MONITOR") and receive a DeviceCapabilities JSON
object. Also, any attempt to pass MAVERICK or PUFFIN to parse_device_type() returns
a GooseError rather than a valid DeviceType (per D-10).

Output:
- DeviceCapabilitiesArgs struct (device_kind: DeviceKind field)
- device_capabilities_bridge() function
- "device.capabilities" added to BRIDGE_METHODS constant (alphabetically)
- "device.capabilities" match arm in the dispatcher
- MAVERICK/PUFFIN arms removed from parse_device_type() in bridge.rs
- Rust unit tests: rejected variants, accepted variants, capabilities response roundtrip
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-CONTEXT.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-RESEARCH.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-PATTERNS.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add device.capabilities bridge method and reject MAVERICK/PUFFIN</name>
  <files>Rust/core/src/bridge.rs</files>
  <read_first>
    - Rust/core/src/bridge.rs lines 180-240 — BRIDGE_METHODS constant; "debug.start_session"
      is at line 232 and "diagnostics.perf_budget" is at line 233; "device.capabilities"
      inserts between these two lines
    - Rust/core/src/bridge.rs lines 2250-2260 — existing match arm pattern to copy
      (request_args::<T> + and_then + map + unwrap_or_else shape)
    - Rust/core/src/bridge.rs lines 9505-9530 — parse_device_type() function;
      MAVERICK and PUFFIN arms are removed from this function only;
      the three fixture parsers (capture_import.rs, fixtures.rs, capture_correlation.rs)
      must NOT be changed
    - Rust/core/src/bridge.rs lines 9505-9515 — request_args::<T> helper shape
    - Rust/core/src/capabilities.rs — DeviceKind, DeviceCapabilities (produced by Plan 01)
    - .planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-PATTERNS.md
      bridge.rs section — exact arm shape and BRIDGE_METHODS insertion point
    - .planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-RESEARCH.md
      Pattern 2 — bridge method pattern and Pitfall 1 (BRIDGE_METHODS out of sync)
  </read_first>
  <behavior>
    - bridge.request("device.capabilities", {"device_kind": "WHOOP4"}) returns
      DeviceCapabilities JSON with wire_protocol="gen4", historical_sync="page_sequence"
    - bridge.request("device.capabilities", {"device_kind": "WHOOP5"}) returns
      DeviceCapabilities JSON with wire_protocol="gen5", historical_sync="stream"
    - bridge.request("device.capabilities", {"device_kind": "HR_MONITOR"}) returns
      DeviceCapabilities JSON with battery_via_r22=false, battery_via_event48=false
    - parse_device_type("MAVERICK") returns Err(GooseError)
    - parse_device_type("PUFFIN") returns Err(GooseError)
    - parse_device_type("GEN4") returns Ok(DeviceType::Gen4)
    - parse_device_type("GEN_4") returns Ok(DeviceType::Gen4)
    - parse_device_type("GOOSE") returns Ok(DeviceType::Goose)
    - parse_device_type("HR_MONITOR") returns Ok(DeviceType::HrMonitor)
    - bridge_methods_constant_matches_dispatcher test still passes
  </behavior>
  <action>
1. At the top of bridge.rs (or near the capabilities import section), add:
   use crate::capabilities::{DeviceCapabilities, DeviceKind};

2. Define DeviceCapabilitiesArgs struct near the other args structs:
   #[derive(Debug, Deserialize)]
   struct DeviceCapabilitiesArgs {
     device_kind: DeviceKind,
   }

3. Define device_capabilities_bridge function:
   fn device_capabilities_bridge(args: DeviceCapabilitiesArgs) -> GooseResult<serde_json::Value> {
     let caps = DeviceCapabilities::for_kind(args.device_kind);
     serde_json::to_value(caps).map_err(|e| GooseError::message(e.to_string()))
   }

4. In the BRIDGE_METHODS constant (lines 183-332), insert "device.capabilities"
   between "debug.start_session" and "diagnostics.perf_budget" to maintain alphabetical
   sort. This is a hard requirement — the bridge_methods_constant_matches_dispatcher
   test will fail if the constant and the dispatcher are not in sync.

5. In the match dispatcher, add the "device.capabilities" arm following the exact shape
   of existing arms (PATTERNS.md bridge.rs section):
     "device.capabilities" => request_args::<DeviceCapabilitiesArgs>(&request)
         .and_then(device_capabilities_bridge)
         .map(|value| bridge_ok(&request.request_id, value))
         .unwrap_or_else(|error| bridge_error(&request.request_id, "method_error", error)),

6. In parse_device_type() (around line 9515), remove the MAVERICK and PUFFIN match arms:
   Remove: "MAVERICK" | "Maverick" | "maverick" => Ok(DeviceType::Maverick),
   Remove: "PUFFIN" | "Puffin" | "puffin" => Ok(DeviceType::Puffin),
   These two lines only — the catch-all error arm and all other arms remain unchanged.
   CRITICAL: Only modify bridge.rs parse_device_type(). Do NOT touch expected_device_type()
   in capture_import.rs, fixtures.rs, or capture_correlation.rs.

7. Add unit tests in bridge.rs #[cfg(test)] block (or existing bridge test file):
   - test_parse_device_type_maverick_rejected: assert parse_device_type("MAVERICK").is_err()
   - test_parse_device_type_puffin_rejected: assert parse_device_type("PUFFIN").is_err()
   - test_parse_device_type_canonical_accepted: assert parse_device_type("GEN4").is_ok(),
     parse_device_type("GEN_4").is_ok(), parse_device_type("GOOSE").is_ok(),
     parse_device_type("HR_MONITOR").is_ok()
   - test_device_capabilities_bridge_whoop4: call device_capabilities_bridge directly,
     assert wire_protocol field deserialized is "gen4"

   Test function names must contain "parse_device_type" or "device_capabilities_bridge".

Run: cargo test --locked -p goose-core bridge_methods_constant_matches_dispatcher 2>&1 | tail -5
Run: cargo test --locked -p goose-core parse_device_type 2>&1 | tail -10
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked -p goose-core bridge_methods_constant_matches_dispatcher 2>&1 | tail -5 && cargo test --locked -p goose-core parse_device_type 2>&1 | tail -5 && cargo test --locked -p goose-core device_capabilities_bridge 2>&1 | tail -5 && cargo test --locked -p goose-core import_frame_batch_rejects_legacy 2>&1 | tail -5</automated>
  </verify>
  <done>
    - "device.capabilities" is in BRIDGE_METHODS between "debug.start_session" and "diagnostics.perf_budget"
    - "device.capabilities" match arm exists using request_args::<DeviceCapabilitiesArgs> shape
    - parse_device_type("MAVERICK") and parse_device_type("PUFFIN") return Err
    - parse_device_type("GEN4"), "GEN_4", "GOOSE", "HR_MONITOR" still return Ok
    - bridge_methods_constant_matches_dispatcher test passes
    - cargo test --locked passes clean
  </done>
</task>


<task type="auto" tdd="true">
  <name>Task 2: Add capture.import_frame_batch regression test proving legacy device_types are rejected (HIGH-1)</name>
  <files>Rust/core/tests/bridge_tests.rs</files>
  <read_first>
    - Rust/core/tests/bridge_tests.rs lines 460-500 — existing GEN4 capture.import_frame_batch
      test; copy the request shape; test name must contain "import_frame_batch_rejects_legacy"
    - Rust/core/src/capture_import.rs lines 70-80 — device_type field uses serde default
      (DeviceType::Goose); supplying "MAVERICK" tests the deserialization path directly
  </read_first>
  <behavior>
    - capture.import_frame_batch with device_type="MAVERICK" returns an error response
      (ok: false or the row is stored as GOOSE after migration — the key constraint is
      that MAVERICK is not persisted to decoded_frames.device_type after Plan 02 migration
      has run and after Plan 03 parse_device_type rejects it)
    - capture.import_frame_batch with device_type="PUFFIN" behaves the same way
    - The test verifies the store does not contain a MAVERICK or PUFFIN device_type row
      after the call
  </behavior>
  <action>
In Rust/core/tests/bridge_tests.rs, add two test functions after the existing
GEN4 capture.import_frame_batch test (around line 495):

test_capture_import_frame_batch_rejects_maverick:
  1. Create an in-memory DB (use the same helper as the existing GEN4 test).
  2. Build a capture.import_frame_batch JSON request with one frame entry where
     device_type is "MAVERICK" (use the same hex bytes as the GEN4 test for simplicity;
     only the device_type changes).
  3. Send via the bridge handle_json path (or call the bridge function directly).
  4. Assert that the response is either an error response (ok: false) OR that
     querying decoded_frames WHERE device_type = 'MAVERICK' returns 0 rows.
     Both outcomes are acceptable — the critical constraint is no MAVERICK row persists.

test_capture_import_frame_batch_rejects_puffin:
  Same shape as above with device_type = "PUFFIN".

NOTE on capture_import.rs internals: capture_import.rs:76 uses
#[serde(default = "default_device_type")] which defaults to DeviceType::Goose.
If "MAVERICK" is not in the serde enum variants (because parse_device_type was the
only recognizer and its MAVERICK arm is removed in Task 1 of this plan), serde will
use the default (Goose). If serde does recognize "MAVERICK" via the DeviceType enum
directly (different code path from parse_device_type), the row will be stored as MAVERICK.
The test must verify which path is taken and confirm that no MAVERICK row reaches the DB.
If DeviceType still deserializes MAVERICK via serde (DeviceType enum itself), add a
normalization step: after import, verify that device_type_name() returns "MAVERICK" for
the variant but the column is updated by migration. This is a diagnostic test — its
primary value is revealing the actual behavior of the import path post-migration.

Test function names must contain "import_frame_batch_rejects_legacy" so the filter works.

Run: cargo test --locked -p goose-core import_frame_batch_rejects_legacy 2>&1 | tail -10
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked -p goose-core import_frame_batch_rejects_legacy 2>&1 | tail -10</automated>
  </verify>
  <done>
    - test_capture_import_frame_batch_rejects_maverick exists in bridge_tests.rs
    - test_capture_import_frame_batch_rejects_puffin exists in bridge_tests.rs
    - Both tests pass: zero MAVERICK/PUFFIN rows in decoded_frames after import calls
    - cargo test --locked passes clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift FFI → device.capabilities | device_kind string input deserialised by serde; unknown values return serde error not panic |
| Swift FFI → parse_device_type | MAVERICK/PUFFIN now return GooseError; caller must handle Err |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-04 | Tampering | device.capabilities with unknown device_kind | mitigate | serde unknown variant returns Err; request_args propagates via and_then to bridge_error response |
| T-83-05 | Tampering | MAVERICK/PUFFIN injected via bridge call after migration | mitigate | parse_device_type rejects both with GooseError; no DeviceType produced |
| T-83-SC | Tampering | cargo installs | accept | No new packages; Cargo.lock unchanged |
</threat_model>

<verification>
cd /Users/francisco/Documents/goose/Rust/core && grep -n "device.capabilities" src/bridge.rs | head -5
cd /Users/francisco/Documents/goose/Rust/core && grep -n "MAVERICK\|PUFFIN" src/bridge.rs | grep "parse_device" | head -5
cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked -p goose-core bridge_methods_constant_matches_dispatcher 2>&1 | tail -5
cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | tail -10
</verification>

<success_criteria>
- "device.capabilities" appears in BRIDGE_METHODS and in the match dispatcher
- MAVERICK/PUFFIN arms removed from bridge.rs parse_device_type() only
- Fixture parsers (capture_import.rs, fixtures.rs, capture_correlation.rs) unchanged
- bridge_methods_constant_matches_dispatcher passes; cargo test --locked clean
- capture.import_frame_batch regression tests pass: MAVERICK/PUFFIN produce zero persisted rows
</success_criteria>

## Artifacts This Phase Produces

| Symbol | File | Kind |
|--------|------|------|
| `DeviceCapabilitiesArgs` | `Rust/core/src/bridge.rs` | struct |
| `device_capabilities_bridge()` | `Rust/core/src/bridge.rs` | fn |
| `"device.capabilities"` | `Rust/core/src/bridge.rs` | bridge method |
| MAVERICK/PUFFIN rejection | `Rust/core/src/bridge.rs` | parse_device_type change |
| import_frame_batch regression tests | `Rust/core/tests/bridge_tests.rs` | tests |

<output>
Create `.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-03-SUMMARY.md` when done
</output>
