---
phase: 83
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/protocol.rs
  - Rust/core/src/capabilities.rs
  - Rust/core/src/lib.rs
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02

must_haves:
  truths:
    - "WireProtocol enum with Gen4/Gen5 variants exists in protocol.rs"
    - "DeviceType gains wire_protocol(), device_kind(), and is_gen5_family() methods"
    - "DeviceKind enum with Whoop4/Whoop5/HrMonitor variants exists in capabilities.rs"
    - "DeviceCapabilities struct with for_kind() factory exists in capabilities.rs"
    - "lib.rs declares pub mod capabilities"
    - "cargo test --locked passes with no regressions"
  artifacts:
    - path: "Rust/core/src/capabilities.rs"
      provides: "DeviceKind enum + DeviceCapabilities struct + for_kind() impl"
      contains: "DeviceKind"
    - path: "Rust/core/src/protocol.rs"
      provides: "WireProtocol enum + DeviceType method extensions"
      contains: "WireProtocol"
    - path: "Rust/core/src/lib.rs"
      provides: "Module declaration for capabilities"
      contains: "pub mod capabilities"
  key_links:
    - from: "Rust/core/src/bridge.rs"
      to: "Rust/core/src/capabilities.rs"
      via: "use crate::capabilities::{DeviceCapabilities, DeviceKind}"
      pattern: "DeviceCapabilities"
    - from: "Rust/core/src/protocol.rs"
      to: "WireProtocol"
      via: "DeviceType::wire_protocol() -> WireProtocol"
      pattern: "wire_protocol"
---

<objective>
Add the Rust foundation types for Phase 83: the WireProtocol enum in protocol.rs,
method extensions on DeviceType (wire_protocol, device_kind, is_gen5_family), and
a new capabilities.rs module containing DeviceKind and DeviceCapabilities.

Purpose: These types are the source of truth consumed by the bridge method (Plan 03)
and by Swift via JSON deserialization (Plans 04-05). All design decisions are finalised
in 83-CONTEXT.md — no architectural choices are left to the executor.

Output:
- WireProtocol { Gen4, Gen5 } in protocol.rs (per D-12)
- DeviceType.wire_protocol(), device_kind(), is_gen5_family() in protocol.rs (per D-12, D-13, D-15)
- capabilities.rs: DeviceKind { Whoop4, Whoop5, HrMonitor } + DeviceCapabilities (per D-13, D-14)
- lib.rs: pub mod capabilities (alphabetical, between calibration and capture_correlation)
- Rust unit tests for all new methods and DeviceCapabilities::for_kind() per DeviceKind
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-CONTEXT.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-RESEARCH.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WireProtocol enum and DeviceType method extensions to protocol.rs</name>
  <files>Rust/core/src/protocol.rs</files>
  <read_first>
    - Rust/core/src/protocol.rs (full file) — existing DeviceType enum at lines 25-33,
      existing impl DeviceType block at lines 35-68; WireProtocol goes after line 68
  </read_first>
  <behavior>
    - DeviceType::Gen4.wire_protocol() == WireProtocol::Gen4
    - DeviceType::Goose.wire_protocol() == WireProtocol::Gen5
    - DeviceType::Maverick.wire_protocol() == WireProtocol::Gen5
    - DeviceType::Puffin.wire_protocol() == WireProtocol::Gen5
    - DeviceType::HrMonitor.wire_protocol() == WireProtocol::Gen5
    - DeviceType::Gen4.is_gen5_family() == false
    - DeviceType::Goose.is_gen5_family() == true
    - DeviceType::HrMonitor.is_gen5_family() == true
    - DeviceType::Gen4.device_kind() == DeviceKind::Whoop4
    - DeviceType::Goose.device_kind() == DeviceKind::Whoop5
    - DeviceType::Maverick.device_kind() == DeviceKind::Whoop5
    - DeviceType::Puffin.device_kind() == DeviceKind::Whoop5 (per D-16)
    - DeviceType::HrMonitor.device_kind() == DeviceKind::HrMonitor
  </behavior>
  <action>
Add to protocol.rs immediately after the closing brace of the existing impl DeviceType
block (after line 68):

1. Add WireProtocol enum with derives (#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)],
   #[serde(rename_all = "snake_case")]) and two variants: Gen4, Gen5.
   Use snake_case serde rename (per RESEARCH.md Pattern 1).

2. Extend the existing impl DeviceType block by adding three new methods after
   expected_frame_len:
   - pub fn wire_protocol(self) -> WireProtocol: Gen4 → WireProtocol::Gen4;
     Maverick | Puffin | Goose | HrMonitor → WireProtocol::Gen5
   - pub fn is_gen5_family(self) -> bool: use matches! macro,
     matches!(self, Maverick | Puffin | Goose | HrMonitor) — per D-15
   - pub fn device_kind(self) -> DeviceKind: Gen4 → DeviceKind::Whoop4;
     Maverick | Puffin | Goose → DeviceKind::Whoop5; HrMonitor → DeviceKind::HrMonitor —
     per D-13. Note: DeviceKind is defined in capabilities.rs; add
     "use crate::capabilities::DeviceKind;" import at top of protocol.rs.

3. Add a Puffin doc comment on the DeviceType enum variant (per D-16):
   "Hardware code name with no known generation mapping — likely unshipped.
   Parses as Gen5-family wire format (8-byte header)."

   NOTE on openwhoop_reference.rs (MEDIUM-6): openwhoop_reference.rs:166 maps Puffin
   to None in whoop_generation_from_device_type() with an assertion test. This is a
   separate, orthogonal context — that function maps DeviceType to an Option<WhoopGeneration>
   for command-frame generation purposes, where Puffin has no known generation. Do NOT
   change openwhoop_reference.rs. The new device_kind() method maps Puffin to
   DeviceKind::Whoop5 for wire-protocol selection only (8-byte Gen5 frame header),
   which does not conflict with whoop_generation_from_device_type() returning None.

4. Write unit tests in a #[cfg(test)] mod at the bottom of protocol.rs covering
   all behavior cases above. Test module name: wire_protocol_tests.
   Run: cargo test --locked -p goose-core wire_protocol_tests 2>&1 | tail -10
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked -p goose-core wire_protocol_tests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - WireProtocol enum with Gen4 and Gen5 variants exists in protocol.rs
    - DeviceType gains wire_protocol(), is_gen5_family(), device_kind() methods
    - Puffin variant has doc comment per D-16
    - All wire_protocol_tests pass
    - cargo test --locked passes clean (no regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create capabilities.rs with DeviceKind, DeviceCapabilities, and unit tests</name>
  <files>Rust/core/src/capabilities.rs, Rust/core/src/lib.rs</files>
  <read_first>
    - Rust/core/src/lib.rs (full file) — existing pub mod declarations at lines 18-60;
      capabilities goes between calibration (line 24) and capture_correlation (line 25)
    - Rust/core/src/protocol.rs — DeviceType enum (needed for context on the mapping)
    - .planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-RESEARCH.md
      Code Examples section — DeviceCapabilities struct and for_kind() values
  </read_first>
  <behavior>
    - DeviceCapabilities::for_kind(DeviceKind::Whoop4) returns:
        wire_protocol = "gen4", historical_sync = "page_sequence",
        battery_via_r22 = false, battery_via_event48 = true,
        battery_via_cmd26 = true, r22_realtime = false
    - DeviceCapabilities::for_kind(DeviceKind::Whoop5) returns:
        wire_protocol = "gen5", historical_sync = "stream",
        battery_via_r22 = true, battery_via_event48 = true,
        battery_via_cmd26 = true, r22_realtime = true
    - DeviceCapabilities::for_kind(DeviceKind::HrMonitor) returns:
        wire_protocol = "gen5", historical_sync = "stream",
        battery_via_r22 = false, battery_via_event48 = false,
        battery_via_cmd26 = false, r22_realtime = false
    - DeviceKind can be deserialised from "WHOOP4", "WHOOP5", "HR_MONITOR" strings
      (serde SCREAMING_SNAKE_CASE rename to match what Swift sends)
    - DeviceCapabilities serialises to snake_case JSON keys
  </behavior>
  <action>
1. Create Rust/core/src/capabilities.rs with:
   - Imports: use serde::{Deserialize, Serialize};
   - DeviceKind enum: #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)],
     #[serde(rename_all = "SCREAMING_SNAKE_CASE")], variants: Whoop4, Whoop5, HrMonitor.
     SCREAMING_SNAKE_CASE produces "WHOOP4", "WHOOP5", "HR_MONITOR" matching Swift's send.
   - DeviceCapabilities struct: #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)],
     #[serde(rename_all = "snake_case")], fields:
       pub wire_protocol: String, pub historical_sync: String,
       pub battery_via_r22: bool, pub battery_via_event48: bool,
       pub battery_via_cmd26: bool, pub r22_realtime: bool
   - impl DeviceCapabilities: pub fn for_kind(kind: DeviceKind) -> Self
     with match arms as specified in RESEARCH.md Code Examples section.
   - #[cfg(test)] mod capabilities_tests with test functions for each DeviceKind variant
     and serde roundtrip test (serialize then deserialize, assert_eq! — requires PartialEq
     derive on DeviceCapabilities, which is added in this step).

2. In Rust/core/src/lib.rs:
   Insert "pub mod capabilities;" between "pub mod calibration;" and
   "pub mod capture_correlation;" (alphabetical order, per PATTERNS.md).

Run: cargo test --locked -p goose-core capabilities_tests 2>&1 | tail -10
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked -p goose-core capabilities_tests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Rust/core/src/capabilities.rs exists with DeviceKind + DeviceCapabilities + for_kind()
    - lib.rs declares pub mod capabilities in alphabetical position
    - capabilities_tests pass for all three DeviceKind variants
    - cargo test --locked passes clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift FFI → bridge.rs | Swift sends device_kind string; Rust deserializes via serde |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | DeviceKind deserialization in capabilities.rs | mitigate | serde rejects unknown variants with error by default; no panic path |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | no new packages in this phase; Cargo.lock unchanged |
</threat_model>

<verification>
cargo test --locked -p goose-core wire_protocol_tests 2>&1 | tail -5
cargo test --locked -p goose-core capabilities_tests 2>&1 | tail -5
cargo test --locked 2>&1 | tail -10
grep -n "pub enum WireProtocol" /Users/francisco/Documents/goose/Rust/core/src/protocol.rs
grep -n "pub struct DeviceCapabilities" /Users/francisco/Documents/goose/Rust/core/src/capabilities.rs
grep -n "pub mod capabilities" /Users/francisco/Documents/goose/Rust/core/src/lib.rs
</verification>

<success_criteria>
- WireProtocol { Gen4, Gen5 } enum exists in protocol.rs with snake_case serde rename
- DeviceType gains wire_protocol(), is_gen5_family(), device_kind() methods
- capabilities.rs exists with DeviceKind { Whoop4, Whoop5, HrMonitor } and DeviceCapabilities::for_kind()
- lib.rs declares pub mod capabilities in alphabetical order
- All unit tests pass; cargo test --locked clean
</success_criteria>

## Artifacts This Phase Produces

| Symbol | File | Kind |
|--------|------|------|
| `WireProtocol` | `Rust/core/src/protocol.rs` | `pub enum` |
| `DeviceType::wire_protocol()` | `Rust/core/src/protocol.rs` | method |
| `DeviceType::is_gen5_family()` | `Rust/core/src/protocol.rs` | method |
| `DeviceType::device_kind()` | `Rust/core/src/protocol.rs` | method |
| `DeviceKind` | `Rust/core/src/capabilities.rs` | `pub enum` |
| `DeviceCapabilities` | `Rust/core/src/capabilities.rs` | `pub struct` |
| `DeviceCapabilities::for_kind()` | `Rust/core/src/capabilities.rs` | `pub fn` |
| `pub mod capabilities` | `Rust/core/src/lib.rs` | module declaration |

<output>
Create `.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-01-SUMMARY.md` when done
</output>
