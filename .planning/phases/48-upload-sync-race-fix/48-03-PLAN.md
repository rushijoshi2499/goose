---
phase: 48-upload-sync-race-fix
plan: 03
type: execute
wave: 2
depends_on:
  - 48-02
files_modified:
  - GooseSwiftTests/GooseUploadServiceTests.swift
autonomous: true
requirements:
  - SYNCR-01

must_haves:
  truths:
    - "test_upload503_leavesSynced0 passes: a 503 response leaves the service without calling markStreamsSynced"
    - "test_upload200_marksSynced1 passes: a 200 response causes markStreamsSynced to be called (observable via URLSession mock)"
    - "All existing GooseUploadServiceTests still pass"
  artifacts:
    - path: "GooseSwiftTests/GooseUploadServiceTests.swift"
      provides: "MockURLProtocol, test_upload503_leavesSynced0, test_upload200_marksSynced1"
      contains: "MockURLProtocol"
  key_links:
    - from: "test_upload503_leavesSynced0"
      to: "GooseUploadService(databasePath:session:)"
      via: "URLSession(configuration:) with MockURLProtocol"
      pattern: "init(databasePath:.*session:"
---

<objective>
Add two Swift unit tests to `GooseSwiftTests/GooseUploadServiceTests.swift` that prove the race fix orchestration is correct: a 503 response must leave rows unsynced, and a 200 response must trigger the mark path (per D-05).

These tests use URLSession injection (via `init(databasePath:session:)` added in Plan 02) and a `MockURLProtocol` to simulate HTTP responses without a real server. Because `GooseUploadService` accesses SQLite via the Rust bridge (which requires a real database file), the tests use a temp directory database instead of `/dev/null` for the upload path tests.

Purpose: Close the XCTest coverage gap identified in VALIDATION.md (SYNCR-01a and SYNCR-01b). The tests verify Swift orchestration — not Rust persistence, which is covered by Plan 01's Rust test.

Output: Two new test methods and a `MockURLProtocol` class in `GooseSwiftTests/GooseUploadServiceTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-upload-sync-race-fix/48-CONTEXT.md
@.planning/phases/48-upload-sync-race-fix/48-RESEARCH.md
@.planning/phases/48-upload-sync-race-fix/48-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MockURLProtocol and race-fix XCTest methods</name>
  <files>GooseSwiftTests/GooseUploadServiceTests.swift</files>

  <read_first>
    Read GooseSwiftTests/GooseUploadServiceTests.swift in full — to see the existing test class structure, the existing service property, and the resolveUploadSourceContent helper — before adding any code.
    Read GooseSwift/GooseUploadService.swift lines 1–50 — to confirm init(databasePath:session:) was added by Plan 02 and to see the exact signature.
    Read .planning/phases/48-upload-sync-race-fix/48-RESEARCH.md section "Pattern 2: URLSession injection for unit testing" — for the verified MockURLProtocol pattern.
  </read_first>

  <behavior>
    - test_upload503_leavesSynced0: MockURLProtocol returns HTTPURLResponse with statusCode=503. GooseUploadService.performUpload completes (all 3 retry attempts). The URLSession mock records that the request was made 3 times (one per retry). markStreamsSynced is NOT called — verifiable indirectly because the mock tracks call count and no sync bridge call follows a 503 in the fixed implementation. Assertion: the upload attempt completes without crash; request count == 3 (exhausted retries).
    - test_upload200_marksSynced1: MockURLProtocol returns HTTPURLResponse with statusCode=200 and JSON body {"upserted": {"hr": 5}}. The upload completes after the first attempt. markStreamsSynced IS called — verifiable by checking that the bridge call sync.mark_synced is invoked. Since the test database is /dev/null (Rust bridge returns empty rows_pending_upload on a non-existent DB), markStreamsSynced will have empty rowIDsByStream and skip all mark_synced calls — which is correct behavior and does not indicate a bug. The assertion focuses on the HTTP layer: request count == 1 (success on first try).
    - MockURLProtocol is defined as a private (file-scope) final class in the test file. It uses a static var `handler: ((URLRequest) -> (HTTPURLResponse, Data?))?` so tests can set/clear it. tearDown calls `MockURLProtocol.handler = nil` to prevent state leakage between tests.
    - The test class already has `private let service = GooseUploadService(databasePath: "/dev/null")`. The race-fix tests create their own local service instances using `init(databasePath:session:)` with a URLSession backed by MockURLProtocol — do not reuse the class-level service property for these tests.
    - Both tests call `service.upload(deviceID: UUID(), deviceType: "GEN4", sinceTimestamp: Date())` which dispatches `Task.detached`. Use `await Task.sleep(nanoseconds: 100_000_000)` (0.1s) to let the detached task complete before asserting request counts. This is acceptable for a unit test — the upload logic is fast when the Rust bridge returns empty data.
  </behavior>

  <action>
    Append to GooseSwiftTests/GooseUploadServiceTests.swift (after the closing brace of the existing `GooseUploadServiceTests` class, at the end of the file):

    SECTION 1 — MockURLProtocol class (file scope, not inside GooseUploadServiceTests):

    Define `private final class MockURLProtocol: URLProtocol`. Properties:
    - `static var handler: ((URLRequest) -> (HTTPURLResponse, Data?))?`
    - `static var requestCount: Int = 0`

    Override `class func canInit(with request:)` → return true.
    Override `class func canonicalRequest(for:)` → return request.
    Override `func startLoading()`:
    - `MockURLProtocol.requestCount += 1`
    - Guard `let handler = MockURLProtocol.handler` else call `client?.urlProtocolDidFinishLoading(self)` and return.
    - Let `(response, data) = handler(request)`.
    - Call `client?.urlProtocol(self, didReceive: response, cacheStoragePolicy: .notAllowed)`.
    - If data is non-nil, call `client?.urlProtocol(self, didLoad: data)`.
    - Call `client?.urlProtocolDidFinishLoading(self)`.
    Override `func stopLoading()` → no-op.

    SECTION 2 — Extension on GooseUploadServiceTests with MARK // MARK: - Race fix tests:

    Helper `private func makeMockSession() -> URLSession`:
    - Creates `URLSessionConfiguration.ephemeral`.
    - Sets `config.protocolClasses = [MockURLProtocol.self]`.
    - Returns `URLSession(configuration: config)`.

    `override func tearDown()`:
    - `MockURLProtocol.handler = nil`
    - `MockURLProtocol.requestCount = 0`
    - `super.tearDown()`

    `func test_upload503_leavesSynced0() async`:
    - Set `MockURLProtocol.requestCount = 0`.
    - Set `MockURLProtocol.handler = { request in (HTTPURLResponse(url: request.url!, statusCode: 503, httpVersion: nil, headerFields: nil)!, nil) }`.
    - Create `let svc = GooseUploadService(databasePath: "/dev/null", session: makeMockSession())`.
    - Call `svc.upload(deviceID: UUID(), deviceType: "GEN4", sinceTimestamp: Date())`.
    - `try? await Task.sleep(nanoseconds: 8_000_000_000)` — 8 seconds to allow 3 retry attempts with backoff (1s + 2s + 4s delays in the retry loop, plus bridge calls which are fast on /dev/null).
    - `XCTAssertEqual(MockURLProtocol.requestCount, 3, "503 should exhaust all 3 retry attempts")`.

    `func test_upload200_marksSynced1() async`:
    - Set `MockURLProtocol.requestCount = 0`.
    - Prepare response body: `let body = try! JSONSerialization.data(withJSONObject: ["upserted": ["hr": 5]])`.
    - Set `MockURLProtocol.handler = { request in (HTTPURLResponse(url: request.url!, statusCode: 200, httpVersion: nil, headerFields: nil)!, body) }`.
    - Create `let svc = GooseUploadService(databasePath: "/dev/null", session: makeMockSession())`.
    - Call `svc.upload(deviceID: UUID(), deviceType: "GEN4", sinceTimestamp: Date())`.
    - `try? await Task.sleep(nanoseconds: 500_000_000)` — 0.5 seconds (no retries expected on 200; bridge calls on /dev/null are instant).
    - `XCTAssertEqual(MockURLProtocol.requestCount, 1, "200 should succeed on first attempt without retrying")`.

    Note on /dev/null database: `GooseUploadService` with databasePath "/dev/null" will have `upload.get_recent_decoded_streams` return empty streams, so `hasData` will be false and `performUpload` will return early WITHOUT making any HTTP request. To get past the hasData guard, the test must either:
    - Use a real temporary database with seeded rows, OR
    - Accept that the test validates the HTTP layer mock setup is correct but performUpload exits early before reaching the HTTP call.

    Since the goal is testing the HTTP 503→no-mark / 200→mark orchestration, use a temporary SQLite file with at minimum one seeded hr_samples row so `get_recent_decoded_streams` returns non-empty data. Create the temp DB using `FileManager.default.temporaryDirectory.appendingPathComponent("goose-test-\(UUID().uuidString).sqlite").path` and pass it as databasePath. In `tearDown`, delete the temp file.

    Add `private var tempDBPath: String = ""` to the test class extension. In `setUp()` (override), create the temp DB path. In `tearDown()`, delete it with `try? FileManager.default.removeItem(atPath: tempDBPath)`.

    For seeding: call `GooseUploadService(databasePath: tempDBPath).refreshPendingRowCount()` first to trigger Rust schema init, then insert a row via a GooseRustBridge call: `GooseRustBridge().request(method: "sync.backfill_streams", args: ["database_path": tempDBPath, "device_id": testID.uuidString, "start_ts": 0.0, "end_ts": Date().timeIntervalSince1970])` — this may fail on an empty DB but the bridge call ensures the schema is created. Alternatively, seed by calling `GooseRustBridge().request(method: "debug.schema_version", args: ["database_path": tempDBPath])` to init the schema. The exact seeding mechanism is at implementer's discretion; what matters is that `get_recent_decoded_streams` returns at least one non-empty stream so performUpload reaches the HTTP retry loop.

    If seeding cannot produce non-empty streams in unit test context, fallback: test the mock plumbing by asserting MockURLProtocol.requestCount directly after confirming the upload task ran (even if it exited early due to empty data — requestCount will be 0, and the test should be documented as validating the mock infrastructure rather than the full orchestration path). Add `XCTSkip("Temp DB seeding required for full orchestration test — mock infrastructure verified")` in this case.
  </action>

  <verify>
    <automated>xcodebuild test -project GooseSwift.xcodeproj -scheme GooseSwift -destination "platform=iOS Simulator,name=iPhone 16" -only-testing:GooseSwiftTests/GooseUploadServiceTests -quiet 2>&1 | grep -E "Test.*passed|Test.*failed|error:" | tail -20</automated>
  </verify>

  <done>
    All GooseUploadServiceTests pass. The output includes `test_upload503_leavesSynced0` and `test_upload200_marksSynced1` in the passing list (or XCTSkip if temp DB seeding is not feasible in CI). No previously passing tests regress. MockURLProtocol is defined in the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → URLSession mock | MockURLProtocol intercepts outbound HTTP in test only — no production traffic affected |
| test → SQLite temp file | Temp database is created in system temp dir and deleted in tearDown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05 | Information Disclosure | temp SQLite file in tearDown not deleted on test crash | accept | Low risk — test data only; FileManager.removeItem in tearDown covers normal path; OS cleans temp dir on next boot |
| T-48-06 | Tampering | MockURLProtocol.handler static state leaking between tests | mitigate | tearDown resets handler = nil and requestCount = 0 before each test |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No new packages — XCTest is Xcode built-in |
</threat_model>

<verification>
After task completion:

1. All existing tests still pass: xcodebuild test -project GooseSwift.xcodeproj -scheme GooseSwift -destination "platform=iOS Simulator,name=iPhone 16" -only-testing:GooseSwiftTests/GooseUploadServiceTests -quiet 2>&1 | grep -E "passed|failed" → all lines say "passed".

2. New test methods present: grep -c "test_upload503_leavesSynced0\|test_upload200_marksSynced1" GooseSwiftTests/GooseUploadServiceTests.swift → must be 2.

3. MockURLProtocol defined: grep -c "MockURLProtocol" GooseSwiftTests/GooseUploadServiceTests.swift → must be >= 3 (class def + at least 2 usages).

4. Rust suite still green: cargo test -p goose-core --manifest-path Rust/core/Cargo.toml 2>&1 | tail -3 → "test result: ok".
</verification>

<success_criteria>
- test_upload503_leavesSynced0 and test_upload200_marksSynced1 exist and pass (or XCTSkip with documented reason)
- MockURLProtocol class is defined in GooseSwiftTests/GooseUploadServiceTests.swift
- All previously passing GooseUploadServiceTests continue to pass
- No new compile errors in the test target
</success_criteria>

## Artifacts this phase produces

New symbols created in GooseSwiftTests/GooseUploadServiceTests.swift:
- `MockURLProtocol` — file-scope final class implementing URLProtocol for HTTP mocking; properties: `static var handler`, `static var requestCount`
- `GooseUploadServiceTests.test_upload503_leavesSynced0()` — async XCTest method; asserts 3 HTTP attempts on 503 (SYNCR-01a)
- `GooseUploadServiceTests.test_upload200_marksSynced1()` — async XCTest method; asserts 1 HTTP attempt on 200 (SYNCR-01b)
- `GooseUploadServiceTests.makeMockSession() -> URLSession` — helper returning URLSession backed by MockURLProtocol
- `GooseUploadServiceTests.setUp()` / `tearDown()` overrides — temp DB lifecycle + mock state reset

<output>
Create `.planning/phases/48-upload-sync-race-fix/48-03-SUMMARY.md` when done.
</output>
