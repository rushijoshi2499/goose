---
phase: 101-hps-telemetry-coach-crash-protocol-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseBLEHistoricalManager.swift
  - GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
  - Rust/core/src/store/mod.rs
  - Rust/core/src/bridge/mod.rs
  - Rust/core/tests/sync_telemetry_round_trip.rs
autonomous: true
requirements:
  - SYNC-12
must_haves:
  truths:
    - "Each historical sync burst logs bytes received, duration_ms, and gap count to the Debug > Logs view"
    - "A sync_telemetry row is persisted in SQLite for every completed burst"
    - "CURRENT_SCHEMA_VERSION is 23 and existing databases migrate cleanly"
    - "sync.record_hps_telemetry bridge method is registered and dispatches correctly"
  artifacts:
    - path: "GooseSwift/GooseBLEHistoricalManager.swift"
      provides: "burstStartedAt: Date? and burstBytesReceived: Int fields"
    - path: "GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift"
      provides: "byte accumulation in handleHistoricalSyncValue; ble.record + bridge call in case .historyEnd"
    - path: "Rust/core/src/store/mod.rs"
      provides: "sync_telemetry table DDL in migrate(), CURRENT_SCHEMA_VERSION = 23, insert_sync_telemetry()"
    - path: "Rust/core/src/bridge/mod.rs"
      provides: "SyncRecordHpsTelemetryArgs, dispatcher arm, sync_record_hps_telemetry_bridge fn, BRIDGE_METHODS entry"
    - path: "Rust/core/tests/sync_telemetry_round_trip.rs"
      provides: "Rust integration test: insert row, assert round-trip"
  key_links:
    - from: "CoreBluetoothBLETransport+HistoricalHandlers.swift"
      to: "Rust/core/src/bridge/mod.rs"
      via: "bridge.request(method: 'sync.record_hps_telemetry', args:) on historicalWriteQueue"
      pattern: "sync\\.record_hps_telemetry"
    - from: "Rust/core/src/bridge/mod.rs"
      to: "Rust/core/src/store/mod.rs"
      via: "GooseStore::open + store.insert_sync_telemetry()"
      pattern: "insert_sync_telemetry"
---

<objective>
Implement HPS sync quality telemetry (SYNC-12): instrument burst boundaries in Swift to capture bytes received and duration, emit a real-time ble.record log per burst, and persist a row to a new sync_telemetry SQLite table via a new Rust bridge method. Schema migrates from v22 to v23.

Purpose: Gives operators and developers per-burst visibility into historical sync quality without needing device-attached Xcode instruments.
Output: New sync_telemetry table, new bridge method sync.record_hps_telemetry, two new fields on GooseBLEHistoricalManager, ble.record call per burst, Rust integration test. Closes issue #162.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GooseSwift/GooseBLEHistoricalManager.swift
@GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
@Rust/core/src/store/mod.rs
@Rust/core/src/bridge/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rust schema v23 + sync_telemetry store + bridge method</name>
  <files>
    Rust/core/src/store/mod.rs,
    Rust/core/src/bridge/mod.rs,
    Rust/core/tests/sync_telemetry_round_trip.rs
  </files>
  <behavior>
    - `CURRENT_SCHEMA_VERSION` == 23
    - `insert_sync_telemetry()` inserts a row; querying by session_id returns the row
    - `sync.record_hps_telemetry` is present in `BRIDGE_METHODS` constant
    - `bridge_methods_constant_matches_dispatcher` test passes
    - `sync_telemetry_round_trip` integration test: open temp db, call insert_sync_telemetry, assert row count == 1
  </behavior>
  <action>
**A. `Rust/core/src/store/mod.rs`**

1. Change `pub const CURRENT_SCHEMA_VERSION: i64 = 22;` to `= 23;` (line ~23).

2. In `migrate()`, append a new version 23 block immediately after the version 22 block. The block must execute these SQL statements in order:
   - `CREATE TABLE IF NOT EXISTS sync_telemetry (id INTEGER PRIMARY KEY AUTOINCREMENT, session_id TEXT NOT NULL, burst_index INTEGER NOT NULL, bytes_received INTEGER NOT NULL, duration_ms INTEGER NOT NULL, missing_packets INTEGER NOT NULL DEFAULT 0, sequence_gaps INTEGER NOT NULL DEFAULT 0, result TEXT NOT NULL DEFAULT 'ok', created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')));`
   - `CREATE INDEX IF NOT EXISTS idx_sync_telemetry_session ON sync_telemetry(session_id);`
   - `INSERT OR IGNORE INTO goose_schema_migrations(version) VALUES (23);`
   - `PRAGMA user_version = 23;`

3. Add `insert_sync_telemetry` as a public method on `GooseStore`. Signature: `pub fn insert_sync_telemetry(&self, session_id: &str, burst_index: i64, bytes_received: i64, duration_ms: i64, missing_packets: i64, sequence_gaps: i64, result: &str) -> GooseResult<()>`. Lock the connection mutex, execute an `INSERT INTO sync_telemetry` with the seven positional parameters.

**B. `Rust/core/src/bridge/mod.rs`**

Follow the five-location pattern exactly (cs:s3-130 rule — all 5 locations required or `bridge_methods_constant_matches_dispatcher` fails):

1. **BRIDGE_METHODS constant** — insert `"sync.record_hps_telemetry"` alphabetically in the existing `BRIDGE_METHODS` array.

2. **Args struct** — add `SyncRecordHpsTelemetryArgs` with fields: `database_path: String`, `session_id: String`, `burst_index: i64`, `bytes_received: i64`, `duration_ms: i64`, `missing_packets: i64`, `sequence_gaps: i64`, `result: String`. Derive `Debug, Clone, Deserialize`.

3. **Dispatcher arm** — in the `match method` block, add:
   `"sync.record_hps_telemetry" =>` arm calling `request_args::<SyncRecordHpsTelemetryArgs>(&request).and_then(sync_record_hps_telemetry_bridge).map(|v| bridge_ok(&request.request_id, v)).unwrap_or_else(|e| bridge_error(&request.request_id, e))`

4. **Bridge fn** — `fn sync_record_hps_telemetry_bridge(args: SyncRecordHpsTelemetryArgs) -> GooseResult<serde_json::Value>`: open store via `GooseStore::open(Path::new(&args.database_path))?`, call `store.insert_sync_telemetry(...)`, return `Ok(serde_json::json!({ "ok": true }))`.

5. **Store fn** — already added in step A above; no duplication needed here.

**C. `Rust/core/tests/sync_telemetry_round_trip.rs`**

Write an integration test `sync_telemetry_round_trip` that:
- Creates a temp db with `make_temp_db()` (follow pattern from existing integration tests in `Rust/core/tests/`).
- Calls `store.insert_sync_telemetry("sess-abc", 0, 4096, 320, 0, 0, "ok")`.
- Queries `SELECT COUNT(*) FROM sync_telemetry WHERE session_id = 'sess-abc'` and asserts count == 1.

Run `cargo test --locked --manifest-path Rust/core/Cargo.toml -- sync_telemetry` to ensure GREEN before moving to Task 2.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && cargo test --locked --manifest-path Rust/core/Cargo.toml -- sync_telemetry 2>&1 | tail -10</automated>
  </verify>
  <done>All sync_telemetry tests pass; bridge_methods_constant_matches_dispatcher passes; CURRENT_SCHEMA_VERSION is 23</done>
</task>

<task type="auto">
  <name>Task 2: Swift burst instrumentation — bytes, duration, ble.record, bridge call</name>
  <files>
    GooseSwift/GooseBLEHistoricalManager.swift,
    GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
  </files>
  <action>
**A. `GooseSwift/GooseBLEHistoricalManager.swift`**

Add two new stored properties to `GooseBLEHistoricalManager` (read the file first to find the correct insertion point near the existing tracking fields):
- `var burstStartedAt: Date? = nil`
- `var burstBytesReceived: Int = 0`

**B. `GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift`**

Three insertion points (read the file first; confirm line numbers against the research findings):

1. **`handleHistoricalSyncValue`** — after the existing processing code in that function, accumulate bytes per D-01:
   `historicalManager.burstBytesReceived += value.count`
   This is the raw BLE notification `Data` value arriving on each historical sync characteristic notification.

2. **`handleHistoricalMetadata` → `case .historyStart`** — reset both new fields immediately when a new burst starts (before or after existing reset logic, same scope):
   ```
   historicalManager.burstStartedAt = Date()
   historicalManager.burstBytesReceived = 0
   ```

3. **`handleHistoricalMetadata` → `case .historyEnd`** — after `historicalSyncBurstsCompleted += 1`, compute telemetry values and emit both the real-time log and the SQLite write (per D-01, both tracks required):

   Compute duration: `let burstDurationMs = historicalManager.burstStartedAt.map { Int(Date().timeIntervalSince($0) * 1000) } ?? 0`

   Capture the values into local constants before the async dispatch: `burstBytes`, `burstIndex` (use `historicalSyncBurstsCompleted` after the increment), `sessionID` from `historicalManager.historicalSyncRunID.uuidString`.

   **Real-time log** — call on the current queue (this closure runs on main; confirm before assuming):
   ```
   record(level: .debug, source: "ble.sync", title: "hps.telemetry",
          body: "session_id=\(sessionID) burst_index=\(burstIndex) bytes=\(burstBytes) duration_ms=\(burstDurationMs) gaps=0 result=ok")
   ```

   **SQLite write** — dispatch to `historicalWriteQueue` (use the same queue and bridge instance used in `flushPendingHistoricalFramesIfNeeded`; check the existing pattern on lines ~133–138 of this file). Inside the async block:
   ```
   let args: [String: Any] = [
     "database_path": historicalDirectWriteDatabasePath,
     "session_id": sessionID,
     "burst_index": burstIndex,
     "bytes_received": burstBytes,
     "duration_ms": burstDurationMs,
     "missing_packets": 0,
     "sequence_gaps": 0,   // SYNC-12: not yet implemented, always 0
     "result": "ok"
   ]
   _ = try? historicalDirectWriteBridge.request(method: "sync.record_hps_telemetry", args: args)
   ```

   Note on thread safety: `ble.record` must be called from the main thread (matches the existing pattern for `record()` in `VitalsAndLogging.swift`). If the `case .historyEnd` code path runs on a background queue, dispatch the `record()` call with `DispatchQueue.main.async { self?.record(...) }`. Confirm by reading the surrounding code context first.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -5</automated>
  </verify>
  <done>Swift builds with no errors; GooseBLEHistoricalManager has burstStartedAt and burstBytesReceived; historyEnd case emits ble.record and queues bridge call</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift → Rust FFI | Telemetry args dict crosses into bridge JSON; session_id is a UUID string from Swift — not user-controlled |
| SQLite write queue | historicalWriteQueue is a serial background queue; concurrent writes to sync_telemetry are serialized by the queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Tampering | sync_telemetry SQLite rows | accept | Internal diagnostic table; no user-facing data; no integrity requirement beyond schema |
| T-101-02 | Denial of Service | burst telemetry write loop | accept | One row per burst; typical sync is 1–5 bursts; write cost negligible |
| T-101-03 | Information Disclosure | ble.record body contains session_id | accept | session_id is a random UUID; diagnostic log is device-local; no PII |
| T-101-SC | Tampering | npm/pip/cargo installs | accept | No new Cargo dependencies introduced in this plan; existing lockfile unchanged |
</threat_model>

<verification>
1. `cargo test --locked --manifest-path Rust/core/Cargo.toml -- sync_telemetry` — all pass
2. `cargo test --locked --manifest-path Rust/core/Cargo.toml -- bridge_methods_constant` — passes
3. `xcodebuild ... build` — BUILD SUCCEEDED, zero errors
4. Manual: run app on simulator, trigger a historical sync, check Debug > Logs for `hps.telemetry` entries
5. Manual: after sync, query `SELECT * FROM sync_telemetry;` via bridge or direct SQLite inspector — rows present
</verification>

<success_criteria>
- sync_telemetry table exists in schema v23; CURRENT_SCHEMA_VERSION == 23
- sync.record_hps_telemetry in BRIDGE_METHODS; bridge_methods_constant_matches_dispatcher passes
- GooseBLEHistoricalManager.burstStartedAt and burstBytesReceived fields exist
- historyStart resets both fields; historyEnd reads them and emits log + bridge call
- Rust integration test sync_telemetry_round_trip passes
- Swift builds clean (zero errors)
- Issue #162 can be closed
</success_criteria>

<output>
Create `.planning/phases/101-hps-telemetry-coach-crash-protocol-cleanup/101-01-SUMMARY.md` when done.
</output>
