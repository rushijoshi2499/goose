---
phase: 101-hps-telemetry-coach-crash-protocol-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/protocol.rs
autonomous: true
requirements:
  - PROTO-10
must_haves:
  truths:
    - "data_packet_domain(24) returns 'v24_biometric_stream', not 'normal_history_with_hr_marker'"
    - "data_packet_domain(9), data_packet_domain(12), data_packet_domain(18) still return 'normal_history_with_hr_marker'"
    - "cargo test --locked passes clean with no regressions"
    - "Any test fixture asserting 'normal_history_with_hr_marker' for packet_k=24 is updated"
  artifacts:
    - path: "Rust/core/src/protocol.rs"
      provides: "data_packet_domain fn with packet_k 24 split to 'v24_biometric_stream'"
  key_links:
    - from: "Rust/core/src/protocol.rs"
      to: "Rust/core/src/protocol.rs"
      via: "data_packet_domain(24) == 'v24_biometric_stream' consistent with parse_data_packet_body_summary routing 24 to parse_v24_body_summary"
      pattern: "v24_biometric_stream"
---

<objective>
Fix PROTO-10: data_packet_domain() groups packet_k=24 under "normal_history_with_hr_marker" but parse_data_packet_body_summary routes packet_k=24 to parse_v24_body_summary (a biometric stream parser, distinct concept). Split the match arm to give packet_k=24 its own domain string "v24_biometric_stream". Run cargo test --locked to confirm no regressions. Closes issue #157.

Purpose: Eliminates the domain label mismatch that causes packet_k=24 frames to be miscategorised in domain-based queries and exports.
Output: Single match arm change in protocol.rs; any fixture updates; clean cargo test. Closes issue #157.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Rust/core/src/protocol.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix data_packet_domain mismatch for packet_k=24 + update test fixtures</name>
  <files>Rust/core/src/protocol.rs</files>
  <behavior>
    - `data_packet_domain(24)` returns `Some("v24_biometric_stream")`
    - `data_packet_domain(9)` returns `Some("normal_history_with_hr_marker")`
    - `data_packet_domain(12)` returns `Some("normal_history_with_hr_marker")`
    - `data_packet_domain(18)` returns `Some("normal_history_with_hr_marker")`
    - `data_packet_domain(99)` returns `None`
    - `cargo test --locked` passes with zero failures
  </behavior>
  <action>
**Step 1: Read `Rust/core/src/protocol.rs`** around `data_packet_domain` (lines ~1229–1241 per research). Confirm the current match arm is:
```
9 | 12 | 18 | 24 => "normal_history_with_hr_marker",
```

**Step 2: Apply the fix** — change that arm to two separate arms:
```
9 | 12 | 18 => "normal_history_with_hr_marker",
24 => "v24_biometric_stream",
```

The `history_hr_marker_offset` function at line ~1244 keeps `packet_k=24` returning `Some(17)` — do NOT change that function. The domain name change is independent of the HR marker offset.

**Step 3: Grep test files for the old domain string applied to packet_k=24**:
Run `grep -rn "normal_history_with_hr_marker" Rust/core/tests/` and `grep -rn "normal_history_with_hr_marker" Rust/core/src/` (excluding the line just changed). For any test that asserts `data_packet_domain(24) == "normal_history_with_hr_marker"` or uses a fixture with packet_k=24 and that domain string, update the expected value to `"v24_biometric_stream"`. Do not change assertions for packet_k 9, 12, or 18.

**Step 4: Run cargo test** — `cargo test --locked --manifest-path Rust/core/Cargo.toml` must pass with zero failures. If any test fails due to the domain string change, fix the fixture (expected value only; do not change test logic).

Note on PROTO-08, PROTO-09, PROTO-11: research confirmed all three are already done (PacketType enum exists and is exhaustive; _ => arm in parse_data_packet_body_summary emits Unknown variant + warning string, not silent; COMMAND_DEFINITIONS with 76 entries exists and is tested). No changes needed for those three. This task covers only PROTO-10.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && cargo test --locked --manifest-path Rust/core/Cargo.toml 2>&1 | tail -15</automated>
  </verify>
  <done>data_packet_domain(24) returns "v24_biometric_stream"; cargo test --locked passes clean; no regressions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust internal | data_packet_domain is a pure function called from within the Rust library; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03-01 | Tampering | domain string in decoded_frames | accept | Domain string is a diagnostic label; no security boundary crossed; value corrected to match actual packet semantics |
| T-101-03-SC | Tampering | cargo installs | accept | No new Cargo dependencies; lockfile unchanged |
</threat_model>

<verification>
1. `cargo test --locked --manifest-path Rust/core/Cargo.toml` — zero failures
2. `grep "v24_biometric_stream" Rust/core/src/protocol.rs` — match found on the data_packet_domain arm
3. `grep -c "9 | 12 | 18 | 24" Rust/core/src/protocol.rs` — must be 0 (old combined arm removed)
4. `grep -c "9 | 12 | 18 =>" Rust/core/src/protocol.rs` — must be 1 (new arm present)
</verification>

<success_criteria>
- data_packet_domain(24) == "v24_biometric_stream"
- data_packet_domain(9 | 12 | 18) == "normal_history_with_hr_marker" unchanged
- cargo test --locked passes clean
- No test fixtures assert "normal_history_with_hr_marker" for packet_k=24
- Issue #157 can be closed
</success_criteria>

<output>
Create `.planning/phases/101-hps-telemetry-coach-crash-protocol-cleanup/101-03-SUMMARY.md` when done.
</output>
