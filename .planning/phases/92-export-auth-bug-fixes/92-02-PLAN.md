---
phase: 92-export-auth-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/MoreDataStore.swift
  - GooseSwift/MoreRawExportViews.swift
autonomous: true
requirements:
  - BUG-EXP-02
  - BUG-EXP-04
must_haves:
  truths:
    - "runFullRawExport() does not set includeRawBytes = true — the user-configured value is preserved"
    - "The Include Database button in MoreRawExportView is disabled (not hidden) when the SQLite file exceeds 20 MB"
    - "The 20 MB threshold is derived from the same fetchSQLiteDBSizeLabel() logic — no new magic number introduced"
  artifacts:
    - path: "GooseSwift/MoreDataStore.swift"
      provides: "runFullRawExport without includeRawBytes override; isDatabaseTooLarge computed var"
      contains: "isDatabaseTooLarge"
    - path: "GooseSwift/MoreRawExportViews.swift"
      provides: "Include Database button with .disabled(store.isDatabaseTooLarge)"
      contains: "isDatabaseTooLarge"
  key_links:
    - from: "GooseSwift/MoreRawExportViews.swift (confirmationDialog)"
      to: "GooseSwift/MoreDataStore.swift (isDatabaseTooLarge)"
      via: ".disabled(store.isDatabaseTooLarge) on the Include Database button"
      pattern: "isDatabaseTooLarge"
---

<objective>
Fix two independent export defaults bugs: (1) runFullRawExport() silently overrides the user's includeRawBytes setting by setting it to true; (2) the "Include Database" button in the export preset sheet is not disabled when the SQLite file is > 20 MB, allowing the user to trigger an OOM-risk export. Per D-08 and D-10.

Purpose: Ensure runFullRawExport() respects the user-configured includeRawBytes flag, and guard the OOM-risk database export button with a size-based disabled state derived from the existing fetchSQLiteDBSizeLabel() logic.

Output:
- GooseSwift/MoreDataStore.swift — runFullRawExport removes the includeRawBytes = true override; adds isDatabaseTooLarge computed property
- GooseSwift/MoreRawExportViews.swift — Include Database button gains .disabled(store.isDatabaseTooLarge)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/92-export-auth-bug-fixes/92-CONTEXT.md

@GooseSwift/MoreDataStore.swift
@GooseSwift/MoreRawExportViews.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove includeRawBytes override in runFullRawExport; add isDatabaseTooLarge computed property</name>
  <files>GooseSwift/MoreDataStore.swift</files>
  <read_first>
    - GooseSwift/MoreDataStore.swift — read fn runFullRawExport (line ~73 in MoreDataStore+Validation.swift extension); note the line includeRawBytes = true at line 82
    - GooseSwift/MoreDataStore.swift — read fn fetchSQLiteDBSizeLabel (line ~462); note the 20 MB threshold check (mb > 20) and the 1_048_576 divisor
  </read_first>
  <action>
    Make two changes to GooseSwift/MoreDataStore.swift (per D-08 and D-10):

    CHANGE 1 — Remove includeRawBytes override in runFullRawExport (per D-08):

    In the runFullRawExport() function body (in MoreDataStore+Validation.swift extension), remove the line:
      includeRawBytes = true

    The function must NOT set includeRawBytes to any value. The user-configured @Published var includeRawBytes value (default false) must flow through unchanged into runRawExport(). All other lines in runFullRawExport() remain unchanged (rawExportStart, rawExportEnd, rawCaptureSessions, rawPacketTypes, rawSensorSignals, rawMetricFamilies, rawAlgorithmIDs, rawAlgorithmVersions, selectedRawFamilies assignment, and runRawExport() call).

    CHANGE 2 — Add isDatabaseTooLarge computed property (per D-10):

    In MoreDataStore.swift, add a computed property immediately after fetchSQLiteDBSizeLabel():

      var isDatabaseTooLarge: Bool {
        guard !databasePath.isEmpty,
              let attrs = try? FileManager.default.attributesOfItem(atPath: databasePath),
              let bytes = attrs[.size] as? Int64 else { return false }
        return Double(bytes) / 1_048_576 > 20
      }

    This property reuses the same threshold (20 MB) and constant (1_048_576) as fetchSQLiteDBSizeLabel(). Do NOT introduce a new named constant or separate the threshold — keep it aligned with the existing label logic so the two stay in sync at a glance. Place the property on a new line immediately after fetchSQLiteDBSizeLabel's closing brace, before canRunRawExport.
  </action>
  <verify>
    <automated>grep -n "isDatabaseTooLarge" /Users/francisco/Documents/goose/GooseSwift/MoreDataStore.swift</automated>
  </verify>
  <done>
    isDatabaseTooLarge computed property exists in MoreDataStore.swift. runFullRawExport() does not contain the line "includeRawBytes = true". The file compiles (verified by Task 2's build step or human UAT).
  </done>
</task>

<task type="auto">
  <name>Task 2: Disable Include Database button when isDatabaseTooLarge is true</name>
  <files>GooseSwift/MoreRawExportViews.swift</files>
  <read_first>
    - GooseSwift/MoreRawExportViews.swift — read the confirmationDialog block (lines ~72–88); locate the Button("Include Database\(sqliteDBSizeLabel)") at line ~79 and its action body at line ~80
    - Note that this button lives inside a .confirmationDialog sheet — SwiftUI .confirmationDialog buttons do not support .disabled() modifier the same way as regular Buttons in a List. Verify the button type; if .confirmationDialog buttons cannot be individually disabled, the correct approach is to make the confirmation dialog's isPresented binding check isDatabaseTooLarge or to add a .disabled() to the triggering "Export" button for the includeDatabase case. The preferred approach per D-10: add a separate Bool @State var showDatabaseTooLargeAlert and show an alert when the user taps "Include Database" while isDatabaseTooLarge is true, telling them to reduce the database size first. This is the iOS-idiomatic fallback when .disabled() is not available inside confirmationDialog.
  </read_first>
  <action>
    In GooseSwift/MoreRawExportViews.swift, implement the database-too-large guard on the "Include Database" button (per D-10).

    The button lives inside a .confirmationDialog. SwiftUI .confirmationDialog buttons cannot be .disabled() individually. Use the following approach:

    1. Add a @State private var showDatabaseTooLargeAlert = false property to MoreRawExportView.
    2. In the "Include Database" button action, check store.isDatabaseTooLarge before calling store.runRawExport(preset: .includeDatabase):
       - If store.isDatabaseTooLarge is true: set showDatabaseTooLargeAlert = true instead of running the export.
       - If store.isDatabaseTooLarge is false: call store.runRawExport(preset: .includeDatabase) as before.
    3. Append a .alert("Database Too Large", isPresented: $showDatabaseTooLargeAlert) modifier on the view body (alongside the existing .confirmationDialog modifier):
       - Title: "Database Too Large"
       - Message: Text("The SQLite database exceeds 20 MB and cannot be included without OOM risk. Use Frames & Metrics or Full Diagnostic instead.")
       - Single Button("OK", role: .cancel) {} action.
    4. Update the button label to show the OOM risk indicator when applicable. The sqliteDBSizeLabel is already set when the confirmationDialog opens (via the "Export" button tap action at line ~66). The existing label text already includes "(NNmb — OOM risk)" when > 20 MB — no label change needed.

    Do NOT restructure the confirmationDialog or remove any other buttons. Only the "Include Database" action body changes (add the isDatabaseTooLarge guard) and the new .alert modifier is appended to the view.
  </action>
  <verify>
    <automated>grep -n "isDatabaseTooLarge\|showDatabaseTooLargeAlert" /Users/francisco/Documents/goose/GooseSwift/MoreRawExportViews.swift</automated>
  </verify>
  <done>
    MoreRawExportViews.swift contains showDatabaseTooLargeAlert state and isDatabaseTooLarge guard in the Include Database action. A .alert("Database Too Large", ...) modifier exists on the view. The file compiles without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Export UI | User triggers Include Database preset; app must guard against OOM before running |
| App → Filesystem | isDatabaseTooLarge reads SQLite file size from filesystem attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-01 | Denial of Service | Include Database export on large DB | mitigate | isDatabaseTooLarge guard blocks the export path; alert shown instead |
| T-92-02-02 | Tampering | includeRawBytes override in runFullRawExport | accept | Bug fixed — function no longer overrides user preference; no security boundary crossed |
| T-92-02-03 | Information Disclosure | SQLite file size read via FileManager.attributesOfItem | accept | Read-only attribute access on the app's own sandbox file; no PII leak |
</threat_model>

<verification>
1. grep MoreDataStore.swift for "includeRawBytes = true" in runFullRawExport — zero matches.
2. grep MoreDataStore.swift for "isDatabaseTooLarge" — at least one match (the computed property).
3. grep MoreRawExportViews.swift for "isDatabaseTooLarge" — at least one match (in button action).
4. grep MoreRawExportViews.swift for "showDatabaseTooLargeAlert" — at least two matches (state declaration and .alert binding).
</verification>

<success_criteria>
- runFullRawExport() body does not contain "includeRawBytes = true"
- isDatabaseTooLarge computed var exists in MoreDataStore.swift, returns true when DB size > 20 MB
- Include Database action in MoreRawExportViews.swift checks isDatabaseTooLarge before running export
- Database Too Large alert shown when user taps Include Database with DB > 20 MB
- Closes #155 Bug 1 (runFullRawExport override), Closes #155 Bug 3 (Include Database OOM guard)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Description |
|---------------|------|-------------|
| `MoreDataStore.isDatabaseTooLarge` | Swift computed var (new) | Returns true when SQLite file > 20 MB; derived from same threshold as fetchSQLiteDBSizeLabel |
| `MoreRawExportView.showDatabaseTooLargeAlert` | Swift @State var (new) | Controls the Database Too Large alert presentation |
| `runFullRawExport()` | Swift fn (modified) | No longer sets includeRawBytes = true; respects user-configured value |
| Include Database button action | Swift closure (modified) | Guards against isDatabaseTooLarge; shows alert instead of running export when DB too large |

<output>
Create `.planning/phases/92-export-auth-bug-fixes/92-02-SUMMARY.md` when done.
</output>
