---
phase: 17-observable-migration
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - GooseSwift/GooseBLEClient.swift
  - GooseSwift/GooseBLEClient+CentralDelegate.swift
  - GooseSwift/GooseBLEClient+DebugAndSync.swift
  - GooseSwift/GooseBLEClient+Commands.swift
  - GooseSwift/GooseBLEClient+Parsing.swift
  - GooseSwift/GooseBLEClient+HistoricalCommands.swift
  - GooseSwift/GooseBLEClient+PeripheralDelegate.swift
  - GooseSwift/GooseBLEClient+HistoricalHandlers.swift
  - GooseSwift/GooseBLEClient+HRMonitor.swift
  - GooseSwift/GooseBLEClient+UserActions.swift
  - GooseSwift/GooseBLEClient+VitalsAndLogging.swift
  - GooseSwift/MoreDataStore.swift
  - GooseSwift/MoreView.swift
  - GooseSwift/HealthSleepSheetsViews.swift
  - GooseSwift/SleepV2ScheduleViews.swift
  - GooseSwift/RootView.swift
  - GooseSwift/HRMonitorView.swift
  - GooseSwift/ConnectionView.swift
  - GooseSwift/FitnessLiveWorkoutViews.swift
  - GooseSwift/SleepBridgeViews.swift
  - GooseSwift/OnboardingStepViews.swift
  - GooseSwift/LiveActivityContentView.swift
  - GooseSwift/DeviceView.swift
  - GooseSwift/FitnessSummaryViews.swift
  - GooseSwift/HealthSleepOverviewViews.swift
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "GooseBLEClient is declared with @Observable, keeps NSObject and @unchecked Sendable, no longer conforms to ObservableObject"
    - "No @Published annotations remain in GooseBLEClient.swift or any GooseBLEClient+*.swift file"
    - "Every view consuming GooseBLEClient via @ObservedObject accesses it as a plain parameter"
    - "MoreDataStore has no Combine route-status subscription; MoreView reactively refreshes route status via onChange modifiers"
  artifacts:
    - path: "GooseSwift/GooseBLEClient.swift"
      provides: "@Observable GooseBLEClient class declaration (NSObject, @unchecked Sendable)"
      contains: "@Observable"
  key_links:
    - from: "GooseSwift/MoreView.swift"
      to: "GooseSwift/MoreDataStore.swift"
      via: ".onChange(of:) calls store.refreshRouteStatus(...)"
      pattern: "refreshRouteStatus"
---

<objective>
Migrate GooseBLEClient from ObservableObject + @Published to the @Observable macro (per D-01), keeping its NSObject base class and @unchecked Sendable conformance, and update every view that consumes it via @ObservedObject (per D-02). Wave 3 of the locked order (D-03). Also finalize the MoreDataStore Combine fix (Critical Pitfall): confirm the Combine pipeline removed in Wave 1 is gone and add reactive `.onChange` modifiers in MoreView so route status updates mid-session.

Purpose: Completes the third @Observable migration. Per-property tracking on GooseBLEClient + the model removes the global objectWillChange broadcasts that produce the "Update NavigationRequestObserver tried to update multiple times per frame" warning at capture startup (PERF-03).
Output: GooseBLEClient as @Observable (NSObject preserved); ~13 view files rewired; MoreView reactive route-status refresh via onChange.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-observable-migration/17-CONTEXT.md
@.planning/phases/17-observable-migration/17-RESEARCH.md
@.planning/phases/17-observable-migration/17-01-SUMMARY.md
@.planning/phases/17-observable-migration/17-02-SUMMARY.md

@GooseSwift/GooseBLEClient.swift
@GooseSwift/MoreDataStore.swift
@GooseSwift/MoreView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert GooseBLEClient to @Observable, keep NSObject + @unchecked Sendable, remove all @Published</name>
  <files>GooseSwift/GooseBLEClient.swift, GooseSwift/GooseBLEClient+CentralDelegate.swift, GooseSwift/GooseBLEClient+DebugAndSync.swift, GooseSwift/GooseBLEClient+Commands.swift, GooseSwift/GooseBLEClient+Parsing.swift, GooseSwift/GooseBLEClient+HistoricalCommands.swift, GooseSwift/GooseBLEClient+PeripheralDelegate.swift, GooseSwift/GooseBLEClient+HistoricalHandlers.swift, GooseSwift/GooseBLEClient+HRMonitor.swift, GooseSwift/GooseBLEClient+UserActions.swift, GooseSwift/GooseBLEClient+VitalsAndLogging.swift</files>
  <action>Per D-01: change `final class GooseBLEClient: NSObject, ObservableObject, @unchecked Sendable` to `@Observable final class GooseBLEClient: NSObject, @unchecked Sendable`. CRITICAL: KEEP `NSObject` (required for CoreBluetooth CBCentralManagerDelegate/CBPeripheralDelegate conformances) and KEEP `@unchecked Sendable`. Only `ObservableObject` is removed; `@Observable` is added before `final`. Remove the `@Published` annotation from all 68 stored properties in GooseBLEClient.swift (bluetoothState through debugCommandSnapshotPath — see verified list lines 7-74 in 17-RESEARCH.md @Published Audit). Each `@Published var x` becomes `var x`, same type and default. Do NOT change names, types, defaults. The 10 extension files carry methods/delegate conformances only (0 @Published today) — sweep each and confirm zero remain; the CBCentralManagerDelegate and CBPeripheralDelegate conformances in those extensions are UNAFFECTED, leave them. CRITICAL thread-safety (Pitfall 5): @Observable adds NO thread safety. GooseBLEClient mutates these properties from CoreBluetooth delegate callbacks. Preserve EVERY existing `DispatchQueue.main.async { ... }`, `if !Thread.isMainThread` guard, and `NSLock` exactly as-is — do not remove or alter any main-thread guard. The `BLEUIStateAggregator`, DispatchQueue, NSLock, Logger, GooseMessageStore stored properties are plain `let` — no change. If `import Combine` exists only for @Published, leave it; Plan 17-04 build sweep flags dead imports.</action>
  <verify>
    <automated>test "$(grep -rc '@Published' GooseSwift/GooseBLEClient*.swift | grep -v ':0$' | wc -l | tr -d ' ')" = "0" &amp;&amp; grep -c '@Observable' GooseSwift/GooseBLEClient.swift &amp;&amp; grep -c 'NSObject' GooseSwift/GooseBLEClient.swift &amp;&amp; grep -c '@unchecked Sendable' GooseSwift/GooseBLEClient.swift</automated>
  </verify>
  <done>Zero @Published across GooseBLEClient.swift and all GooseBLEClient+*.swift; class declaration is `@Observable final class GooseBLEClient: NSObject, @unchecked Sendable`; ObservableObject removed; NSObject + @unchecked Sendable retained; all main-thread guards preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize MoreDataStore Combine fix and add reactive onChange in MoreView</name>
  <files>GooseSwift/MoreDataStore.swift, GooseSwift/MoreView.swift</files>
  <action>Per the locked Critical Pitfall fix. STEP 1 (verify front-loaded removal): The Combine pipeline (bindRouteStatus, bleStatusCancellables, model.$/ble.$ publishers, import Combine) was removed in Plan 17-01 Task 3. Confirm MoreDataStore.swift has no `bleStatusCancellables`, no `Publishers`, no `AnyCancellable`, no `.$`, no `bindRouteStatus`. If any remain (e.g. 17-01 only removed the model publisher), remove them now: delete `bleStatusCancellables`, delete the whole `bindRouteStatus(ble:model:)` method (keep `refreshRouteStatus`), and remove `import Combine` only if no other Combine token (`Publishers`, `Publisher`, `AnyCancellable`, `.sink`, `.store(in:`) remains in the file. STEP 2 (reactive refresh — the locked fix point 4): in MoreView, ensure `.onAppear` calls `store.refreshRouteStatus(ble: model.ble, model: model)` (already redirected in 17-01), and ADD `.onChange` modifiers to the same view body so route status updates mid-session now that the Combine debounce is gone: `.onChange(of: model.helloSummary) { store.refreshRouteStatus(ble: model.ble, model: model) }`, `.onChange(of: model.ble.connectionState) { store.refreshRouteStatus(ble: model.ble, model: model) }`, and `.onChange(of: model.ble.hrConnectionState) { store.refreshRouteStatus(ble: model.ble, model: model) }`. Use the two-parameter onChange closure form `(_, _) in` if the project's Swift version requires it; match the onChange signature already used elsewhere in the codebase. With @Observable, these onChange modifiers fire only when the specific property changes — more efficient than the old MergeMany+debounce pipeline (per CONTEXT.md). Place the onChange modifiers on the same container that holds the existing onAppear.</action>
  <verify>
    <automated>test "$(grep -c 'bleStatusCancellables\|AnyCancellable\|Publishers\|bindRouteStatus\|\.\$' GooseSwift/MoreDataStore.swift | tr -d ' ')" = "0" &amp;&amp; test "$(grep -c 'onChange' GooseSwift/MoreView.swift | tr -d ' ')" -ge 3 &amp;&amp; grep -c 'refreshRouteStatus' GooseSwift/MoreView.swift</automated>
  </verify>
  <done>MoreDataStore.swift free of all Combine route-status code; MoreView has at least 3 onChange modifiers (helloSummary, connectionState, hrConnectionState) each calling refreshRouteStatus; onAppear still calls refreshRouteStatus.</done>
</task>

<task type="auto">
  <name>Task 3: Remove @ObservedObject GooseBLEClient wrappers in all consumer views</name>
  <files>GooseSwift/HealthSleepSheetsViews.swift, GooseSwift/SleepV2ScheduleViews.swift, GooseSwift/RootView.swift, GooseSwift/HRMonitorView.swift, GooseSwift/ConnectionView.swift, GooseSwift/FitnessLiveWorkoutViews.swift, GooseSwift/SleepBridgeViews.swift, GooseSwift/OnboardingStepViews.swift, GooseSwift/LiveActivityContentView.swift, GooseSwift/DeviceView.swift, GooseSwift/FitnessSummaryViews.swift, GooseSwift/HealthSleepOverviewViews.swift</files>
  <action>Per D-02 (Pitfall 3): remove the `@ObservedObject` wrapper from every declaration targeting GooseBLEClient, leaving a plain stored parameter. Transform `@ObservedObject var ble: GooseBLEClient` to `var ble: GooseBLEClient` (preserve property name and access level). Sites from 17-RESEARCH.md View-Side Wiring Inventory (@ObservedObject GooseBLEClient): HealthSleepSheetsViews.swift:224 :760, SleepV2ScheduleViews.swift:86, RootView.swift:64, HRMonitorView.swift:15 :161 :226, ConnectionView.swift:15, FitnessLiveWorkoutViews.swift:10 :134, SleepBridgeViews.swift:8 :91, OnboardingStepViews.swift:196 :348, LiveActivityContentView.swift:25, DeviceView.swift:21 :307 :456 :562, FitnessSummaryViews.swift:9, HealthSleepOverviewViews.swift:9. Line numbers will have shifted from prior waves' edits in shared files (RootView, HRMonitorView, ConnectionView, DeviceView, SleepBridgeViews, LiveActivityContentView, SleepV2ScheduleViews, HealthSleepOverviewViews) — match on `@ObservedObject` + `GooseBLEClient`, not absolute lines. Do NOT touch out-of-scope ObservableObject types (PacketMonitorModel, GooseMessageStore, etc.).</action>
  <verify>
    <automated>test "$(grep -rn '@ObservedObject' GooseSwift/ | grep 'GooseBLEClient' | grep -v '//' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>Zero @ObservedObject references to GooseBLEClient in non-comment code; all consumers use plain parameters; out-of-scope ObservableObject wrappers untouched.</done>
</task>

</tasks>

<verification>
Project must compile with all three classes migrated:
`xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -sdk iphonesimulator -arch arm64 CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED"`
Expect BUILD SUCCEEDED. No `Value of type 'GooseBLEClient' has no member '$...'` errors. No ObservableObject-not-found crashes. CoreBluetooth delegate conformances intact (no `does not conform to CBCentralManagerDelegate` errors).
At this point GooseAppModel, HealthDataStore, GooseBLEClient are all @Observable. Only out-of-scope ObservableObject types (AppRouter, PacketMonitorModel, GooseMessageStore, MoreDataStore, etc.) remain — verified clean by Plan 17-04.
</verification>

<success_criteria>
- GooseBLEClient uses @Observable, keeps NSObject + @unchecked Sendable, no ObservableObject (PERF-03 contributor)
- Zero @Published in GooseBLEClient.swift and all GooseBLEClient+*.swift
- All @ObservedObject GooseBLEClient sites → plain parameter
- MoreDataStore free of Combine route-status code; MoreView uses onChange + onAppear refreshRouteStatus
- All CoreBluetooth main-thread guards preserved unchanged
- xcodebuild build succeeds (BUILD SUCCEEDED)
</success_criteria>

<output>
Create `.planning/phases/17-observable-migration/17-03-SUMMARY.md` when done.
</output>
