---
phase: 17-observable-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseAppModel.swift
  - GooseSwift/GooseAppModel+ActivityTimeline.swift
  - GooseSwift/GooseAppModel+ActivityRecording.swift
  - GooseSwift/GooseAppModel+HealthCapture.swift
  - GooseSwift/GooseAppModel+Lifecycle.swift
  - GooseSwift/GooseAppModel+OvernightRecovery.swift
  - GooseSwift/GooseAppModel+OvernightRun.swift
  - GooseSwift/GooseAppModel+NotificationPipeline.swift
  - GooseSwift/GooseAppModel+Upload.swift
  - GooseSwift/GooseAppModel+OvernightState.swift
  - GooseSwift/GooseAppModel+PacketPublishing.swift
  - GooseSwift/MoreCaptureViews.swift
  - GooseSwift/HealthDashboardViews.swift
  - GooseSwift/MoreView.swift
  - GooseSwift/LiveActivityView.swift
  - GooseSwift/MoreProfileViews.swift
  - GooseSwift/HRMonitorView.swift
  - GooseSwift/LiveActivityContentView.swift
  - GooseSwift/ConnectionView.swift
  - GooseSwift/RootView.swift
  - GooseSwift/HomeDashboardView.swift
  - GooseSwift/DeviceView.swift
  - GooseSwift/MoreRemoteServerViews.swift
  - GooseSwift/HealthRecoveryStressViews.swift
  - GooseSwift/OnboardingView.swift
  - GooseSwift/MoreDebugViews.swift
  - GooseSwift/HealthMetricFamilyStrainViews.swift
  - GooseSwift/CoachView.swift
  - GooseSwift/MoreInfoViews.swift
  - GooseSwift/HealthView.swift
  - GooseSwift/CoachChatScreen.swift
autonomous: true
requirements: [PERF-01, PERF-03]
must_haves:
  truths:
    - "GooseAppModel is declared with @Observable and no longer conforms to ObservableObject"
    - "No @Published annotations remain in GooseAppModel.swift or any GooseAppModel+*.swift file"
    - "Every view that previously used @EnvironmentObject GooseAppModel now uses @Environment(GooseAppModel.self)"
    - "Every view that previously used @ObservedObject GooseAppModel accesses it as a plain parameter"
    - "App compiles after this wave with model.$helloSummary Combine usage in MoreDataStore handled"
  artifacts:
    - path: "GooseSwift/GooseAppModel.swift"
      provides: "@Observable GooseAppModel class declaration"
      contains: "@Observable"
  key_links:
    - from: "GooseSwift/GooseAppModel.swift"
      to: "SwiftUI views"
      via: "@Environment(GooseAppModel.self) per-property tracking"
      pattern: "@Environment\\(GooseAppModel.self\\)"
---

<objective>
Migrate GooseAppModel from ObservableObject + @Published to the Swift @Observable macro (per D-01), and update every view that consumes it via @EnvironmentObject, @ObservedObject, or @StateObject (per D-02). This is the first wave of the locked migration order (D-03) — GooseAppModel must migrate atomically with all its view-side consumers so the app compiles and runs at the end of this wave.

Purpose: Per-property observation tracking eliminates global objectWillChange broadcasts from GooseAppModel, the principal cause of the "Update NavigationRequestObserver tried to update multiple times per frame" warning at capture startup (PERF-03).
Output: GooseAppModel as @Observable; 22 view files rewired to @Environment / plain parameters; MoreDataStore's model.$helloSummary Combine reference resolved so the wave compiles.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-observable-migration/17-CONTEXT.md
@.planning/phases/17-observable-migration/17-RESEARCH.md

@GooseSwift/GooseAppModel.swift
@GooseSwift/MoreDataStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert GooseAppModel to @Observable and remove all @Published</name>
  <files>GooseSwift/GooseAppModel.swift, GooseSwift/GooseAppModel+ActivityTimeline.swift, GooseSwift/GooseAppModel+ActivityRecording.swift, GooseSwift/GooseAppModel+HealthCapture.swift, GooseSwift/GooseAppModel+Lifecycle.swift, GooseSwift/GooseAppModel+OvernightRecovery.swift, GooseSwift/GooseAppModel+OvernightRun.swift, GooseSwift/GooseAppModel+NotificationPipeline.swift, GooseSwift/GooseAppModel+Upload.swift, GooseSwift/GooseAppModel+OvernightState.swift, GooseSwift/GooseAppModel+PacketPublishing.swift</files>
  <action>Per D-01: change the class declaration from `@MainActor final class GooseAppModel: ObservableObject` to `@MainActor @Observable final class GooseAppModel` (remove the ObservableObject conformance, add the @Observable macro before final). Remove the `@Published` annotation from all 52 stored properties in GooseAppModel.swift (lines covering onboardingComplete through connectedDeviceGeneration — see the verified list in 17-RESEARCH.md @Published Audit). Each `@Published var x` becomes `var x` with the same type and default. Do NOT change any property name, type, or default value. Do NOT add @Published anywhere. The extension files (GooseAppModel+*.swift) carry methods only and have 0 @Published today — sweep each one anyway and confirm zero @Published remain; if Combine `import Combine` exists in GooseAppModel.swift solely for @Published, leave it only if still used elsewhere, otherwise the build sweep in Plan 17-04 catches dead imports. Preserve all existing @MainActor isolation and main-thread guards exactly — @Observable adds no thread safety (Pitfall 5).</action>
  <verify>
    <automated>test "$(grep -rc '@Published' GooseSwift/GooseAppModel*.swift | grep -v ':0$' | wc -l | tr -d ' ')" = "0" &amp;&amp; grep -c '@Observable' GooseSwift/GooseAppModel.swift</automated>
  </verify>
  <done>grep across GooseAppModel.swift and all GooseAppModel+*.swift returns 0 @Published; GooseAppModel.swift contains exactly one @Observable on the class declaration; class no longer conforms to ObservableObject.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire all view consumers of GooseAppModel to @Environment and plain parameters</name>
  <files>GooseSwift/MoreCaptureViews.swift, GooseSwift/HealthDashboardViews.swift, GooseSwift/MoreView.swift, GooseSwift/LiveActivityView.swift, GooseSwift/MoreProfileViews.swift, GooseSwift/HRMonitorView.swift, GooseSwift/LiveActivityContentView.swift, GooseSwift/ConnectionView.swift, GooseSwift/RootView.swift, GooseSwift/HomeDashboardView.swift, GooseSwift/DeviceView.swift, GooseSwift/MoreRemoteServerViews.swift, GooseSwift/HealthRecoveryStressViews.swift, GooseSwift/OnboardingView.swift, GooseSwift/MoreDebugViews.swift, GooseSwift/HealthMetricFamilyStrainViews.swift, GooseSwift/CoachView.swift, GooseSwift/MoreInfoViews.swift, GooseSwift/HealthView.swift, GooseSwift/CoachChatScreen.swift</files>
  <action>Per D-02, replace every `@EnvironmentObject` declaration that targets GooseAppModel with `@Environment(GooseAppModel.self)`, preserving the property name and access level. Exact sites (file:line from 17-RESEARCH.md View-Side Wiring Inventory): MoreCaptureViews.swift:57, HealthDashboardViews.swift:562, MoreView.swift:11, LiveActivityView.swift:7, MoreProfileViews.swift:89, HRMonitorView.swift:5, HRMonitorView.swift:14, LiveActivityContentView.swift:22, ConnectionView.swift:4, ConnectionView.swift:13, RootView.swift:4, HomeDashboardView.swift:4, DeviceView.swift:5, DeviceView.swift:19, MoreRemoteServerViews.swift:34, HealthRecoveryStressViews.swift:8, HealthRecoveryStressViews.swift:221, OnboardingView.swift:9, MoreDebugViews.swift:4, HealthMetricFamilyStrainViews.swift:7, HealthMetricFamilyStrainViews.swift:397, CoachView.swift:4, MoreInfoViews.swift:79, HealthView.swift:7. Transform `@EnvironmentObject private var model: GooseAppModel` to `@Environment(GooseAppModel.self) private var model` (drop the `: GooseAppModel` type suffix — the type is the key). For @ObservedObject GooseAppModel sites, remove the wrapper entirely so the variable is a plain stored parameter: CoachChatScreen.swift:6 (`@ObservedObject var appModel: GooseAppModel` becomes `var appModel: GooseAppModel`), DeviceView.swift:305 and DeviceView.swift:457 (`@ObservedObject var model: GooseAppModel` becomes `var model: GooseAppModel`). IMPORTANT scope guard: do NOT touch `@EnvironmentObject private var router: AppRouter` (AppShellView.swift:4 and similar) — AppRouter is out of scope (still ObservableObject). Do NOT touch `.environmentObject(...)` injection-site calls in this wave; those are handled in Plan 17-04 along with GooseSwiftApp. Line numbers may have shifted slightly after Task 1 edits in shared files (HealthDashboardViews, DeviceView) — match on the @EnvironmentObject/@ObservedObject + GooseAppModel pattern, not the absolute line.</action>
  <verify>
    <automated>test "$(grep -rn '@EnvironmentObject' GooseSwift/ | grep 'GooseAppModel' | grep -v '//' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -rn '@ObservedObject' GooseSwift/ | grep 'GooseAppModel' | grep -v '//' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>Zero @EnvironmentObject and zero @ObservedObject references to GooseAppModel remain in non-comment code; all use @Environment(GooseAppModel.self) or plain parameters; AppRouter @EnvironmentObject sites untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve MoreDataStore model.$helloSummary so Wave 1 compiles</name>
  <files>GooseSwift/MoreDataStore.swift</files>
  <action>The Combine pipeline in MoreDataStore.bindRouteStatus (lines 154-167) subscribes to `model.$helloSummary` (alongside `ble.$connectionState` and `ble.$hrConnectionState`). After Task 1, `model.$helloSummary` no longer compiles because @Observable does not synthesize Published publishers (Pitfall 1). Per the locked Critical Pitfall fix, this wave only needs to unblock compilation for the model publisher; the full Combine removal and onChange wiring is owned by Plan 17-03 (Wave 3) when the ble.$ publishers also break. To keep waves atomic and compiling, take the minimal correct step now: remove the entire `bindRouteStatus(ble:model:)` Combine pipeline and the `bleStatusCancellables` set in this wave (since ALL three publishers — including the two ble ones — must go together for the file to compile cleanly once 17-03 lands, and removing them now is harmless because the only caller is MoreView.onAppear which we redirect to refreshRouteStatus). Concretely: (1) delete the `bleStatusCancellables` property (line 115); (2) delete the `bindRouteStatus(ble:model:)` method body entirely (lines ~154-168), keeping `refreshRouteStatus(ble:model:)` intact; (3) remove `import Combine` (line 1) if no other Combine usage remains in the file — grep the file for `AnyCancellable`, `Publishers`, `Publisher`, `.sink`, `.store(in:` first and only remove the import if all are gone; (4) update the single caller MoreView.onAppear (line 102) from `store.bindRouteStatus(ble: model.ble, model: model)` to `store.refreshRouteStatus(ble: model.ble, model: model)`. NOTE: this means Plan 17-03's MoreDataStore Combine-removal sub-task becomes a verification step (confirm already removed) plus adding the .onChange modifiers in MoreView for reactive updates. Document in the SUMMARY that the Combine removal was front-loaded here.</action>
  <verify>
    <automated>test "$(grep -c 'bleStatusCancellables\|AnyCancellable\|Publishers\|\.\$' GooseSwift/MoreDataStore.swift | tr -d ' ')" = "0" &amp;&amp; test "$(grep -c 'bindRouteStatus' GooseSwift/MoreView.swift | tr -d ' ')" = "0"</automated>
  </verify>
  <done>MoreDataStore.swift has no Combine publisher subscriptions, no bleStatusCancellables, no model.$ or ble.$ usage; bindRouteStatus method removed; MoreView.onAppear calls refreshRouteStatus directly; file compiles.</done>
</task>

</tasks>

<verification>
After all tasks, the project must compile with GooseAppModel migrated:
`xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -sdk iphonesimulator -arch arm64 CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED"`
Expect BUILD SUCCEEDED. No `Value of type 'GooseAppModel' has no member '$...'` errors. No `@EnvironmentObject ... GooseAppModel` runtime crashes (injection sites still use .environmentObject — see note below).

NOTE on injection sites: GooseSwiftApp.swift still uses `.environmentObject(model)` at end of this wave. Because GooseAppModel is now @Observable (not ObservableObject), `.environmentObject(model)` will fail to compile (GooseAppModel no longer conforms to ObservableObject). Therefore the GooseSwiftApp `.environmentObject(model)` → `.environment(model)` change and the other injection-site changes listed for Plan 17-04 that feed GooseAppModel into views MUST be done together for a clean build. To keep Wave 1 compilable, ALSO update in this wave the minimal injection sites that inject GooseAppModel: GooseSwiftApp.swift:6 (@StateObject→@State), GooseSwiftApp.swift:16 (.environmentObject(model)→.environment(model)), HRMonitorView.swift:9, ConnectionView.swift:8, DeviceView.swift:9, LiveActivityView.swift:15, CoachView.swift:672, HealthPreviews.swift:14, HealthPreviews.swift:23. Keep GooseSwiftApp.swift:17-18 (model.packetMonitor, model.ble.messageStore) as .environmentObject — those are out-of-scope ObservableObject types. Add these files to files_modified mentally; Plan 17-04 will re-verify them as a final sweep rather than redo.
</verification>

<success_criteria>
- GooseAppModel uses @Observable, no ObservableObject conformance (PERF-01)
- Zero @Published in GooseAppModel.swift and all GooseAppModel+*.swift files
- All 24 @EnvironmentObject GooseAppModel sites → @Environment(GooseAppModel.self)
- All 3 @ObservedObject GooseAppModel sites → plain parameter
- GooseAppModel injection sites use .environment(model) / @State
- MoreDataStore no longer references model.$ or ble.$ Combine publishers
- xcodebuild build succeeds (BUILD SUCCEEDED)
</success_criteria>

<output>
Create `.planning/phases/17-observable-migration/17-01-SUMMARY.md` when done. Document that the MoreDataStore Combine pipeline was fully removed in this wave (front-loaded from Wave 3) and that GooseAppModel injection sites were updated here, leaving Plan 17-04 to verify rather than redo.
</output>
