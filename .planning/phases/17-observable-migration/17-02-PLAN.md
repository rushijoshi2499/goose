---
phase: 17-observable-migration
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - GooseSwift/HealthDataStore.swift
  - GooseSwift/HealthDataStore+ActivitySnapshots.swift
  - GooseSwift/HealthDataStore+Cardio.swift
  - GooseSwift/HealthDataStore+Sleep.swift
  - GooseSwift/HealthDataStore+CoachSummaries.swift
  - GooseSwift/HealthDataStore+PacketInputs.swift
  - GooseSwift/HealthDataStore+Snapshots.swift
  - GooseSwift/HealthDataStore+Trends.swift
  - GooseSwift/HealthDataStore+StaticSnapshots.swift
  - GooseSwift/HealthDataStore+StressEnergy.swift
  - GooseSwift/HealthDataStore+Utilities.swift
  - GooseSwift/HealthDataStore+Vitals.swift
  - GooseSwift/AppShellView.swift
  - GooseSwift/HealthDashboardViews.swift
  - GooseSwift/MoreView.swift
  - GooseSwift/MoreRawExportViews.swift
  - GooseSwift/CoachChatScreen.swift
  - GooseSwift/SleepBridgeViews.swift
  - GooseSwift/SleepV2ScheduleViews.swift
  - GooseSwift/HomeDashboardView.swift
  - GooseSwift/HealthSupplementalViews.swift
  - GooseSwift/HealthCardioViews.swift
  - GooseSwift/CoachView.swift
  - GooseSwift/HealthView.swift
  - GooseSwift/HealthRecoveryStressViews.swift
  - GooseSwift/HealthSleepOverviewViews.swift
  - GooseSwift/HealthMetricFamilyStrainViews.swift
autonomous: true
requirements: [PERF-02]
must_haves:
  truths:
    - "HealthDataStore is declared with @Observable and no longer conforms to ObservableObject"
    - "No @Published annotations remain in HealthDataStore.swift or any HealthDataStore+*.swift file"
    - "Every view consuming HealthDataStore via @ObservedObject accesses it as a plain parameter"
    - "AppShellView owns healthStore via @State; HealthDashboardViews custom init uses State(initialValue:)"
  artifacts:
    - path: "GooseSwift/HealthDataStore.swift"
      provides: "@Observable HealthDataStore class declaration"
      contains: "@Observable"
  key_links:
    - from: "GooseSwift/AppShellView.swift"
      to: "GooseSwift/HealthDataStore.swift"
      via: "@State private var healthStore = HealthDataStore()"
      pattern: "@State private var healthStore"
---

<objective>
Migrate HealthDataStore from ObservableObject + @Published to the @Observable macro (per D-01), and update every view that consumes it via @ObservedObject or @StateObject (per D-02). Wave 2 of the locked order (D-03). HealthDataStore must migrate atomically with its consumers so the app compiles and runs at the end of this wave.

Purpose: Per-property tracking for HealthDataStore (PERF-02) — health/metric views only re-render when a property they read changes.
Output: HealthDataStore as @Observable; ~16 view files rewired; AppShellView and HealthDashboardViews ownership migrated from @StateObject to @State.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-observable-migration/17-CONTEXT.md
@.planning/phases/17-observable-migration/17-RESEARCH.md
@.planning/phases/17-observable-migration/17-01-SUMMARY.md

@GooseSwift/HealthDataStore.swift
@GooseSwift/AppShellView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert HealthDataStore to @Observable and remove all @Published</name>
  <files>GooseSwift/HealthDataStore.swift, GooseSwift/HealthDataStore+ActivitySnapshots.swift, GooseSwift/HealthDataStore+Cardio.swift, GooseSwift/HealthDataStore+Sleep.swift, GooseSwift/HealthDataStore+CoachSummaries.swift, GooseSwift/HealthDataStore+PacketInputs.swift, GooseSwift/HealthDataStore+Snapshots.swift, GooseSwift/HealthDataStore+Trends.swift, GooseSwift/HealthDataStore+StaticSnapshots.swift, GooseSwift/HealthDataStore+StressEnergy.swift, GooseSwift/HealthDataStore+Utilities.swift, GooseSwift/HealthDataStore+Vitals.swift</files>
  <action>Per D-01: change `@MainActor final class HealthDataStore: ObservableObject` to `@MainActor @Observable final class HealthDataStore` (remove ObservableObject conformance, add @Observable macro). Remove the `@Published` annotation from all 25 stored properties in HealthDataStore.swift (algorithmDefinitions through heartRateTimelineStatus — see verified list in 17-RESEARCH.md @Published Audit). Each `@Published var x` becomes `var x`, same type and default. Do NOT change names, types, or defaults. Leave `hkHRVHistory` and `hkRHRHistory` as-is (already plain `var`, no @Published). The 11 extension files carry methods only (0 @Published today) — sweep each and confirm zero remain. Preserve @MainActor isolation exactly. If `import Combine` exists in HealthDataStore.swift only for @Published, leave it; the Plan 17-04 build sweep flags dead imports.</action>
  <verify>
    <automated>test "$(grep -rc '@Published' GooseSwift/HealthDataStore*.swift | grep -v ':0$' | wc -l | tr -d ' ')" = "0" &amp;&amp; grep -c '@Observable' GooseSwift/HealthDataStore.swift</automated>
  </verify>
  <done>Zero @Published across HealthDataStore.swift and all HealthDataStore+*.swift; HealthDataStore.swift has exactly one @Observable; no ObservableObject conformance.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate HealthDataStore ownership — @StateObject to @State (incl. custom init)</name>
  <files>GooseSwift/AppShellView.swift, GooseSwift/HealthDashboardViews.swift</files>
  <action>Per D-02 and the locked Critical Pitfall fix: (1) AppShellView.swift:5 — change `@StateObject private var healthStore = HealthDataStore()` to `@State private var healthStore = HealthDataStore()`. (2) HealthDashboardViews.swift:324 — change `@StateObject private var store: HealthDataStore` to `@State private var store: HealthDataStore`. (3) HealthDashboardViews.swift:332 (inside the custom `init(route:previewState:)` at lines 326-333) — change `_store = StateObject(wrappedValue: store)` to `_store = State(initialValue: store)`. CRITICAL (Pitfall 4): use `State(initialValue:)` NOT `State(wrappedValue:)` — @State's initializer takes `initialValue:`. There is a second init at HealthDashboardViews.swift:345 `init(route:store:selectedDate:)` — read both inits before editing; if it also assigns `_store`, apply the same State(initialValue:) transform; if it stores into a different property leave it. Line 324/332 numbers may have shifted after Plan 17-01 edited HealthDashboardViews.swift:562 — match on the `StateObject` token, not the line.</action>
  <verify>
    <automated>test "$(grep -c 'StateObject' GooseSwift/AppShellView.swift | tr -d ' ')" = "0" &amp;&amp; test "$(grep -c 'StateObject(wrappedValue:\|@StateObject' GooseSwift/HealthDashboardViews.swift | tr -d ' ')" = "0" &amp;&amp; grep -c 'State(initialValue:' GooseSwift/HealthDashboardViews.swift</automated>
  </verify>
  <done>AppShellView healthStore uses @State; HealthDashboardViews store uses @State with State(initialValue:) in custom init(s); no StateObject token remains referring to HealthDataStore.</done>
</task>

<task type="auto">
  <name>Task 3: Remove @ObservedObject HealthDataStore wrappers in all consumer views</name>
  <files>GooseSwift/MoreView.swift, GooseSwift/MoreRawExportViews.swift, GooseSwift/CoachChatScreen.swift, GooseSwift/HealthDashboardViews.swift, GooseSwift/SleepBridgeViews.swift, GooseSwift/SleepV2ScheduleViews.swift, GooseSwift/HomeDashboardView.swift, GooseSwift/HealthSupplementalViews.swift, GooseSwift/HealthCardioViews.swift, GooseSwift/CoachView.swift, GooseSwift/HealthView.swift, GooseSwift/HealthRecoveryStressViews.swift, GooseSwift/HealthSleepOverviewViews.swift, GooseSwift/HealthMetricFamilyStrainViews.swift</files>
  <action>Per D-02 (Pitfall 3): remove the `@ObservedObject` wrapper from every declaration that targets HealthDataStore, leaving a plain stored parameter. Transform `@ObservedObject var store: HealthDataStore` to `var store: HealthDataStore` (preserve the property name and access level). Sites from 17-RESEARCH.md View-Side Wiring Inventory (@ObservedObject HealthDataStore): MoreView.swift:13, MoreRawExportViews.swift:144 and :145, CoachChatScreen.swift:5, HealthDashboardViews.swift:342 :361 :387 :473 :563, SleepBridgeViews.swift:7, SleepV2ScheduleViews.swift:85, HomeDashboardView.swift:6, HealthSupplementalViews.swift:7 :52 :92 :118, HealthCardioViews.swift:8 :20 :28, CoachView.swift:6, HealthView.swift:8, HealthRecoveryStressViews.swift:9 :222, HealthSleepOverviewViews.swift:8, HealthMetricFamilyStrainViews.swift:10 :398 :836. Line numbers may have shifted from Plan 17-01/Task 2 edits in shared files — match on `@ObservedObject` + `HealthDataStore`, not absolute lines. Do NOT touch @ObservedObject targeting GooseBLEClient (handled in Plan 17-03) or out-of-scope ObservableObject types.</action>
  <verify>
    <automated>test "$(grep -rn '@ObservedObject' GooseSwift/ | grep 'HealthDataStore' | grep -v '//' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>Zero @ObservedObject references to HealthDataStore in non-comment code; all consumers use plain parameters; GooseBLEClient @ObservedObject sites untouched.</done>
</task>

</tasks>

<verification>
Project must compile with HealthDataStore migrated:
`xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -sdk iphonesimulator -arch arm64 CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED"`
Expect BUILD SUCCEEDED. No `Value of type 'HealthDataStore' has no member '$...'`, no `State(wrappedValue:)` errors, no ObservableObject-not-found crashes for HealthDataStore.
GooseBLEClient remains ObservableObject + @Published at the end of this wave — its consumers are untouched, so it still compiles.
</verification>

<success_criteria>
- HealthDataStore uses @Observable, no ObservableObject conformance (PERF-02)
- Zero @Published in HealthDataStore.swift and all HealthDataStore+*.swift
- All @ObservedObject HealthDataStore sites → plain parameter
- AppShellView + HealthDashboardViews ownership via @State; custom init uses State(initialValue:)
- xcodebuild build succeeds (BUILD SUCCEEDED)
</success_criteria>

<output>
Create `.planning/phases/17-observable-migration/17-02-SUMMARY.md` when done.
</output>
