---
phase: 17-observable-migration
plan: 04
type: execute
wave: 4
depends_on: ["17-03"]
files_modified:
  - GooseSwift/GooseSwiftApp.swift
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]
must_haves:
  truths:
    - "GooseSwiftApp injects GooseAppModel via .environment(model) and owns it via @State"
    - "No ObservableObject/@Published/@EnvironmentObject/@ObservedObject/@StateObject references to the three migrated classes remain in non-comment code"
    - "The full project builds successfully (BUILD SUCCEEDED)"
    - "Out-of-scope ObservableObject types (AppRouter, PacketMonitorModel, GooseMessageStore, MoreDataStore) are untouched and still injected with .environmentObject"
  artifacts:
    - path: "GooseSwift/GooseSwiftApp.swift"
      provides: "App entry point injecting GooseAppModel via .environment"
      contains: ".environment(model)"
  key_links:
    - from: "GooseSwift/GooseSwiftApp.swift"
      to: "SwiftUI environment"
      via: ".environment(model) for @Observable GooseAppModel"
      pattern: "\\.environment\\(model\\)"
---

<objective>
Final wave (D-03 Wave 4): confirm all injection sites for the three migrated classes use @State + .environment, run a global sweep proving no legacy observation wrappers remain for the migrated classes, and verify the full project builds. Confirms PERF-01, PERF-02, PERF-03 structurally complete.

Purpose: Guarantee the migration is complete and the app compiles end-to-end; the out-of-scope ObservableObject types stay correctly wired with the old API.
Output: Verified clean GooseSwiftApp injection; passing global sweep; BUILD SUCCEEDED; PERF-03 runtime check guidance for the developer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-observable-migration/17-CONTEXT.md
@.planning/phases/17-observable-migration/17-RESEARCH.md
@.planning/phases/17-observable-migration/17-01-SUMMARY.md
@.planning/phases/17-observable-migration/17-02-SUMMARY.md
@.planning/phases/17-observable-migration/17-03-SUMMARY.md

@GooseSwift/GooseSwiftApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify/finalize GooseSwiftApp injection sites for migrated classes</name>
  <files>GooseSwift/GooseSwiftApp.swift</files>
  <action>Plan 17-01 already updated the GooseAppModel injection sites (GooseSwiftApp.swift:6 @StateObject→@State, :16 .environmentObject(model)→.environment(model)) so Wave 1 could compile. This task VERIFIES they are correct and fixes any that were missed. Confirm: (1) GooseSwiftApp.swift owns the model via `@State private var model = GooseAppModel()` (not @StateObject); (2) the body injects `.environment(model)` (not `.environmentObject(model)`). CRITICAL — leave these UNCHANGED (out of scope, still ObservableObject): `.environmentObject(model.packetMonitor)` (PacketMonitorModel) and `.environmentObject(model.ble.messageStore)` (GooseMessageStore) at GooseSwiftApp.swift:17-18, and any `@EnvironmentObject private var router: AppRouter` / `.environmentObject(router)` for AppRouter. If AppRouter is still `ObservableObject` (it is — verified `final class AppRouter: ObservableObject`), it is NOT migrated in this phase; its @StateObject/@EnvironmentObject/.environmentObject wiring stays. Only the three target classes' injection sites use the new API.</action>
  <verify>
    <automated>grep -c '\.environment(model)' GooseSwift/GooseSwiftApp.swift &amp;&amp; test "$(grep -c '@StateObject private var model\|\.environmentObject(model)' GooseSwift/GooseSwiftApp.swift | tr -d ' ')" = "0" &amp;&amp; grep -c 'packetMonitor\|messageStore' GooseSwift/GooseSwiftApp.swift</automated>
  </verify>
  <done>GooseSwiftApp owns model via @State and injects .environment(model); no @StateObject model or .environmentObject(model) remain; packetMonitor/messageStore .environmentObject lines preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Global sweep — no legacy observation wrappers for the three migrated classes</name>
  <files>GooseSwift/</files>
  <action>Run a repository-wide sweep proving the migration is complete for the three target classes. The sweep must show ZERO non-comment occurrences of legacy observation API tied to GooseAppModel, HealthDataStore, or GooseBLEClient: (a) no `ObservableObject` conformance on any of the three class declarations; (b) no `@Published` in GooseAppModel*.swift, HealthDataStore*.swift, GooseBLEClient*.swift; (c) no `@EnvironmentObject ... GooseAppModel`; (d) no `@ObservedObject ... (GooseAppModel|HealthDataStore|GooseBLEClient)`; (e) no `@StateObject` owning any of the three; (f) no `.environmentObject(model)`; (g) no `.$` Combine publisher access on the three. EXPECTED remaining ObservableObject references are ONLY the out-of-scope classes (AppRouter, PacketMonitorModel, GooseMessageStore, MoreDataStore, ActivitySessionModel, ActivityLocationTracker, OpenAICoachChatModel, MoreRemoteServerViewModel) — these are correct and must NOT be changed. If the sweep surfaces any leftover wrapper for a migrated class (e.g. a view missed because a line shifted), fix it in place by removing the wrapper / converting to @Environment per D-02, then re-run. Also report any now-dead `import Combine` lines in the three migrated class files or MoreDataStore and remove them only if grep confirms no Combine token remains in that file.</action>
  <verify>
    <automated>test "$(grep -rn '@Published' GooseSwift/GooseAppModel*.swift GooseSwift/HealthDataStore*.swift GooseSwift/GooseBLEClient*.swift | grep -v '//' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -rn '@EnvironmentObject' GooseSwift/ | grep 'GooseAppModel' | grep -v '//' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -rn '@ObservedObject\|@StateObject' GooseSwift/ | grep -E 'GooseAppModel|HealthDataStore|GooseBLEClient' | grep -v '//' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -rn '\.environmentObject(model)' GooseSwift/ | grep -v '//' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>Sweep returns zero legacy wrappers for the three migrated classes; only out-of-scope ObservableObject types remain (AppRouter, PacketMonitorModel, GooseMessageStore, MoreDataStore, etc.); any dead Combine imports removed.</done>
</task>

<task type="auto">
  <name>Task 3: Full build verification and PERF-03 runtime check guidance</name>
  <files>GooseSwift/</files>
  <action>Build the full project for the simulator and confirm success: `xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -sdk iphonesimulator -arch arm64 CODE_SIGNING_ALLOWED=NO`. The build must report BUILD SUCCEEDED with zero `error:` lines. If errors appear, they will most likely be: (1) a missed @ObservedObject/@EnvironmentObject wrapper on a migrated class (line shifted across waves) — fix per D-02; (2) a leftover `.$` Combine access — fix per the MoreDataStore pattern; (3) `State(wrappedValue:)` instead of `State(initialValue:)` (Pitfall 4); (4) an accidentally-removed NSObject or main-thread guard in GooseBLEClient (Pitfall 5) — restore. Resolve all errors and rebuild until BUILD SUCCEEDED. For PERF-03 (the "Update NavigationRequestObserver tried to update multiple times per frame" warning), this is a runtime-only signal not catchable by the build; record in the SUMMARY the manual verification steps for the developer: launch the app on simulator/device, connect to a WHOOP device, start a health-packet capture, and inspect the Xcode console — the NavigationRequestObserver warning should no longer appear at capture startup. Note this manual step as the final acceptance for PERF-03.</action>
  <verify>
    <automated>xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -sdk iphonesimulator -arch arm64 CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -5</automated>
  </verify>
  <done>xcodebuild reports BUILD SUCCEEDED with zero error: lines; SUMMARY documents the manual PERF-03 console-inspection step (launch → connect → start capture → confirm no NavigationRequestObserver warning).</done>
</task>

</tasks>

<verification>
Final phase verification:
1. `xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -sdk iphonesimulator -arch arm64 CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED"` → BUILD SUCCEEDED, no error: lines.
2. Sweep: `grep -rn "ObservableObject\|@Published\|@EnvironmentObject\|@StateObject\|@ObservedObject" GooseSwift/ | grep -v "//"` → remaining hits are ONLY out-of-scope types (AppRouter, PacketMonitorModel, GooseMessageStore, MoreDataStore, ActivitySessionModel, ActivityLocationTracker, OpenAICoachChatModel, MoreRemoteServerViewModel).
3. Manual (developer): launch app, connect WHOOP, start capture, confirm no "NavigationRequestObserver tried to update multiple times per frame" warning in console (PERF-03).
</verification>

<success_criteria>
- GooseSwiftApp injects GooseAppModel via .environment + @State; out-of-scope injections preserved
- Zero legacy observation wrappers for the three migrated classes anywhere in GooseSwift/
- Full project: BUILD SUCCEEDED, zero error: lines
- PERF-01, PERF-02 structurally complete; PERF-03 runtime check documented for developer
</success_criteria>

<output>
Create `.planning/phases/17-observable-migration/17-04-SUMMARY.md` when done, including the manual PERF-03 verification steps and confirmation that all out-of-scope ObservableObject types were left intact.
</output>
