---
phase: 114-harvard-sleep-need-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/sleep_need.rs
  - Rust/core/src/lib.rs
autonomous: true
requirements:
  - SLP-NEED-01
must_haves:
  truths:
    - "compute_sleep_need(Some(22), &[], None) returns base_need_minutes == 480.0 (18-25 bracket)"
    - "compute_sleep_need(None, &[], None) returns base_need_minutes == 450.0 (26-64 default)"
    - "compute_sleep_need(Some(70), &[], None) returns base_need_minutes == 420.0 (65+ bracket)"
    - "compute_sleep_need with empty history returns debt_adjustment_minutes == 0.0 (cold start)"
    - "compute_sleep_need with prior_strain >= 15.0 returns strain_adjustment_minutes == 15.0"
    - "compute_sleep_need with prior_strain >= 10.0 (and < 15.0) returns strain_adjustment_minutes == 6.0"
    - "compute_sleep_need with prior_strain < 10.0 returns strain_adjustment_minutes == 0.0"
    - "total_need_minutes == base_need_minutes + debt_adjustment_minutes + strain_adjustment_minutes"
  artifacts:
    - path: "Rust/core/src/sleep_need.rs"
      provides: "compute_sleep_need + compute_sleep_need_with_store + SleepNeedResult"
      contains: "pub fn compute_sleep_need"
      min_lines: 80
    - path: "Rust/core/src/lib.rs"
      provides: "pub mod sleep_need declaration"
      contains: "pub mod sleep_need"
  key_links:
    - from: "Rust/core/src/sleep_need.rs"
      to: "Rust/core/src/baselines.rs"
      via: "imports EwmaState and ALPHA for EWMA debt computation"
      pattern: "use crate::baselines::\\{?\\s*ALPHA"
    - from: "Rust/core/src/sleep_need.rs"
      to: "Rust/core/src/store/mod.rs"
      via: "compute_sleep_need_with_store calls store.external_sleep_sessions_between"
      pattern: "external_sleep_sessions_between"
---

<objective>
Create the pure Rust sleep-need algorithm module `Rust/core/src/sleep_need.rs` implementing the Harvard sleep need model: age-bracket baseline + EWMA-based sleep debt + strain adjustment. This implements SLP-NEED-01.

Purpose: Replace the hardcoded 480-minute sleep-need constant with an algorithm that adapts to age, recent sleep history, and prior-day strain. This plan delivers the algorithm only; Plan 114-02 wires it into the bridge and replaces the hardcoded call sites.

Output:
- `Rust/core/src/sleep_need.rs` — pure algorithm (`compute_sleep_need`) + store-backed wrapper (`compute_sleep_need_with_store`) + `SleepNeedResult` struct + 8 inline unit tests
- `pub mod sleep_need;` registered in `Rust/core/src/lib.rs`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-harvard-sleep-need-model/114-CONTEXT.md
@.planning/phases/114-harvard-sleep-need-model/114-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan (114-01):
- New file: `Rust/core/src/sleep_need.rs`
- `pub struct SleepNeedResult { pub base_need_minutes: f64, pub debt_adjustment_minutes: f64, pub strain_adjustment_minutes: f64, pub total_need_minutes: f64 }`
- `pub fn compute_sleep_need(age_years: Option<u8>, history: &[f64], prior_strain: Option<f64>) -> SleepNeedResult`
- `pub fn compute_sleep_need_with_store(store: &GooseStore, age_years: Option<u8>, prior_strain: Option<f64>) -> GooseResult<SleepNeedResult>`
- Private helpers: `age_bracket_baseline(Option<u8>) -> f64`, `ewma_debt(&[f64], f64) -> f64`, `strain_adjustment(Option<f64>) -> f64`, `last_5_sleep_durations(&GooseStore) -> GooseResult<Vec<f64>>`
- `pub mod sleep_need;` line added to `Rust/core/src/lib.rs` (alphabetical position between `sleep_staging` ordering — see task for exact placement)

Symbols added by Plan 114-02 (NOT this plan): `sleep.compute_need` in BRIDGE_METHODS, `SleepComputeNeedArgs` struct, dispatcher arm, `sleep_compute_need_bridge` impl fn, `age_years` fields on options/args structs.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sleep_need.rs algorithm module with SleepNeedResult and pure compute_sleep_need</name>
  <files>Rust/core/src/sleep_need.rs, Rust/core/src/lib.rs</files>
  <read_first>
    - Rust/core/src/baselines.rs — read the EWMA engine: `pub const ALPHA: f64 = 0.0483;`, `pub struct EwmaState { pub mean, pub variance, pub night_count }`, and `pub fn fold(&mut self, x: f64)`. Confirm `EwmaState` implements `Default` (or how it is constructed) before using `EwmaState::default()`. If it does not derive Default, construct it with explicit zero fields instead.
    - Rust/core/src/lib.rs lines 1-70 — read the `pub mod` block to find the exact alphabetical insertion point for `pub mod sleep_need;` (existing: `pub mod sleep_staging;` at line 60, `pub mod sleep_validation;` at line 61). `sleep_need` sorts before `sleep_staging`, so insert `pub mod sleep_need;` immediately BEFORE the `pub mod sleep_staging;` line.
    - Rust/core/src/store/mod.rs — confirm the `GooseStore` type path and the `GooseResult` alias used crate-wide (search for `pub type GooseResult` and `pub struct GooseStore`). Used only for type signatures in this task (the store-backed fn is Task 2).
  </read_first>
  <behavior>
    - compute_sleep_need(Some(22), &[], None) → SleepNeedResult { base_need_minutes: 480.0, debt_adjustment_minutes: 0.0, strain_adjustment_minutes: 0.0, total_need_minutes: 480.0 }
    - compute_sleep_need(None, &[], None) → base_need_minutes: 450.0, total_need_minutes: 450.0
    - compute_sleep_need(Some(70), &[], None) → base_need_minutes: 420.0
    - compute_sleep_need(Some(25), &[], None) → base_need_minutes: 480.0 (boundary: 25 is in 18-25 bracket)
    - compute_sleep_need(Some(26), &[], None) → base_need_minutes: 450.0 (boundary: 26 is in 26-64 bracket)
    - compute_sleep_need(Some(65), &[], None) → base_need_minutes: 420.0 (boundary: 65 is in 65+ bracket)
    - compute_sleep_need(None, &[], Some(16.0)) → strain_adjustment_minutes: 15.0
    - compute_sleep_need(None, &[], Some(15.0)) → strain_adjustment_minutes: 15.0 (>= 15.0 inclusive)
    - compute_sleep_need(None, &[], Some(12.0)) → strain_adjustment_minutes: 6.0
    - compute_sleep_need(None, &[], Some(10.0)) → strain_adjustment_minutes: 6.0 (>= 10.0 inclusive)
    - compute_sleep_need(None, &[], Some(9.9)) → strain_adjustment_minutes: 0.0
    - compute_sleep_need(None, &[f64::NAN as anything], Some(f64::NAN)) → strain_adjustment_minutes: 0.0 (NaN comparisons are false, fall through to 0.0)
    - compute_sleep_need(None, &[360.0; 5], None) → debt_adjustment_minutes > 0.0 (consistently undersleeping vs 450.0 base)
    - compute_sleep_need(None, &[450.0, 460.0, 455.0, 470.0, 450.0], None) → debt_adjustment_minutes == 0.0 (adequate sleep, EWMA mean >= base, debt clamped at 0)
  </behavior>
  <action>
    Create `Rust/core/src/sleep_need.rs` as a pure algorithm module with zero bridge imports.

    Imports: `use crate::baselines::{ALPHA, EwmaState};` and `use crate::store::GooseStore;` and the crate's `GooseResult` alias (use the exact path discovered in read_first — likely `use crate::GooseResult;`). Note: `ALPHA` is imported for documentation/consistency even though `EwmaState::fold()` applies it internally; if the compiler warns about an unused `ALPHA` import, remove `ALPHA` from the import list and import only `EwmaState`. Do not silence the warning with `#[allow]`.

    Define the result struct (derive Debug, Clone, Copy, PartialEq):
    `pub struct SleepNeedResult { pub base_need_minutes: f64, pub debt_adjustment_minutes: f64, pub strain_adjustment_minutes: f64, pub total_need_minutes: f64 }`

    Define the pure entry point:
    `pub fn compute_sleep_need(age_years: Option<u8>, history: &[f64], prior_strain: Option<f64>) -> SleepNeedResult`
    Body: `let base = age_bracket_baseline(age_years); let debt = ewma_debt(history, base); let strain = strain_adjustment(prior_strain);` then return `SleepNeedResult { base_need_minutes: base, debt_adjustment_minutes: debt, strain_adjustment_minutes: strain, total_need_minutes: base + debt + strain }`.

    Define private helper `fn age_bracket_baseline(age_years: Option<u8>) -> f64` with match arms:
    - `Some(a) if a <= 25 => 480.0` (18-25: 8h)
    - `Some(a) if a >= 65 => 420.0` (65+: 7h)
    - `_ => 450.0` (26-64 and None: 7.5h, per D-03)

    Define private helper `fn ewma_debt(history: &[f64], base_need: f64) -> f64`: if `history.is_empty()` return `0.0`; otherwise construct an `EwmaState` (via `EwmaState::default()` or explicit zero fields per read_first finding), loop `for &d in history { state.fold(d); }`, then return `(base_need - state.mean).max(0.0)`. The `.max(0.0)` clamps debt to non-negative — adequate sleep yields zero debt, never negative.

    Define private helper `fn strain_adjustment(prior_strain: Option<f64>) -> f64` with match arms:
    - `Some(s) if s >= 15.0 => 15.0` (+0.25h)
    - `Some(s) if s >= 10.0 => 6.0` (+0.1h)
    - `_ => 0.0` (also catches None and NaN, since NaN comparisons return false)

    History MUST be folded oldest-first (chronological). EwmaState::fold weights the last element most heavily; document this ordering contract in a `//` comment above `compute_sleep_need`.

    The store-backed wrapper `compute_sleep_need_with_store` and `last_5_sleep_durations` helper are added in Task 2 — leave them out of this task to keep the pure algorithm and the I/O wrapper as separate commits.

    Add inline tests in a `#[cfg(test)] mod tests { use super::*; ... }` block at the bottom of the file covering every case in <behavior>. Name the strain tests explicitly: `strain_above_15_adds_15_minutes`, `strain_above_10_adds_6_minutes`, `strain_below_10_adds_0_minutes`. Name age tests: `age_bracket_18_25_returns_480`, `age_bracket_none_returns_450`, `age_bracket_65_plus_returns_420`, and boundary tests `age_25_in_young_bracket`, `age_26_in_mid_bracket`, `age_65_in_old_bracket`. Name debt tests: `cold_start_no_history_zero_debt`, `ewma_debt_positive_when_undersleeping`, `ewma_debt_zero_when_adequate`. Add `total_need_is_sum_of_components`.

    Register the module: in `Rust/core/src/lib.rs`, insert `pub mod sleep_need;` immediately before the `pub mod sleep_staging;` line (alphabetical order within the `pub mod` block).
  </action>
  <verify>
    <automated>cd Rust/core && cargo test --locked sleep_need 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `Rust/core/src/sleep_need.rs` exists and contains `pub fn compute_sleep_need` and `pub struct SleepNeedResult` with all four documented f64 fields.
    - `Rust/core/src/lib.rs` contains `pub mod sleep_need;` placed alphabetically before `pub mod sleep_staging;`.
    - `cd Rust/core && cargo test --locked sleep_need` compiles and all inline unit tests pass (zero failures).
    - All 8+ behaviors from <behavior> are covered by named tests; age and strain boundary cases (25/26, 65; 15.0/10.0/9.9) are explicitly tested.
    - No `#[allow(...)]` attribute is used to suppress unused-import or dead-code warnings introduced by this task.
    - `cd Rust/core && cargo build --locked` succeeds with no new warnings from sleep_need.rs.
  </acceptance_criteria>
  <done>compute_sleep_need is implemented as a pure function with correct age-bracket, EWMA-debt, and strain-adjustment behavior; module is registered in lib.rs; all inline unit tests pass under `cargo test --locked`.</done>
</task>

<task type="auto">
  <name>Task 2: Add store-backed compute_sleep_need_with_store wrapper with last-5-session self-query</name>
  <files>Rust/core/src/sleep_need.rs</files>
  <read_first>
    - Rust/core/src/sleep_need.rs — the module created in Task 1 (read the full current contents before editing).
    - Rust/core/src/store/sleep.rs — read `external_sleep_sessions_between` (around line 113): confirm its exact signature, parameter types (it takes `i64` start/end bounds), and that it returns `GooseResult<Vec<ExternalSleepSessionRow>>`.
    - Rust/core/src/store/mod.rs (around line 832) — read `struct ExternalSleepSessionRow`: confirm it has `pub duration_ms: i64` and `pub end_time_unix_ms: i64` fields. Use the EXACT field names found here. Do NOT tuple-destructure — `external_sleep_sessions_between` returns a Vec of named structs, so map via field access (per project skill cs:s1-369 step 5).
  </read_first>
  <action>
    Add two items to `Rust/core/src/sleep_need.rs` (do not modify the pure functions from Task 1).

    1. Private helper `fn last_5_sleep_durations(store: &GooseStore) -> GooseResult<Vec<f64>>`:
    - Call `let mut sessions = store.external_sleep_sessions_between(0, i64::MAX)?;`
    - Sort ascending by end time: `sessions.sort_by_key(|s| s.end_time_unix_ms);` (use the exact field name confirmed in read_first).
    - Build durations in minutes, applying the 60-minute nap guard, taking the last 5, restoring chronological order:
      Map each session to `session.duration_ms as f64 / 60_000.0`, filter out any value `< 60.0` (nap guard — mirrors the 60-minute threshold used by `external_sleep_history_nights_for_sleep_v1`). After filtering chronologically, take the last (most recent) 5. Implementation: filter into a `Vec<f64>` in chronological order, then if `len > 5` drain/slice to keep only the final 5 elements (preserving chronological/oldest-first order so EWMA fold weights the most recent night most heavily). Return `Ok(durations)`.
    - Access struct fields by name (`s.duration_ms`, `s.end_time_unix_ms`). Do NOT use tuple-destructuring lambdas — the store returns named `ExternalSleepSessionRow` structs (project skill cs:s1-369: tuple destructuring against a named struct causes E0308).

    2. Public store-backed wrapper:
    `pub fn compute_sleep_need_with_store(store: &GooseStore, age_years: Option<u8>, prior_strain: Option<f64>) -> GooseResult<SleepNeedResult>`
    Body: `let history = last_5_sleep_durations(store)?; Ok(compute_sleep_need(age_years, &history, prior_strain))`.

    Add an inline test `cold_start_with_store_empty_db_returns_base_only` in the existing `#[cfg(test)] mod tests`: open an in-memory store via `GooseStore::open_in_memory().expect(...)` (confirm this constructor exists in store/mod.rs; if the in-memory constructor has a different name, use the canonical test constructor used elsewhere in the crate's inline tests), call `compute_sleep_need_with_store(&store, None, None)`, assert `base_need_minutes == 450.0`, `debt_adjustment_minutes == 0.0`, `total_need_minutes == 450.0`. Import `use crate::store::GooseStore;` inside the test module if not already imported at module scope.
  </action>
  <verify>
    <automated>cd Rust/core && cargo test --locked sleep_need 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `compute_sleep_need_with_store(store, age_years, prior_strain) -> GooseResult<SleepNeedResult>` is defined and public.
    - `last_5_sleep_durations` calls `store.external_sleep_sessions_between(0, i64::MAX)`, sorts by `end_time_unix_ms`, converts `duration_ms` to minutes via `/ 60_000.0`, excludes durations `< 60.0`, and keeps at most the 5 most-recent sessions in chronological order.
    - The mapping accesses `ExternalSleepSessionRow` fields by name (no tuple destructuring); code compiles without E0308.
    - `cd Rust/core && cargo test --locked sleep_need` passes including the new cold-start-with-store test.
    - On an empty database, `compute_sleep_need_with_store(&store, None, None)` returns total_need_minutes == 450.0 with zero debt.
  </acceptance_criteria>
  <done>compute_sleep_need_with_store self-queries the last 5 non-nap sleep sessions from SQLite and composes them into the pure algorithm; cold-start on an empty DB returns the base bracket with zero debt; all sleep_need tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored DB row → algorithm | `external_sleep_sessions.duration_ms` / `end_time_unix_ms` read from SQLite (already written by trusted ingestion paths) |
| caller args → algorithm | `age_years`, `prior_strain` are passed into the pure function; no network or auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering | corrupted `duration_ms` (e.g. i64::MIN) in a DB row | mitigate | `duration_ms as f64 / 60_000.0` yields a negative value that fails the `< 60.0` nap guard and is excluded from history |
| T-114-02 | Denial of Service | `prior_strain = f64::NAN` / `f64::INFINITY` from a future caller | mitigate | `strain_adjustment` match arms (`s >= 15.0`, `s >= 10.0`) evaluate false for NaN, falling through to `0.0`; no panic |
| T-114-03 | Information Disclosure | none — no new external output, no PII in sleep durations beyond what is already stored | accept | algorithm reads only durations already persisted by trusted paths; no new egress |
</threat_model>

<verification>
- `cd Rust/core && cargo test --locked sleep_need 2>&1` — all unit + store tests pass.
- `cd Rust/core && cargo build --locked 2>&1` — compiles clean, no new warnings.
- Manual review: confirm `perf_budget.rs:677` was NOT touched (out of scope for this plan).
</verification>

<success_criteria>
- `Rust/core/src/sleep_need.rs` exists with `compute_sleep_need`, `compute_sleep_need_with_store`, and `SleepNeedResult`.
- Age brackets correct: 18-25 → 480.0, 26-64/None → 450.0, 65+ → 420.0.
- EWMA debt uses `baselines::EwmaState::fold()` (alpha 0.0483); cold-start debt = 0.0.
- Strain adjustment: ≥15 → +15, ≥10 → +6, else 0.
- `pub mod sleep_need;` registered in lib.rs.
- All inline tests pass under `cargo test --locked`.
</success_criteria>

<output>
Create `.planning/phases/114-harvard-sleep-need-model/114-01-SUMMARY.md` when done.
</output>
