---
phase: 114-harvard-sleep-need-model
plan: 02
type: execute
wave: 2
depends_on:
  - 114-01
files_modified:
  - Rust/core/src/bridge/mod.rs
  - Rust/core/src/bridge/sleep.rs
  - Rust/core/src/bridge/metrics.rs
  - Rust/core/src/metric_features.rs
  - Rust/core/tests/bridge_tests.rs
autonomous: true
requirements:
  - SLP-NEED-02
must_haves:
  truths:
    - "sleep.compute_need bridge method round-trips on an empty DB and returns base_need_minutes == 450.0, total_need_minutes == 450.0"
    - "bridge_methods_constant_matches_dispatcher test passes (sleep.compute_need registered + dispatched)"
    - "bridge_methods_constant_is_sorted_and_unique test passes (sleep.compute_need in correct alphabetical slot)"
    - "SleepFeatureScoreOptions::default().sleep_need_minutes == 450.0 (was 480.0)"
    - "RecoveryFeatureScoreOptions::default().sleep_need_minutes == 450.0 (was 480.0)"
    - "bridge/metrics.rs no longer contains unwrap_or(480.0) at the two sleep_need_minutes call sites"
    - "perf_budget.rs:677 still contains literal 480.0 (unchanged)"
  artifacts:
    - path: "Rust/core/src/bridge/sleep.rs"
      provides: "SleepComputeNeedArgs + sleep.compute_need dispatch arm + sleep_compute_need_bridge impl"
      contains: "sleep_compute_need_bridge"
    - path: "Rust/core/src/bridge/mod.rs"
      provides: "sleep.compute_need entry in BRIDGE_METHODS"
      contains: "sleep.compute_need"
    - path: "Rust/core/tests/bridge_tests.rs"
      provides: "sleep.compute_need round-trip integration test"
      contains: "sleep.compute_need"
  key_links:
    - from: "Rust/core/src/bridge/sleep.rs"
      to: "Rust/core/src/sleep_need.rs"
      via: "sleep_compute_need_bridge calls compute_sleep_need_with_store"
      pattern: "compute_sleep_need_with_store"
    - from: "Rust/core/src/bridge/metrics.rs"
      to: "Rust/core/src/sleep_need.rs"
      via: "score call sites call compute_sleep_need_with_store instead of unwrap_or(480.0)"
      pattern: "compute_sleep_need_with_store"
    - from: "Rust/core/src/bridge/mod.rs"
      to: "Rust/core/src/bridge/sleep.rs"
      via: "bridge_methods_constant_matches_dispatcher scans include_str!(sleep.rs) for the dispatch arm"
      pattern: "sleep.compute_need"
---

<objective>
Register the `sleep.compute_need` bridge method and replace all four hardcoded `480.0` algorithm call sites with the Phase 114-01 sleep-need algorithm. This implements SLP-NEED-02.

Purpose: Expose the sleep-need algorithm over the JSON FFI bridge so Phase 120 (Sleep Need UI) can call it, and make the live sleep/recovery scoring paths use the dynamic value instead of the hardcoded 480-minute constant.

Output:
- `sleep.compute_need` added to `BRIDGE_METHODS` (alphabetical) + dispatch arm + `SleepComputeNeedArgs` struct + `sleep_compute_need_bridge` impl in `bridge/sleep.rs`
- `SleepFeatureScoreOptions` and `RecoveryFeatureScoreOptions` Default `sleep_need_minutes` changed 480.0 → 450.0, with new `age_years: Option<u8>` field (default None)
- `bridge/metrics.rs:3243` and `:3341` `unwrap_or(480.0)` replaced with a dynamic `compute_sleep_need_with_store` call; `age_years` plumbed into the two score Args structs
- `perf_budget.rs:677` left UNCHANGED as literal 480.0
- Round-trip integration test in `tests/bridge_tests.rs`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-harvard-sleep-need-model/114-CONTEXT.md
@.planning/phases/114-harvard-sleep-need-model/114-RESEARCH.md
@Rust/core/src/sleep_need.rs
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan (114-02):
- `"sleep.compute_need"` string added to `BRIDGE_METHODS` array in `Rust/core/src/bridge/mod.rs` (between `"sleep.add_correction_label"` and `"sleep.import_external_history"`)
- `struct SleepComputeNeedArgs { database_path: String, age_years: Option<u8>, prior_strain: Option<f64> }` in `Rust/core/src/bridge/sleep.rs`
- Dispatch arm `"sleep.compute_need" => ...` in `bridge/sleep.rs`
- `fn sleep_compute_need_bridge(args: SleepComputeNeedArgs) -> GooseResult<serde_json::Value>` in `bridge/sleep.rs`
- New field `pub age_years: Option<u8>` on `SleepFeatureScoreOptions` and `RecoveryFeatureScoreOptions` (`metric_features.rs`), default `None`
- New field `age_years: Option<u8>` on `SleepFeatureScoreArgs` and `RecoveryFeatureScoreArgs` (`bridge/metrics.rs`)
- New integration test in `Rust/core/tests/bridge_tests.rs` covering the `sleep.compute_need` round-trip

Consumes from Plan 114-01: `crate::sleep_need::{compute_sleep_need, compute_sleep_need_with_store, SleepNeedResult}`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register sleep.compute_need bridge method (BRIDGE_METHODS + Args + dispatch + impl)</name>
  <files>Rust/core/src/bridge/mod.rs, Rust/core/src/bridge/sleep.rs</files>
  <read_first>
    - Rust/core/src/bridge/mod.rs lines 180-195 — the `BRIDGE_METHODS` array around the sleep entries. Confirm `"sleep.add_correction_label",` is at line 183 and `"sleep.import_external_history",` at line 184. Insert the new entry BETWEEN them.
    - Rust/core/src/bridge/mod.rs lines 1080-1200 — read `bridge_methods_constant_matches_dispatcher` and `bridge_methods_constant_is_sorted_and_unique`. Note line 1109 `include_str!("sleep.rs")` — the scanner reads sleep.rs at compile time. Understand the exact dispatch-arm format the scanner matches (a line starting with `"<method>"` containing a `.` and ending the method string with `=>`).
    - Rust/core/src/bridge/sleep.rs — read the full file: the top imports (confirm `use serde_json::json;` and the `Deserialize` import), the `dispatch_sleep` match function and an existing sibling arm (e.g. `"sleep.add_correction_label"`), an existing Args struct pattern, an existing bridge impl fn, and the connection-acquisition helper used by sibling functions (`acquire_bridge_conn` or equivalent — use the EXACT helper name found in the file). Also confirm how `request_args::<T>`, `bridge_ok`, and `bridge_error` are used in sibling arms.
    - Rust/core/src/sleep_need.rs — confirm the signature `pub fn compute_sleep_need_with_store(store: &GooseStore, age_years: Option<u8>, prior_strain: Option<f64>) -> GooseResult<SleepNeedResult>` and the `SleepNeedResult` field names.
  </read_first>
  <action>
    Wire the bridge method across the two files. Both edits MUST land together (the dispatcher-consistency test fails if BRIDGE_METHODS and the dispatch arm disagree).

    1. In `Rust/core/src/bridge/mod.rs` BRIDGE_METHODS array: insert the line `    "sleep.compute_need",` immediately after `    "sleep.add_correction_label",` and before `    "sleep.import_external_history",`. This is the correct alphabetical slot (`compute` < `import`).

    2. In `Rust/core/src/bridge/sleep.rs`:

    a. Add the import for the algorithm at the top with the other crate imports: `use crate::sleep_need::compute_sleep_need_with_store;`

    b. Add the Args struct (match the existing sibling derive/style):
    `#[derive(Debug, Clone, Deserialize)] struct SleepComputeNeedArgs { database_path: String, #[serde(default)] age_years: Option<u8>, #[serde(default)] prior_strain: Option<f64> }`

    c. Add the dispatch arm inside `dispatch_sleep`, placed alongside sibling sleep arms. The arm's first line MUST be formatted exactly so the scanner recognizes it:
    `"sleep.compute_need" => request_args::<SleepComputeNeedArgs>(request)`
    then `.and_then(sleep_compute_need_bridge)`, then map success via `bridge_ok(&request.request_id, value)` and failure via `bridge_error(&request.request_id, "method_error", error)` — copy the EXACT chaining pattern used by the sibling `"sleep.add_correction_label"` arm (the helper names and signatures must match what that arm uses).

    d. Add the bridge impl fn:
    `fn sleep_compute_need_bridge(args: SleepComputeNeedArgs) -> GooseResult<serde_json::Value>` that:
    - acquires the store via the same helper sibling functions use (e.g. `let store = acquire_bridge_conn(&args.database_path)?;` — use the exact helper name from read_first),
    - calls `let result = compute_sleep_need_with_store(&store, args.age_years, args.prior_strain)?;`,
    - returns `Ok(serde_json::json!({ "schema": "goose.sleep-need-result.v1", "base_need_minutes": result.base_need_minutes, "debt_adjustment_minutes": result.debt_adjustment_minutes, "strain_adjustment_minutes": result.strain_adjustment_minutes, "total_need_minutes": result.total_need_minutes }))`. Use bare `json!` if the file already imports it; otherwise fully-qualify as `serde_json::json!` to match the file's existing convention.

    Do NOT change the perf_budget.rs file in this task.
  </action>
  <verify>
    <automated>cd Rust/core && cargo test --locked bridge_methods_constant_matches_dispatcher bridge_methods_constant_is_sorted_and_unique 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `BRIDGE_METHODS` contains `"sleep.compute_need"` positioned between `"sleep.add_correction_label"` and `"sleep.import_external_history"`.
    - `bridge/sleep.rs` contains `struct SleepComputeNeedArgs` with `database_path: String`, `age_years: Option<u8>`, `prior_strain: Option<f64>` (latter two `#[serde(default)]`).
    - `bridge/sleep.rs` contains a `"sleep.compute_need" =>` dispatch arm and the `sleep_compute_need_bridge` impl fn that calls `compute_sleep_need_with_store`.
    - `cd Rust/core && cargo test --locked bridge_methods_constant_matches_dispatcher bridge_methods_constant_is_sorted_and_unique` passes (both tests green).
    - `cd Rust/core && cargo build --locked` compiles with no errors and no new warnings.
  </acceptance_criteria>
  <done>sleep.compute_need is registered in BRIDGE_METHODS, dispatched in bridge/sleep.rs, and backed by sleep_compute_need_bridge calling the 114-01 algorithm; both dispatcher-consistency tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded 480.0 in options Defaults and metrics call sites; add age_years plumbing</name>
  <files>Rust/core/src/metric_features.rs, Rust/core/src/bridge/metrics.rs</files>
  <read_first>
    - Rust/core/src/metric_features.rs lines 145-180 — `SleepFeatureScoreOptions` (line 149, derives Debug+Clone+Copy) and `RecoveryFeatureScoreOptions` (line 159, derives Clone). Note `Option<u8>` is Copy, so adding `age_years` to the Copy struct is safe.
    - Rust/core/src/metric_features.rs lines 240-270 — the two Default impls. Confirm `sleep_need_minutes: 480.0` at line 247 (SleepFeatureScoreOptions) and line 263 (RecoveryFeatureScoreOptions).
    - Rust/core/src/bridge/metrics.rs lines 1083-1160 — `SleepFeatureScoreArgs` (line 1083, `sleep_need_minutes: Option<f64>` at 1094) and `RecoveryFeatureScoreArgs` (line 1114, `sleep_need_minutes` at 1151). Read how these Args are deserialized and which fields use `#[serde(default)]`.
    - Rust/core/src/bridge/metrics.rs lines 3230-3350 — the two construction sites at line 3243 and 3341 where `sleep_need_minutes: args.sleep_need_minutes.unwrap_or(480.0)` builds the options structs. Confirm a `store` binding is in scope at each site (the bridge fn already opened a store); note its exact variable name.
    - Rust/core/src/sleep_need.rs — confirm `compute_sleep_need_with_store` signature and return type.
    - Rust/core/src/perf_budget.rs line 677 — confirm `sleep_need_minutes: 480.0` here. This line MUST NOT be modified (D-01 Claude's Discretion: perf budget test, not algorithm).
  </read_first>
  <action>
    Replace the four in-scope hardcoded `480.0` occurrences and plumb `age_years`. Leave perf_budget.rs untouched.

    1. In `Rust/core/src/metric_features.rs`:
    - Add field `pub age_years: Option<u8>,` to `SleepFeatureScoreOptions` (struct still derives Copy — `Option<u8>` is Copy).
    - Add field `pub age_years: Option<u8>,` to `RecoveryFeatureScoreOptions`.
    - In `Default for SleepFeatureScoreOptions` (line ~247): change `sleep_need_minutes: 480.0,` to `sleep_need_minutes: 450.0,` and add `age_years: None,`.
    - In `Default for RecoveryFeatureScoreOptions` (line ~263): change `sleep_need_minutes: 480.0,` to `sleep_need_minutes: 450.0,` and add `age_years: None,`.

    2. In `Rust/core/src/bridge/metrics.rs`:
    - Add the import `use crate::sleep_need::compute_sleep_need_with_store;` with the other crate imports at the top.
    - Add `#[serde(default)] age_years: Option<u8>,` to `SleepFeatureScoreArgs` and to `RecoveryFeatureScoreArgs`.
    - At the line 3243 construction site, replace `sleep_need_minutes: args.sleep_need_minutes.unwrap_or(480.0),` with:
      `sleep_need_minutes: args.sleep_need_minutes.unwrap_or_else(|| compute_sleep_need_with_store(&store, args.age_years, None).map(|r| r.total_need_minutes).unwrap_or(450.0)),`
      (use the exact in-scope store variable name from read_first; fall back to 450.0 — the new None-bracket base — if the store query errors, NOT 480.0). Also set the new options field: add `age_years: args.age_years,` to this options construction.
    - At the line 3341 construction site, apply the same replacement for `RecoveryFeatureScoreOptions`, and add `age_years: args.age_years,` to that options construction.

    Do NOT modify `perf_budget.rs:677` — it stays `sleep_need_minutes: 480.0` (perf budget literal, per D-01).

    After editing, format only the touched files: `cargo fmt -- src/metric_features.rs src/bridge/metrics.rs` (per project skill cs:s3-157 — do not run `cargo fmt --all`).
  </action>
  <verify>
    <automated>cd Rust/core && cargo test --locked 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `SleepFeatureScoreOptions::default().sleep_need_minutes == 450.0` and `RecoveryFeatureScoreOptions::default().sleep_need_minutes == 450.0`.
    - Both options structs have an `age_years: Option<u8>` field defaulting to `None`; `SleepFeatureScoreOptions` still compiles with its `Copy` derive.
    - `bridge/metrics.rs` no longer contains `unwrap_or(480.0)` — verified by `grep -n 'unwrap_or(480.0)' Rust/core/src/bridge/metrics.rs` returning no matches.
    - The two call sites call `compute_sleep_need_with_store(&store, args.age_years, None)` and pass `age_years: args.age_years` into the options.
    - `perf_budget.rs:677` still contains literal `480.0` — verified by `grep -n '480.0' Rust/core/src/perf_budget.rs` returning the line.
    - `cd Rust/core && cargo test --locked` passes the full suite with zero failures.
  </acceptance_criteria>
  <done>The four in-scope 480.0 occurrences are replaced (Defaults → 450.0, call sites → dynamic compute), age_years is plumbed through both options and Args structs, perf_budget.rs is untouched, and the full suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add sleep.compute_need bridge round-trip integration test</name>
  <files>Rust/core/tests/bridge_tests.rs</files>
  <read_first>
    - Rust/core/tests/bridge_tests.rs — read the file header and 2-3 existing sleep.* round-trip tests. Confirm the exact test harness: the `request(...)` helper signature, how a temp DB path is created (`tempfile::tempdir()` then `.join("goose.sqlite")`), and how the response is asserted (`response.ok`, `response.result.unwrap()`, indexing `result["field"]`). Mirror the existing style exactly.
  </read_first>
  <action>
    Add an integration test `sleep_compute_need_returns_default_age_bracket` to `Rust/core/tests/bridge_tests.rs` following the existing harness style:
    - Create a temp dir and db path (`tempfile::tempdir()`, `.path().join("goose.sqlite")`, to a `String`).
    - Issue a request with schema `goose.bridge.request.v1`, a `request_id`, `method: "sleep.compute_need"`, and `args: { "database_path": db_path }` (no age, no strain — exercises the None/cold-start path).
    - Assert `response.ok` is true (print `response.error` on failure for diagnostics).
    - From `response.result.unwrap()`, assert `result["base_need_minutes"] == 450.0`, `result["debt_adjustment_minutes"] == 0.0`, `result["strain_adjustment_minutes"] == 0.0`, `result["total_need_minutes"] == 450.0`.
    - Add a second test `sleep_compute_need_applies_strain_and_age`: same setup but `args: { "database_path": db_path, "age_years": 22, "prior_strain": 16.0 }`; assert `result["base_need_minutes"] == 480.0`, `result["strain_adjustment_minutes"] == 15.0`, `result["total_need_minutes"] == 495.0` (480 + 0 debt + 15 strain).

    Use exact JSON-value comparison consistent with sibling tests (serde_json numeric comparison — compare against `450.0` style float literals as the existing tests do; if sibling tests compare via `.as_f64()`, mirror that).
  </action>
  <verify>
    <automated>cd Rust/core && cargo test --locked sleep_compute_need 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `Rust/core/tests/bridge_tests.rs` contains `sleep_compute_need_returns_default_age_bracket` and `sleep_compute_need_applies_strain_and_age`.
    - On an empty DB with no args, the bridge returns base 450.0 / debt 0.0 / strain 0.0 / total 450.0.
    - With `age_years: 22, prior_strain: 16.0`, the bridge returns base 480.0 / strain 15.0 / total 495.0.
    - `cd Rust/core && cargo test --locked sleep_compute_need` passes both tests.
    - `cd Rust/core && cargo test --locked` full suite remains green.
  </acceptance_criteria>
  <done>Two bridge round-trip tests prove sleep.compute_need works end-to-end through the FFI request path for both the cold-start default and the age+strain case; full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift/FFI caller → bridge | `database_path`, `age_years`, `prior_strain` arrive as JSON args over `goose_bridge_handle_json` |
| bridge → SQLite | bridge opens/reads `external_sleep_sessions` via `acquire_bridge_conn` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-04 | Tampering | malformed `database_path` arg | mitigate | `acquire_bridge_conn` validates/opens the path (same hardening as all sibling bridge methods); errors return a bridge_error, not a panic |
| T-114-05 | Input Validation | `age_years` outside expected range / `prior_strain` NaN/Inf | mitigate | `age_years: Option<u8>` saturates to u8; algorithm match arms are total and NaN-safe (T-114-02 from 114-01) |
| T-114-06 | Denial of Service | extremely large `external_sleep_sessions` table | accept | query is bounded by `external_sleep_sessions_between`; only last 5 sessions used; acceptable for self-hosted single-user app |
| T-114-SC | Tampering | npm/pip/cargo installs | n/a | no new dependencies added this phase (all crate-internal) |
</threat_model>

<verification>
- `cd Rust/core && cargo test --locked 2>&1` — full suite green, including `bridge_methods_constant_matches_dispatcher`, `bridge_methods_constant_is_sorted_and_unique`, and the new sleep.compute_need round-trip tests.
- `grep -n 'unwrap_or(480.0)' Rust/core/src/bridge/metrics.rs` — returns nothing.
- `grep -n '480.0' Rust/core/src/perf_budget.rs` — still returns line 677 (unchanged).
- `grep -n '480.0' Rust/core/src/metric_features.rs` — returns nothing (both defaults now 450.0).
</verification>

<success_criteria>
- `sleep.compute_need` registered in BRIDGE_METHODS, dispatched, and impl-backed by the 114-01 algorithm.
- Bridge round-trips correctly for cold-start and age+strain cases.
- Both options Defaults are 450.0; both have `age_years: Option<u8>` (default None).
- Both metrics.rs call sites use dynamic compute instead of `unwrap_or(480.0)`.
- `perf_budget.rs:677` unchanged at 480.0.
- `cargo test --locked` passes clean, including the dispatcher-consistency tests.
</success_criteria>

<output>
Create `.planning/phases/114-harvard-sleep-need-model/114-02-SUMMARY.md` when done.
</output>
