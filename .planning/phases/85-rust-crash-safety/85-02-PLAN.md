---
phase: 85-rust-crash-safety
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - Rust/core/src/store.rs
autonomous: true
requirements: [ARCH-03]
must_haves:
  truths:
    - "store.rs has zero .unwrap() calls remaining (all 62 test-code unwraps became .expect())"
    - "The file-level #![allow(clippy::unwrap_used)] shield is removed from store.rs"
    - "store.rs compiles cleanly under deny(clippy::unwrap_used) with no shield"
  artifacts:
    - path: "Rust/core/src/store.rs"
      provides: "Test code using .expect() instead of .unwrap(); no allow shield"
  key_links:
    - from: "Rust/core/src/store.rs"
      to: "Rust/core/src/lib.rs deny lint"
      via: "shield removal exposes store.rs to the deny lint"
      pattern: "store\\.rs"
---

<objective>
Convert all 62 test-code `.unwrap()` calls in `store.rs` to `.expect("descriptive message")`
(D-03), then remove the `#![allow(clippy::unwrap_used)]` shield that Plan 1 added to the top
of the file.

store.rs has **zero** production unwrap violations (confirmed by clippy on HEAD) — all 62
`.unwrap()` are inside `#[cfg(test)]` blocks (first at line ~8992). Because the deny lint is
`cfg_attr(not(test), ...)`, removing the shield is safe once the production code is clean
(which it already is). The `.expect()` conversion is the D-03 quality improvement, and
`row.get(N)` Sqlite-backed calls already propagate via `?` (D-02) so no production change is
needed.

Purpose: Shrink the allow list by one module and improve test-failure readability.
Output: store.rs with `.expect()` test code and no allow shield.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-rust-crash-safety/85-CONTEXT.md
@.planning/phases/85-rust-crash-safety/85-RESEARCH.md
@Rust/core/src/error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert store.rs test .unwrap() to .expect() and remove shield</name>
  <read_first>
    - Rust/core/src/store.rs (line ~1: the #![allow(clippy::unwrap_used)] shield added by Plan 1; test blocks starting at line ~8992 where all 62 .unwrap() live)
    - 85-RESEARCH.md "Pattern 5: Test .unwrap() → .expect()" and "Revised Plan Scope Summary" (store.rs row: 0 production, 62 test)
    - 85-CONTEXT.md D-02 (Sqlite #[from] propagation) and D-03 (test conversion)
  </read_first>
  <files>Rust/core/src/store.rs</files>
  <action>
    Replace every `.unwrap()` in store.rs (all 62 are inside `#[cfg(test)]` blocks, first at
    line ~8992 per RESEARCH) with `.expect("descriptive message")` per D-03. Messages describe
    the call-site expectation (e.g., `.expect("migration should apply on fresh db")`,
    `.expect("row count query should succeed")`). Do NOT change any production function
    signatures — store.rs production code already uses `?` with `GooseError::Sqlite` via the
    `#[from]` impl (D-02), so no production work is required.

    After the conversion is complete, REMOVE the `#![allow(clippy::unwrap_used)]` inner
    attribute that Plan 1 added at the top of store.rs. This exposes store.rs to the deny lint;
    because all production code is clean and tests are exempt, clippy must report zero
    store.rs violations.
  </action>
  <verify>
    <automated>test "$(grep -c '\.unwrap()' Rust/core/src/store.rs)" -eq 0 && ! grep -q '#!\[allow(clippy::unwrap_used)\]' Rust/core/src/store.rs && cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.unwrap()' Rust/core/src/store.rs` returns 0
    - `grep -c '#!\[allow(clippy::unwrap_used)\]' Rust/core/src/store.rs` returns 0 (shield removed)
    - `cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used` reports no store.rs violation
    - `cargo test --locked --manifest-path Rust/core/Cargo.toml --lib` exits 0
  </acceptance_criteria>
  <done>store.rs has zero `.unwrap()`, no allow shield, and passes the deny lint; lib tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite persistence layer → callers | `row.get(N)` failures must propagate as typed `GooseError::Sqlite`, never panic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03 | Tampering | store.rs silent failure on malformed row data | mitigate | Production paths already use `?` → `GooseError::Sqlite` (D-02); shield removal proves no production unwrap remains |
| T-85-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no audit table required |
</threat_model>

<verification>
- `cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used` reports no store.rs violation
- `cargo test --locked --manifest-path Rust/core/Cargo.toml --lib` exits 0
</verification>

<success_criteria>
- store.rs has zero `.unwrap()` (62 converted to `.expect()`)
- store.rs no longer carries the allow shield
- store.rs passes the deny lint and all lib tests pass
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `Rust/core/src/store.rs` — modified: 62 test `.unwrap()` → `.expect()`; `#![allow(clippy::unwrap_used)]` shield removed
</artifacts_produced>

<output>
Create `.planning/phases/85-rust-crash-safety/85-02-SUMMARY.md` when done
</output>
