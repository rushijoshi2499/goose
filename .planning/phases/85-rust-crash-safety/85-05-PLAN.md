---
phase: 85-rust-crash-safety
plan: 05
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - Rust/core/src/energy_rollup.rs
  - Rust/core/src/step_discovery.rs
  - Rust/core/src/openwhoop_reference.rs
  - Rust/core/src/exercise_detection.rs
autonomous: true
requirements: [ARCH-03]
must_haves:
  truths:
    - "The 2 production .unwrap() sites (energy_rollup.rs:1744, step_discovery.rs:1024) no longer panic and no longer double-call official_label_policy_issue_action"
    - "All four small files have zero .unwrap() calls remaining"
    - "The file-level #![allow(clippy::unwrap_used)] shield is removed from all four files"
    - "All four files compile cleanly under deny(clippy::unwrap_used) with no shield"
  artifacts:
    - path: "Rust/core/src/energy_rollup.rs"
      provides: "Production if-let match-arm guard replacing double-call+unwrap"
    - path: "Rust/core/src/step_discovery.rs"
      provides: "Production if-let match-arm guard replacing double-call+unwrap"
    - path: "Rust/core/src/openwhoop_reference.rs"
      provides: "Test code using .expect(); no allow shield"
    - path: "Rust/core/src/exercise_detection.rs"
      provides: "Test code using .expect(); no allow shield"
  key_links:
    - from: "small-file production code"
      to: "Rust/core/src/lib.rs deny lint"
      via: "shield removal exposes the last 2 production unwrap sites to the deny lint"
      pattern: "official_label_policy_issue_action"
---

<objective>
Close the final 2 production `.unwrap()` violations and convert the remaining small-file test
`.unwrap()` to `.expect()` (D-03), then remove the `#![allow(clippy::unwrap_used)]` shields
from all four small files.

Production fixes (RESEARCH Pattern 6 / Pitfall 5, verified against HEAD):
- `energy_rollup.rs:1744` and `step_discovery.rs:1024` both use the match-arm pattern
  `_ if official_label_policy_issue_action(issue).is_some() => (..., official_label_policy_issue_action(issue).unwrap(), ...)`
  which calls the function twice and unwraps. Refactor each to an `if let` match-arm guard:
  `_ if let Some(action) = official_label_policy_issue_action(issue) => (..., action, ...)`,
  using the bound `action` in the arm body (call `.to_string()` on it in step_discovery.rs
  where the arm type is `String`). `if let` guards in match arms are stable in edition 2024
  / MSRV 1.96 (RESEARCH assumption A1).

Test conversions (D-03): openwhoop_reference.rs (3 test unwraps), exercise_detection.rs (1).
energy_rollup.rs and step_discovery.rs have 0 test unwraps.

Purpose: Eliminate the last 2 of the 5 production crash-safety holes and clear the small-file
shields so Plan 6's gate can confirm zero allow attributes remain.
Output: four small files with no `.unwrap()`, no allow shields, passing the deny lint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-rust-crash-safety/85-CONTEXT.md
@.planning/phases/85-rust-crash-safety/85-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 2 production unwrap sites via if-let match-arm guards</name>
  <read_first>
    - Rust/core/src/energy_rollup.rs lines 1737-1750 (energy_validation_next_actions match with double-call+unwrap at 1744)
    - Rust/core/src/step_discovery.rs lines 1015-1029 (step_capture_validation_next_actions match with double-call+unwrap at 1024)
    - 85-RESEARCH.md "Pattern 6" and "Pitfall 5" (if-let guard eliminates both the double call and the unwrap)
  </read_first>
  <files>Rust/core/src/energy_rollup.rs, Rust/core/src/step_discovery.rs</files>
  <action>
    In energy_rollup.rs at line ~1741-1745, replace the match arm
    `_ if official_label_policy_issue_action(issue).is_some() => ("energy:validation", issue.as_str(), official_label_policy_issue_action(issue).unwrap())`
    with `_ if let Some(action) = official_label_policy_issue_action(issue) => ("energy:validation", issue.as_str(), action)`.
    The arm body uses the bound `action` directly (no second call, no unwrap).

    In step_discovery.rs at line ~1022-1025, replace the match arm
    `_ if official_label_policy_issue_action(issue).is_some() => ("Official WHOOP label provenance is not marked as validation-only", official_label_policy_issue_action(issue).unwrap().to_string())`
    with `_ if let Some(action) = official_label_policy_issue_action(issue) => ("Official WHOOP label provenance is not marked as validation-only", action.to_string())`.
    Keep `.to_string()` because the arm type is `String`.

    Do NOT change the surrounding match arms or function signatures.
  </action>
  <verify>
    <automated>! grep -n 'official_label_policy_issue_action(issue).unwrap()' Rust/core/src/energy_rollup.rs && ! grep -n 'official_label_policy_issue_action(issue).unwrap()' Rust/core/src/step_discovery.rs && grep -q 'if let Some(action) = official_label_policy_issue_action' Rust/core/src/energy_rollup.rs && grep -q 'if let Some(action) = official_label_policy_issue_action' Rust/core/src/step_discovery.rs && cargo build --manifest-path Rust/core/Cargo.toml --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'official_label_policy_issue_action(issue).unwrap()' Rust/core/src/energy_rollup.rs` returns 0
    - `grep -c 'official_label_policy_issue_action(issue).unwrap()' Rust/core/src/step_discovery.rs` returns 0
    - `grep -c 'if let Some(action) = official_label_policy_issue_action' Rust/core/src/energy_rollup.rs` returns 1
    - `grep -c 'if let Some(action) = official_label_policy_issue_action' Rust/core/src/step_discovery.rs` returns 1
    - `cargo build --manifest-path Rust/core/Cargo.toml --lib` exits 0
  </acceptance_criteria>
  <done>Both production sites use if-let guards (single call, no unwrap); crate builds.</done>
</task>

<task type="auto">
  <name>Task 2: Convert small-file test .unwrap() to .expect() and remove all four shields</name>
  <read_first>
    - Rust/core/src/openwhoop_reference.rs (3 test .unwrap() inside #[cfg(test)]; shield at line ~1)
    - Rust/core/src/exercise_detection.rs (1 test .unwrap() inside #[cfg(test)]; shield at line ~1)
    - Rust/core/src/energy_rollup.rs and Rust/core/src/step_discovery.rs (shields at line ~1; 0 test unwraps)
    - 85-RESEARCH.md "Pattern 5" and "Revised Plan Scope Summary" (small files: 2 prod + 4 test)
  </read_first>
  <files>
    Rust/core/src/openwhoop_reference.rs,
    Rust/core/src/exercise_detection.rs,
    Rust/core/src/energy_rollup.rs,
    Rust/core/src/step_discovery.rs
  </files>
  <action>
    Convert the 3 test `.unwrap()` in openwhoop_reference.rs and the 1 test `.unwrap()` in
    exercise_detection.rs to `.expect("descriptive message")` per D-03.

    Then REMOVE the `#![allow(clippy::unwrap_used)]` inner attribute Plan 1 added at the top of
    ALL FOUR files: energy_rollup.rs, step_discovery.rs, openwhoop_reference.rs,
    exercise_detection.rs. After Task 1 fixed the 2 production sites and this task converts the
    4 test sites, all four files must contain zero `.unwrap()`.
  </action>
  <verify>
    <automated>for f in energy_rollup step_discovery openwhoop_reference exercise_detection; do test "$(grep -c '\.unwrap()' Rust/core/src/$f.rs)" -eq 0 || exit 1; grep -q '#!\[allow(clippy::unwrap_used)\]' Rust/core/src/$f.rs && exit 1; done; cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.unwrap()'` returns 0 for each of energy_rollup.rs, step_discovery.rs, openwhoop_reference.rs, exercise_detection.rs
    - `grep -c '#!\[allow(clippy::unwrap_used)\]'` returns 0 for each of the four files (shields removed)
    - `cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used` reports no violation in any of the four files
    - `cargo test --locked --manifest-path Rust/core/Cargo.toml --lib` exits 0
  </acceptance_criteria>
  <done>All four small files have zero `.unwrap()`, no allow shields, and pass the deny lint; lib tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation issue strings → next-action resolution | A None policy action must not panic the validation report loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-07 | Denial of Service | energy_rollup.rs:1744 / step_discovery.rs:1024 unwrap-on-None in validation loop | mitigate | if-let match-arm guard binds the Some value once, eliminating both the unwrap and the duplicate call (Pitfall 5) |
| T-85-08 | Tampering | Test-only unwrap masking reference/exercise-detection failures | mitigate | D-03 conversion to `.expect("...")`; shield removal proves no production unwrap remains |
| T-85-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no audit table required |
</threat_model>

<verification>
- `cargo build --manifest-path Rust/core/Cargo.toml --lib` exits 0
- `cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used` reports no violation in the four files
- `cargo test --locked --manifest-path Rust/core/Cargo.toml --lib` exits 0
</verification>

<success_criteria>
- 2 production unwrap sites fixed via if-let match-arm guards (no double-call)
- All four small files have zero `.unwrap()` and no allow shields
- All four files pass the deny lint and all lib tests pass
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `Rust/core/src/energy_rollup.rs` — modified: production if-let guard at ~1741-1745; `#![allow]` shield removed
- `Rust/core/src/step_discovery.rs` — modified: production if-let guard at ~1022-1025; `#![allow]` shield removed
- `Rust/core/src/openwhoop_reference.rs` — modified: 3 test `.unwrap()` → `.expect()`; `#![allow]` shield removed
- `Rust/core/src/exercise_detection.rs` — modified: 1 test `.unwrap()` → `.expect()`; `#![allow]` shield removed
</artifacts_produced>

<output>
Create `.planning/phases/85-rust-crash-safety/85-05-SUMMARY.md` when done
</output>
