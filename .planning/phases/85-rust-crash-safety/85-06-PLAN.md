---
phase: 85-rust-crash-safety
plan: 06
type: execute
wave: 3
depends_on: ["85-01", "85-02", "85-03", "85-04", "85-05"]
files_modified: []
autonomous: true
requirements: [ARCH-03]
must_haves:
  truths:
    - "cargo clippy --lib -- -D clippy::unwrap_used passes with zero violations (ARCH-03 SC1)"
    - "catch_unwind exists at the bridge dispatcher entry point (ARCH-03 SC2 — already satisfied, verified not reimplemented)"
    - "cargo test --locked passes the full suite including integration tests (ARCH-03 SC3)"
    - "No #![allow(clippy::unwrap_used)] shields remain in any module"
    - "clippy::unnecessary_unwrap is still present in the lib.rs allow block (D-05 preserved)"
  artifacts: []
  key_links:
    - from: "deny(clippy::unwrap_used) lint"
      to: "all production modules"
      via: "zero-shield, zero-violation gate"
      pattern: "deny\\(clippy::unwrap_used\\)"
---

<objective>
Verification-only gate plan: confirm Phase 85 is complete with no production code changes.
This plan runs the lint and test gates and verifies the structural invariants — it MUST NOT
modify any production source file.

Verifies the three ARCH-03 success criteria:
- SC1: `cargo clippy --lib -- -D clippy::unwrap_used` passes with zero violations.
- SC2: `catch_unwind` already exists at `goose_bridge_handle_json` (bridge.rs ~3101-3119,
  added Phase 84). D-06: VERIFY it exists; do NOT reimplement.
- SC3: `cargo test --locked` passes the full suite (unit + 45 integration test files).

Also verifies the cleanup invariants: no `#![allow(clippy::unwrap_used)]` shields remain in
any module, and `clippy::unnecessary_unwrap` is still in the lib.rs allow block (D-05).

Purpose: Prove the crash-safety gate holds end-to-end before `/gsd-verify-work`.
Output: 85-06-SUMMARY.md recording the gate results. No source changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-rust-crash-safety/85-CONTEXT.md
@.planning/phases/85-rust-crash-safety/85-RESEARCH.md
@.planning/phases/85-rust-crash-safety/85-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify lint, catch_unwind, and structural cleanup invariants</name>
  <read_first>
    - 85-RESEARCH.md "Validation Architecture" → "Phase Requirements → Test Map" (exact gate commands)
    - 85-CONTEXT.md D-05 (keep unnecessary_unwrap) and D-06 (verify catch_unwind, do not reimplement)
    - Rust/core/src/bridge.rs lines 3101-3119 (existing catch_unwind — read-only verification)
  </read_first>
  <files></files>
  <action>
    Run the SC1 lint gate: `cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used`.
    It MUST exit 0 with zero violations. Scope to `--lib` only (Pitfall 3: bin crates are
    separate compile units and have unrelated lint warnings out of scope for Phase 85).

    Verify SC2 (read-only, D-06): confirm `catch_unwind` is present in bridge.rs via
    `grep -n 'catch_unwind' Rust/core/src/bridge.rs`. Do NOT modify or reimplement it.

    Verify structural cleanup: confirm no `#![allow(clippy::unwrap_used)]` remains anywhere in
    `Rust/core/src/` via `grep -rl 'allow(clippy::unwrap_used)' Rust/core/src/` (must return
    nothing). Confirm `clippy::unnecessary_unwrap` is still in lib.rs (D-05) and the deny
    attribute is present.

    This task makes NO source changes. If any gate fails, report the failing file/line in the
    summary and stop — do NOT patch it here (gap-closure handles fixes).
  </action>
  <verify>
    <automated>cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used && grep -q 'catch_unwind' Rust/core/src/bridge.rs && grep -q 'clippy::unnecessary_unwrap' Rust/core/src/lib.rs && grep -q 'cfg_attr(not(test), deny(clippy::unwrap_used))' Rust/core/src/lib.rs && test -z "$(grep -rl 'allow(clippy::unwrap_used)' Rust/core/src/)"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used` exits 0 (SC1)
    - `grep -c 'catch_unwind' Rust/core/src/bridge.rs` returns at least 1 (SC2 — verified present)
    - `grep -rl 'allow(clippy::unwrap_used)' Rust/core/src/` returns nothing (all shields removed)
    - `grep -c 'clippy::unnecessary_unwrap' Rust/core/src/lib.rs` returns 1 (D-05 preserved)
    - `grep -c 'cfg_attr(not(test), deny(clippy::unwrap_used))' Rust/core/src/lib.rs` returns 1
  </acceptance_criteria>
  <done>SC1 lint passes; catch_unwind verified present; no shields remain; D-05 and deny attribute confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite passes</name>
  <read_first>
    - 85-VALIDATION.md "Sampling Rate" (full suite at wave merge / before verify-work)
    - 85-RESEARCH.md "Open Questions" #2 (full suite >60s; run at gate, not per-plan)
  </read_first>
  <files></files>
  <action>
    Run the full test suite: `cargo test --locked --manifest-path Rust/core/Cargo.toml`
    (unit + 45 integration test files). It MUST exit 0 (SC3). This task makes NO source
    changes. If any test fails, record the failing test name in the summary and stop.
  </action>
  <verify>
    <automated>cargo test --locked --manifest-path Rust/core/Cargo.toml 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --locked --manifest-path Rust/core/Cargo.toml` exits 0 (SC3 — full suite green)
    - No source files were modified by this plan (`git status --short Rust/core/src/` shows no changes from this plan)
  </acceptance_criteria>
  <done>Full test suite (unit + integration) passes; Phase 85 ARCH-03 success criteria all satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deny lint gate → future commits | The gate is the enforcement boundary that prevents reintroduction of production `.unwrap()` |
| Swift → Rust FFI | catch_unwind verified as the last line of defence against panic-crossing-ABI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-09 | Denial of Service | Regression leaving a production unwrap uncaught | mitigate | SC1 clippy gate fails the build if any production `.unwrap()` survives; SC2 catch_unwind verified as residual net |
| T-85-10 | Repudiation | Silent gate skip (shield left in place) | mitigate | Gate explicitly asserts zero `allow(clippy::unwrap_used)` remains across `Rust/core/src/` |
| T-85-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no audit table required |
</threat_model>

<verification>
- `cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used` exits 0
- `cargo test --locked --manifest-path Rust/core/Cargo.toml` exits 0
- `grep -rl 'allow(clippy::unwrap_used)' Rust/core/src/` returns nothing
- `grep -c 'catch_unwind' Rust/core/src/bridge.rs` >= 1
</verification>

<success_criteria>
- ARCH-03 SC1: deny lint passes with zero production unwrap violations
- ARCH-03 SC2: catch_unwind verified present at bridge dispatcher (not reimplemented)
- ARCH-03 SC3: full `cargo test --locked` suite passes
- No allow shields remain; D-05 unnecessary_unwrap preserved; no source changes in this plan
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `.planning/phases/85-rust-crash-safety/85-06-SUMMARY.md` — gate results record (no source changes; verification-only plan)
</artifacts_produced>

<output>
Create `.planning/phases/85-rust-crash-safety/85-06-SUMMARY.md` when done
</output>
