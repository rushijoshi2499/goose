---
phase: 47-device-id-namespace-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/store.rs
  - Rust/core/src/capture_import.rs
  - Rust/core/src/bridge.rs
  - Rust/core/tests/capture_import_tests.rs
autonomous: true
requirements: [DEVID-01, DEVID-02]

must_haves:
  truths:
    - "Schema migration adds device_uuid TEXT (nullable) to raw_evidence and decoded_frames; existing rows get NULL"
    - "An index idx_raw_evidence_by_device_uuid on raw_evidence(device_uuid, captured_at) exists"
    - "insert_raw_evidence persists device_uuid when supplied and NULL when absent"
    - "raw_evidence read paths return device_uuid in RawEvidenceRow"
    - "capture.import_frame_batch propagates device_uuid from Swift JSON through to raw_evidence"
    - "upload.get_raw_frames_for_upload bridge response includes device_uuid per frame"
  artifacts:
    - path: "Rust/core/src/store.rs"
      provides: "device_uuid on RawEvidenceInput/RawEvidenceRow/DecodedFrameRow, migration, insert+read paths"
      contains: "device_uuid"
    - path: "Rust/core/src/capture_import.rs"
      provides: "device_uuid field on CapturedFrameInput + pass-through to RawEvidenceInput"
      contains: "device_uuid"
    - path: "Rust/core/tests/capture_import_tests.rs"
      provides: "5 new tests for DEVID-01/02"
      contains: "test_migration_adds_device_uuid"
  key_links:
    - from: "capture_import.rs CapturedFrameInput"
      to: "store.rs RawEvidenceInput.device_uuid"
      via: "frame.device_uuid.as_deref() in import function"
      pattern: "device_uuid"
    - from: "store.rs insert_raw_evidence"
      to: "raw_evidence.device_uuid column"
      via: "params![] ?9 binding"
      pattern: "device_uuid"
    - from: "bridge.rs upload_get_raw_frames_for_upload_bridge"
      to: "RawEvidenceRow.device_uuid"
      via: "json!({}) frame serialisation"
      pattern: "\"device_uuid\""
---

<objective>
Add `device_uuid TEXT NULL` to the Rust SQLite storage layer and wire it through the full Rust data path: schema migration, struct fields, insert, read, capture-import deserialisation, and the upload bridge response.

Purpose: This is the storage foundation for DEVID-01 (column exists, filled on capture) and the Rust half of DEVID-02 (consistent identifier flows into the upload payload). The iOS and server tiers (Plans 02 and 03) connect to this contract.
Output: `device_uuid` persisted on `raw_evidence`/`decoded_frames`, returned in read paths, propagated from `capture.import_frame_batch`, and emitted in `upload.get_raw_frames_for_upload`. 5 new passing tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-device-id-namespace-resolution/47-CONTEXT.md
@.planning/phases/47-device-id-namespace-resolution/47-RESEARCH.md
@.planning/phases/47-device-id-namespace-resolution/47-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols created or extended by Plan 01 (newly added — exclude from drift verification):

- `RawEvidenceInput.device_uuid: Option<&str>` (new field) — `Rust/core/src/store.rs`
- `RawEvidenceRow.device_uuid: Option<String>` (new field, `#[serde(default)]`) — `Rust/core/src/store.rs`
- `DecodedFrameRow.device_uuid: Option<String>` (new field, `#[serde(default)]`) — `Rust/core/src/store.rs`
- `CapturedFrameInput.device_uuid: Option<String>` (new field, `#[serde(default)]`) — `Rust/core/src/capture_import.rs`
- `raw_evidence.device_uuid` SQLite column (new) + index `idx_raw_evidence_by_device_uuid` on `raw_evidence(device_uuid, captured_at)`
- `decoded_frames.device_uuid` SQLite column (new, no index)
- JSON key `"device_uuid"` added to `upload_get_raw_frames_for_upload_bridge` per-frame response
- Tests: `test_migration_adds_device_uuid`, `test_insert_raw_evidence_with_uuid`, `test_query_raw_evidence_by_uuid`, `test_query_raw_evidence_by_device_model`, `test_capture_import_propagates_device_uuid` — `Rust/core/tests/capture_import_tests.rs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 + RED): Add device_uuid to structs, migration, and write 5 failing tests</name>
  <files>Rust/core/src/store.rs, Rust/core/src/capture_import.rs, Rust/core/tests/capture_import_tests.rs</files>
  <read_first>
    - Rust/core/src/store.rs lines 165-220 (RawEvidenceInput, RawEvidenceRow, DecodedFrameRow structs — current field set)
    - Rust/core/src/store.rs lines 1107-1145 (raw_evidence + decoded_frames CREATE TABLE — confirm column name captured_at on raw_evidence)
    - Rust/core/src/store.rs lines 6861-6890 (ensure_raw_evidence_columns + ensure_decoded_frame_columns — additive migration pattern)
    - Rust/core/src/capture_import.rs lines 64-90 (CapturedFrameInput struct + the import function that builds RawEvidenceInput)
    - Rust/core/tests/capture_import_tests.rs (existing test file — replicate its test harness, GooseStore open helper, and assertion style)
    - .planning/phases/47-device-id-namespace-resolution/47-PATTERNS.md (struct + migration + insert exact analogs)
  </read_first>
  <action>
    Add the `device_uuid` field to four Rust structs and extend the additive migrations.

    Structs in `store.rs`: add `pub device_uuid: Option<&'a str>` to `RawEvidenceInput<'a>` (after `capture_session_id`); add `#[serde(default)] pub device_uuid: Option<String>` to both `RawEvidenceRow` and `DecodedFrameRow` (after their last field). Use `#[serde(default)]` so existing serialised payloads deserialise with `None`. The column is nullable with no DEFAULT — existing rows stay NULL, no backfill (D-07).

    Struct in `capture_import.rs`: add `#[serde(default)] pub device_uuid: Option<String>` to `CapturedFrameInput` (copy the `capture_session_id` Option pattern exactly).

    Migration in `store.rs ensure_raw_evidence_columns()`: extend the `(column, ddl)` tuple array with `("device_uuid", "device_uuid TEXT")`. After the for-loop (still inside the function, before `Ok(())`), add an idempotent index creation (D-08): `CREATE INDEX IF NOT EXISTS idx_raw_evidence_by_device_uuid ON raw_evidence(device_uuid, captured_at)`. CRITICAL: the column is `captured_at`, NOT `ts` — `raw_evidence` has no `ts` column (Pitfall 1). Do NOT bump `PRAGMA user_version`.

    Migration in `store.rs ensure_decoded_frame_columns()`: extend the tuple array with `("device_uuid", "device_uuid TEXT")`. Do NOT add an index on decoded_frames (success criteria only require the raw_evidence index).

    Then write 5 tests in `Rust/core/tests/capture_import_tests.rs` (RED — they will fail to compile/pass until Task 2 wires insert/read/import):
    - `test_migration_adds_device_uuid`: open a fresh GooseStore, assert `raw_evidence` and `decoded_frames` both contain a `device_uuid` column (query `table_columns_unchecked` or `PRAGMA table_info`), and assert the index `idx_raw_evidence_by_device_uuid` exists (query `sqlite_master WHERE type='index'`).
    - `test_insert_raw_evidence_with_uuid`: insert a RawEvidenceInput with `device_uuid: Some("test-uuid-1234")`, read it back, assert returned `device_uuid == Some("test-uuid-1234".to_string())`.
    - `test_query_raw_evidence_by_uuid`: insert two rows with different device_uuid, query and assert the row with the target uuid is returned with correct device_uuid.
    - `test_query_raw_evidence_by_device_model`: insert rows, read back via the existing read path, assert `device_model` still returns correctly alongside `device_uuid` (regression guard that device_model path is unaffected).
    - `test_capture_import_propagates_device_uuid`: build a CapturedFrameInput JSON with `"device_uuid":"uuid-from-swift"`, run it through the capture import function (same path `capture.import_frame_batch` uses), then read the raw_evidence row and assert `device_uuid == Some("uuid-from-swift")`.

    Do NOT implement the insert/read/import wiring in this task — only structs, migration, and tests. Tests are expected to FAIL or rows to return NULL device_uuid until Task 2.
  </action>
  <verify>
    <automated>cd Rust/core && cargo build -p goose-core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "device_uuid" Rust/core/src/store.rs` returns ≥ 3 (RawEvidenceInput, RawEvidenceRow, DecodedFrameRow + migrations)
    - `grep "device_uuid" Rust/core/src/capture_import.rs` matches the new CapturedFrameInput field
    - `grep "idx_raw_evidence_by_device_uuid" Rust/core/src/store.rs` matches and the index references `captured_at` (NOT `ts`)
    - `grep -v '^[[:space:]]*//' Rust/core/src/store.rs | grep -c "PRAGMA user_version = 20"` returns 0 (no user_version bump added)
    - `grep -c "fn test_migration_adds_device_uuid\|fn test_insert_raw_evidence_with_uuid\|fn test_query_raw_evidence_by_uuid\|fn test_query_raw_evidence_by_device_model\|fn test_capture_import_propagates_device_uuid" Rust/core/tests/capture_import_tests.rs` returns 5
    - `cargo build -p goose-core` succeeds (crate compiles with new struct fields)
  </acceptance_criteria>
  <done>Four structs carry device_uuid, both ensure_*_columns migrations add the column (raw_evidence also gets the index on captured_at), 5 tests exist, crate compiles. Tests not yet green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Wire insert, read, capture-import, and upload bridge response</name>
  <files>Rust/core/src/store.rs, Rust/core/src/capture_import.rs, Rust/core/src/bridge.rs</files>
  <read_first>
    - Rust/core/src/store.rs lines 2177-2235 (insert_raw_evidence — INSERT column list + params! binding)
    - Rust/core/src/store.rs lines 5079-5148 (raw_evidence() and raw_evidence_between() read paths — SELECT column list + RawEvidenceRow mapper)
    - Rust/core/src/capture_import.rs lines 64-90 (the function building RawEvidenceInput from CapturedFrameInput)
    - Rust/core/src/bridge.rs lines 3590-3624 (upload_get_raw_frames_for_upload_bridge — json!({}) frame block)
    - .planning/phases/47-device-id-namespace-resolution/47-PATTERNS.md (insert + read + bridge exact analogs)
    - Rust/core/tests/capture_import_tests.rs (the 5 tests written in Task 1 — these define the target behaviour)
  </read_first>
  <action>
    Implement the wiring so the Task 1 tests pass.

    `insert_raw_evidence` (store.rs): add `device_uuid` as the 9th column in the `INSERT OR IGNORE INTO raw_evidence (...)` column list and `?9` in the VALUES, then add `input.device_uuid` as the 9th entry in the `params![]` macro. Order must match the column list exactly.

    Read paths `raw_evidence()` and `raw_evidence_between()` (store.rs): add `device_uuid` to the end of each SELECT column list and `device_uuid: row.get(8)?` to the `RawEvidenceRow { ... }` mapper in both functions. Confirm the column index (8) matches the position in the SELECT (zero-based, after capture_session_id at index 7).

    Capture import (capture_import.rs): in the function that constructs `RawEvidenceInput` from `CapturedFrameInput`, pass `device_uuid: frame.device_uuid.as_deref()` so the `Option<String>` becomes `Option<&str>`.

    Upload bridge (bridge.rs `upload_get_raw_frames_for_upload_bridge`): add `"device_uuid": r.device_uuid,` to the per-frame `json!({})` block. `serde_json` serialises `Option<String>::None` as JSON `null` (correct — iOS reads it as nil).

    Run the 5 tests and ensure all pass.
  </action>
  <verify>
    <automated>cd Rust/core && cargo test -p goose-core test_migration_adds_device_uuid test_insert_raw_evidence_with_uuid test_query_raw_evidence_by_uuid test_query_raw_evidence_by_device_model test_capture_import_propagates_device_uuid 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 5 named tests pass (`test result: ok` with 0 failed for the filtered set)
    - `grep -A30 "INSERT OR IGNORE INTO raw_evidence" Rust/core/src/store.rs | grep -c device_uuid` ≥ 1 (column present in INSERT)
    - `grep "\"device_uuid\": r.device_uuid" Rust/core/src/bridge.rs` matches inside upload_get_raw_frames_for_upload_bridge
    - `cd Rust/core && cargo test -p goose-core` full suite: no NEW failures vs baseline (regression guard)
  </acceptance_criteria>
  <done>insert, both read paths, capture-import pass-through, and the upload bridge response all carry device_uuid. All 5 phase tests green; full goose-core suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift JSON → Rust bridge (capture.import_frame_batch) | device_uuid arrives as untrusted JSON string; stored as opaque TEXT via parameterised insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | insert_raw_evidence device_uuid binding | mitigate | device_uuid bound via params![] (parameterised) — never string-interpolated into SQL |
| T-47-02 | Information disclosure | device_uuid value | accept | CoreBluetooth UUID is an opaque device-link identifier, no PII; routes already gated by Bearer auth at server tier |
| T-47-SC | Tampering | cargo installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) — no install tasks |
</threat_model>

<verification>
- `cargo test -p goose-core` full suite green
- `device_uuid` column present on `raw_evidence` and `decoded_frames` after fresh DB open
- Index `idx_raw_evidence_by_device_uuid` exists on `raw_evidence(device_uuid, captured_at)`
- Upload bridge response includes `device_uuid` per frame
</verification>

<success_criteria>
- DEVID-01: device_uuid column exists on both tables (nullable, existing rows NULL), index on raw_evidence, filled on capture via import
- DEVID-02 (Rust half): device_uuid flows from capture import into raw_evidence and out through the upload bridge response — consistent identifier ready for the iOS upload payload
- 5 new tests green; no regressions
</success_criteria>

<output>
Create `.planning/phases/47-device-id-namespace-resolution/47-01-SUMMARY.md` when done
</output>
