---
phase: 47-device-id-namespace-resolution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/db/init.sql
  - server/ingest/app/main.py
  - server/ingest/app/store.py
  - server/ingest/app/read.py
  - server/ingest/tests/test_ingest_frames_api.py
  - server/ingest/tests/test_read_api.py
autonomous: true
requirements: [DEVID-02]

must_haves:
  truths:
    - "raw_frames has device_uuid TEXT (nullable) via idempotent ALTER TABLE ADD COLUMN IF NOT EXISTS; index raw_frames_device_uuid on (device_uuid, captured_at)"
    - "IngestFrame Pydantic model accepts optional device_uuid; insert_raw_frames_batch persists it"
    - "GET /v1/export/frames/{device_id} resolves UUID-format device_id via device_uuid column and non-UUID via device_model column (bidirectional)"
    - "device_id is never string-interpolated into SQL — both branches parameterised (%s)"
  artifacts:
    - path: "server/db/init.sql"
      provides: "device_uuid column + index on raw_frames"
      contains: "device_uuid"
    - path: "server/ingest/app/read.py"
      provides: "bidirectional read_device_frames + _is_uuid helper"
      contains: "_is_uuid"
    - path: "server/ingest/app/main.py"
      provides: "device_uuid field on IngestFrame"
      contains: "device_uuid"
    - path: "server/ingest/app/store.py"
      provides: "device_uuid persisted in insert_raw_frames_batch"
      contains: "device_uuid"
  key_links:
    - from: "IngestFrame.device_uuid"
      to: "insert_raw_frames_batch INSERT raw_frames"
      via: "f.get('device_uuid') parameterised bind"
      pattern: "device_uuid"
    - from: "export_device_frames route"
      to: "read_device_frames bidirectional WHERE clause"
      via: "_is_uuid(device_id) branch"
      pattern: "_is_uuid"
---

<objective>
Add `device_uuid` to the FastAPI/TimescaleDB server tier: schema migration on `raw_frames`, ingest model + persistence, and bidirectional export lookup that accepts either a CoreBluetooth UUID or a device_model string for `GET /v1/export/frames/{device_id}`.

Purpose: Server half of DEVID-02 — completes the consistent-identifier loop so the iOS app can upload with device_uuid and import via either UUID or model, with zero caller configuration (D-06).
Output: raw_frames.device_uuid column + index, IngestFrame accepts device_uuid, ingest persists it, export lookup is bidirectional and parameterised.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-device-id-namespace-resolution/47-CONTEXT.md
@.planning/phases/47-device-id-namespace-resolution/47-RESEARCH.md
@.planning/phases/47-device-id-namespace-resolution/47-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols created or extended by Plan 03 (newly added — exclude from drift verification):

- `raw_frames.device_uuid` TEXT column (new) + index `raw_frames_device_uuid` on `raw_frames(device_uuid, captured_at)` — `server/db/init.sql`
- `IngestFrame.device_uuid: str | None = None` (new Pydantic field) — `server/ingest/app/main.py`
- `device_uuid` bind in `insert_raw_frames_batch` INSERT — `server/ingest/app/store.py`
- `_is_uuid(s: str) -> bool` (new helper) + bidirectional WHERE clause in `read_device_frames` — `server/ingest/app/read.py`
- New tests for device_uuid ingest + bidirectional export — `test_ingest_frames_api.py`, `test_read_api.py`

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + ingest model + persistence</name>
  <files>server/db/init.sql, server/ingest/app/main.py, server/ingest/app/store.py</files>
  <read_first>
    - server/db/init.sql lines 108-137 (raw_frames CREATE TABLE, hypertable, dedup index, device_generation ADD COLUMN IF NOT EXISTS pattern)
    - server/ingest/app/main.py lines 432-466 (IngestFramesDevice, IngestFrame model, ingest_frames route, insert_raw_frames_batch call)
    - server/ingest/app/store.py lines 36-62 (insert_raw_frames_batch INSERT column list + f.get() binds)
    - .planning/phases/47-device-id-namespace-resolution/47-PATTERNS.md (init.sql ADD COLUMN analog, parameterised psycopg analog)
  </read_first>
  <action>
    init.sql: after the existing raw_frames block (after the dedup unique index around line 121, alongside the other ADD COLUMN IF NOT EXISTS migrations), add an idempotent migration: `ALTER TABLE raw_frames ADD COLUMN IF NOT EXISTS device_uuid TEXT;` followed by `CREATE INDEX IF NOT EXISTS raw_frames_device_uuid ON raw_frames (device_uuid, captured_at);`. Do NOT add a DEFAULT — NULL is correct for pre-migration rows (D-07). Add a brief comment "Phase 47: CoreBluetooth peripheral UUID on raw_frames. Idempotent."

    main.py: add `device_uuid: str | None = None` to the `IngestFrame` Pydantic model (after `sensitivity`). This makes uploads from iOS that include device_uuid valid; uploads omitting it still validate (None).

    store.py insert_raw_frames_batch: add `device_uuid` to the INSERT column list (after `sensitivity`) and a matching `%s` placeholder, then add `f.get("device_uuid")` to the params tuple in the same position. The dedup ON CONFLICT clause is unchanged (still device_id, captured_at, frame_hex) — device_uuid is not part of the conflict key.
  </action>
  <verify>
    <automated>cd server && grep -n "device_uuid" db/init.sql ingest/app/main.py ingest/app/store.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN IF NOT EXISTS device_uuid TEXT" server/db/init.sql` ≥ 1 with NO `DEFAULT` on that line
    - `grep "raw_frames_device_uuid" server/db/init.sql` matches and references `(device_uuid, captured_at)`
    - `grep "device_uuid: str | None = None" server/ingest/app/main.py` matches inside IngestFrame
    - `grep -A20 "def insert_raw_frames_batch" server/ingest/app/store.py | grep -c device_uuid` ≥ 2 (column list + f.get bind)
    - Source assertion: ON CONFLICT clause in insert_raw_frames_batch still reads `(device_id, captured_at, frame_hex)` — device_uuid NOT added to conflict key
  </acceptance_criteria>
  <done>raw_frames has device_uuid column + index (idempotent, nullable, no default); IngestFrame accepts device_uuid; insert_raw_frames_batch persists it without altering dedup behaviour.</done>
</task>

<task type="auto">
  <name>Task 2: Bidirectional export lookup + tests</name>
  <files>server/ingest/app/read.py, server/ingest/tests/test_ingest_frames_api.py, server/ingest/tests/test_read_api.py</files>
  <read_first>
    - server/ingest/app/read.py lines 296-385 (read_device_frames — archive batch path + raw_frames query at 357-381)
    - server/ingest/app/main.py lines 469-485 (export_device_frames route — calls read.read_device_frames; no route change needed)
    - server/ingest/tests/test_ingest_frames_api.py (existing ingest-frames API tests — replicate fixture/client setup and assertion style)
    - server/ingest/tests/test_read_api.py (existing read API tests — replicate the conn/db fixture and request pattern)
    - .planning/phases/47-device-id-namespace-resolution/47-RESEARCH.md Pattern 4 + Code Examples (uuid.UUID try/except, archive path uses device_id PK = UUID, no change there)
  </read_first>
  <action>
    read.py: add a module-level helper near the top — `import uuid as _uuid` and `def _is_uuid(s: str) -> bool:` that returns True when `_uuid.UUID(s)` succeeds, False on `ValueError`. In `read_device_frames`, modify ONLY the `raw_frames` query (lines ~358-372): compute `is_uuid = _is_uuid(device_id)`, set `device_clause = "device_uuid = %s"` when UUID else `"device_model = %s"`, and substitute that clause into the WHERE (f-string ONLY for the clause name, NEVER for the value). The bound parameter stays `device_id` in both branches — the value is always a `%s` parameter, never interpolated (T-47-05, V5 input validation). Do NOT change the archive `raw_batches` path: that path uses `devices.device_id` as the PK which is already the CoreBluetooth UUID — D-06 / RESEARCH A3.

    Tests: in test_ingest_frames_api.py add a test that POSTs /v1/ingest-frames with a frame carrying `device_uuid` and asserts the row is stored with that device_uuid (query raw_frames). In test_read_api.py add two tests: (a) insert a raw_frames row with a known device_uuid, GET /v1/export/frames/{uuid} and assert the frame is returned; (b) insert a raw_frames row with a known device_model and NULL device_uuid, GET /v1/export/frames/{device_model_string} (a non-UUID value) and assert the frame is returned via the device_model branch. Use the existing test fixtures/conftest for DB + client.
  </action>
  <verify>
    <automated>cd server/ingest && python -m pytest tests/test_ingest_frames_api.py tests/test_read_api.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def _is_uuid" server/ingest/app/read.py` matches and the body uses `_uuid.UUID(` in a try/except ValueError
    - Source assertion: in read_device_frames the raw_frames WHERE uses `device_clause` (computed from _is_uuid) and the value is bound as `%s` with `device_id` as the parameter — NO f-string interpolation of `device_id` value
    - Source assertion: the `raw_batches` archive query is unchanged (still `WHERE device_id = %s`)
    - New ingest test: POST with device_uuid → row stored with that device_uuid (pytest green)
    - New read tests: UUID path returns frame via device_uuid; non-UUID path returns frame via device_model (both pytest green)
    - `python -m pytest tests/test_ingest_frames_api.py tests/test_read_api.py -q` exits 0
  </acceptance_criteria>
  <done>read_device_frames does bidirectional lookup (UUID→device_uuid, else→device_model), fully parameterised; archive path untouched; 3 new tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /v1/export/frames/{device_id} | device_id path param is untrusted; flows into SQL — must be parameterised |
| HTTP client → /v1/ingest-frames | device_uuid in frame body; stored as opaque TEXT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-05 | Tampering | export_device_frames device_id path param (SQL injection) | mitigate | device_id always bound as %s in both UUID and device_model branches; only the clause column name (a fixed string literal) varies — value never interpolated (ASVS V5) |
| T-47-06 | Tampering | UUID format bypass | mitigate | uuid.UUID() gate decides branch; both branches parameterised so a malformed value cannot inject SQL |
| T-47-07 | Information disclosure | device_uuid in raw_frames | accept | opaque device identifier, no PII; all /v1 routes gated by require_auth Bearer |
| T-47-SC | Tampering | pip installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: N/A; uuid is stdlib) — no install tasks |
</threat_model>

<verification>
- `python -m pytest tests/test_ingest_frames_api.py tests/test_read_api.py -q` green
- raw_frames.device_uuid column + index present after init.sql apply
- export lookup returns rows for both UUID and device_model inputs
- device_id never interpolated into SQL
</verification>

<success_criteria>
- DEVID-02 (server half): raw_frames carries device_uuid, ingest persists it, export accepts both UUID and device_model bidirectionally with zero caller config (D-06)
- All SQL parameterised; archive path unchanged; dedup behaviour preserved
</success_criteria>

<output>
Create `.planning/phases/47-device-id-namespace-resolution/47-03-SUMMARY.md` when done
</output>
