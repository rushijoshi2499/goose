---
phase: 20-upstream-fixes-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/AppShellView.swift
  - GooseSwift/GooseAppModel.swift
  - GooseSwift/GooseBLEClient+HistoricalHandlers.swift
  - GooseSwift/GooseBLEClient+Parsing.swift
  - GooseSwift/GooseBLEClient.swift
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-04, SYNC-05]
requirements_addressed: [SYNC-01, SYNC-02, SYNC-03, SYNC-04, SYNC-05]

must_haves:
  truths:
    - "The Gen4 historical-sync completion callback path captures its store/coordinator weakly, with no retain cycle; teardown added only if a real strong capture exists (SYNC-01)"
    - "Every Gen4 historical per-sync counter increment uses wrapping arithmetic so a long sync cannot trap on overflow (SYNC-02)"
    - "The Gen4 command-frame builder's 4-byte padding behaviour is documented in source so future edits do not silently break frame length (SYNC-03)"
    - "The connected device-generation property carries a queue-confinement doc comment describing which actor may read/write it (SYNC-04)"
    - "Gen4 detection lowercases the UUID string before any hasPrefix comparison so uppercase CoreBluetooth UUIDs still detect as Gen4 (SYNC-05)"
  artifacts:
    - path: "GooseSwift/AppShellView.swift"
      provides: "Historical-sync wiring confirmed free of strong closure capture (SYNC-01)"
    - path: "GooseSwift/GooseAppModel.swift"
      provides: "onHistoricalSync callback closures with weak self capture + SYNC-04 confinement comment"
    - path: "GooseSwift/GooseBLEClient+HistoricalHandlers.swift"
      provides: "Wrapping-arithmetic counter increments (SYNC-02)"
    - path: "GooseSwift/GooseBLEClient+Parsing.swift"
      provides: "buildV5CommandFrame padding doc comment (SYNC-03); lowercased Gen4 UUID detection (SYNC-05)"
    - path: "GooseSwift/GooseBLEClient.swift"
      provides: "Read-context for callback property declarations (SYNC-01)"
  key_links:
    - from: "GooseAppModel.onHistoricalSyncProgress"
      to: "GooseBLEClient.onHistoricalSyncProgress"
      via: "callback assignment with weak self capture"
      pattern: "onHistoricalSync.*weak self"
    - from: "GooseBLEClient+Parsing.generation(from:)"
      to: "uuid.uuidString.lowercased()"
      via: "lowercase before hasPrefix"
      pattern: "lowercased.*hasPrefix"
---

<objective>
Apply the five Gen4 historical-sync correctness fixes (SYNC-01 through SYNC-05) sourced from upstream b-nnett/goose PR #26 review.

IMPORTANT — fork drift: the requirement text names upstream symbols (onHistoricalSyncCompleted, gen4HistoricalPageSeq, buildGen4CommandFrame, activeDeviceGeneration, WhoopGeneration.detect) that DO NOT exist verbatim in this fork. The fork uses different identifiers. This plan maps each upstream name to the actual fork symbol and applies (or documents-as-already-satisfied) the fix against the real code. Each task begins by verifying the current state before editing.

Purpose: clean the Gen4 sync foundation before the v5.0 algorithm work begins. All fixes are surgical — minimal diffs, no refactoring, no new abstractions, no behaviour change.
Output: doc comments and small edits across five Swift files; xcodebuild green afterward (per SYNC-01..SYNC-05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-upstream-fixes-storage/20-CONTEXT.md

@GooseSwift/AppShellView.swift
@GooseSwift/GooseBLETypes.swift

Fork-vs-upstream symbol map (verified 2026-06-06 by grep):
- SYNC-01 upstream onHistoricalSyncCompleted closure in AppShellView -> fork: AppShellView has NO such closure; healthStore is @State value type at AppShellView.swift:5. The historical-sync callbacks live in GooseAppModel.swift:369-374 (ble.onHistoricalSyncProgress / ble.onHistoricalRangeTelemetry), already assigned with weak self. Verify and document; add .onDisappear teardown ONLY if a real strong closure capture is found.
- SYNC-02 upstream gen4HistoricalPageSeq wrapping increment -> fork: no gen4HistoricalPageSeq. Per-sync counters in GooseBLEClient+HistoricalHandlers.swift use plain increment at lines 26, 494, 672 (historicalPacketsReceivedThisSync, historicalRangePendingResponses, coalescedHistoricalSyncProgressCallbackCount).
- SYNC-03 upstream buildGen4CommandFrame padding -> fork: builder is buildV5CommandFrame at GooseBLEClient+Parsing.swift:944, padding logic at lines 947-950 (pads payload to next 4-byte multiple with zero bytes before CRC).
- SYNC-04 upstream activeDeviceGeneration -> fork: connectedDeviceGeneration String? at GooseAppModel.swift:59 (@MainActor) — mutated in GooseAppModel+Lifecycle.swift:113/119.
- SYNC-05 upstream UUID lowercase before hasPrefix 61080002 -> fork: GooseBLEClient+Parsing.swift:356-363 generation(from:) already lowercases at line 358; WearableDescriptor.isCommandUUID (GooseBLETypes.swift:16-19) already lowercases. Verify BOTH Gen4 prefixes: 61080001 service, 61080002 command.

Code-comment convention (from CLAUDE.md): this codebase uses // comments only — NEVER /// doc comments. Natural sentence case. 2-space indentation. The wrapping add operator in Swift is the ampersand-plus-equals form.
</context>

<tasks>

<task type="auto">
  <name>Task 1: SYNC-01 + SYNC-04 — verify weak capture of historical-sync callbacks and document device-generation confinement</name>
  <files>GooseSwift/AppShellView.swift, GooseSwift/GooseAppModel.swift, GooseSwift/GooseBLEClient.swift</files>
  <read_first>
    - GooseSwift/AppShellView.swift (full — confirm no retaining closure; healthStore is @State value at line 5)
    - GooseSwift/GooseAppModel.swift lines 360-380 (the ble.onHistoricalSyncProgress and ble.onHistoricalRangeTelemetry assignments and their capture lists)
    - GooseSwift/GooseAppModel.swift line 59 (var connectedDeviceGeneration String? = nil)
    - GooseSwift/GooseAppModel+Lifecycle.swift lines 108-122 (where connectedDeviceGeneration is set/cleared)
    - GooseSwift/GooseBLEClient.swift lines 40-90 (state-property block + callback property declarations onHistoricalSyncProgress / onHistoricalRangeTelemetry)
  </read_first>
  <action>
    SYNC-01: The upstream onHistoricalSyncCompleted closure does not exist in this fork. The fork's historical-sync completion path is the onHistoricalSyncProgress (terminal isTerminal/failed) and onHistoricalRangeTelemetry callbacks assigned in GooseAppModel.swift (~lines 369 and 374), both ALREADY using weak self capture. Confirm by reading that both assignments use a weak self capture list. If both already do, no capture change is needed — add a one-line // comment immediately above each assignment stating the weak capture is intentional to avoid a GooseAppModel-GooseBLEClient retain cycle (per SYNC-01, upstream PR #26). If you find ANY historical-sync callback closure that captures self, healthStore, or another reference type strongly, convert it to a weak capture. AppShellView.swift captures healthStore only as a SwiftUI @State value passed into child views — this is not a closure capture and needs no teardown; do not add a spurious teardown. Add a .onDisappear closure that nils ble.onHistoricalSyncProgress and ble.onHistoricalRangeTelemetry to the AppShellView TabView ONLY IF you discover a real strong closure capture bound to AppShellView's lifecycle. Record in SUMMARY which branch applied.
    SYNC-04: Add a // comment block (2-3 lines, natural sentence case, no triple-slash) immediately above var connectedDeviceGeneration in GooseAppModel.swift:59 stating the queue confinement: this property is owned by the @MainActor GooseAppModel and is only read/written on the main actor (set from connect/disconnect handling in GooseAppModel+Lifecycle.swift); BLE-thread code must not touch it directly.
  </action>
  <verify>
    <automated>grep -niE "weak self" GooseSwift/GooseAppModel.swift; grep -n "connectedDeviceGeneration" GooseSwift/GooseAppModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - Both historical-sync callback assignments in GooseAppModel.swift use a weak self capture (manual read confirms it on the onHistoricalSyncProgress and onHistoricalRangeTelemetry assignment lines).
    - No historical-sync closure in the codebase captures a reference type strongly (confirmation recorded in SUMMARY).
    - connectedDeviceGeneration in GooseAppModel.swift has a 2-3 line // queue-confinement comment directly above it naming main-actor confinement.
    - SUMMARY records whether a .onDisappear teardown was needed (expected: not needed) and why.
  </acceptance_criteria>
  <done>SYNC-01 capture is weak (or made weak) and documented; SYNC-04 confinement comment present on connectedDeviceGeneration.</done>
</task>

<task type="auto">
  <name>Task 2: SYNC-02 — wrapping arithmetic on Gen4 historical per-sync counters</name>
  <files>GooseSwift/GooseBLEClient+HistoricalHandlers.swift</files>
  <read_first>
    - GooseSwift/GooseBLEClient+HistoricalHandlers.swift lines 20-30 (historicalPacketsReceivedThisSync increment, line ~26)
    - GooseSwift/GooseBLEClient+HistoricalHandlers.swift lines 488-498 (historicalRangePendingResponses increment, line ~494)
    - GooseSwift/GooseBLEClient+HistoricalHandlers.swift lines 666-676 (coalescedHistoricalSyncProgressCallbackCount increment, line ~672)
    - The var declarations of these three counters (grep their declarations to confirm integer types) before editing.
  </read_first>
  <action>
    The upstream gen4HistoricalPageSeq symbol does not exist in this fork. The equivalent monotonic per-sync counters that increment during a Gen4 historical sync and could overflow on a very long sync are historicalPacketsReceivedThisSync (line ~26), historicalRangePendingResponses (line ~494), and coalescedHistoricalSyncProgressCallbackCount (line ~672). Convert each plain increment (counter += 1) on these three counters to the Swift wrapping-add form (counter &+= 1) so a long sync wraps instead of trapping. Do not change any other arithmetic in the file. Do NOT convert any decrement on these counters silently: if a -= 1 exists on historicalRangePendingResponses (pending count is bounded; a trap there indicates a real bug), leave it and record the site in SUMMARY. Optionally add a single one-line // comment noting wrapping is intentional for SYNC-02.
  </action>
  <verify>
    <automated>grep -n "historicalPacketsReceivedThisSync" GooseSwift/GooseBLEClient+HistoricalHandlers.swift; grep -n "historicalRangePendingResponses" GooseSwift/GooseBLEClient+HistoricalHandlers.swift; grep -n "coalescedHistoricalSyncProgressCallbackCount" GooseSwift/GooseBLEClient+HistoricalHandlers.swift</automated>
  </verify>
  <acceptance_criteria>
    - Reading the three increment sites shows each uses the wrapping-add form (counter &+= 1); none uses the plain += 1 form (manual confirmation against grep output, ignoring comment lines).
    - grep for the three counter names shows the increment lines now contain the ampersand-plus-equals operator at lines ~26, ~494, ~672.
    - No other arithmetic operator in the file was changed.
  </acceptance_criteria>
  <done>All three Gen4 historical per-sync increment sites use wrapping addition; no trapping/wrapping mix remains on these counters.</done>
</task>

<task type="auto">
  <name>Task 3: SYNC-03 + SYNC-05 — document command-frame padding and confirm lowercased Gen4 UUID detection</name>
  <files>GooseSwift/GooseBLEClient+Parsing.swift</files>
  <read_first>
    - GooseSwift/GooseBLEClient+Parsing.swift lines 944-971 (buildV5CommandFrame — padding at 947-950)
    - GooseSwift/GooseBLEClient+Parsing.swift lines 352-363 (generation(from:) — lowercases at 358, prefixes 61080001 / fd4b0001)
    - GooseSwift/GooseBLETypes.swift lines 11-33 (WearableDescriptor.isCommandUUID lowercases at 18; whoopGen4 command prefix 61080002 at line 32)
  </read_first>
  <action>
    SYNC-03: The upstream buildGen4CommandFrame is buildV5CommandFrame in this fork (GooseBLEClient+Parsing.swift:944). Its padding logic (lines 947-950) pads the payload up to the next 4-byte multiple with zero bytes before CRC. Add a // comment (natural sentence case, 2-3 lines, no triple-slash) immediately above the line that computes padding (the let padding = ... line, ~947) documenting WHY: the WHOOP Gen4/Gen5 command frame requires the payload length to be a multiple of 4 bytes; the trailing zero padding satisfies that and is counted in both the declared length and the payload CRC. State the value was confirmed against PacketLogger captures (per SYNC-03 / upstream PR #26). Do not change the padding computation itself.
    SYNC-05: Confirm the Gen4 UUID comparison already lowercases before hasPrefix. generation(from:) lowercases at line 358 before hasPrefix 61080001 (service prefix). The COMMAND UUID prefix 61080002 is matched via WearableDescriptor.isCommandUUID (GooseBLETypes.swift:18) which also lowercases. If BOTH already lowercase, no code change is required for SYNC-05 — add a one-line // comment above the let lower = uuid.uuidString.lowercased() line (Parsing.swift:358) noting lowercasing is required for case-insensitive Gen4 detection per SYNC-05. If you find any raw hasPrefix("61080001") or hasPrefix("61080002") against a non-lowercased string anywhere in GooseSwift/, fix it to lowercase first. Record the SYNC-05 result (already-satisfied vs fixed) in SUMMARY.
  </action>
  <verify>
    <automated>grep -n "let padding" GooseSwift/GooseBLEClient+Parsing.swift; grep -rn "hasPrefix" GooseSwift/GooseBLEClient+Parsing.swift GooseSwift/GooseBLETypes.swift; grep -n "lowercased" GooseSwift/GooseBLEClient+Parsing.swift</automated>
  </verify>
  <acceptance_criteria>
    - A // comment of 2-3 lines sits directly above the let padding line in buildV5CommandFrame explaining the 4-byte-multiple requirement and that it is CRC-counted (SYNC-03).
    - generation(from:) lowercases the UUID before hasPrefix 61080001 (line 358), and the 61080002 command prefix path goes through a lowercased comparison; grep shows no hasPrefix against a non-lowercased UUID string in GooseSwift/ (SYNC-05).
    - SUMMARY records SYNC-05 as already-satisfied or fixed, with the line evidence.
  </acceptance_criteria>
  <done>SYNC-03 padding documented; SYNC-05 confirmed/forced to lowercase before both Gen4 prefix comparisons.</done>
</task>

<task type="auto">
  <name>Task 4: Build gate — xcodebuild compiles the iOS target after all SYNC fixes</name>
  <files>(none — verification only)</files>
  <read_first>
    - git diff --stat (confirm only the five files in files_modified changed)
  </read_first>
  <action>
    All five SYNC-01..SYNC-05 edits/doc-comments are now in place. Compile the iOS target to prove nothing broke. Prefer XcodeBuildMCP (build_sim against the GooseSwift scheme on an available iOS simulator). If MCP build is unavailable, run xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination "platform=iOS Simulator,name=iPhone 16" build. Confirm BUILD SUCCEEDED with no new warnings introduced by the edits. Then confirm the working-tree diff is limited to the five files in files_modified and consists of doc comments plus the three wrapping-operator conversions (run git diff --stat). Record the build result and the diff stat in the SUMMARY.
  </action>
  <verify>
    <automated>git diff --stat -- GooseSwift/AppShellView.swift GooseSwift/GooseAppModel.swift "GooseSwift/GooseBLEClient+HistoricalHandlers.swift" "GooseSwift/GooseBLEClient+Parsing.swift" GooseSwift/GooseBLEClient.swift</automated>
  </verify>
  <acceptance_criteria>
    - The GooseSwift iOS target builds successfully (BUILD SUCCEEDED) after all SYNC fixes.
    - No new compiler warnings are introduced by the edits.
    - git diff --stat shows changes confined to the five files in files_modified, with small line counts (comments + three wrapping operators).
  </acceptance_criteria>
  <done>xcodebuild green; diff confined to the five SYNC files.</done>
</task>

</tasks>

<verification>
- xcodebuild (GooseSwift scheme, iOS simulator) succeeds.
- git diff is limited to the five files in files_modified; changes are doc comments plus three wrapping-operator conversions.
- SUMMARY documents, per requirement, whether each SYNC fix required a code change or was already satisfied (SYNC-01 capture, SYNC-05 lowercase are expected already-satisfied + documented; SYNC-02 is a real change; SYNC-03/SYNC-04 are doc comments).
</verification>

<success_criteria>
- SYNC-01: historical-sync callbacks capture weakly (confirmed/forced) and documented; teardown added only if a real strong capture existed.
- SYNC-02: all three Gen4 historical per-sync increments use wrapping addition.
- SYNC-03: buildV5CommandFrame 4-byte padding is documented in source.
- SYNC-04: connectedDeviceGeneration has a main-actor queue-confinement comment.
- SYNC-05: Gen4 UUID detection lowercases before both 61080001 (service) and 61080002 (command) prefix comparisons.
- xcodebuild green.
</success_criteria>

<artifacts_this_phase_produces>
Symbols created/modified by this plan (Swift):
- GooseSwift/GooseAppModel.swift — // queue-confinement comment on connectedDeviceGeneration (SYNC-04); // intent comments on onHistoricalSyncProgress / onHistoricalRangeTelemetry weak-capture assignments (SYNC-01).
- GooseSwift/GooseBLEClient+HistoricalHandlers.swift — historicalPacketsReceivedThisSync, historicalRangePendingResponses, coalescedHistoricalSyncProgressCallbackCount increments converted to wrapping addition (SYNC-02).
- GooseSwift/GooseBLEClient+Parsing.swift — // padding-rationale comment above buildV5CommandFrame padding (SYNC-03); // lowercase-rationale comment above generation(from:) lowercasing (SYNC-05).
- GooseSwift/AppShellView.swift — .onDisappear teardown ONLY if a real strong capture is found (expected: unchanged).
No new public types, functions, or bridge methods are introduced.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/20-upstream-fixes-storage/20-01-SUMMARY.md` when done.
</output>
