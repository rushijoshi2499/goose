---
phase: 73-smart-alarm-wake-window-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseWakeWindowManager.swift
  - GooseSwift.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - HAP-04

must_haves:
  truths:
    - "GooseWakeWindowManager.swift exists in GooseSwift/ with correct RE-gate doc comment"
    - "GooseWakeWindowManager.swift is registered at exactly 4 locations in project.pbxproj"
    - "The class compiles — no functional implementation, no syntax errors"
    - "Project builds without errors after registration (xcodebuild succeeds)"
  artifacts:
    - path: "GooseSwift/GooseWakeWindowManager.swift"
      provides: "HAP-04 RE-gated stub class"
      contains: "GooseWakeWindowManager"
    - path: "GooseSwift.xcodeproj/project.pbxproj"
      provides: "Xcode build target registration for GooseWakeWindowManager.swift"
      contains: "GooseWakeWindowManager.swift"
  key_links:
    - from: "GooseWakeWindowManager.swift"
      to: "project.pbxproj PBXSourcesBuildPhase"
      via: "PBXBuildFile entry + PBXSourcesBuildPhase files list"
      pattern: "GooseWakeWindowManager\\.swift in Sources"
---

<objective>
HAP-04 (stub only): Create GooseWakeWindowManager.swift as a documented RE-gated stub and register it in the Xcode project so it compiles as part of the GooseSwift target.

The class must compile but has NO functional implementation. It exists to satisfy HAP-04's structural requirement and to document the BTSnoop + Ghidra prerequisites that must be completed before any wake-window logic can be written.

Purpose: Delivers the HAP-04 structural deliverable — a compilable stub with clear RE gate documentation. No functional implementation until prerequisites in .planning/research/whoop-re/ are complete.
Output: 1 new Swift file + 4 edits to project.pbxproj.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/francisco/Documents/goose/.planning/phases/73-smart-alarm-wake-window-engine/73-CONTEXT.md
@/Users/francisco/Documents/goose/.planning/phases/73-smart-alarm-wake-window-engine/73-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GooseWakeWindowManager.swift stub</name>
  <files>GooseSwift/GooseWakeWindowManager.swift</files>
  <read_first>
    No file to read first — this is a new file. The exact content is fully specified in CONTEXT.md decisions (D-02) and RESEARCH.md Pattern 4.
  </read_first>
  <action>
    Create `GooseSwift/GooseWakeWindowManager.swift` with EXACTLY this content (per D-02, HAP-04 CONTEXT.md decision):

    ```
    import Foundation

    // HAP-04: Wake-Window Engine — RE-GATED
    //
    // Implementation requires:
    // 1. BTSnoop capture of STRAP_DRIVEN_ALARM_EXECUTED packets, documented in
    //    .planning/research/whoop-re/SetAlarmInfoCommandPacketRev4.md
    // 2. Ghidra decompilation of SetAlarmInfoCommandPacketRev4 field layout,
    //    documented in the same file.
    //
    // Do not add functional implementation until both prerequisites are complete.
    final class GooseWakeWindowManager {
      // Stub — not yet functional. See comment above.
    }
    ```

    No additional methods, properties, or protocol conformances. No GooseAppModel integration. The file must be exactly this — no more, no less. Any functional addition violates the HAP-04 RE-gate contract.
  </action>
  <verify>
    <automated>grep -c "GooseWakeWindowManager\|RE-GATED\|SetAlarmInfoCommandPacketRev4" /Users/francisco/Documents/goose/GooseSwift/GooseWakeWindowManager.swift</automated>
  </verify>
  <done>
    File exists at GooseSwift/GooseWakeWindowManager.swift.
    Contains "GooseWakeWindowManager", "RE-GATED", and "SetAlarmInfoCommandPacketRev4" in the doc comment.
    Class body is empty (stub only).
  </done>
</task>

<task type="auto">
  <name>Task 2: Register GooseWakeWindowManager.swift in project.pbxproj at 4 locations</name>
  <files>GooseSwift.xcodeproj/project.pbxproj</files>
  <read_first>
    Before editing project.pbxproj, run the UUID discovery sequence:

    Step 1 — find the highest existing E1/E2 UUID pair index:
    `grep -oE 'E[12]00000000000000000000[0-9A-Fa-f]{2}' /Users/francisco/Documents/goose/GooseSwift.xcodeproj/project.pbxproj | sort -u | tail -10`

    Step 2 — look at an existing recently-added file for the exact UUID format (e.g., GooseStrainAccumulator.swift which was added in Phase 69):
    `grep -n "GooseStrainAccumulator" /Users/francisco/Documents/goose/GooseSwift.xcodeproj/project.pbxproj`

    Step 3 — verify the chosen NN values are free:
    After choosing NN from step 1, confirm no collision:
    `grep "E100000000000000000000{NN}" /Users/francisco/Documents/goose/GooseSwift.xcodeproj/project.pbxproj`
    `grep "E200000000000000000000{NN}" /Users/francisco/Documents/goose/GooseSwift.xcodeproj/project.pbxproj`
    Both must return empty.

    Read the pbxproj section headers so you know where to insert each of the 4 locations:
    `grep -n "PBXBuildFile\|PBXFileReference\|PBXGroup\|PBXSourcesBuildPhase\|GooseStrainAccumulator\|GooseWakeWindowManager" /Users/francisco/Documents/goose/GooseSwift.xcodeproj/project.pbxproj | head -60`
  </read_first>
  <action>
    Register GooseWakeWindowManager.swift at exactly 4 locations in project.pbxproj, using the T1/T2 UUID scheme (T1 = PBXBuildFile UUID, T2 = PBXFileReference UUID):

    UUID selection protocol:
    - Run the step-1 grep to find the highest existing NN value.
    - Choose NN = (highest existing) + 1 (hex, zero-padded to 2 chars).
    - T1 = E100000000000000000000{NN}
    - T2 = E200000000000000000000{NN}
    - Verify both are free (step 3 grep returns empty). If not free, increment NN.

    Location 1 — PBXBuildFile section:
    Insert a new line in the `/* Begin PBXBuildFile section */` block:
    `{T1} /* GooseWakeWindowManager.swift in Sources */ = {isa = PBXBuildFile; fileRef = {T2} /* GooseWakeWindowManager.swift */; };`
    Insert adjacent to the GooseStrainAccumulator.swift build file entry for consistent ordering.

    Location 2 — PBXFileReference section:
    Insert a new line in the `/* Begin PBXFileReference section */` block:
    `{T2} /* GooseWakeWindowManager.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = GooseWakeWindowManager.swift; sourceTree = "<group>"; };`
    Insert adjacent to GooseStrainAccumulator.swift file reference.

    Location 3 — PBXGroup children list:
    Find the PBXGroup that contains GooseStrainAccumulator.swift in its children array.
    Add `{T2} /* GooseWakeWindowManager.swift */,` as the next sibling in that children list (alphabetical or adjacent to GooseStrainAccumulator).

    Location 4 — PBXSourcesBuildPhase files list:
    Find the `/* Sources */` build phase for the GooseSwift target (the one that already contains GooseStrainAccumulator.swift in Sources).
    Add `{T1} /* GooseWakeWindowManager.swift in Sources */,` in that files list.

    After all 4 edits, validate:
    `grep -c 'GooseWakeWindowManager.swift' /Users/francisco/Documents/goose/GooseSwift.xcodeproj/project.pbxproj`
    Must return exactly 4. If it returns fewer, a location is missing. If more, there is a duplicate.

    Then build to confirm:
    `xcodebuild build -project /Users/francisco/Documents/goose/GooseSwift.xcodeproj -scheme GooseSwift -destination "platform=iOS Simulator,name=iPhone 16 Pro" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -20`
  </action>
  <verify>
    <automated>grep -v '^#' /Users/francisco/Documents/goose/GooseSwift.xcodeproj/project.pbxproj | grep -c 'GooseWakeWindowManager.swift'</automated>
  </verify>
  <done>
    `grep -c 'GooseWakeWindowManager.swift' project.pbxproj` returns exactly 4.
    xcodebuild exits 0 — project compiles with the new stub file in the build target.
    GooseWakeWindowManager appears in all 4 required pbxproj sections (PBXBuildFile, PBXFileReference, PBXGroup children, PBXSourcesBuildPhase files).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GooseWakeWindowManager.swift (stub) | No trust boundary — stub has no I/O, no network, no data access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-04 | Information Disclosure | GooseWakeWindowManager stub | accept | Stub class exposes no data; doc comment describes a future RE workflow, not implementation secrets |
| T-73-05 | Tampering | project.pbxproj UUID uniqueness | mitigate | UUID discovery protocol (grep highest NN, verify collision-free before inserting) prevents duplicate UUIDs causing build failures |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — file creation + pbxproj edits only |
</threat_model>

<verification>
1. `ls /Users/francisco/Documents/goose/GooseSwift/GooseWakeWindowManager.swift` — file exists
2. `grep -c 'GooseWakeWindowManager.swift' /Users/francisco/Documents/goose/GooseSwift.xcodeproj/project.pbxproj` — returns 4
3. `grep "RE-GATED\|SetAlarmInfoCommandPacketRev4" /Users/francisco/Documents/goose/GooseSwift/GooseWakeWindowManager.swift` — returns 2 hits (doc comment intact)
4. xcodebuild exits 0 — GooseWakeWindowManager.swift compiles as part of GooseSwift target
</verification>

<success_criteria>
- GooseWakeWindowManager.swift exists with the exact RE-gate doc comment
- The class body is empty (stub only — no functional implementation)
- project.pbxproj contains exactly 4 references to GooseWakeWindowManager.swift
- xcodebuild exits 0 with GooseWakeWindowManager.swift compiled into the target
</success_criteria>

<output>
Create `.planning/phases/73-smart-alarm-wake-window-engine/73-02-SUMMARY.md` when done.
</output>
