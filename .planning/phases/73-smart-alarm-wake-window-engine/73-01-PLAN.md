---
phase: 73-smart-alarm-wake-window-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseAppModel.swift
  - GooseSwift/CoachRouteViews.swift
autonomous: true
requirements:
  - HAP-03

must_haves:
  truths:
    - "An 'ALARME DE DESPERTAR' section appears at the bottom of CoachSleepRouteView"
    - "DatePicker shows hour+minute selector; label is hidden (section title provides context)"
    - "Arm button calls setWhoopAlarm(at:) + buzz(loops: 2) when tapped while connected"
    - "Cancel button calls disableWhoopAlarms() and clears alarmIsArmed + scheduledAlarmTime"
    - "Button is disabled with status message when connectionState != 'ready'"
    - "DatePicker is disabled when WHOOP is disconnected OR alarm is already armed"
    - "alarmIsArmed resets to false on BLE disconnect"
    - "GooseAppModel exposes scheduledAlarmTime: Date? and alarmIsArmed: Bool"
    - "Project builds without errors after changes (xcodebuild succeeds)"
  artifacts:
    - path: "GooseSwift/GooseAppModel.swift"
      provides: "scheduledAlarmTime and alarmIsArmed stored properties"
      contains: "scheduledAlarmTime"
    - path: "GooseSwift/CoachRouteViews.swift"
      provides: "WakeAlarmSection embedded in CoachSleepRouteView"
      contains: "ALARME DE DESPERTAR"
  key_links:
    - from: "CoachSleepRouteView"
      to: "GooseAppModel"
      via: "@Environment(GooseAppModel.self) private var model"
      pattern: "@Environment\\(GooseAppModel\\.self\\)"
    - from: "wakeAlarmSection"
      to: "model.ble.setWhoopAlarm"
      via: "Button arm action"
      pattern: "setWhoopAlarm"
    - from: "wakeAlarmSection"
      to: "model.ble.disableWhoopAlarms"
      via: "Button cancel action"
      pattern: "disableWhoopAlarms"
---

<objective>
HAP-03: Add a Wake Alarm section to the Sleep Coach screen.

The user picks a target wakeup time with a DatePicker, taps "Armar Alarme" (which calls setWhoopAlarm(at:) + buzz(loops: 2)), and can tap "Cancelar Alarme" (which calls disableWhoopAlarms() and clears state). The section is always visible in Sleep Coach. When WHOOP is not connected the button is disabled and a status message is shown.

Purpose: Delivers HAP-03 — single-shot BLE alarm scheduling from the Sleep Coach screen.
Output: Two edited Swift files. No new files. No package changes.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/francisco/Documents/goose/.planning/PROJECT.md
@/Users/francisco/Documents/goose/.planning/ROADMAP.md
@/Users/francisco/Documents/goose/.planning/phases/73-smart-alarm-wake-window-engine/73-CONTEXT.md
@/Users/francisco/Documents/goose/.planning/phases/73-smart-alarm-wake-window-engine/73-RESEARCH.md
@/Users/francisco/Documents/goose/.planning/phases/73-smart-alarm-wake-window-engine/73-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alarm state properties to GooseAppModel</name>
  <files>GooseSwift/GooseAppModel.swift</files>
  <read_first>
    Read GooseAppModel.swift lines 1-60 to locate the stored property block.
    The research confirms: add after liveWorkoutStrain (line ~33), before any method definitions.
    Pattern confirmed: @MainActor @Observable final class — properties are plain vars, no @Published.
  </read_first>
  <action>
    Add two stored properties to GooseAppModel immediately after `var liveWorkoutStrain: Double = 0` (per D-01 CONTEXT.md decisions for HAP-03):

    - `var scheduledAlarmTime: Date? = nil    // HAP-03`
    - `var alarmIsArmed: Bool = false         // HAP-03`

    Additionally, add a lifecycle hook so alarmIsArmed resets to false when BLE disconnects (per UI-SPEC §7 state machine). The hook belongs in the same place where connectionState changes are observed. Search GooseAppModel for where connectionState transitions to "disconnected"/"idle" and add `alarmIsArmed = false` there. If connectionState is set via a property observer or a method (e.g., a handleDisconnect method), add the reset in that code path. This is the correct behaviour: a strap that disconnects mid-arm is unreliable; showing "armed" for an alarm that may not fire is unsafe.

    No other changes to GooseAppModel.swift in this task.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -n "scheduledAlarmTime\|alarmIsArmed" GooseSwift/GooseAppModel.swift</automated>
  </verify>
  <done>GooseAppModel.swift contains both `scheduledAlarmTime: Date?` and `alarmIsArmed: Bool` as stored properties. Both are adjacent (within 5 lines). The file compiles (verified by the build step in Task 2).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Wake Alarm section to CoachSleepRouteView and inject GooseAppModel</name>
  <files>GooseSwift/CoachRouteViews.swift</files>
  <read_first>
    Read CoachRouteViews.swift lines 80-145 to confirm the VStack structure and insertion point.
    Read lines 360-415 to confirm CoachInfoGroup and CoachInfoRow signatures.
    Read GooseBLEClient+UserActions.swift lines 345-380 to confirm setWhoopAlarm(at:) and disableWhoopAlarms() signatures.
    Read BreatheView.swift lines 55-80 for the disconnected-state HStack pattern to copy verbatim.
  </read_first>
  <action>
    Make two independent edits to CoachRouteViews.swift:

    EDIT A — Inject GooseAppModel environment into CoachSleepRouteView:
    The view currently declares `var healthStore: HealthDataStore` as its only stored property. Add immediately after that declaration (per HAP-03 research, Pitfall 2):
    `@Environment(GooseAppModel.self) private var model`

    Also add a view-local state property for the alarm picker:
    `@State private var alarmTime: Date = Calendar.current.date(bySettingHour: 7, minute: 0, second: 0, of: Date()) ?? Date()`

    EDIT B — Append Wake Alarm section at the bottom of the VStack body.
    Location: inside the top-level VStack(spacing: 18), after the `if let sleep { CoachInfoGroup(title: "DÍVIDA DE SONO") { ... } }` block, before the closing of that VStack and before the `.padding(16)` modifier (research confirmed: line 118 area).

    Implement as a private computed property `wakeAlarmSection` on CoachSleepRouteView, called from the VStack body. Structure per UI-SPEC §6:

    - CoachInfoGroup title: "ALARME DE DESPERTAR" (ALL-CAPS, matches other group titles)
    - VStack(spacing: 12) inside the group containing:
      1. DatePicker labeled "Hora de acordar" with `.labelsHidden()`, `displayedComponents: .hourAndMinute`, bound to `$alarmTime`. Disabled (`.disabled(isDisconnected || model.alarmIsArmed)`), opacity 0.4 when disabled.
         Add `.accessibilityHint("Conecta o WHOOP para ativar")` when `isDisconnected` is true.
      2. Status message row (when `isDisconnected && !model.alarmIsArmed`): HStack(spacing: 8) containing Image(systemName: "sensor.tag.radiowaves.forward").foregroundStyle(.secondary) and Text("Conecta o WHOOP para usar o alarme").font(.caption).foregroundStyle(.secondary). Apply `.accessibilityElement(children: .combine)` to the HStack.
      3. Arm/Cancel button — single Button that switches label and action based on `model.alarmIsArmed`:
         - Label: `model.alarmIsArmed ? "Cancelar Alarme" : "Armar Alarme"` (per UI-SPEC §8)
         - Action when NOT armed: call `model.ble.setWhoopAlarm(at: alarmTime)`, then `model.ble.buzz(loops: 2)`, then `model.alarmIsArmed = true`, then `model.scheduledAlarmTime = alarmTime`
         - Action when armed: call `model.ble.disableWhoopAlarms()`, then `model.alarmIsArmed = false`, then `model.scheduledAlarmTime = nil`
         - Style: `.font(.body.weight(.semibold))`, foregroundStyle and background computed from arm/cancel state per UI-SPEC §6d color table (indigo / red, .opacity(0.14) fill), `.frame(maxWidth: .infinity, minHeight: 44)`, `.background(...)` with `RoundedRectangle(cornerRadius: 10, style: .continuous)`, `.disabled(isDisconnected)`, `.accessibilityLabel(model.alarmIsArmed ? "Cancelar alarme armado" : "Armar alarme de despertar")`

    Define `private var isDisconnected: Bool { model.ble.connectionState != "ready" }` as a helper computed property on the view.

    CRITICAL API NOTES (from RESEARCH.md):
    - Call `model.ble.setWhoopAlarm(at: alarmTime)` — NOT `writeAlarmCommand`. The wrapper handles alarmID, nextFutureAlarmDate, and payload framing.
    - Call `model.ble.disableWhoopAlarms()` — NOT `writeAlarmCommand(.disableAll)`.
    - Do NOT add a `canWriteAlarm` check inside the button action — `setWhoopAlarm`/`disableWhoopAlarms` guard internally.
    - The `@Observable` model is accessed via `@Environment` — do NOT use `@ObservedObject` or `@EnvironmentObject`.

    After all edits, run xcodebuild to verify the project compiles. Fix any compiler errors before finishing.

    xcodebuild invocation:
    `xcodebuild build -project /Users/francisco/Documents/goose/GooseSwift.xcodeproj -scheme GooseSwift -destination "platform=iOS Simulator,name=iPhone 16 Pro" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -20`
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -n "ALARME DE DESPERTAR\|setWhoopAlarm\|disableWhoopAlarms\|alarmIsArmed\|@Environment(GooseAppModel" GooseSwift/CoachRouteViews.swift</automated>
  </verify>
  <done>
    CoachRouteViews.swift contains:
    - "@Environment(GooseAppModel.self)" on CoachSleepRouteView
    - "ALARME DE DESPERTAR" section title
    - "setWhoopAlarm" call in arm action
    - "disableWhoopAlarms" call in cancel action
    - xcodebuild exits 0 (no compiler errors)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftUI button → GooseBLEClient | User-initiated alarm arm/cancel crosses into BLE write; button actions run on @MainActor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | DatePicker alarmTime | accept | DatePicker produces valid Date values; no free-text input; no validation needed (ASVS V5) |
| T-73-02 | Denial of Service | buzz(loops:) on rapid taps | accept | canWriteAlarm gate inside writeAlarmCommand blocks concurrent commands; pendingAlarmCommand != nil check prevents queue flooding |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure Swift edits to existing files |
</threat_model>

<verification>
1. `grep -n "scheduledAlarmTime\|alarmIsArmed" GooseSwift/GooseAppModel.swift` — must show 2+ hits
2. `grep -n "ALARME DE DESPERTAR" GooseSwift/CoachRouteViews.swift` — must return 1 hit
3. `grep -n "setWhoopAlarm\|disableWhoopAlarms" GooseSwift/CoachRouteViews.swift` — must return 2+ hits
4. xcodebuild exits 0 for GooseSwift scheme (simulator destination)
</verification>

<success_criteria>
- GooseAppModel has `scheduledAlarmTime: Date?` and `alarmIsArmed: Bool` stored properties
- CoachSleepRouteView has `@Environment(GooseAppModel.self) private var model`
- "ALARME DE DESPERTAR" section is visible at the bottom of the Sleep Coach screen
- Arm action calls `setWhoopAlarm(at:)` + `buzz(loops: 2)` and sets `alarmIsArmed = true`
- Cancel action calls `disableWhoopAlarms()` and clears both alarm properties
- DatePicker and button are disabled when connectionState != "ready"
- alarmIsArmed resets to false on BLE disconnect
- xcodebuild succeeds with zero errors
</success_criteria>

<output>
Create `.planning/phases/73-smart-alarm-wake-window-engine/73-01-SUMMARY.md` when done.
</output>
