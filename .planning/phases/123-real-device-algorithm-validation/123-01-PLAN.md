---
phase: "123"
plan: "123-01"
type: execute
wave: 1
depends_on: ["Phase 111"]
files_modified:
  - Rust/core/tests/algorithm_compare_tests.rs
  - .planning/phases/123-real-device-algorithm-validation/123-VALIDATION-ARTIFACT.md
autonomous: true
requirements: [VAL-HRV-04, VAL-SLP-04]

must_haves:
  truths:
    - "≥7 HRV fixture tests pass via compare_hrv_goose_to_reference with report.pass == true"
    - "≥7 sleep fixture tests pass via compare_sleep_goose_to_reference / compare_sleep_v1_goose_to_reference with report.pass == true"
    - "cargo test --locked passes for the full Rust/core suite"
    - "123-VALIDATION-ARTIFACT.md documents fixture coverage, delta tolerance, cargo test status, and hardware-gate deferral"
  artifacts:
    - Rust/core/tests/algorithm_compare_tests.rs (6 new HRV test functions + 4 new sleep test functions appended)
    - .planning/phases/123-real-device-algorithm-validation/123-VALIDATION-ARTIFACT.md (new file)
  key_links:
    - "Each HRV fixture feeds HrvInput into compare_hrv_goose_to_reference and asserts report.pass"
    - "Each sleep fixture feeds SleepInput/SleepV1Input into compare_sleep_goose_to_reference / compare_sleep_v1_goose_to_reference and asserts report.pass"
    - "123-VALIDATION-ARTIFACT.md cites both requirement IDs (VAL-HRV-04, VAL-SLP-04) and names SC-1 / SC-2 as hardware-gated deferred items"
---

<objective>
Extend `Rust/core/tests/algorithm_compare_tests.rs` with 6 additional HRV fixture tests and 4
additional sleep fixture tests, bringing totals to ≥7 for each metric family. All fixtures are
synthetic — no physical device required. Write `123-VALIDATION-ARTIFACT.md` to satisfy the SC-3
documentation gate for this phase.

Purpose: VAL-HRV-04 and VAL-SLP-04 require ≥7 synthetic session-scale fixtures to demonstrate
algorithmic correctness across a representative range of physiological scenarios. The existing
1 HRV + 3 sleep passing fixtures are insufficient on their own.

Output:
- Appended test functions in `Rust/core/tests/algorithm_compare_tests.rs`
- `.planning/phases/123-real-device-algorithm-validation/123-VALIDATION-ARTIFACT.md`
- `cargo test --locked` green for the full suite
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Rust/core/tests/algorithm_compare_tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 6 HRV fixture tests (per D-01, D-02, D-05)</name>
  <files>Rust/core/tests/algorithm_compare_tests.rs</files>
  <behavior>
    - hrv_low_hrv_high_stress_fixture: 20 intervals at ~800ms ±5ms jitter → RMSSD ≈ 7ms; report.pass == true; 4 zero-delta comparable fields
    - hrv_high_hrv_well_recovered_fixture: 20 intervals at ~800ms ±80ms jitter → RMSSD ≈ 113ms; report.pass == true
    - hrv_bradycardic_resting_fixture: 20 intervals at ~1330ms ±10ms (45 BPM); report.pass == true
    - hrv_long_window_moderate_fixture: 60 intervals at ~950ms ±30ms (63 BPM); report.pass == true
    - hrv_young_age_bracket_fixture: 30 intervals at ~700ms ±25ms (85 BPM); report.pass == true
    - hrv_older_age_bracket_fixture: 30 intervals at ~1050ms ±35ms (57 BPM); report.pass == true
    - All 6 new tests assert report.pass and iterate report.deltas asserting assert_close(delta.absolute_delta, 0.0)
    - All fixtures use rr_timestamps_s: None and stage_segments: None (per D-05 — no new functions)
  </behavior>
  <action>
Append 6 new `#[test]` functions to `Rust/core/tests/algorithm_compare_tests.rs` immediately
before the `assert_close` helper function at the bottom of the file (do not modify existing
tests). Each function calls `compare_hrv_goose_to_reference` — the only HRV comparison
function (per D-05).

RR interval design rules:
- Keep adjacent interval differences ≤ 200ms to avoid ectopic filter removal (A1 assumption).
- Use ≥ 20 intervals per fixture — fewer causes `not_enough_valid_rr_intervals` failure.
- Central RR values: low-stress ±5ms jitter → RMSSD ≈ 7ms; high-HRV ±80ms jitter;
  bradycardic ~1330ms; long-window 60 intervals; young ~700ms; older ~1050ms.

For high-HRV fixture (±80ms): alternate pairs such that adjacent diff ≤ 200ms —
e.g., [800.0, 880.0, 760.0, 840.0, ...] keeps each step at 80ms, within limit.

Struct construction for every HRV fixture:
- `start_time`: any ISO-8601 UTC string (e.g., "2026-05-27T00:00:00Z")
- `end_time`: any later UTC string
- `rr_intervals_ms`: Vec<f64> with ≥ 20 values
- `input_ids`: vec!["fixture.hrv.session{N}.{scenario_label}".to_string()]
- `rr_timestamps_s: None`
- `stage_segments: None`

Assertion pattern (same as existing test at lines 29–39):
```
assert!(report.pass, "{:?}", report.errors);
for delta in report.deltas {
    assert_close(delta.absolute_delta, 0.0);
}
```

Name the 6 functions (no collisions with existing):
1. `hrv_low_hrv_high_stress_fixture`
2. `hrv_high_hrv_well_recovered_fixture`
3. `hrv_bradycardic_resting_fixture`
4. `hrv_long_window_moderate_fixture`
5. `hrv_young_age_bracket_fixture`
6. `hrv_older_age_bracket_fixture`

After adding, run:
  cd Rust/core && cargo test --locked hrv_
All 7 hrv_ tests (existing + 6 new) must pass before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked hrv_ 2>&1 | tail -20</automated>
  </verify>
  <done>
    `cargo test --locked hrv_` reports 7 passing tests (1 existing + 6 new). No failures.
    Each new function is present in algorithm_compare_tests.rs with the fixture label in input_ids.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 4 sleep fixture tests + write 123-VALIDATION-ARTIFACT.md (per D-01, D-03, D-04, D-05)</name>
  <files>
    Rust/core/tests/algorithm_compare_tests.rs,
    .planning/phases/123-real-device-algorithm-validation/123-VALIDATION-ARTIFACT.md
  </files>
  <behavior>
    - sleep_deep_heavy_low_disturbance_fixture (v0): 450min sleep / 460min TIB / disturbance_count 1; report.pass == true; comparable_fields.len() == 7; all deltas ≈ 0.0
    - sleep_short_session_fragmented_fixture (v0): 300min sleep / 360min TIB / disturbance_count 10; report.pass == true; all deltas ≈ 0.0
    - sleep_long_session_low_efficiency_fixture (v0): 480min sleep / 570min TIB / disturbance_count 3; report.pass == true; all deltas ≈ 0.0
    - sleep_v1_rem_heavy_fixture (v1): 360min sleep / 480min TIB / disturbance_count 15; model_status.sleep_permission_granted: true; report.pass == true; report.goose_algorithm_id == "goose.sleep.v1"; all deltas ≈ 0.0
    - 123-VALIDATION-ARTIFACT.md exists with fixture count, delta tolerance, cargo test status, and SC-1/SC-2 deferral note
  </behavior>
  <action>
Append 4 new `#[test]` functions to `Rust/core/tests/algorithm_compare_tests.rs` immediately
before the `assert_close` helper (after the 6 HRV tests from Task 1).

Sleep v0 fixture construction rules (3 new, using `compare_sleep_goose_to_reference`):
- `start_time` / `end_time`: ISO-8601 UTC strings spanning the sleep session
- `sleep_duration_minutes`: actual sleep time (f64)
- `time_in_bed_minutes`: must be >= sleep_duration_minutes to avoid negative wake_minutes
- `sleep_need_minutes`: target sleep (use 480.0 unless the scenario is short-session)
- `disturbance_count`: i64
- `midpoint_deviation_minutes`: f64; use 0.0 for fragmented, small values for normal
- `input_ids`: vec!["fixture.sleep.session{N}.{label}".to_string()]
- `..Default::default()` for all remaining optional fields

Sleep v0 assertion pattern (same as existing test at lines 57–95):
```
assert!(report.pass, "{:?}", report.errors);
assert_eq!(report.comparable_fields.len(), 7);
for delta in &report.deltas {
    assert_close(delta.absolute_delta, 0.0);
}
```

Sleep v1 fixture construction rules (1 new, using `compare_sleep_v1_goose_to_reference`):
Wrap `SleepInput` in `SleepV1Input`. Required fields (omitting any causes v1 gate failure):
- `sleep.wake_after_sleep_onset_minutes`: must be set explicitly (not default 0.0) when TIB >>
  sleep_duration, to ensure WASO ≤ (TIB - sleep_duration); set to
  `time_in_bed_minutes - sleep_duration_minutes` or a smaller value.
- `model_status.sleep_permission_granted: true` — mandatory for v1 output gate
- `model_status.imported_platform_sleep_nights`: any i64 ≥ 1 (use 7)
- `data_coverage_fraction: Some(0.85)`

Sleep v1 assertion pattern (same as existing test at lines 99–144):
```
assert!(report.pass, "{:?}", report.errors);
assert_eq!(report.goose_algorithm_id, "goose.sleep.v1");
for delta in &report.deltas {
    assert_close(delta.absolute_delta, 0.0);
}
```

Name the 4 functions (no collisions with existing):
1. `sleep_deep_heavy_low_disturbance_fixture`
2. `sleep_short_session_fragmented_fixture`
3. `sleep_long_session_low_efficiency_fixture`
4. `sleep_v1_rem_heavy_fixture`

Concrete field values per fixture:
- deep_heavy: sleep_duration 450.0, time_in_bed 460.0, sleep_need 480.0, disturbance_count 1, midpoint_deviation 10.0
- short_session: sleep_duration 300.0, time_in_bed 360.0, sleep_need 480.0, disturbance_count 10, midpoint_deviation 60.0
- long_session: sleep_duration 480.0, time_in_bed 570.0, sleep_need 480.0, disturbance_count 3, midpoint_deviation 20.0
- v1_rem_heavy: sleep_duration 360.0, time_in_bed 480.0, sleep_need 480.0, disturbance_count 15, midpoint_deviation 0.0, wake_after_sleep_onset_minutes 120.0

After adding sleep tests, run the full suite:
  cd Rust/core && cargo test --locked

Then write `.planning/phases/123-real-device-algorithm-validation/123-VALIDATION-ARTIFACT.md`
with the following sections:
1. **Phase:** 123 — Real-Device Algorithm Validation (proxy fixtures)
2. **Requirements addressed:** VAL-HRV-04, VAL-SLP-04
3. **Fixture coverage:** HRV: 7 (1 pre-existing + 6 new); Sleep: 7 (3 pre-existing + 4 new)
4. **Delta tolerance:** HRV — report.pass gate (zero absolute_delta on RMSSD/SDNN/PNN50/mean_nn);
   Sleep — zero absolute_delta on all 7 comparable fields; no explicit ≤1ms threshold enforced by
   the comparison function beyond report.pass.
5. **cargo test --locked status:** PASS (record actual date and output summary)
6. **Hardware-gated criteria (deferred):**
   - SC-1: Real overnight RMSSD validation — deferred pending WHOOP 5 device access
   - SC-2: Real sleep staging concordance ≥70% — deferred pending WHOOP 5 device access
7. **SC-3 status:** COMPLETE — ≥7 HRV + ≥7 sleep proxy fixtures documented here
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | tail -30</automated>
  </verify>
  <done>
    `cargo test --locked` passes the full Rust/core suite with no failures.
    `algorithm_compare_tests.rs` contains at least 7 functions whose names match `sleep_*` (including existing ones).
    `123-VALIDATION-ARTIFACT.md` exists and contains sections for fixture coverage, delta tolerance, cargo test status, and SC-1/SC-2 deferral.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code only | All changes are in `#[cfg(test)]` context; no user-facing surface, no network, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-01 | Tampering | Synthetic RR intervals | low | accept | Fixtures are test-only; physiologically plausible values reviewed by planner; no runtime impact |
</threat_model>

<verification>
Run after both tasks complete:

```bash
cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | grep -E "^test result|FAILED|error\["
```

Expected output: `test result: ok. N passed; 0 failed`

Count HRV fixtures:
```bash
grep -c "fn hrv_" /Users/francisco/Documents/goose/Rust/core/tests/algorithm_compare_tests.rs
```
Expected: ≥ 7

Count sleep fixtures (passing — excludes failure-mode test):
```bash
grep -c "fn sleep_" /Users/francisco/Documents/goose/Rust/core/tests/algorithm_compare_tests.rs
```
Expected: ≥ 7

Confirm artifact exists:
```bash
ls /Users/francisco/Documents/goose/.planning/phases/123-real-device-algorithm-validation/123-VALIDATION-ARTIFACT.md
```
</verification>

<success_criteria>
- `cargo test --locked` passes with 0 failures in Rust/core
- ≥7 HRV fixture functions present in algorithm_compare_tests.rs (all assert report.pass)
- ≥7 sleep fixture functions present in algorithm_compare_tests.rs (all assert report.pass)
- 123-VALIDATION-ARTIFACT.md documents: fixture counts, delta tolerance policy, cargo test PASS, SC-1/SC-2 deferred as hardware-gated
- No new comparison functions created (D-05)
- No Swift files modified
- No new Cargo dependencies added
</success_criteria>

<output>
Create `.planning/phases/123-real-device-algorithm-validation/123-01-SUMMARY.md` when done.
</output>
