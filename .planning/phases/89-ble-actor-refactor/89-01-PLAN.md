---
phase: 89-ble-actor-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/BLETransport.swift
  - GooseSwift/CoreBluetoothBLETransport.swift
  - GooseSwift/CoreBluetoothBLETransport+BatteryCommands.swift
  - GooseSwift/CoreBluetoothBLETransport+CentralDelegate.swift
  - GooseSwift/CoreBluetoothBLETransport+Commands.swift
  - GooseSwift/CoreBluetoothBLETransport+DebugAndSync.swift
  - GooseSwift/CoreBluetoothBLETransport+Haptics.swift
  - GooseSwift/CoreBluetoothBLETransport+HistoricalCommands.swift
  - GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
  - GooseSwift/CoreBluetoothBLETransport+HRMonitor.swift
  - GooseSwift/CoreBluetoothBLETransport+Parsing.swift
  - GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift
  - GooseSwift/CoreBluetoothBLETransport+UserActions.swift
  - GooseSwift/CoreBluetoothBLETransport+VitalsAndLogging.swift
autonomous: true
requirements:
  - ARCH-05

must_haves:
  truths:
    - "BLETransport protocol exists at GooseSwift/BLETransport.swift"
    - "GooseBLEClient class is renamed to CoreBluetoothBLETransport and conforms to BLETransport"
    - "All 12 extension files are renamed from GooseBLEClient+X.swift to CoreBluetoothBLETransport+X.swift"
    - "All extension declarations inside those files say 'extension CoreBluetoothBLETransport'"
    - "No file in GooseSwift/ still contains the string 'class GooseBLEClient'"
  artifacts:
    - path: "GooseSwift/BLETransport.swift"
      provides: "BLETransport protocol"
      contains: "protocol BLETransport"
    - path: "GooseSwift/CoreBluetoothBLETransport.swift"
      provides: "Concrete BLE implementation"
      contains: "class CoreBluetoothBLETransport"
  key_links:
    - from: "GooseSwift/BLETransport.swift"
      to: "GooseSwift/CoreBluetoothBLETransport.swift"
      via: "protocol conformance declaration"
      pattern: "CoreBluetoothBLETransport.*BLETransport"
---

<objective>
Extract the BLETransport protocol from the GooseBLEClient public surface and rename
GooseBLEClient + all 12 extension files to CoreBluetoothBLETransport (per D1).

Purpose: Establishes the abstraction boundary that BLESessionCoordinator (Plan 02) and
GooseAppModel's `any BLETransport` type (Plan 02) depend on. Internals of each extension
file are left unchanged — only class name and file name change.

Output: BLETransport.swift (new protocol), CoreBluetoothBLETransport.swift (renamed class
with conformance), 12 renamed extension files.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GooseSwift/GooseBLEClient.swift
@GooseSwift/GooseBLEClient+UserActions.swift
@GooseSwift/GooseBLETypes.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BLETransport protocol</name>
  <files>GooseSwift/BLETransport.swift</files>
  <action>
    Create GooseSwift/BLETransport.swift containing the BLETransport protocol (per D1).
    The protocol must capture only the surface that GooseAppModel and views call directly on
    `ble` — not internal implementation details.

    Required protocol members (derived from all `ble.` call sites in GooseAppModel*.swift,
    views, MoreDataStore, NotificationFrameParsing, CodexCoachSupport):

    State properties (read-only from protocol perspective):
    - var bluetoothState: String { get }
    - var connectionState: String { get }
    - var isScanning: Bool { get }
    - var liveHeartRateBPM: Int? { get }
    - var liveHeartRateSource: String { get }
    - var liveHeartRateUpdatedAt: Date? { get }
    - var restingHeartRateEstimateBPM: Double? { get }
    - var restingHeartRateEstimateSampleCount: Int { get }
    - var restingHeartRateEstimateSource: String { get }
    - var restingHeartRateEstimateUpdatedAt: Date? { get }
    - var liveHRVRMSSD: Double? { get }
    - var liveHRVRRIntervalCount: Int { get }
    - var liveHRVSource: String { get }
    - var liveHRVUpdatedAt: Date? { get }
    - var hrConnectionState: String { get }
    - var activeDeviceName: String { get }
    - var activeDeviceIdentifier: UUID? { get }
    - var selectedDeviceID: UUID? { get set }
    - var connectedPeripheralUUID: String? { get }
    - var connectedAt: Date? { get }
    - var connectedCapabilities: DeviceCapabilities? { get }
    - var lastSyncAt: Date? { get }
    - var batteryLevelPercent: Int? { get }
    - var batteryUpdatedAt: Date? { get }
    - var batteryIsCharging: Bool? { get }
    - var batteryPowerStatus: String { get }
    - var firmwareVersion: String? { get }
    - var modelNumber: String? { get }
    - var hardwareRevision: String? { get }
    - var softwareRevision: String? { get }
    - var manufacturerName: String? { get }
    - var historicalPacketCount: Int { get }
    - var isHistoricalSyncing: Bool { get }
    - var historicalSyncStatus: String { get }
    - var historicalSyncFraction: Double? { get }
    - var lastHistoricalSyncCompletedAt: Date? { get }
    - var physiologyCaptureStatus: String { get }
    - var lastPhysiologyCommandSummary: String { get }
    - var highFrequencyHistorySyncActive: Bool { get }
    - var highFrequencyHistorySyncStatus: String { get }
    - var debugCommandStatus: String { get }
    - var debugCommandResponses: [GooseDebugCommandResponse] { get }
    - var canWriteAlarm: Bool { get }
    - var canWriteHighFrequencyHistorySync: Bool { get }
    - var canSyncHistorical: Bool { get }
    - var canSyncClock: Bool { get }
    - var syncToast: GooseSyncToast? { get }
    - var lastSyncFailure: GooseSyncFailure? { get }
    - var syncFailureSheet: GooseSyncFailure? { get set }
    - var discoveredDevices: [GooseDiscoveredDevice] { get }
    - var discoveredHRDevices: [GooseDiscoveredDevice] { get }
    - var hrBluetoothState: String { get }
    - var reconnectState: String { get }
    - var hrReconnectState: String { get }
    - var historicalSyncRunID: UUID { get }
    - var lastHistoricalRangeCommandStatus: String { get }
    - var alarmCommandStatus: String { get }
    - var lastAlarmCommandFrameHex: String { get }
    - var lastAlarmResponseSummary: String { get }
    - var lastAlarmResponsePayloadHex: String { get }
    - var lastAlarmEventSummary: String { get }
    - var lastAlarmEventPayloadHex: String { get }
    - var lastAlarmScheduledAt: Date? { get }
    - var lastAlarmID: Int? { get }
    - var strapClockDate: Date? { get }
    - var strapClockOffsetSeconds: TimeInterval? { get }
    - var strapClockUpdatedAt: Date? { get }
    - var strapClockStatus: String { get }
    - var highFrequencyHistorySyncDisplaySummary: String { get }
    - var lastHighFrequencyHistorySyncResponse: String { get }
    - var lastHighFrequencyHistorySyncEvent: String { get }
    - var debugResearchCommands: [GooseDebugCommandDefinition] { get }
    - var invalidFrameCount: Int { get }
    - var historicalSyncBurstsCompleted: Int { get }
    - var historicalSyncPagesTotal: Int? { get }

    Callback properties (settable by GooseAppModel):
    - var onNotification: ((GooseNotificationEvent) -> Void)? { get set }
    - var onRawNotification: ((GooseNotificationEvent) -> Void)? { get set }
    - var onRawNotificationWithContext: ((GooseNotificationEvent, GooseBLENotificationContext) -> Void)? { get set }
    - var onCommandWrite: ((GooseCommandWriteEvent) -> Void)? { get set }
    - var onLiveHeartRate: ((Int, String, Date) -> Void)? { get set }
    - var onHRVSample: ((Double, Int, String, Date) -> Void)? { get set }
    - var onHRSpike: ((Int, String) -> Void)? { get set }
    - var onConnectionStateChange: ((String) -> Void)? { get set }
    - var onHRConnectionStateChange: ((String) -> Void)? { get set }
    - var onHistoricalSyncProgress: ((GooseHistoricalSyncProgress) -> Void)? { get set }
    - var onHistoricalRangeTelemetry: ((GooseHistoricalRangeTelemetry) -> Void)? { get set }
    - var onMessage: ((GooseMessage) -> Void)? { get set }

    Sub-object accessors (used by views and MoreDataStore):
    - var messageStore: GooseMessageStore { get }
    - var hrMonitorManager: GooseBLEHRMonitorManager { get }
    - var bondingManager: GooseBLEBondingManager { get }
    - var dataValidator: GooseBLEDataValidator { get }
    - var historicalDirectWriteDatabasePath: String { get set }

    Action methods:
    - func requestBluetooth()
    - func startScan()
    - func startScan(reason: String, clearDiscovered: Bool)
    - func stopScan()
    - func stopScan(reason: String)
    - func reconnectRemembered()
    - func updateConnectionState(_ state: String)
    - func record(source: String, title: String)
    - func record(source: String, title: String, body: String)
    - func recordLiveHeartRate(bpm: Int, source: String, capturedAt: Date)
    - func syncHistoricalPackets()
    - func writeClockCommand()
    - func setWhoopAlarm(at date: Date, alarmID: Int)
    - func disableWhoopAlarms()
    - func buzz()
    - func applyBatteryLevel(_ level: Int, isCharging: Bool?, source: String)
    - func startPhysiologySignalCapture()
    - func stopPhysiologySignalCapture()
    - func startMovementHeartRateCapture()
    - func stopMovementHeartRateCapture()
    - func enterHighFrequencyHistorySync()
    - func exitHighFrequencyHistorySync()
    - func sendDebugResearchCommand(_ command: GooseDebugCommandDefinition)
    - func previewHelloWorldToast()
    - func setDebugCommandStatus(_ status: String)

    Mark the protocol with a comment: "BLETransport — the interface GooseAppModel and views
    depend on. CoreBluetoothBLETransport is the sole concrete implementation."

    Do not add AnyObject or class constraints. The protocol may be used with `any BLETransport`
    existentials (Swift 5.7+ opaque/existential syntax). Swift 6 strict concurrency: do not add
    @MainActor to the protocol itself — all conforming properties are already guarded by
    the class's own isolation model (the class is not an actor).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "protocol BLETransport" GooseSwift/BLETransport.swift</automated>
  </verify>
  <done>GooseSwift/BLETransport.swift exists and contains "protocol BLETransport" with all listed members.</done>
</task>

<task type="auto">
  <name>Task 2: Rename GooseBLEClient to CoreBluetoothBLETransport across all 13 files</name>
  <files>
    GooseSwift/CoreBluetoothBLETransport.swift,
    GooseSwift/CoreBluetoothBLETransport+BatteryCommands.swift,
    GooseSwift/CoreBluetoothBLETransport+CentralDelegate.swift,
    GooseSwift/CoreBluetoothBLETransport+Commands.swift,
    GooseSwift/CoreBluetoothBLETransport+DebugAndSync.swift,
    GooseSwift/CoreBluetoothBLETransport+Haptics.swift,
    GooseSwift/CoreBluetoothBLETransport+HistoricalCommands.swift,
    GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift,
    GooseSwift/CoreBluetoothBLETransport+HRMonitor.swift,
    GooseSwift/CoreBluetoothBLETransport+Parsing.swift,
    GooseSwift/CoreBluetoothBLETransport+PeripheralDelegate.swift,
    GooseSwift/CoreBluetoothBLETransport+UserActions.swift,
    GooseSwift/CoreBluetoothBLETransport+VitalsAndLogging.swift
  </files>
  <action>
    Perform the rename in two parts:

    PART A — File creation and class rename (per D1):

    For each source file GooseBLEClient{suffix}.swift, create the corresponding
    CoreBluetoothBLETransport{suffix}.swift with all content identical except:
    1. The class declaration: "final class GooseBLEClient" → "final class CoreBluetoothBLETransport"
       with ": BLETransport" added to the conformance list (main file only).
    2. Each "extension GooseBLEClient" → "extension CoreBluetoothBLETransport" (all 12 extension files).
    3. Any "GooseBLEClient(" constructor calls within the file → "CoreBluetoothBLETransport("
       (only the self-referential init call in the main file, if any).

    Do NOT change:
    - Internal implementation logic
    - DispatchQueue labels (they use reverse-DNS strings, not type names)
    - Logger category strings
    - The string literal "GooseBLEClient alarm write" in SleepBridgeViews.swift (that file
      is not in scope for this task)
    - UserDefaults keys or any other string constants

    PART B — Delete old files:

    After creating all 13 new files and verifying they compile (xcodebuild check is in the
    verify step), delete the original GooseBLEClient*.swift files using the Bash tool
    (git rm GooseSwift/GooseBLEClient.swift GooseSwift/GooseBLEClient+*.swift).

    PART C — Update all consumer files that reference the concrete type name:

    The following files hold "GooseBLEClient" as a type annotation or parameter type and
    must be updated to use "any BLETransport" or "CoreBluetoothBLETransport" as appropriate:

    Files that pass `ble` as a parameter typed GooseBLEClient — update the parameter type
    to "any BLETransport":
    - GooseSwift/MoreDataStore.swift (3 method signatures: refreshRouteStatus, liveNotificationCaptureSummary, startCapture)
    - GooseSwift/CodexCoachSupport.swift (devicePayload parameter)
    - GooseSwift/GooseAppModel+NotificationPipeline.swift (pipeline factory parameter)

    Files that store `ble` as a property typed GooseBLEClient — update the property type
    to "any BLETransport":
    - GooseSwift/SleepV2ScheduleViews.swift
    - GooseSwift/HealthSleepSheetsViews.swift (2 occurrences)
    - GooseSwift/HRMonitorView.swift (3 occurrences)
    - GooseSwift/SleepBridgeViews.swift (2 occurrences)
    - GooseSwift/RootView.swift
    - GooseSwift/FitnessLiveWorkoutViews.swift (2 occurrences)
    - GooseSwift/ConnectionView.swift
    - GooseSwift/DeviceView.swift (5 occurrences)
    - GooseSwift/HomeDashboardView.swift
    - GooseSwift/NotificationFrameParsing.swift
    - GooseSwift/LiveActivityContentView.swift
    - GooseSwift/OnboardingStepViews.swift (2 occurrences)

    GooseAppModel.swift is NOT updated in this task — that update is in Plan 02
    (it changes the type and init together with BLESessionCoordinator).

    Note: The string literal "GooseBLEClient alarm write" in SleepBridgeViews.swift is a
    display string, not a type reference — leave it unchanged.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -rn "class GooseBLEClient\|extension GooseBLEClient" GooseSwift/ | wc -l</automated>
  </verify>
  <done>
    Zero occurrences of "class GooseBLEClient" or "extension GooseBLEClient" in GooseSwift/.
    All 13 CoreBluetoothBLETransport*.swift files exist.
    All original GooseBLEClient*.swift files are deleted.
    Consumer files use "any BLETransport" for the ble parameter/property type.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Protocol existential → concrete | `any BLETransport` hides CoreBluetoothBLETransport; callers cannot access CoreBluetooth internals directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering | Protocol surface | accept | Protocol is additive — no existing behaviour removed; all existing guards remain in place |
| T-89-SC | Tampering | No new package installs in this plan | accept | No npm/pip/cargo installs; not applicable |
</threat_model>

<verification>
After both tasks complete, run a build to catch any missing protocol members or type errors:

```
cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "error:|warning:|Build succeeded|Build FAILED" | tail -30
```

Success: "Build succeeded" with zero new errors.
</verification>

<success_criteria>
1. GooseSwift/BLETransport.swift exists with "protocol BLETransport"
2. GooseSwift/CoreBluetoothBLETransport.swift contains "final class CoreBluetoothBLETransport" conforming to BLETransport
3. 12 CoreBluetoothBLETransport+X.swift extension files exist
4. Zero GooseBLEClient*.swift files remain
5. All consumer files use "any BLETransport" for the ble type
6. iOS build succeeds (xcodebuild, no new errors)
</success_criteria>

<output>
Create `.planning/phases/89-ble-actor-refactor/89-01-SUMMARY.md` when done.
</output>
