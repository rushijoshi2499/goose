---
phase: 71-coach-vow-noopapp-notifications-hr-decimation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/CoachView.swift
autonomous: true
requirements:
  - FEAT-01
must_haves:
  truths:
    - "Coach tab shows at most 1 contextual VOW nudge above CoachRoutesSection"
    - "Nudge resolves by priority: Critical Recovery > Low Recovery > High Strain > Low HRV"
    - "Tapping the xmark or swiping down dismisses the card for the session"
    - "No nudge is shown when all thresholds are within healthy range"
    - "VOW computation uses only existing healthStore snapshot calls — no new bridge methods"
  artifacts:
    - path: "GooseSwift/CoachView.swift"
      provides: "CoachVOWNudge private enum + CoachVOWCard private struct + insertion in CoachOverviewScreen"
      contains: "CoachVOWNudge"
  key_links:
    - from: "CoachOverviewScreen.body (LazyVStack)"
      to: "CoachVOWCard"
      via: "if !vowDismissed, let nudge = CoachVOWNudge.resolve(healthStore: healthStore)"
      pattern: "CoachVOWNudge\\.resolve"
    - from: "CoachVOWNudge.resolve"
      to: "healthStore.snapshot(for: .recovery)"
      via: "Double(healthStore.snapshot(for: .recovery).value)"
      pattern: "snapshot\\(for: \\.recovery\\)"
---

<objective>
Implement FEAT-01: Coach VOW (Voice of WHOOP) contextual nudge card in CoachView.swift.

Purpose: Surfaces a single high-priority health nudge at the top of the Coach tab, computed locally from existing HealthDataStore snapshot data. No new bridge methods or SQLite reads required.

Output: Private enum CoachVOWNudge (resolve logic + display properties) and private struct CoachVOWCard (SwiftUI view) — both defined in CoachView.swift. Card inserted between CoachJournalCard and CoachRoutesSection in CoachOverviewScreen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-CONTEXT.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-RESEARCH.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-PATTERNS.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CoachVOWNudge enum and CoachVOWCard view to CoachView.swift</name>
  <files>GooseSwift/CoachView.swift</files>
  <read_first>
    Read GooseSwift/CoachView.swift focusing on:
    - Lines 437–510: CoachOverviewScreen struct — find existing @State vars (e.g. showingJournal), LazyVStack content order, healthStore parameter
    - Lines 685–710: coachCardSurface(tint:prominent:) modifier — copy its exact signature for .coachCardSurface(tint: nudge.tint) call
    - Locate CoachJournalCard and CoachRoutesSection callsites inside the LazyVStack to confirm the insertion point
    - Confirm HealthMetricSnapshot.value type: read GooseSwift/HealthModels.swift around struct HealthMetricSnapshot to verify value is String
    Read GooseSwift/HeartRateSeriesStores.swift briefly to confirm HRVSeriesStore.shared.dailyEstimate()?.rmssdMS exists and its return type
  </read_first>
  <action>
    Add a private enum CoachVOWNudge to CoachView.swift (place it immediately before CoachVOWCard, after CoachJournalCard or in the private types region at the bottom of the file — match the file's existing private type placement pattern).

    CoachVOWNudge has four cases per D-01 thresholds (FEAT-01):
    - criticalRecovery(Double) — recovery < 33
    - lowRecovery(Double)      — recovery < 66
    - highStrain(Double)       — strain > 18
    - lowHRV(Double)           — hrv < 30ms (weekly average via HRVSeriesStore.shared.dailyEstimate()?.rmssdMS)

    static func resolve(healthStore: HealthDataStore) -> CoachVOWNudge? evaluates in priority order. Parse snapshot.value with Double(healthStore.snapshot(for: .recovery).value) — value is always String ("68" or "--"); guard against nil parse. Strain: Double(healthStore.snapshot(for: .strain).value). HRV: HRVSeriesStore.shared.dailyEstimate()?.rmssdMS.

    Computed properties on CoachVOWNudge:
    - var title: String — "Critical Recovery" / "Low Recovery" / "High Strain" / "Low HRV"
    - var body: String — exact copy from UI-SPEC copywriting contract
    - var systemImage: String — "heart.slash" / "heart" / "figure.run" / "waveform.path.ecg"
    - var tint: Color — .red / .orange / .orange / .blue

    Add private struct CoachVOWCard: View with:
    - let nudge: CoachVOWNudge
    - let onDismiss: () -> Void
    - body: HStack(spacing: 12) with icon frame 36×36 (tint at 12% opacity, RoundedRectangle cornerRadius 8), VStack(.leading, spacing: 4) with Text(nudge.title) .subheadline.weight(.semibold) and Text(nudge.body) .caption .foregroundStyle(.secondary) lineLimit(2), Spacer(), dismiss Button with Image(systemName: "xmark") .caption2.weight(.semibold) .foregroundStyle(.tertiary) and accessibility label "Dismiss nudge"
    - .padding(12) on HStack root
    - .coachCardSurface(tint: nudge.tint) — no prominent: argument (defaults false)
    - .accessibilityElement(children: .combine)
    - .accessibilityLabel("\(nudge.title). \(nudge.body). Double-tap to dismiss.")
    - .gesture(DragGesture().onEnded { if $0.translation.height > 30 { onDismiss() } })

    In CoachOverviewScreen:
    - Add @State private var vowDismissed = false alongside existing @State vars
    - In the LazyVStack body, insert between CoachJournalCard and CoachRoutesSection:
      if !vowDismissed, let nudge = CoachVOWNudge.resolve(healthStore: healthStore) {
        CoachVOWCard(nudge: nudge) { vowDismissed = true }
          .opacity(vowDismissed ? 0 : 1)
          .animation(.easeOut(duration: 0.2), value: vowDismissed)
      }
    - Do NOT animate the resolve logic — only animate dismiss transition

    Icon font size: .system(size: 17, weight: .semibold) inside the 36×36 frame.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "CoachVOWNudge" GooseSwift/CoachView.swift</automated>
  </verify>
  <done>
    CoachVOWNudge enum with resolve(healthStore:) static func exists in CoachView.swift.
    CoachVOWCard struct with dismiss gesture and coachCardSurface modifier exists.
    CoachOverviewScreen.body inserts the card between CoachJournalCard and CoachRoutesSection.
    Project builds without error: xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| healthStore.snapshot(for:) → CoachVOWNudge | snapshot.value is a String from SQLite; parsed locally with Double() — never trust raw value is numeric |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01-01 | Information Disclosure | CoachVOWNudge.body copy | accept | Body text is static strings from UI-SPEC; no user PII interpolated |
| T-71-01-02 | Tampering | Double(snapshot.value) parse | mitigate | Parse with guard let; nil-safe fallback returns nil (no nudge shown) rather than crashing or showing incorrect threshold |
</threat_model>

<verification>
1. grep -c "CoachVOWNudge" GooseSwift/CoachView.swift — must be > 0
2. grep -c "CoachVOWCard" GooseSwift/CoachView.swift — must be > 0
3. grep "vowDismissed" GooseSwift/CoachView.swift — must appear in @State declaration and condition
4. xcodebuild build must succeed (no compile errors)
</verification>

<success_criteria>
- CoachVOWNudge enum with all four cases and resolve(healthStore:) → CoachVOWNudge? in CoachView.swift
- CoachVOWCard view with dismiss xmark button, swipe-down gesture, coachCardSurface modifier, accessibility label
- CoachOverviewScreen.body inserts the guarded card expression between CoachJournalCard and CoachRoutesSection
- Project compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-01-SUMMARY.md` when done
</output>
