---
phase: 87-store-rs-split
plan: 05
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - Rust/core/src/store/activity.rs
  - Rust/core/src/store/mod.rs
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "store/activity.rs contains all 49 activity domain methods as impl GooseStore"
    - "Each method acquires self.conn.lock().map_err(...) at entry"
    - "insert_exercise_session and insert_exercise_sessions_batch closures use |conn| (fixed in Plan 01)"
    - "Methods removed from store/mod.rs — no duplicates"
    - "cargo test --locked passes after activity move"
  artifacts:
    - path: "Rust/core/src/store/activity.rs"
      provides: "impl GooseStore — activity sessions, intervals, labels, debug sessions/commands/events, table introspection, exercise sessions, journal, workout, apple_daily"
      exports:
        - insert_activity_session
        - update_activity_session
        - delete_activity_session
        - activity_session
        - activity_sessions_between
        - activity_sessions_by_type
        - activity_sessions_by_source
        - activity_sessions_by_sync_status
        - activity_sessions_by_custom_label
        - activity_sessions_by_external_activity_type_code
        - activity_sessions_by_external_activity_type_name
        - insert_activity_metric
        - insert_activity_metrics
        - activity_metric
        - activity_metrics_for_session
        - activity_metrics_for_sessions
        - activity_metrics_by_name
        - activity_metrics_for_session_in_window
        - activity_metrics_in_window
        - insert_activity_interval
        - activity_interval
        - activity_intervals_for_session
        - activity_intervals_in_window
        - insert_activity_label
        - activity_label
        - activity_labels_for_session
        - activity_labels_by_type
        - insert_debug_session
        - debug_session
        - debug_sessions_between
        - insert_debug_command
        - debug_command
        - debug_commands_for_session
        - debug_commands_between
        - next_debug_event_sequence
        - insert_debug_event
        - debug_events_for_session
        - debug_events_between
        - debug_events_after_sequence
        - table_count
        - table_columns
        - foreign_keys_enabled
        - integrity_check
        - insert_exercise_session
        - insert_exercise_sessions_batch
        - exercise_sessions_between
        - insert_journal
        - insert_workout
        - insert_apple_daily
  key_links:
    - from: "Rust/core/src/store/activity.rs"
      to: "Rust/core/src/store/mod.rs"
      via: "use super::GooseStore — submodule declared via mod activity in mod.rs"
      pattern: "use super::GooseStore"
    - from: "Rust/core/src/bridge/activity.rs"
      to: "insert_activity_metrics"
      via: "immediate_transaction closure inlined in Plan 01 — no longer calls store.insert_activity_metrics"
      pattern: "immediate_transaction"
---

<objective>
Move all 49 activity domain methods from store/mod.rs into store/activity.rs. Includes activity sessions, intervals, labels, debug subsystem, table introspection helpers, exercise sessions (which used immediate_transaction — already fixed in Plan 01), journal, workout, apple_daily.

Purpose: Completes the domain split. After this plan and Plans 02/03/04, store/mod.rs retains only infrastructure (struct, types, open/migrate/schema/transaction) and store/ directory is fully split.

Output: store/activity.rs with 49 methods, store/mod.rs with those methods removed.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-store-rs-split/87-CONTEXT.md
@.planning/phases/87-store-rs-split/87-RESEARCH.md
@.planning/phases/87-store-rs-split/87-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move 49 activity methods from store/mod.rs into store/activity.rs</name>
  <files>Rust/core/src/store/activity.rs, Rust/core/src/store/mod.rs</files>

  <read_first>
    - Rust/core/src/store/mod.rs — lines 2552–3275 (activity sessions: insert_activity_session through activity_metrics_in_window — 19 methods)
    - Rust/core/src/store/mod.rs — lines 4538–5165 (activity intervals and labels: insert_activity_interval through activity_labels_by_type — 8 methods)
    - Rust/core/src/store/mod.rs — lines 6195–6620 (debug sessions/commands/events + table introspection helpers: insert_debug_session through integrity_check — 16 methods)
    - Rust/core/src/store/mod.rs — lines 6743–6870 (exercise sessions: insert_exercise_session, insert_exercise_sessions_batch, exercise_sessions_between — 3 methods; note: first two use immediate_transaction, already changed to |conn| in Plan 01)
    - Rust/core/src/store/mod.rs — lines 6957–7040 (insert_journal, insert_workout, insert_apple_daily — 3 methods)
    - Rust/core/src/store/mod.rs — lines 1–80 (private helpers used: validate_required, validate_allowed, is_allowed_activity_type, is_allowed_sync_status, is_allowed_detection_method, is_allowed_interval_type, is_allowed_label_type; ALLOWED_* constants)
  </read_first>

  <action>
  Step 1 — Verify insert_exercise_session and insert_exercise_sessions_batch closures in store/mod.rs before moving. Both methods use `immediate_transaction`. After Plan 01, both closures must use `|conn| { conn.execute(...) }`. If not updated, fix before moving.

  Step 2 — Audit all private helpers referenced by activity methods. Expected:
    - `validate_required`, `validate_allowed`
    - `is_allowed_activity_type`, `is_allowed_sync_status`, `is_allowed_detection_method`
    - `is_allowed_interval_type`, `is_allowed_label_type`
    - Relevant ALLOWED_* constant arrays

  Step 3 — Collect all pub types needed. Expected: ActivitySessionInput, ActivityMetricInput, ActivityIntervalInput, ActivityLabelInput, DebugSessionRow, DebugCommandRow, DebugEventRow, ExerciseSessionInput, and others. All defined in store/mod.rs.

  Step 4 — Write store/activity.rs:
    - `use super::GooseStore;`
    - `use super::{validate_required, validate_allowed, is_allowed_activity_type, is_allowed_sync_status, is_allowed_detection_method, is_allowed_interval_type, is_allowed_label_type};` (only helpers actually used)
    - `use super::{ActivitySessionInput, ActivityMetricInput, ...};` for all pub types needed
    - `use crate::{GooseError, GooseResult};`
    - `use rusqlite::{params, named_params, Connection, ...};`
    - Additional imports if required (serde_json for JSON columns, chrono for timestamps)
    - Single `impl GooseStore { ... }` block with all 49 methods

  Step 5 — For each method, confirm lock acquisition at entry:
    `let conn = self.conn.lock().map_err(|_| GooseError::message("store mutex poisoned"))?;`

  Step 6 — Remove all 49 methods from store/mod.rs. The methods span 6 line ranges (noted in read_first). Remove each block with Edit. Verify the file still compiles by watching for orphaned closing braces.

  Step 7 — After removal, store/mod.rs should contain only:
    - All imports
    - All `pub struct`, `pub type`, `pub enum`, `pub const` definitions
    - 7 infrastructure methods (open, open_existing_current, open_read_only, open_in_memory, immediate_transaction, migrate, schema_version) plus private helpers
    - 4 `mod` declarations (sleep, capture, metrics, activity)
    - `#[cfg(test)]` blocks
  Verify by counting `pub fn` in mod.rs — should be 7 or close to it.
  </action>

  <verify>
    <automated>cd Rust/core && cargo test --locked 2>&1 | grep -E '(^error|test result|FAILED)' | head -20</automated>
  </verify>

  <done>
  `cargo test --locked` shows no errors and no FAILED tests. `grep -c 'fn insert_activity_session' Rust/core/src/store/activity.rs` returns 1. `grep -c 'fn insert_activity_session' Rust/core/src/store/mod.rs` returns 0. `grep -c '^    pub fn' Rust/core/src/store/mod.rs` returns 7 (infrastructure methods only).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store/ submodule | activity.rs is private submodule; debug sessions and exercise data stay inside store layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-05 | Tampering | insert_exercise_session/batch immediate_transaction | mitigate | Verify |conn| closure before moving — deadlock if |store| closure remains |
| T-87-SC | Tampering | cargo installs | accept | No new packages |
</threat_model>

<verification>
```bash
cd Rust/core && cargo test --locked 2>&1 | tail -5

# Spot-check method locations:
grep -c 'fn insert_activity_session' Rust/core/src/store/activity.rs   # Expected: 1
grep -c 'fn insert_activity_session' Rust/core/src/store/mod.rs         # Expected: 0

grep -c 'fn insert_exercise_session' Rust/core/src/store/activity.rs   # Expected: 1
grep -c 'fn table_count' Rust/core/src/store/activity.rs               # Expected: 1

# Verify immediate_transaction closures in exercise methods:
grep -A 5 'fn insert_exercise_session' Rust/core/src/store/activity.rs | grep '|conn|'
# Expected: at least one match (the immediate_transaction closure)

# Verify store/mod.rs only has infrastructure:
grep -c '^    pub fn' Rust/core/src/store/mod.rs
# Expected: 7 (open, open_existing_current, open_read_only, open_in_memory, immediate_transaction, migrate, schema_version)
```
</verification>

<success_criteria>
- All 49 activity methods in store/activity.rs, none in store/mod.rs
- insert_exercise_session and insert_exercise_sessions_batch use `|conn|` closures inside immediate_transaction
- store/mod.rs retains only 7 infrastructure pub fn methods
- `cargo test --locked` passes with zero failures
</success_criteria>

<output>
Create `.planning/phases/87-store-rs-split/87-05-SUMMARY.md` when done.
</output>
