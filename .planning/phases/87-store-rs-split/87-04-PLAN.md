---
phase: 87-store-rs-split
plan: 04
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - Rust/core/src/store/metrics.rs
  - Rust/core/src/store/mod.rs
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "store/metrics.rs contains all 49 metrics domain methods as impl GooseStore"
    - "Each method acquires self.conn.lock().map_err(...) at entry"
    - "Methods removed from store/mod.rs — no duplicates"
    - "cargo test --locked passes after metrics move"
  artifacts:
    - path: "Rust/core/src/store/metrics.rs"
      provides: "impl GooseStore — daily/hourly activity metrics, recovery metrics, EWMA baselines, provenance, debug features, algorithm definitions/runs/preferences, calibration, command validation, gravity/IMU/resp/v24 sensor rows, metric series"
      exports:
        - insert_daily_recovery_metric
        - upsert_daily_recovery_metric
        - daily_recovery_metric
        - daily_recovery_metrics_between
        - daily_recovery_metrics_all_ordered
        - ewma_baseline_update
        - insert_metric_provenance
        - upsert_metric_provenance
        - metric_provenance
        - metric_provenance_for_metric
        - insert_metric_debug_feature
        - metric_debug_feature
        - metric_debug_features_between
        - upsert_algorithm_definition
        - algorithm_definition
        - set_algorithm_preference
        - algorithm_preference
        - algorithm_preferences
        - insert_algorithm_run
        - algorithm_run
        - algorithm_runs_overlapping
        - metric_values_for_run
        - metric_components_for_run
        - insert_calibration_run
        - calibration_run
        - calibration_runs_overlapping
        - insert_calibration_label
        - calibration_label
        - calibration_labels_between
        - upsert_command_validation_record
        - command_validation_record
        - command_validation_records
        - insert_gravity_rows
        - gravity_rows_between
        - insert_gravity2_batch
        - gravity2_samples_between
        - resp_samples_between
        - insert_v24_biometric_batch
        - v24_biometric_samples_between
        - insert_metric_series
        - query_metric_series_range
        - insert_daily_activity_metric
        - upsert_daily_activity_metric
        - daily_activity_metric
        - daily_activity_metrics_between
        - insert_hourly_activity_metric
        - upsert_hourly_activity_metric
        - hourly_activity_metric
        - hourly_activity_metrics_between
  key_links:
    - from: "Rust/core/src/store/metrics.rs"
      to: "Rust/core/src/store/mod.rs"
      via: "use super::GooseStore — submodule declared via mod metrics in mod.rs"
      pattern: "use super::GooseStore"
---

<objective>
Move all 49 metrics domain methods from store/mod.rs into store/metrics.rs. This is the largest domain by method count (46 per RESEARCH, but daily/hourly activity metrics add 8 more bringing it to ~49 — verify exact count by reading the source).

Purpose: Isolates the metrics, recovery, calibration, algorithm, gravity/IMU, and sensor time-series domain into a single file for editor navigation and review clarity.

Output: store/metrics.rs with ~49 methods, store/mod.rs with those methods removed.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-store-rs-split/87-CONTEXT.md
@.planning/phases/87-store-rs-split/87-RESEARCH.md
@.planning/phases/87-store-rs-split/87-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move metrics/calibration/algorithm/sensor methods into store/metrics.rs</name>
  <files>Rust/core/src/store/metrics.rs, Rust/core/src/store/mod.rs</files>

  <read_first>
    - Rust/core/src/store/mod.rs — lines 3275–3725 (insert_daily_activity_metric, upsert_daily_activity_metric, daily_activity_metric, daily_activity_metrics_between, insert_hourly_activity_metric, upsert_hourly_activity_metric, hourly_activity_metric, hourly_activity_metrics_between, insert_daily_recovery_metric)
    - Rust/core/src/store/mod.rs — lines 3723–4130 (recovery metrics, ewma_baseline_update)
    - Rust/core/src/store/mod.rs — lines 4115–4400 (metric_provenance, metric_debug_feature*)
    - Rust/core/src/store/mod.rs — lines 5389–6200 (algorithm definitions, runs, preferences, calibration, command_validation)
    - Rust/core/src/store/mod.rs — lines 6618–6730 (gravity_rows, gravity2, resp_samples)
    - Rust/core/src/store/mod.rs — lines 6847–6890 (insert_v24_biometric_batch, v24_biometric_samples_between) — NOTE: insert_v24_biometric_batch uses immediate_transaction; after Plan 01 the closure should use |conn|. Verify before moving.
    - Rust/core/src/store/mod.rs — lines 7036–7070 (insert_metric_series, query_metric_series_range)
    - Rust/core/src/store/mod.rs — lines 1–80 (identify private helpers: validate_required, validate_allowed, is_allowed_metric_unit, is_allowed_calibration_label_source, is_allowed_profile_platform_context, and any ALLOWED_* constants used)
  </read_first>

  <action>
  Step 1 — Before moving, verify `insert_v24_biometric_batch` closure in store/mod.rs.
  This method uses `immediate_transaction`. After Plan 01, the closure must be `|conn| { conn.execute(...) }` — not `|store| { store.conn.execute(...) }`. If it was not updated in Plan 01 Task 1, fix it now before moving.

  Step 2 — Audit all private helpers and constants referenced by metrics methods:
    - `validate_required`, `validate_allowed`
    - `is_allowed_metric_unit`, `is_allowed_calibration_label_source`, `is_allowed_profile_platform_context`
    - ALLOWED_* constant arrays (e.g., `ALLOWED_METRIC_UNITS`, `ALLOWED_CALIBRATION_LABEL_SOURCES`)
    - Any `sha2` or hashing used in calibration methods
  Record the full list — these stay in mod.rs and are imported via `use super::`.

  Step 3 — Collect all pub types needed by metrics methods. Expected:
    DailyActivityMetricInput, HourlyActivityMetricInput, DailyRecoveryMetricInput, MetricProvenanceInput, MetricDebugFeatureInput, AlgorithmPreferenceRecord, CalibrationLabelInput, CommandValidationRecord, GravityRow, and ~10 more. All are defined in store/mod.rs.

  Step 4 — Write store/metrics.rs:
    - `use super::GooseStore;`
    - `use super::{all private helpers actually used};`
    - `use super::{all pub types actually used};`
    - `use crate::{GooseError, GooseResult};`
    - `use rusqlite::{params, named_params, Connection, ...};` — match what the methods use
    - Any additional imports (serde_json, chrono, etc.) the methods require
    - Single `impl GooseStore { ... }` block with all methods

  Step 5 — For each method, confirm the lock acquisition pattern:
    `let conn = self.conn.lock().map_err(|_| GooseError::message("store mutex poisoned"))?;`
  Methods already updated in Plan 01 should have this. Verify; fix any that don't.

  Step 6 — Remove all metrics methods from store/mod.rs. The methods are scattered across several non-contiguous line ranges. Remove each block carefully using Edit, verifying surrounding methods are not disturbed.

  Step 7 — Confirm method count: `grep -c '^    pub fn' Rust/core/src/store/metrics.rs` should be 49 (or the actual count from reading the source — accept any count matching what was moved as long as it matches the method list in must_haves.artifacts).
  </action>

  <verify>
    <automated>cd Rust/core && cargo test --locked 2>&1 | grep -E '(^error|test result|FAILED)' | head -20</automated>
  </verify>

  <done>
  `cargo test --locked` shows no errors and no FAILED tests. `grep -c 'fn insert_daily_recovery_metric' Rust/core/src/store/metrics.rs` returns 1. `grep -c 'fn insert_daily_recovery_metric' Rust/core/src/store/mod.rs` returns 0. `grep -c 'fn ewma_baseline_update' Rust/core/src/store/metrics.rs` returns 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store/ submodule | metrics.rs is private submodule; calibration and algorithm data stay inside store layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04 | Tampering | insert_v24_biometric_batch immediate_transaction | mitigate | Verify closure uses |conn| before moving — deadlock risk if not fixed per Plan 01 |
| T-87-SC | Tampering | cargo installs | accept | No new packages |
</threat_model>

<verification>
```bash
cd Rust/core && cargo test --locked 2>&1 | tail -5

# Spot-check method locations:
grep -c 'fn ewma_baseline_update' Rust/core/src/store/metrics.rs     # Expected: 1
grep -c 'fn ewma_baseline_update' Rust/core/src/store/mod.rs          # Expected: 0

grep -c 'fn insert_calibration_run' Rust/core/src/store/metrics.rs   # Expected: 1
grep -c 'fn insert_v24_biometric_batch' Rust/core/src/store/metrics.rs # Expected: 1

# Confirm no immediate_transaction deadlock — verify closure signature in moved method:
grep -A 5 'fn insert_v24_biometric_batch' Rust/core/src/store/metrics.rs | grep 'FnOnce\|immediate_transaction\||conn'
```
</verification>

<success_criteria>
- All ~49 metrics/calibration/algorithm/sensor methods in store/metrics.rs, none in store/mod.rs
- insert_v24_biometric_batch correctly uses `|conn|` closure (not `|store|`)
- Each method acquires the mutex lock at entry
- `cargo test --locked` passes with zero failures
</success_criteria>

<output>
Create `.planning/phases/87-store-rs-split/87-04-SUMMARY.md` when done.
</output>
