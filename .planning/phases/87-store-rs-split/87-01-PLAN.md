---
phase: 87-store-rs-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/store.rs
  - Rust/core/src/store/mod.rs
  - Rust/core/src/store/sleep.rs
  - Rust/core/src/store/capture.rs
  - Rust/core/src/store/metrics.rs
  - Rust/core/src/store/activity.rs
  - Rust/core/src/capture_import.rs
  - Rust/core/src/bridge/activity.rs
  - Rust/core/src/bridge/sleep.rs
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "store/mod.rs exists with GooseStore struct using Arc<Mutex<Connection>> and pub(super) conn field"
    - "immediate_transaction closure signature is FnOnce(&Connection) — not &GooseStore — eliminating deadlock risk"
    - "All 9 immediate_transaction call sites updated: 5 in store.rs methods, 2 in bridge/activity.rs, 1 in bridge/sleep.rs, 1 in capture_import.rs"
    - "Four empty domain stub files exist (store/sleep.rs, store/capture.rs, store/metrics.rs, store/activity.rs) each declaring use super::GooseStore"
    - "store.rs is deleted via git rm; store/mod.rs is the sole module entry point"
    - "cargo build --lib compiles with zero errors after Wave 1"
  artifacts:
    - path: "Rust/core/src/store/mod.rs"
      provides: "GooseStore struct, all pub types, open/migrate/schema/transaction, private helpers"
      contains: "pub(super) conn: Arc<Mutex<Connection>>"
    - path: "Rust/core/src/store/sleep.rs"
      provides: "empty impl GooseStore stub for sleep domain"
    - path: "Rust/core/src/store/capture.rs"
      provides: "empty impl GooseStore stub for capture domain"
    - path: "Rust/core/src/store/metrics.rs"
      provides: "empty impl GooseStore stub for metrics domain"
    - path: "Rust/core/src/store/activity.rs"
      provides: "empty impl GooseStore stub for activity domain"
  key_links:
    - from: "Rust/core/src/lib.rs"
      to: "Rust/core/src/store/mod.rs"
      via: "pub mod store; resolves to store/mod.rs automatically once store.rs is deleted"
      pattern: "pub mod store"
    - from: "Rust/core/src/bridge/activity.rs"
      to: "immediate_transaction"
      via: "closure now receives &Connection not &GooseStore"
      pattern: "immediate_transaction"
---

<objective>
Create the store/ skeleton: move the entire store.rs content into store/mod.rs, change the GooseStore struct to use Arc<Mutex<Connection>>, fix immediate_transaction to eliminate the deadlock, update all 9 call sites, and create 4 empty domain stub files. Delete store.rs via git rm.

Purpose: Unblocks Wave 2 plans which will fill the domain stub files. After Wave 1 compiles, the codebase is in an identical functional state — all 139 methods still live in store/mod.rs and the public API is unchanged.

Output: store/mod.rs (full store content + Arc<Mutex> + fixed immediate_transaction), 4 empty stubs, no store.rs.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/87-store-rs-split/87-CONTEXT.md
@.planning/phases/87-store-rs-split/87-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create store/mod.rs from store.rs — struct change + immediate_transaction fix</name>
  <files>Rust/core/src/store/mod.rs, Rust/core/src/store.rs (deleted)</files>

  <read_first>
    - Rust/core/src/store.rs — lines 1–200 (struct def, imports, CURRENT_SCHEMA_VERSION, GooseStore struct at line 166)
    - Rust/core/src/store.rs — lines 1050–1200 (open*, migrate, immediate_transaction at line 1091)
    - Rust/core/src/store.rs — lines 1840–1930 (schema_version, mirror_overnight_batch — first immediate_transaction call site)
    - Rust/core/src/store.rs — lines 6735–6870 (insert_exercise_session, insert_exercise_sessions_batch, insert_v24_biometric_batch — immediate_transaction call sites)
    - Rust/core/src/store.rs — lines 7480–7640 (backfill_streams_from_decoded_frames — immediate_transaction call site)
    - Rust/core/src/bridge/activity.rs — lines 690–820 (two immediate_transaction call sites)
    - Rust/core/src/bridge/sleep.rs — lines 465–510 (one immediate_transaction call site)
    - Rust/core/src/capture_import.rs — lines 238–270 (one immediate_transaction call site)
  </read_first>

  <action>
  Step 1 — Create the directory and copy store.rs content into store/mod.rs:
  Run `mkdir -p Rust/core/src/store` then copy the entire store.rs content as store/mod.rs starting point.

  Step 2 — Change the GooseStore struct field (per D-02). In store/mod.rs, locate the struct at line ~166:
    BEFORE: `conn: Connection,`
    AFTER:  `pub(super) conn: Arc<Mutex<Connection>>,`

  Add the required imports at the top of store/mod.rs (after existing `use rusqlite` lines):
    `use std::sync::{Arc, Mutex};`

  Step 3 — Update the 5 `open*` constructor methods and `migrate` to wrap connections in Arc::new(Mutex::new(...)):
  Every place that constructs `Self { conn }` becomes `Self { conn: Arc::new(Mutex::new(conn)) }`.
  Methods affected: `open`, `open_existing_current`, `open_read_only`, `open_in_memory`.

  Step 4 — Update all methods that previously called `self.conn.X(...)` directly to acquire the lock first.
  Pattern for every public method that accesses `self.conn`:
    `let conn = self.conn.lock().map_err(|_| GooseError::message("store mutex poisoned"))?;`
  Then use `conn.X(...)` instead of `self.conn.X(...)`.

  The `migrate` private method also accesses `self.conn` — apply the same lock pattern.
  `schema_version` (line ~1848) accesses `self.conn` — apply lock pattern.
  `configure_read_write_connection` and `configure_read_only_connection` are private functions that receive `&Connection` — they do NOT need locking changes since they receive already-acquired connections.

  Step 5 — Fix `immediate_transaction` (CRITICAL — prevents deadlock per RESEARCH Pitfall 1).
  Change the closure signature from `FnOnce(&GooseStore)` to `FnOnce(&Connection)`.
  New implementation:
  ```
  pub fn immediate_transaction<F, T>(&self, operation: F) -> GooseResult<T>
  where
      F: FnOnce(&Connection) -> GooseResult<T>,
  {
      let conn = self.conn.lock().map_err(|_| GooseError::message("store mutex poisoned"))?;
      conn.execute_batch("BEGIN IMMEDIATE TRANSACTION")?;
      match operation(&conn) {
          Ok(value) => {
              conn.execute_batch("COMMIT")?;
              Ok(value)
          }
          Err(error) => {
              let _ = conn.execute_batch("ROLLBACK");
              Err(error)
          }
      }
  }
  ```
  Note: `&Connection` is the already-locked `MutexGuard<Connection>` deref — the lock is held for the duration of the entire transaction.

  Step 6 — Update the 5 immediate_transaction call sites INSIDE store/mod.rs (which was store.rs):

  a) `mirror_overnight_batch` (line ~1860): closure was `|store| { store.conn.execute(...) }` — change to `|conn| { conn.execute(...) }`. All `store.conn` references become `conn` references.

  b) `insert_exercise_session` (line ~6745): closure was `|store| { store.conn.execute(...) }` — change to `|conn| { conn.execute(...) }`.

  c) `insert_exercise_sessions_batch` (line ~6779): same pattern — `|conn| { conn.execute(...) }`.

  d) `insert_v24_biometric_batch` (line ~6853): same pattern — `|conn| { conn.execute(...) }`.

  e) `backfill_streams_from_decoded_frames` (line ~7570): same pattern — `|conn| { conn.execute(...) }`.

  Step 7 — Update the `#[cfg(test)]` blocks in store/mod.rs that access `store.conn` directly.
  These are inside `#[cfg(test)]` so `.unwrap()` is permitted. Change:
    `store.conn.execute(...)` → `store.conn.lock().unwrap().execute(...)`
  Grep for `\.conn\.` inside `#[cfg(test)]` blocks and update each occurrence.

  Step 8 — Add the 4 submodule declarations at the end of the `GooseStore` struct section (before the first `impl GooseStore` block) in store/mod.rs:
  ```
  mod sleep;
  mod capture;
  mod metrics;
  mod activity;
  ```

  Step 9 — Delete the original store.rs: run `git rm Rust/core/src/store.rs`
  Do NOT use `rm` — use `git rm` so the deletion is staged.
  </action>

  <verify>
    <automated>cd Rust/core && cargo build --lib 2>&1 | tail -20</automated>
  </verify>

  <done>
  `cargo build --lib` exits 0 with no errors. store/mod.rs exists. store.rs is gone (git rm'd). The GooseStore struct has `pub(super) conn: Arc<Mutex<Connection>>`. immediate_transaction signature uses `FnOnce(&Connection)`. All 5 in-file call sites use `|conn|` closures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 4 empty domain stub files + update 4 external immediate_transaction call sites</name>
  <files>
    Rust/core/src/store/sleep.rs,
    Rust/core/src/store/capture.rs,
    Rust/core/src/store/metrics.rs,
    Rust/core/src/store/activity.rs,
    Rust/core/src/bridge/activity.rs,
    Rust/core/src/bridge/sleep.rs,
    Rust/core/src/capture_import.rs
  </files>

  <read_first>
    - Rust/core/src/bridge/activity.rs — lines 690–820 (two immediate_transaction call sites at lines ~700 and ~804)
    - Rust/core/src/bridge/sleep.rs — lines 460–510 (immediate_transaction call site at line ~475)
    - Rust/core/src/capture_import.rs — lines 235–280 (immediate_transaction call site at line ~248)
  </read_first>

  <action>
  Step 1 — Create 4 empty domain stub files. Each file must:
  - Import `use super::GooseStore;` so the impl block compiles
  - Contain an empty `impl GooseStore {}` block as a placeholder
  - Include the standard imports needed by domain methods (add these now so Wave 2 executors can fill methods without adding imports): `use crate::{GooseError, GooseResult};`, `use rusqlite::params;`

  Content pattern for each stub:
  ```
  use crate::{GooseError, GooseResult};
  use rusqlite::params;
  use super::GooseStore;

  impl GooseStore {}
  ```

  Create: store/sleep.rs, store/capture.rs, store/metrics.rs, store/activity.rs

  Note: `GooseError` and `GooseResult` may be imported differently depending on crate structure — read the existing imports in store.rs/store/mod.rs to match the exact import path used there.

  Step 2 — Update 4 external immediate_transaction call sites.

  bridge/activity.rs line ~700:
  BEFORE: `store.immediate_transaction(|store| store.insert_activity_metrics(&inputs))?;`
  The closure called `store.insert_activity_metrics` which would deadlock (acquires conn lock). Since `insert_activity_metrics` will live in store/activity.rs after Wave 2, the closure body must be inlined here with the `&Connection` pattern.
  Read the `insert_activity_metrics` implementation (lines ~2977 in current store.rs/mod.rs) to understand the SQL it runs, then inline it:
  AFTER: `store.immediate_transaction(|conn| { /* inlined SQL from insert_activity_metrics */ })?;`

  bridge/activity.rs line ~804:
  BEFORE: `store.immediate_transaction(|store| store.insert_activity_metrics(inputs))`
  Same inlining required.

  bridge/sleep.rs line ~475:
  BEFORE: `.immediate_transaction(|store| { store.conn... })`
  AFTER: `.immediate_transaction(|conn| { conn... })` — rename `store` to `conn`, remove `.conn` dereference.

  capture_import.rs line ~248:
  BEFORE: `store.immediate_transaction(|store| { store.conn.execute(...) })`
  AFTER: `store.immediate_transaction(|conn| { conn.execute(...) })` — rename parameter, remove `.conn`.

  For bridge/activity.rs where the closure calls a public store method: read the actual SQL in `insert_activity_metrics` (store/mod.rs lines ~2977–3065) and inline the relevant INSERT statements directly in the `|conn|` closure, using `conn.execute(...)` with the same params. Do not call `store.insert_activity_metric` or `store.insert_activity_metrics` inside the closure — that would re-acquire the mutex and deadlock.
  </action>

  <verify>
    <automated>cd Rust/core && cargo build --lib 2>&1 | grep -E '^error' | head -20</automated>
  </verify>

  <done>
  `cargo build --lib` produces zero `error` lines. Four stub files exist. All 4 external immediate_transaction call sites use `|conn|` closures that do not call store methods from inside the transaction closure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust module boundary | store/ submodules are pub(super) scoped — conn is not accessible from bridge/ or external crates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Tampering | GooseStore.conn visibility | mitigate | pub(super) limits field access to store/ family only; bridge/ cannot access conn directly |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — pure refactoring, std::sync only |
</threat_model>

<verification>
After both tasks:

```bash
cd Rust/core && cargo build --lib
```

Must exit 0. Then confirm:

```bash
ls Rust/core/src/store/
# Expected: activity.rs  capture.rs  metrics.rs  mod.rs  sleep.rs

ls Rust/core/src/store.rs 2>/dev/null && echo "ERROR: store.rs still exists" || echo "OK: store.rs deleted"

grep 'pub(super) conn' Rust/core/src/store/mod.rs
# Expected: one match

grep 'FnOnce(&Connection)' Rust/core/src/store/mod.rs
# Expected: one match (immediate_transaction signature)
```
</verification>

<success_criteria>
- store/mod.rs contains the full store.rs content with Arc<Mutex<Connection>> substitution
- immediate_transaction signature is `FnOnce(&Connection)` — verified by grep
- store.rs is absent from the filesystem (git rm'd)
- 4 empty stub files exist and compile
- All 9 immediate_transaction call sites use `|conn|` closures with no store method calls inside
- `cargo build --lib` exits 0
</success_criteria>

<output>
Create `.planning/phases/87-store-rs-split/87-01-SUMMARY.md` when done.
</output>
