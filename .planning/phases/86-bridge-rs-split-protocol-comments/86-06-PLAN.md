---
phase: 86-bridge-rs-split-protocol-comments
plan: 06
type: execute
wave: 5
depends_on:
  - 86-05
files_modified: []
autonomous: false
requirements:
  - ARCH-01
  - COMM-01

must_haves:
  truths:
    - "cargo test --locked passes on the full suite (lib + integration tests)"
    - "cargo clippy --lib --no-deps -- -D warnings exits 0"
    - "wc -l Rust/core/src/bridge/mod.rs shows mod.rs is a lean router (target ≤ 500 lines)"
    - "All 6 bridge domain files exist and none are stubs"
    - "bridge.rs no longer exists"
    - "bridge_methods_constant_matches_dispatcher passes"
    - "At least 6 'empirically verified' comments exist in protocol.rs"
    - "ARCH-01 and COMM-01 requirements are verifiably complete"
  artifacts:
    - path: "Rust/core/src/bridge/mod.rs"
      provides: "Lean router"
    - path: "Rust/core/src/bridge/metrics.rs"
      provides: "Full domain dispatch"
    - path: "Rust/core/src/bridge/sleep.rs"
      provides: "Full domain dispatch"
    - path: "Rust/core/src/bridge/capture.rs"
      provides: "Full domain dispatch"
    - path: "Rust/core/src/bridge/activity.rs"
      provides: "Full domain dispatch"
    - path: "Rust/core/src/bridge/debug.rs"
      provides: "Full domain dispatch"
    - path: "Rust/core/src/protocol.rs"
      provides: "Wire-format offset comments at 11 sites"
  key_links:
    - from: "CI"
      to: "Rust/core"
      via: "cargo test --locked on ubuntu-latest + macos-15"
      pattern: "cargo test"
---

<objective>
Phase gate: verify the complete Phase 86 work meets all success criteria before handing off to /gsd-verify-work. Run cargo test --locked (full suite including integration tests) and cargo clippy --lib, then pause for human confirmation of the module structure.

Purpose: Confirm ARCH-01 (bridge split complete, ≤ 100 line router) and COMM-01 (offset comments present) are both satisfied. This plan produces no file changes — it is purely verification.

Output: Confirmed green CI gate; human checkpoint sign-off.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-CONTEXT.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full cargo test --locked and cargo clippy gate</name>
  <files></files>
  <read_first>
    - .planning/phases/86-bridge-rs-split-protocol-comments/86-05-SUMMARY.md — confirm all prior plans completed successfully before running gate
  </read_first>
  <action>
    Run the full verification suite. Fix any failures before marking this task done.

    Step 1: Structural checks:
    - Confirm bridge.rs does NOT exist: `ls Rust/core/src/bridge.rs 2>&1` should return error
    - List bridge/ directory: `ls Rust/core/src/bridge/` should show mod.rs + 5 domain files
    - Line count: `wc -l Rust/core/src/bridge/mod.rs`
    - COMM-01 spot-check: `grep -c "empirically verified" Rust/core/src/protocol.rs` should be ≥ 6
    - COMM-01 spot-check: `grep -c "empirically verified" Rust/core/src/bridge/metrics.rs` should be ≥ 2

    Step 2: Lint gate:
    `cd Rust/core && cargo clippy --lib --no-deps -- -D warnings`
    If failures: fix. Common issues: unused imports after domain file moves, dead_code warnings on moved helpers.

    Step 3: Unit test gate:
    `cd Rust/core && cargo test --lib`
    Critical tests that must pass:
    - bridge_methods_constant_matches_dispatcher
    - bridge_methods_constant_is_sorted_and_unique
    - battery_parse_tests (all battery unit tests)

    Step 4: Full integration test gate:
    `cd Rust/core && cargo test --locked 2>&1 | tail -30`
    This runs all integration tests in Rust/core/tests/ including bridge_tests.rs, exercise_detection_tests.rs, v24_biometric_bridge_tests.rs.
    Pre-existing failure exemption: export_tests sensor_sample_rows (18 vs 19) is a pre-existing failure from Phase 84 (confirmed in STATE.md deferred items) — do NOT attempt to fix it here.

    Step 5: Final counts for SUMMARY:
    - `grep -c "pub(crate) fn dispatch_" Rust/core/src/bridge/mod.rs` (should be 0 — all dispatch fns in domain files)
    - Count arms per domain file: `grep -c '"[a-z_]*\.' Rust/core/src/bridge/metrics.rs`
    - Total line count across all 6 bridge files: `wc -l Rust/core/src/bridge/*.rs`
  </action>
  <verify>
    <automated>ls /Users/francisco/Documents/goose/Rust/core/src/bridge.rs 2>&1 | grep -c "No such file"</automated>
    <automated>ls /Users/francisco/Documents/goose/Rust/core/src/bridge/</automated>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | grep -E "^(test result|FAILED|error\[)" | head -20</automated>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo clippy --lib --no-deps -- -D warnings 2>&1 | grep "^error" | head -10</automated>
    <automated>grep -c "empirically verified" /Users/francisco/Documents/goose/Rust/core/src/protocol.rs</automated>
  </verify>
  <acceptance_criteria>
    - `ls Rust/core/src/bridge.rs` returns "No such file" (bridge.rs deleted)
    - `ls Rust/core/src/bridge/` lists exactly: mod.rs, metrics.rs, sleep.rs, capture.rs, activity.rs, debug.rs
    - `cargo clippy --lib --no-deps -- -D warnings` exits 0
    - `cargo test --lib` exits 0 (all unit and inline tests pass)
    - `cargo test --locked` exits 0 OR only fails on the pre-existing export_tests::sensor_sample_rows failure (not new failures)
    - `grep -c "empirically verified" Rust/core/src/protocol.rs` returns ≥ 6
    - `grep -c "empirically verified" Rust/core/src/bridge/metrics.rs` returns ≥ 2
    - `wc -l Rust/core/src/bridge/mod.rs` shows a file that is demonstrably leaner than the original 11,186-line bridge.rs
  </acceptance_criteria>
  <done>Full cargo test --locked and clippy pass. All structural criteria for ARCH-01 and COMM-01 confirmed. Pre-existing export_tests failure (if present) documented as pre-existing in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 86 complete: bridge.rs (11,186 lines) has been split into bridge/mod.rs (lean router) + 5 domain files (metrics.rs, sleep.rs, capture.rs, activity.rs, debug.rs). Protocol offset comments added at 14 wire-format decode sites. cargo test --locked passes.
  </what-built>
  <how-to-verify>
    Run these checks to confirm the split is clean:

    1. `ls Rust/core/src/bridge/` — should show 6 files, no bridge.rs
    2. `wc -l Rust/core/src/bridge/*.rs` — see line distribution across domain files
    3. `grep -c "empirically verified" Rust/core/src/protocol.rs` — should be ≥ 6
    4. `grep "fn dispatch_" Rust/core/src/bridge/mod.rs` — should return 0 (all dispatch fns in domain files)
    5. `grep "fn dispatch_metrics" Rust/core/src/bridge/metrics.rs` — should return 1
    6. Sample a few bridge method calls work correctly (optional but recommended):
       `cd Rust/core && cargo test --lib bridge_methods_constant_matches_dispatcher -- --nocapture`

    Confirm:
    - The module structure looks correct (6 files in bridge/)
    - The protocol.rs offset comments are legible and accurate
    - No unexpected regressions reported from cargo test
  </how-to-verify>
  <resume-signal>Type "approved" if the split looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate | cargo test --locked is the final arbiter of correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-11 | Denial of Service | Pre-existing export_tests failure masking new failures | mitigate | Gate task explicitly checks that only the pre-existing sensor_sample_rows failure is present; any new failures block approval |
</threat_model>

<verification>
Phase 86 gate criteria:
1. `ls Rust/core/src/bridge.rs` → No such file
2. `ls Rust/core/src/bridge/` → 6 files
3. `cargo test --locked` → passes (pre-existing export_tests exempted if documented)
4. `cargo clippy --lib --no-deps -- -D warnings` → exits 0
5. `grep -c "empirically verified" Rust/core/src/protocol.rs` → ≥ 6
6. Human checkpoint approved
</verification>

<success_criteria>
All Phase 86 success criteria from ROADMAP are satisfied:
1. ARCH-01 SC1: bridge/mod.rs is a thin router (≤ 100-line routing function); 5 domain files exist
2. ARCH-01 SC2: D-02 equivalent dispatch mechanism (5 pub(crate) dispatch_* functions) handles all 152 bridge methods
3. COMM-01 SC3: Each WHOOP wire-format decode site carries offset comment with byte offsets, data type, verification date, source reference
4. ARCH-01 SC4: cargo test --locked passes with reorganised module structure
</success_criteria>

<output>
Create `.planning/phases/86-bridge-rs-split-protocol-comments/86-06-SUMMARY.md` when done.
</output>
