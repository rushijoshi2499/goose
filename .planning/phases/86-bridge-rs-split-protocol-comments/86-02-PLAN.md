---
phase: 86-bridge-rs-split-protocol-comments
plan: 02
type: execute
wave: 2
depends_on:
  - 86-01
files_modified:
  - Rust/core/src/bridge/metrics.rs
  - Rust/core/src/bridge/sleep.rs
  - Rust/core/src/bridge/capture.rs
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "dispatch_metrics handles all 51 arms from metrics.*, metric_series.*, exercise.*, biometrics.*, battery.*, calibration.*, openwhoop.*, diagnostics.* namespaces"
    - "dispatch_sleep handles all arms from sleep.*, overnight.*, health_sync.* namespaces"
    - "dispatch_capture handles all arms from capture.*, protocol.*, historical_sync.*, sync.* namespaces"
    - "cargo test --lib passes after each file is filled"
    - "No .unwrap() in production code in any of the 3 new domain files"
  artifacts:
    - path: "Rust/core/src/bridge/metrics.rs"
      provides: "Full dispatch_metrics implementation — all metrics namespace arms"
      min_lines: 800
    - path: "Rust/core/src/bridge/sleep.rs"
      provides: "Full dispatch_sleep implementation — sleep/overnight/health_sync arms"
      min_lines: 200
    - path: "Rust/core/src/bridge/capture.rs"
      provides: "Full dispatch_capture implementation — capture/protocol/historical_sync/sync arms"
      min_lines: 200
  key_links:
    - from: "Rust/core/src/bridge/mod.rs"
      to: "Rust/core/src/bridge/metrics.rs"
      via: "metrics::dispatch_metrics(&request) in handle_bridge_request_inner router"
      pattern: "dispatch_metrics"
    - from: "Rust/core/src/bridge/metrics.rs"
      to: "Rust/core/src/metrics.rs"
      via: "use crate::metrics::{...} for HRV, exercise, and biometric functions"
      pattern: "use crate::metrics"
---

<objective>
Fill metrics.rs, sleep.rs, and capture.rs with their real domain dispatch functions, moving all relevant match arms and companion *_bridge functions from bridge.rs history (now in mod.rs or in the deleted bridge.rs — use SUMMARY 86-01 to confirm where they ended up). After this plan, these three domains are fully operational.

Purpose: Parallelisable with Plan 86-03 (activity + debug) since files_modified have zero overlap.

Output:
- bridge/metrics.rs: ~800+ lines, 51 dispatch arms, all *Args structs and *_bridge helpers co-located
- bridge/sleep.rs: sleep/overnight/health_sync arms and helpers
- bridge/capture.rs: capture/protocol/historical_sync/sync arms and helpers (no ios.* arms — namespace does not exist as bridge methods per RESEARCH)
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-CONTEXT.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-RESEARCH.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill bridge/metrics.rs with all metrics-domain arms and helpers</name>
  <files>
    Rust/core/src/bridge/metrics.rs
  </files>
  <read_first>
    - Rust/core/src/bridge/mod.rs — locate the handle_bridge_request_inner router to confirm which method strings route to dispatch_metrics; also read the shared utilities section to understand what is available via super::
    - Rust/core/src/bridge.rs (git show HEAD~1:Rust/core/src/bridge.rs OR use git to view the deleted file) — alternatively: the original bridge.rs was deleted in Plan 86-01; use `git show HEAD:Rust/core/src/bridge.rs | grep -n "metrics\.\|metric_series\.\|exercise\.\|biometrics\.\|battery\.\|calibration\.\|openwhoop\.\|diagnostics\." | head -60` to find the line ranges, then `git show HEAD:Rust/core/src/bridge.rs | sed -n 'START,ENDp'` to extract function bodies
    - Rust/core/src/metrics.rs — types and functions used by metrics bridge arms
    - Rust/core/src/calibration.rs — types used by calibration.* arms
    - Rust/core/src/capabilities.rs — DeviceCapabilities used by openwhoop/diagnostics arms
  </read_first>
  <action>
    The goal is to move ALL match arms and their companion *_bridge functions for namespaces: metrics.*, metric_series.*, exercise.*, biometrics.*, battery.*, calibration.*, openwhoop.*, diagnostics.* from their current location (mod.rs still holds them from Plan 86-01) into bridge/metrics.rs.

    Step 1: Identify where metrics-domain code currently lives. Check bridge/mod.rs — if Plan 86-01 kept all bridge function bodies in mod.rs (likely), extract metrics domain functions from there. If mod.rs is already lean, the functions were not moved yet; extract them from git history: `git show HEAD~1:Rust/core/src/bridge.rs`.

    Step 2: Fill bridge/metrics.rs with:

    a) `use` imports. Each import must be derived from the functions being moved. Typical imports:
       - `use super::{BridgeRequest, BridgeResponse, bridge_ok, bridge_error, request_args, open_bridge_store, metric_result_to_value};`
       - `use crate::{GooseResult, GooseError};`
       - `use crate::metrics::{...all metric types and functions referenced...};`
       - `use crate::calibration::{...};`
       - `use crate::capabilities::{...};`
       - `use serde::{Deserialize, Serialize};` if needed for *Args structs
       - `use serde_json::Value;` if needed

    b) All `#[derive(Deserialize)]` Args structs for the metrics domain — keep co-located with their dispatch arms (do NOT put them in mod.rs).

    c) The `pub(crate) fn dispatch_metrics(request: &BridgeRequest) -> BridgeResponse` function with ALL match arms for the 8 namespaces. The catch-all arm must be:
       `_ => unreachable!("dispatch_metrics called with non-metrics method: {}", request.method)`
       (unreachable! is allowed — it is a programmer error, not a runtime error, and deny(clippy::unwrap_used) does not fire on unreachable!).

    d) All private `*_bridge` helper functions and any metric-specific utility functions (e.g., parse_event48_battery, parse_cmd26_battery belong here since they are battery.* domain).

    Step 3: Remove the metrics-domain code from bridge/mod.rs — replace with the stub that now calls metrics::dispatch_metrics (which was already set up in Plan 86-01's router).

    Step 4: Run `cargo test --lib` and fix any `cannot find` errors by adding the missing `use` imports.

    Clippy rule: no new .unwrap() in production code. Use ? or .map_err(|e| GooseError::new(...)).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --lib 2>&1 | grep -E "^(test |FAILED|ok|error)" | head -30</automated>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo clippy --lib --no-deps -- -D warnings 2>&1 | grep "^error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib` exits 0 (or has only pre-existing failures unrelated to metrics)
    - `grep -c "\"metrics\." Rust/core/src/bridge/metrics.rs` shows count > 10 (real arms present)
    - `grep "\.unwrap()" Rust/core/src/bridge/metrics.rs | grep -v "#\[cfg(test\)\]" | grep -v "^[[:space:]]*//"` returns 0 matches
    - `cargo clippy --lib --no-deps -- -D warnings` exits 0
  </acceptance_criteria>
  <done>bridge/metrics.rs contains the full dispatch_metrics implementation. All metrics-domain arms and their helper functions are co-located in this file. cargo test --lib passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fill bridge/sleep.rs and bridge/capture.rs</name>
  <files>
    Rust/core/src/bridge/sleep.rs
    Rust/core/src/bridge/capture.rs
  </files>
  <read_first>
    - Rust/core/src/bridge/mod.rs — confirm current state after Task 1; see what sleep/capture domain code remains
    - Rust/core/src/health_sync.rs — types used by health_sync.* arms
    - Rust/core/src/historical_sync.rs — types for historical_sync.* arms
    - Rust/core/src/capture_import.rs — types for capture.* arms
  </read_first>
  <action>
    Apply the same pattern as Task 1 to the two remaining domains in this plan.

    For bridge/sleep.rs:
    - Namespaces: sleep.*, overnight.*, health_sync.*
    - Imports: use super::{BridgeRequest, BridgeResponse, bridge_ok, bridge_error, request_args, open_bridge_store}; plus crate modules for sleep/overnight/health_sync functions
    - Dispatch function: `pub(crate) fn dispatch_sleep(request: &BridgeRequest) -> BridgeResponse`
    - Catch-all: `_ => unreachable!("dispatch_sleep called with non-sleep method: {}", request.method)`
    - Move all sleep-domain *Args structs and *_bridge helpers co-located

    For bridge/capture.rs:
    - Namespaces: capture.*, protocol.*, historical_sync.*, sync.*
    - NOTE: ios.* is NOT a bridge namespace — it is a string constant value inside default_raw_notification_source() which stays in mod.rs; do NOT add ios.* arms to dispatch_capture
    - Imports: use super::{...}; plus crate modules for capture/protocol/historical_sync/sync
    - Dispatch function: `pub(crate) fn dispatch_capture(request: &BridgeRequest) -> BridgeResponse`
    - Catch-all: `_ => unreachable!("dispatch_capture called with non-capture method: {}", request.method)`
    - Move all capture-domain *Args structs and *_bridge helpers co-located

    For both files: after moving, remove the domain code from bridge/mod.rs. Run `cargo test --lib` after each file to confirm no regressions.

    Clippy: no .unwrap() in production code in either file.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --lib 2>&1 | tail -5</automated>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo clippy --lib --no-deps -- -D warnings 2>&1 | grep "^error" | head -10</automated>
    <automated>grep -c "pub(crate) fn dispatch_sleep" /Users/francisco/Documents/goose/Rust/core/src/bridge/sleep.rs</automated>
    <automated>grep -c "pub(crate) fn dispatch_capture" /Users/francisco/Documents/goose/Rust/core/src/bridge/capture.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub(crate) fn dispatch_sleep" Rust/core/src/bridge/sleep.rs` returns 1
    - `grep -c "pub(crate) fn dispatch_capture" Rust/core/src/bridge/capture.rs` returns 1
    - `grep "\.unwrap()" Rust/core/src/bridge/sleep.rs | grep -v "#\[cfg(test\)\]" | grep -v "//"` returns 0
    - `grep "\.unwrap()" Rust/core/src/bridge/capture.rs | grep -v "#\[cfg(test\)\]" | grep -v "//"` returns 0
    - `cargo test --lib` exits 0 (metrics, sleep, capture domains fully operational)
    - `cargo clippy --lib --no-deps -- -D warnings` exits 0
  </acceptance_criteria>
  <done>bridge/sleep.rs and bridge/capture.rs contain full dispatch implementations for their domains. cargo test --lib passes. Three of five domains are now operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mod.rs router → domain files | Each dispatch_<domain> receives the full &BridgeRequest; unreachable! catch-all protects against router routing to wrong domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03 | Tampering | use import omissions in domain files | mitigate | cargo build --lib catches every missing import at compile time; CI enforces |
| T-86-04 | Denial of Service | ios.* namespace incorrectly added as dispatch arm | accept | ios.* does not exist as bridge methods; only as a string constant in default_raw_notification_source; no dispatch arm needed |
</threat_model>

<verification>
After Wave 2 Plan 02:
- `grep -c "pub(crate) fn dispatch_metrics" Rust/core/src/bridge/metrics.rs` returns 1
- `grep -c "pub(crate) fn dispatch_sleep" Rust/core/src/bridge/sleep.rs` returns 1
- `grep -c "pub(crate) fn dispatch_capture" Rust/core/src/bridge/capture.rs` returns 1
- `cd Rust/core && cargo test --lib` exits 0
- `cargo clippy --lib --no-deps -- -D warnings` exits 0
</verification>

<success_criteria>
metrics.rs, sleep.rs, and capture.rs contain their full domain dispatch implementations. Three of five bridge domains are operational. cargo test --lib and clippy pass.
</success_criteria>

<output>
Create `.planning/phases/86-bridge-rs-split-protocol-comments/86-02-SUMMARY.md` when done.
</output>
