---
phase: 86-bridge-rs-split-protocol-comments
plan: 03
type: execute
wave: 2
depends_on:
  - 86-01
files_modified:
  - Rust/core/src/bridge/activity.rs
  - Rust/core/src/bridge/debug.rs
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "dispatch_activity handles all arms from activity.*, workout.*, apple_daily.*, journal.*, timeline.* namespaces"
    - "dispatch_debug handles all arms from debug.*, commands.*, core.*, settings.*, storage.*, store.*, export.*, upload.*, privacy.*, ui_coverage.*, device.* namespaces"
    - "validation.* and local_health.* alias arms are in dispatch_debug (not dispatch_capture)"
    - "cargo test --lib passes after both files are filled"
    - "No .unwrap() in production code in either domain file"
  artifacts:
    - path: "Rust/core/src/bridge/activity.rs"
      provides: "Full dispatch_activity implementation — activity/workout/apple_daily/journal/timeline arms"
      min_lines: 300
    - path: "Rust/core/src/bridge/debug.rs"
      provides: "Full dispatch_debug implementation — debug + 10 other namespaces + validation/local_health aliases"
      min_lines: 400
  key_links:
    - from: "Rust/core/src/bridge/mod.rs"
      to: "Rust/core/src/bridge/activity.rs"
      via: "activity::dispatch_activity(&request) in handle_bridge_request_inner"
      pattern: "dispatch_activity"
    - from: "Rust/core/src/bridge/mod.rs"
      to: "Rust/core/src/bridge/debug.rs"
      via: "debug::dispatch_debug(&request) in handle_bridge_request_inner"
      pattern: "dispatch_debug"
---

<objective>
Fill activity.rs and debug.rs with their full domain dispatch implementations. This plan runs in parallel with Plan 86-02 (zero file overlap). After both Wave 2 plans complete, all 5 domain files are operational and bridge/mod.rs is the lean router it is designed to be.

Purpose: Parallelisable with Plan 86-02. Together they complete the full domain migration.

Output:
- bridge/activity.rs: activity/workout/apple_daily/journal/timeline arms + helpers
- bridge/debug.rs: 11 namespaces + validation.*/local_health.* legacy alias arms + helpers
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-CONTEXT.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-RESEARCH.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill bridge/activity.rs with activity-domain arms and helpers</name>
  <files>
    Rust/core/src/bridge/activity.rs
  </files>
  <read_first>
    - Rust/core/src/bridge/mod.rs — confirm current state; locate activity-domain code that remains in mod.rs after Plan 86-01
    - Rust/core/src/activity_sessions.rs — types for activity.* arms
    - Rust/core/src/activity_candidates.rs — types for activity detection arms
    - Rust/core/src/activity_identity.rs — types if referenced
    - Rust/core/src/energy_rollup.rs — used by apple_daily.* arms if present
  </read_first>
  <action>
    Move all activity-domain code to bridge/activity.rs.

    Namespaces handled by dispatch_activity: activity.*, workout.*, apple_daily.*, journal.*, timeline.*

    Step 1: Identify the activity-domain match arms and their companion *_bridge functions. They are currently in bridge/mod.rs (left there from Plan 86-01). Use grep to find them: `grep -n '"activity\.\|"workout\.\|"apple_daily\.\|"journal\.\|"timeline\.' Rust/core/src/bridge/mod.rs`.

    Step 2: Write bridge/activity.rs:

    a) `use` imports derived from all functions and types referenced:
       - `use super::{BridgeRequest, BridgeResponse, bridge_ok, bridge_error, request_args, open_bridge_store};`
       - `use crate::{GooseResult, GooseError};`
       - `use crate::activity_sessions::{...};`
       - `use crate::activity_candidates::{...};` (if used)
       - `use crate::energy_rollup::{...};` (if apple_daily.* uses it)
       - `use serde::{Deserialize, Serialize};`
       - Any other crate items referenced by the arms

    b) All `#[derive(Deserialize)]` *Args structs co-located with dispatch arms.

    c) `pub(crate) fn dispatch_activity(request: &BridgeRequest) -> BridgeResponse` with all activity-domain match arms.

    d) Catch-all: `_ => unreachable!("dispatch_activity called with non-activity method: {}", request.method)`

    e) All private *_bridge helper functions for this domain.

    Step 3: After moving, remove the activity-domain code from bridge/mod.rs.

    Step 4: `cargo test --lib` — fix any missing imports. No .unwrap() in production code.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --lib 2>&1 | tail -5</automated>
    <automated>grep -c "pub(crate) fn dispatch_activity" /Users/francisco/Documents/goose/Rust/core/src/bridge/activity.rs</automated>
    <automated>grep "\.unwrap()" /Users/francisco/Documents/goose/Rust/core/src/bridge/activity.rs | grep -v "#\[cfg(test\)\]" | grep -v "//" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub(crate) fn dispatch_activity" Rust/core/src/bridge/activity.rs` returns 1
    - `grep "\.unwrap()" Rust/core/src/bridge/activity.rs | grep -v "#\[cfg(test\)\]" | grep -v "//"` returns 0 lines
    - `cargo test --lib` exits 0
    - `cargo clippy --lib --no-deps -- -D warnings` exits 0
  </acceptance_criteria>
  <done>bridge/activity.rs contains the full dispatch_activity implementation. All activity-domain namespaces are operational.</done>
</task>

<task type="auto">
  <name>Task 2: Fill bridge/debug.rs including validation.* alias arms</name>
  <files>
    Rust/core/src/bridge/debug.rs
  </files>
  <read_first>
    - Rust/core/src/bridge/mod.rs — locate debug-domain code (the largest group: 11+ namespaces); also look for validation.* and local_health.* alias arms which are NOT in BRIDGE_METHODS but must be in dispatch_debug
    - Rust/core/src/local_health_validation.rs — functions called by local_health.* / validation.* arms
    - Rust/core/src/export.rs — functions for export.* arms
    - Rust/core/src/debug_ws.rs — functions for debug.* arms if any
  </read_first>
  <action>
    Move all debug-domain code to bridge/debug.rs. This is the largest domain with 11 namespaces plus legacy alias arms.

    Namespaces handled by dispatch_debug: debug.*, commands.*, core.*, settings.*, storage.*, store.*, export.*, upload.*, privacy.*, ui_coverage.*, device.*

    Additionally: validation.* and local_health.* legacy alias arms (NOT in BRIDGE_METHODS, but present as real match arms in the dispatcher — per RESEARCH Pitfall 4 and 5). These must go in dispatch_debug.

    Step 1: Find all debug-domain arms: `grep -n '"debug\.\|"commands\.\|"core\.\|"settings\.\|"storage\.\|"store\.\|"export\.\|"upload\.\|"privacy\.\|"ui_coverage\.\|"device\.\|"validation\.\|"local_health\.' Rust/core/src/bridge/mod.rs`.

    Step 2: Write bridge/debug.rs:

    a) `use` imports derived from all functions referenced:
       - `use super::{BridgeRequest, BridgeResponse, bridge_ok, bridge_error, request_args, open_bridge_store, register_built_in_definitions, BRIDGE_METHODS};`
       - Note: BRIDGE_METHODS is needed for the `core.list_methods` arm which reads it
       - `use crate::{GooseResult, GooseError};`
       - `use crate::local_health_validation::{...};`
       - `use crate::export::{...};`
       - Any other crate items

    b) All *Args structs co-located.

    c) `pub(crate) fn dispatch_debug(request: &BridgeRequest) -> BridgeResponse` with ALL match arms:
       - The 11 namespaces listed in D-01
       - The validation.* | local_health.* alias arms verbatim from the original dispatcher

       IMPORTANT for alias arms: The `bridge_methods_constant_matches_dispatcher` test scanner extracts only the first quoted string on each arm line. The multi-name arms `"validation.local_health_manifest_scaffold" | "local_health.validation_manifest_scaffold" => {...}` will only scan "validation.local_health_manifest_scaffold" (the first). This pre-existing behaviour is acceptable — do NOT refactor these arm patterns; keep them verbatim.

    d) Catch-all: `_ => unreachable!("dispatch_debug called with non-debug method: {}", request.method)`

    e) All private *_bridge helper functions for this domain (largest set — register_built_in_definitions may stay in mod.rs if it's a shared utility, but debug-specific helpers move here).

    Step 3: Remove the debug-domain code from bridge/mod.rs. After this step, mod.rs should be lean — check `wc -l Rust/core/src/bridge/mod.rs` and aim for ≤ 400 lines (shared utilities + router + FFI + BRIDGE_METHODS).

    Step 4: `cargo test --lib` and fix errors. No .unwrap() in production code.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --lib 2>&1 | tail -5</automated>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo clippy --lib --no-deps -- -D warnings 2>&1 | grep "^error" | head -10</automated>
    <automated>grep -c "pub(crate) fn dispatch_debug" /Users/francisco/Documents/goose/Rust/core/src/bridge/debug.rs</automated>
    <automated>grep -c "validation\." /Users/francisco/Documents/goose/Rust/core/src/bridge/debug.rs</automated>
    <automated>wc -l /Users/francisco/Documents/goose/Rust/core/src/bridge/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub(crate) fn dispatch_debug" Rust/core/src/bridge/debug.rs` returns 1
    - `grep -c "validation\." Rust/core/src/bridge/debug.rs` returns > 0 (alias arms present)
    - `grep "\.unwrap()" Rust/core/src/bridge/debug.rs | grep -v "#\[cfg(test\)\]" | grep -v "//"` returns 0 lines
    - `cargo test --lib` exits 0
    - `cargo clippy --lib --no-deps -- -D warnings` exits 0
    - `wc -l Rust/core/src/bridge/mod.rs` shows mod.rs is leaner than before (target: < 600 lines after all domain code removed)
  </acceptance_criteria>
  <done>bridge/debug.rs contains the full dispatch_debug implementation including validation.* alias arms. All 5 bridge domains are now operational. bridge/mod.rs is a lean router. cargo test --lib and clippy pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch_debug | Handles core.list_methods which reads BRIDGE_METHODS — must use super::BRIDGE_METHODS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-05 | Spoofing | validation.* alias arms silently dropped | mitigate | grep for alias arms in dispatch_debug; cargo test --lib includes bridge_methods_constant_matches_dispatcher which will catch arm count mismatches |
| T-86-06 | Information Disclosure | local_health_validation functions exposed via debug.* arms | accept | These functions pre-exist; no new exposure introduced by the structural refactor |
</threat_model>

<verification>
After Wave 2 Plan 03 completes (and Plan 02 also complete):
- All 5 dispatch_* functions exist in their respective domain files
- `cargo test --lib` exits 0
- `cargo clippy --lib --no-deps -- -D warnings` exits 0
- `wc -l Rust/core/src/bridge/mod.rs` target < 600 lines
</verification>

<success_criteria>
All 5 bridge domain files contain their full dispatch implementations. bridge/mod.rs is a lean router with shared utilities only. cargo test --lib and clippy pass.
</success_criteria>

<output>
Create `.planning/phases/86-bridge-rs-split-protocol-comments/86-03-SUMMARY.md` when done.
</output>
