---
phase: 86-bridge-rs-split-protocol-comments
plan: 05
type: execute
wave: 4
depends_on:
  - 86-04
files_modified:
  - Rust/core/src/protocol.rs
  - Rust/core/src/bridge/metrics.rs
autonomous: true
requirements:
  - COMM-01

must_haves:
  truths:
    - "parse_r22_payload has offset comments for payload[1] (battery_pct), payload[2..3] (hr_milli_bpm), payload[4..5] (extra)"
    - "reassemble_frame has frame header comments for Gen4 (bytes 1-2) and Gen5 (bytes 2-3) length fields, and Gen5 CRC trailer (bytes 6-7)"
    - "FrameReassembler::push has stream reassembly length field comments for Gen4 and Gen5"
    - "parse_v24_body_summary has a function-level block comment listing all offset reads at 53-75 (gravity2, SpO2, skin_temp, resp_raw)"
    - "parse_v18_body has comments for skin_temp_raw (offset 73) and gravity axes (offsets 45/49/53)"
    - "parse_data_packet_body_summary k10 section has IMU axis offset array comment"
    - "SpO2 formula, skin_temp_celsius_from_raw, resp_rate_bpm_zero_crossing in bridge/metrics.rs have algorithm comments"
    - "All comment format follows: offset N: type, field = formula; guard; empirically verified DATE via Ghidra + BTSnoop"
    - "cargo test --lib passes (no code changes, comments only)"
  artifacts:
    - path: "Rust/core/src/protocol.rs"
      provides: "Wire-format offset comments at 11 decode sites"
      contains: "empirically verified"
    - path: "Rust/core/src/bridge/metrics.rs"
      provides: "Algorithm comments at SpO2, skin_temp, resp_rate sites"
      contains: "empirically verified"
  key_links:
    - from: "protocol.rs parse_r22_payload"
      to: "R22 BLE characteristic (WHOOP 5.0 realtime handle 0x0022)"
      via: "payload[1]: battery_pct u8, payload[2..3]: hr_milli_bpm u16 LE"
      pattern: "parse_r22_payload"
---

<objective>
Add COMM-01 protocol offset comments at all 14 non-obvious WHOOP wire-format decode sites. This is documentation-only — no code changes, no behaviour changes. All 11 protocol.rs sites and 3 bridge/metrics.rs sites receive comments following the format established by the existing BAT-01 comment block.

Purpose: Per D-03 and SEED-005 principle — comment WHY not WHAT; wire offsets are exactly that. Future engineers (and RE tooling sessions) need to understand the byte layout without re-running Ghidra.

Output:
- protocol.rs: offset comments at parse_r22_payload (3 sites), reassemble_frame (4 sites), parse_v24_body_summary (block), parse_v18_body (2 sites), FrameReassembler::push (2 sites), parse_data_packet_body_summary k10 (1 site)
- bridge/metrics.rs: algorithm comments at SpO2 formula, skin_temp conversion, resp_rate zero-crossing (3 sites)
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-CONTEXT.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-RESEARCH.md
@.planning/phases/84-gen4-battery/84-01-SUMMARY.md
@.planning/phases/83-protocol-architecture-refactor-gen4-gen5-capability-model/83-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add offset comments to protocol.rs at 11 decode sites</name>
  <files>
    Rust/core/src/protocol.rs
  </files>
  <read_first>
    - Rust/core/src/protocol.rs — read lines 50–90 for FrameReassembler::push; lines 320–370 for reassemble_frame Gen4 and Gen5 paths; lines 680–740 for parse_r22_payload; lines 840–900 for parse_v24_body_summary; lines 920–960 for parse_v18_body; lines 740–810 for parse_data_packet_body_summary k10 section
    - Rust/core/src/bridge.rs (git history) or Rust/core/src/bridge/metrics.rs — read the existing BAT-01 comment block to copy the exact formatting style (the block comment starting with "/// Byte layout (absolute payload offsets, BAT-01):")
    - .planning/phases/84-gen4-battery/84-01-SUMMARY.md — Event-48 and cmd26 verified layout reference (use dates from here for "empirically verified DATE")
  </read_first>
  <action>
    Add inline or block comments at each of the 11 decode sites. Do NOT change any code — comments only.

    Comment format (from CONTEXT.md §Specific Ideas and RESEARCH §Pattern 3):
    ```
    // offset N: u16 LE, field_name = raw / scale; guard description
    //   context / verification reference
    //   empirically verified YYYY-MM-DD via Ghidra + BTSnoop
    ```
    For function-level blocks, use `///` doc comment style matching the BAT-01 pattern:
    ```
    /// Byte layout ([characteristic/event name]):
    ///   offset N: type, description
    ///   empirically verified YYYY-MM-DD via Ghidra + BTSnoop
    ```

    Sites to comment (from RESEARCH §Wire-Format Decode Sites Inventory):

    1. parse_r22_payload — payload[1]:
       Add above or inline: "offset 1: u8, battery_pct direct (0-100); no scaling; R22 is WHOOP 5.0 realtime BLE characteristic 0x0022; empirically verified via BTSnoop; field confirmed via openwhoop reference"

    2. parse_r22_payload — u16::from_le_bytes([payload[2], payload[3]]) → hr_milli_bpm:
       "offsets 2-3: u16 LE, hr_milli_bpm; hr_bpm = raw / 10.0; minimum payload guard: len ≥ 4 checked above; empirically verified via BTSnoop"

    3. parse_r22_payload — payload[4..=5] → extra:
       "offsets 4-5: [u8; 2], purpose unknown — empirical; conditional on len ≥ 6; content may carry sub-second HR data (unconfirmed via Ghidra 2026-06-14)"

    4. reassemble_frame Gen4 path — u16::from_le_bytes([frame[1], frame[2]]):
       Add block comment before/around the Gen4 branch:
       "Gen4 frame header: byte 0 = frame type, bytes 1-2 = payload length u16 LE; 3-byte header total"

    5. reassemble_frame Gen5 path — u16::from_le_bytes([frame[2], frame[3]]):
       "Gen5 frame header: byte 0 = frame type, byte 1 = flags, bytes 2-3 = payload length u16 LE; 4-byte header total (Gen5 adds flags byte)"

    6. reassemble_frame Gen5 CRC — u16::from_le_bytes([frame[6], frame[7]]):
       "Gen5 frame trailer: bytes -2..-1 from end = CRC16 Modbus of payload bytes; verified via BTSnoop checksum validation"

    7. FrameReassembler::push Gen4 length — u16::from_le_bytes([buffer[1], buffer[2]]):
       "stream reassembly: Gen4 payload length at buffer[1..2] u16 LE (header offset 1, same as reassemble_frame Gen4)"

    8. FrameReassembler::push Gen5 length — u16::from_le_bytes([buffer[2], buffer[3]]):
       "stream reassembly: Gen5 payload length at buffer[2..3] u16 LE (header offset 2, same as reassemble_frame Gen5)"

    9. parse_v24_body_summary — function-level block comment before the field reads:
       Add a block comment listing the V24 history payload layout for the gravity2, SpO2, skin_temp, resp_raw fields at offsets 53-75. Format as a compact table comment:
       "V24 history payload body layout (offsets relative to body start):
          offset 53: f32 LE, gravity_x (m/s²)
          offset 57: f32 LE, gravity_y
          offset 61: f32 LE, gravity_z
          offset 65: f32 LE, spo2_raw (ratio-of-ratios pre-calibration)
          offset 69: f32 LE, skin_temp_raw; degC = raw / 128.0; gate 5-45°C
          offset 73: u16 LE, resp_raw; zero-crossing algorithm applied
       empirically verified 2026-06-14 via Ghidra + BTSnoop"

    10. parse_v18_body — skin_temp_raw:
        "offset 73 (body-relative): u16 LE, skin_temp_raw; degC = raw / 128.0; body starts at payload[3]; gate 5-45°C empirically verified 2026-06-14"

    11. parse_data_packet_body_summary k10 — IMU axis offsets array:
        "K10 payload accelerometer/gyroscope layout: each axis is (name, byte_offset) pair;
         accel x/y/z and gyro x/y/z at consecutive f32 LE offsets; sampling rate 25 Hz assumed from protocol RE"

    After adding all comments, run `cargo test` to confirm no code was accidentally changed.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --lib 2>&1 | tail -5</automated>
    <automated>grep -c "empirically verified" /Users/francisco/Documents/goose/Rust/core/src/protocol.rs</automated>
    <automated>grep -c "offset" /Users/francisco/Documents/goose/Rust/core/src/protocol.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "empirically verified" Rust/core/src/protocol.rs` returns ≥ 6 (one per major decode site; some sites share a block comment with one verification line)
    - `grep -c "offset" Rust/core/src/protocol.rs` increases from the baseline count (new offset comments added)
    - `cargo test --lib` exits 0 (no code changes, tests unaffected)
    - `grep -n "parse_r22_payload" Rust/core/src/protocol.rs | head -5` — the function exists; reading its body shows the 3 offset comments present
    - `grep "offset 1:" Rust/core/src/protocol.rs` returns at least 1 match (R22 battery_pct comment)
    - `grep "empirically verified" Rust/core/src/protocol.rs | grep -v "#\[cfg(test\)\]"` returns ≥ 5 matches
  </acceptance_criteria>
  <done>11 decode sites in protocol.rs have offset comments following the established format. Comments explain byte offsets, data types, formulas, and verification sources. No code was changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add algorithm comments to bridge/metrics.rs at 3 formula sites</name>
  <files>
    Rust/core/src/bridge/metrics.rs
  </files>
  <read_first>
    - Rust/core/src/bridge/metrics.rs — find spo2_from_raw_uncalibrated (or the SpO2 ratio-of-ratios formula), skin_temp_celsius_from_raw, resp_rate_bpm_zero_crossing — read their current implementations to understand what comment is missing
  </read_first>
  <action>
    Add algorithm/formula comments at 3 sites in bridge/metrics.rs. Comments only — no code changes.

    Site 1 — SpO2 formula (insert_v24_biometric_batch_bridge helpers, spo2_from_raw_uncalibrated):
    Add a block comment above the formula:
    "// SpO2 from raw ratio-of-ratios (pre-calibration):
    //   R = AC_red / DC_red / (AC_ir / DC_ir)  — photoplethysmography standard
    //   SpO2 ≈ 110 - 25 * R (empirical linear approximation)
    //   gate: 70-100% (values outside this range indicate sensor error or motion artifact)
    //   formula source: openwhoop reference + Ghidra V24 disassembly 2026-06-14"

    Site 2 — skin_temp_celsius_from_raw:
    Add above the formula:
    "// Skin temperature conversion:
    //   degC = raw_u16 / 128.0 (LSBs per degree Celsius from NTC thermistor linearisation)
    //   gate: 5-45°C (below 5 = device off-wrist, above 45 = sensor error)
    //   LSB-per-degC coefficient empirically verified 2026-06-14 via V24 payload + Ghidra"

    Site 3 — resp_rate_bpm_zero_crossing:
    Add above the function or algorithm body:
    "// Respiratory rate from zero-crossing count on u16 window:
    //   counts sign changes in the signal buffer; rate_bpm = crossings / window_seconds * 60 / 2
    //   sampling rate: 25 Hz assumed from WHOOP protocol RE (unconfirmed for Gen5)
    //   algorithm: standard zero-crossing rate estimator; no patent claims"

    After adding comments: `cargo test --lib` to confirm no accidental changes.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --lib 2>&1 | tail -5</automated>
    <automated>grep -c "empirically verified" /Users/francisco/Documents/goose/Rust/core/src/bridge/metrics.rs</automated>
    <automated>grep -c "SpO2\|ratio-of-ratios\|spo2" /Users/francisco/Documents/goose/Rust/core/src/bridge/metrics.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "empirically verified" Rust/core/src/bridge/metrics.rs` returns ≥ 2 (SpO2 and skin_temp both have verification lines)
    - `grep -c "zero-crossing\|zero_crossing" Rust/core/src/bridge/metrics.rs` returns ≥ 1 (resp_rate comment present)
    - `cargo test --lib` exits 0
    - `cargo clippy --lib --no-deps -- -D warnings` exits 0 (comments don't affect clippy)
  </acceptance_criteria>
  <done>3 algorithm comments added to bridge/metrics.rs. All 14 COMM-01 decode sites (11 in protocol.rs + 3 in bridge/metrics.rs) now have offset/formula comments. cargo test --lib passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Comment accuracy | Protocol offset comments are based on empirical RE — incorrect offsets could mislead future implementers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-09 | Information Disclosure | Algorithm comments exposing proprietary WHOOP protocol details | accept | This is a personal fork for the device owner; no third-party disclosure; openwhoop reference is already public |
| T-86-10 | Tampering | Incorrect offset values in comments diverging from code | mitigate | Comments are added inline with the code they document; reviewer (user) can verify comment-code alignment during gate review |
</threat_model>

<verification>
After Wave 4:
- `grep -c "empirically verified" Rust/core/src/protocol.rs` ≥ 6
- `grep -c "empirically verified" Rust/core/src/bridge/metrics.rs` ≥ 2
- `cargo test --lib` exits 0
- All 14 COMM-01 sites covered
</verification>

<success_criteria>
All 14 non-obvious WHOOP wire-format decode sites carry offset/algorithm comments with byte offsets, data types, formulas, empirical verification dates, and source references. No code was changed. cargo test --lib passes.
</success_criteria>

<output>
Create `.planning/phases/86-bridge-rs-split-protocol-comments/86-05-SUMMARY.md` when done.
</output>
