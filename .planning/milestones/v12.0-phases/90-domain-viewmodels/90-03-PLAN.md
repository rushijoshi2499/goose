---
phase: 90-domain-viewmodels
plan: 03
type: execute
wave: 3
depends_on: [90-02]
files_modified:
  - GooseSwift/GooseAppModel+Upload.swift
  - GooseSwift/GooseAppModel+HealthCapture.swift
  - GooseSwift/GooseAppModel+Lifecycle.swift
autonomous: true
requirements: [ARCH-06]
must_haves:
  truths:
    - "GooseAppModel+Upload.swift writes all sync properties through syncState.xxx"
    - "GooseAppModel+HealthCapture.swift writes homeActivityTimelineItems and homeActivityTimelineStatus through healthState.xxx"
    - "GooseAppModel+Lifecycle.swift writes serverReachable through syncState.serverReachable"
    - "No direct self.xxx writes remain on GooseAppModel extension files for any of the 36 migrated properties"
  artifacts:
    - path: "GooseSwift/GooseAppModel+Upload.swift"
      provides: "Upload/sync extension using syncState composition"
      contains: "syncState."
    - path: "GooseSwift/GooseAppModel+HealthCapture.swift"
      provides: "Health capture extension using healthState composition"
      contains: "healthState."
    - path: "GooseSwift/GooseAppModel+Lifecycle.swift"
      provides: "Lifecycle extension using syncState for serverReachable"
      contains: "syncState.serverReachable"
  key_links:
    - from: "GooseAppModel+Upload.swift"
      to: "syncState"
      via: "direct property access on self.syncState"
      pattern: "syncState\\.syncPendingRowCount"
    - from: "GooseAppModel+HealthCapture.swift"
      to: "healthState"
      via: "direct property access on self.healthState"
      pattern: "healthState\\.homeActivityTimelineItems"
---

<objective>
Update the three GooseAppModel extension files that still write directly to migrated properties.
After Plan 02, GooseAppModel.swift no longer owns those var properties — extension files that
reference self.xxx for any migrated property will fail to compile. This plan threads all
remaining extension-level writes through the domain objects.

Files with remaining migrated property writes (confirmed by audit):
- GooseAppModel+Upload.swift: 17 sync property writes + 3 serverReachable writes
- GooseAppModel+HealthCapture.swift: 3 healthState property writes
- GooseAppModel+Lifecycle.swift: 3 serverReachable writes

Purpose: Make the codebase compile after Plans 01+02. Extensions are still on GooseAppModel;
they access domain objects as self.syncState.xxx and self.healthState.xxx.
Output: Three updated extension files, zero remaining direct self writes to migrated properties.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-domain-viewmodels/90-CONTEXT.md
@.planning/phases/90-domain-viewmodels/90-02-SUMMARY.md
@GooseSwift/GooseAppModel+Upload.swift
@GooseSwift/GooseAppModel+HealthCapture.swift
@GooseSwift/GooseAppModel+Lifecycle.swift
@GooseSwift/SyncState.swift
@GooseSwift/HealthState.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GooseAppModel+Upload.swift to write through syncState</name>
  <files>GooseSwift/GooseAppModel+Upload.swift</files>
  <action>
    Edit GooseSwift/GooseAppModel+Upload.swift. Replace every direct write and read of a
    migrated SyncState property with access through self.syncState (or syncState when inside
    a method body without ambiguity). The extension remains on GooseAppModel — no structural
    changes to the extension declaration or method signatures.

    Properties to redirect (all belong to SyncState):
      apnsDeviceToken, hasPendingUploadAfterReconnect, isNetworkReachable,
      lastUploadAt, pendingBatchCount, lastSyncedCount, syncPendingRowCount,
      uploadErrorState, serverImportInProgress, serverImportLastFrameCount,
      connectionTestRunning, connectionTestResult, serverReachable

    Transformation pattern for reads/writes in method bodies (no weak self capture needed
    since these are synchronous method bodies):
      apnsDeviceToken = token            → syncState.apnsDeviceToken = token
      guard apnsDeviceToken != nil       → guard syncState.apnsDeviceToken != nil
      if isNetworkReachable              → if syncState.isNetworkReachable
      hasPendingUploadAfterReconnect     → syncState.hasPendingUploadAfterReconnect

    Transformation pattern for Task/closure captures (already use self?. or weak self):
      self?.lastUploadAt = ...           → self?.syncState.lastUploadAt = ...
      self?.pendingBatchCount = ...      → self?.syncState.pendingBatchCount = ...
      self?.lastSyncedCount = ...        → self?.syncState.lastSyncedCount = ...
      self?.syncPendingRowCount = ...    → self?.syncState.syncPendingRowCount = ...
      self?.uploadErrorState = ...       → self?.syncState.uploadErrorState = ...
      self?.serverImportInProgress = ... → self?.syncState.serverImportInProgress = ...
      self?.serverImportLastFrameCount = ... → self?.syncState.serverImportLastFrameCount = ...
      self?.connectionTestRunning = ...  → self?.syncState.connectionTestRunning = ...
      self?.connectionTestResult = ...   → self?.syncState.connectionTestResult = ...
      self?.serverReachable = ...        → self?.syncState.serverReachable = ...

    Also handle await MainActor.run patterns:
      await MainActor.run { [weak self] in self?.serverImportInProgress = false }
      → await MainActor.run { [weak self] in self?.syncState.serverImportInProgress = false }

    The method applyNetworkReachability(_:) defined on SyncState is NOT called here —
    this extension sets serverReachable directly (three sites at lines ~345, ~364, ~386).

    Do NOT change method names, function signatures, or any non-property logic.
    Do NOT change access to properties that remain on GooseAppModel (rustStatus, helloSummary,
    work items, queues, pipelines, etc.).
  </action>
  <verify>
    <automated>grep -cE "\bself\??\.( )*(syncPendingRowCount|pendingBatchCount|lastSyncedCount|serverImportInProgress|serverImportLastFrameCount|lastUploadAt|uploadErrorState|hasPendingUploadAfterReconnect|connectionTestRunning|connectionTestResult|apnsDeviceToken|serverReachable)\b" /Users/francisco/Documents/goose/GooseSwift/GooseAppModel+Upload.swift</automated>
  </verify>
  <done>grep returns 0 — no bare self.xxx writes remain for any SyncState property in GooseAppModel+Upload.swift. All accesses go through syncState.</done>
</task>

<task type="auto">
  <name>Task 2: Update GooseAppModel+HealthCapture.swift and GooseAppModel+Lifecycle.swift</name>
  <files>GooseSwift/GooseAppModel+HealthCapture.swift, GooseSwift/GooseAppModel+Lifecycle.swift</files>
  <action>
    Part A — GooseAppModel+HealthCapture.swift:

    This file refreshes the activity timeline (confirmed audit: 3 writes at lines ~54-62):
      self.homeActivityTimelineItems = refresh.items
      self.homeActivityTimelineStatus = refresh.status
      self.homeActivityTimelineStatus = "Activity timeline failed"

    Replace each:
      self.homeActivityTimelineItems = refresh.items    → self.healthState.homeActivityTimelineItems = refresh.items
      self.homeActivityTimelineStatus = refresh.status  → self.healthState.homeActivityTimelineStatus = refresh.status
      self.homeActivityTimelineStatus = "Activity timeline failed" → self.healthState.homeActivityTimelineStatus = "Activity timeline failed"

    No other migrated properties exist in this file (verified by audit). Do not change any
    guard statement, generation check, or other logic.

    Part B — GooseAppModel+Lifecycle.swift:

    This file has exactly 3 serverReachable writes (confirmed audit at lines ~345, ~364, ~386):
      Task { @MainActor in self.serverReachable = nil }
      Task { @MainActor in self.serverReachable = false }
      Task { @MainActor in self.serverReachable = isReachable }   (or similar)

    Replace each:
      self.serverReachable = nil      → self.syncState.serverReachable = nil
      self.serverReachable = false    → self.syncState.serverReachable = false
      self.serverReachable = isReachable → self.syncState.serverReachable = isReachable

    No other migrated properties exist in Lifecycle (verified by audit: 0 additional hits).
    Do not change any other logic, method bodies, or extension structure.
  </action>
  <verify>
    <automated>grep -cE "\bself\??\.( )*(homeActivityTimelineItems|homeActivityTimelineStatus|serverReachable)\b" /Users/francisco/Documents/goose/GooseSwift/GooseAppModel+HealthCapture.swift /Users/francisco/Documents/goose/GooseSwift/GooseAppModel+Lifecycle.swift</automated>
  </verify>
  <done>grep returns 0 across both files — no bare self writes remain for the 3 HealthCapture health properties or the 3 Lifecycle serverReachable assignments. All go through healthState / syncState.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension methods → domain objects | Extension methods on GooseAppModel mutate domain objects they do not own; @MainActor enforcement is the safety boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-03a | Tampering | syncState writes in Task closures | accept | All Task { @MainActor in ... } closures run on @MainActor; Swift 6 strict concurrency enforces this at compile time |
| T-90-03b | Tampering | healthState writes in MainActor.run | accept | await MainActor.run ensures main-actor isolation; same as prior pattern |
| T-90-SC | Tampering | No new package installs | accept | Pure Swift edits; no package manager changes |
</threat_model>

<verification>
After both tasks complete:
- grep -cE "self\??\.syncPendingRowCount|self\??\.pendingBatchCount|self\??\.lastUploadAt|self\??\.serverReachable" GooseSwift/GooseAppModel+Upload.swift → 0
- grep -cE "self\??\.homeActivityTimelineItems|self\??\.homeActivityTimelineStatus" GooseSwift/GooseAppModel+HealthCapture.swift → 0
- grep -cE "self\??\.serverReachable" GooseSwift/GooseAppModel+Lifecycle.swift → 0
- grep -c "syncState\." GooseSwift/GooseAppModel+Upload.swift → ≥ 17
- grep -c "healthState\." GooseSwift/GooseAppModel+HealthCapture.swift → ≥ 3
Full build verified by Plan 04's build gate.
</verification>

<success_criteria>
All three extension files compile against the new GooseAppModel that no longer owns the migrated
var properties. Every property write in Upload, HealthCapture, and Lifecycle goes through the
appropriate domain object (syncState or healthState). Zero direct self.xxx writes remain for
any of the 36 migrated properties across all GooseAppModel extension files.
</success_criteria>

<output>
Create `.planning/phases/90-domain-viewmodels/90-03-SUMMARY.md` when done.
</output>
