---
phase: 90-domain-viewmodels
plan: 04
type: execute
wave: 4
depends_on: [90-03]
files_modified:
  - GooseSwift/GooseSwiftApp.swift
  - GooseSwift/MoreRemoteServerViews.swift
  - GooseSwift/DeviceView.swift
  - GooseSwift/CoachRouteViews.swift
  - GooseSwift/HomeDashboardView.swift
  - GooseSwift/FitnessLiveWorkoutViews.swift
  - GooseSwift/MoreDebugViews.swift
  - GooseSwift/CodexCoachSupport.swift
  - GooseSwift/LocalizedStatusStrings.swift
  - GooseSwift/RootView.swift
autonomous: true
requirements: [ARCH-06]
must_haves:
  truths:
    - "GooseSwiftApp injects bleState, syncState, and healthState via .environment() alongside model"
    - "MoreRemoteServerViews reads all sync properties from syncState (not model)"
    - "DeviceView reads connectedDeviceGeneration and respiratoryPacketWatch properties from bleState/healthState"
    - "CoachRouteViews reads alarmIsArmed and scheduledAlarmTime from bleState"
    - "HomeDashboardView reads homeActivityTimelineItems from healthState"
    - "FitnessLiveWorkoutViews reads liveWorkoutStrain from bleState"
    - "CodexCoachSupport reads all health/activity status strings from healthState"
    - "LocalizedStatusStrings reads health packet capture properties from healthState"
    - "RootView and MoreDebugViews update onboardingComplete through bleState"
    - "iOS build compiles with zero errors and zero new warnings"
  artifacts:
    - path: "GooseSwift/GooseSwiftApp.swift"
      provides: "Root environment injection for all three domain objects"
      contains: ".environment(model.bleState)"
    - path: "GooseSwift/MoreRemoteServerViews.swift"
      provides: "Sync status view consuming SyncState"
      contains: "syncState."
  key_links:
    - from: "GooseSwiftApp WindowGroup"
      to: "BLEState / SyncState / HealthState"
      via: ".environment(model.bleState) / .environment(model.syncState) / .environment(model.healthState)"
      pattern: "environment\\(model\\.bleState\\)"
    - from: "MoreRemoteServerViews"
      to: "SyncState"
      via: "@Environment(SyncState.self) private var syncState"
      pattern: "syncState\\.serverReachable"
---

<objective>
Complete the domain ViewModel refactor by injecting the three domain objects at the root scene
and updating every view file that currently reads migrated properties directly off model.xxx.

After Plans 01-03 the domain objects own all properties and the extension files write through
them. This plan is the final sweep: views must read through the domain objects and the project
must build clean.

Purpose: Achieve the ARCH-06 goal — SwiftUI's observation system only re-renders the subtree
that observes the changed domain object. A 1 Hz HR update (BLEState) must not trigger redraws
in MoreRemoteServerViews (SyncState-only).
Output: Updated GooseSwiftApp + 9 view files. Full xcodebuild succeeds with zero errors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-domain-viewmodels/90-CONTEXT.md
@.planning/phases/90-domain-viewmodels/90-03-SUMMARY.md
@GooseSwift/GooseSwiftApp.swift
@GooseSwift/BLEState.swift
@GooseSwift/SyncState.swift
@GooseSwift/HealthState.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject domain objects in GooseSwiftApp and update sync-only view MoreRemoteServerViews</name>
  <files>GooseSwift/GooseSwiftApp.swift, GooseSwift/MoreRemoteServerViews.swift</files>
  <action>
    Part A — GooseSwiftApp.swift:

    Locate the WindowGroup body where model is currently injected:
      .environment(model)
      .environment(model.healthStore)

    Add three new environment injections immediately after .environment(model):
      .environment(model.bleState)
      .environment(model.syncState)
      .environment(model.healthState)

    Order must be:
      .environment(model)
      .environment(model.bleState)
      .environment(model.syncState)
      .environment(model.healthState)
      .environment(model.healthStore)
      .environmentObject(model.packetMonitor)
      .environmentObject(model.ble.messageStore)
      .environmentObject(router)

    No other changes to GooseSwiftApp.swift.

    Part B — MoreRemoteServerViews.swift:

    This view reads: serverReachable, connectionTestRunning, connectionTestResult,
    lastUploadAt, lastSyncedCount, pendingBatchCount, syncPendingRowCount,
    serverImportInProgress, serverImportLastFrameCount — all SyncState properties.
    It also calls model.testServerConnection(), model.triggerManualUpload(),
    model.triggerBackfillAndUpload(), model.importHistoricalDataFromServer(),
    model.checkServerHealth() — these method calls remain on model (methods stay on
    GooseAppModel, only properties moved).

    There are also Preview macro sites that write directly to a GooseAppModel mock
    (m.serverReachable = nil, m.lastUploadAt = nil, m.pendingBatchCount = 0, etc.) —
    update those preview mock writes to go through m.syncState.xxx too.

    Steps:
    1. Add @Environment(SyncState.self) private var syncState to the view struct that
       currently declares @Environment(GooseAppModel.self) private var model.
       Keep the model environment property — it is still needed for method calls.

    2. Replace all model.xxx reads for SyncState properties with syncState.xxx:
         model.serverReachable      → syncState.serverReachable
         model.connectionTestRunning → syncState.connectionTestRunning
         model.connectionTestResult  → syncState.connectionTestResult
         model.lastUploadAt          → syncState.lastUploadAt
         model.lastSyncedCount       → syncState.lastSyncedCount
         model.pendingBatchCount     → syncState.pendingBatchCount
         model.syncPendingRowCount   → syncState.syncPendingRowCount
         model.serverImportInProgress → syncState.serverImportInProgress
         model.serverImportLastFrameCount → syncState.serverImportLastFrameCount

    3. In Preview macro bodies: replace m.serverReachable = with m.syncState.serverReachable =,
       m.lastUploadAt = with m.syncState.lastUploadAt =, etc.

    4. Leave all model.triggerXxx(), model.testServerConnection(), model.checkServerHealth()
       calls unchanged — those are method calls, not property reads.
  </action>
  <verify>
    <automated>grep -c "environment(model.bleState)" /Users/francisco/Documents/goose/GooseSwift/GooseSwiftApp.swift && grep -cE "\bmodel\.(serverReachable|connectionTestRunning|connectionTestResult|lastUploadAt|lastSyncedCount|pendingBatchCount|syncPendingRowCount|serverImportInProgress|serverImportLastFrameCount)\b" /Users/francisco/Documents/goose/GooseSwift/MoreRemoteServerViews.swift</automated>
  </verify>
  <done>First grep returns 1 (bleState injection present). Second grep returns 0 (no bare model.syncProperty reads remain in MoreRemoteServerViews).</done>
</task>

<task type="auto">
  <name>Task 2: Update remaining view files and run the build gate</name>
  <files>GooseSwift/DeviceView.swift, GooseSwift/CoachRouteViews.swift, GooseSwift/HomeDashboardView.swift, GooseSwift/FitnessLiveWorkoutViews.swift, GooseSwift/MoreDebugViews.swift, GooseSwift/CodexCoachSupport.swift, GooseSwift/LocalizedStatusStrings.swift, GooseSwift/RootView.swift</files>
  <action>
    For each file below, add the required @Environment declaration and replace all
    model.xxx / appModel.xxx reads for migrated properties with domain object access.
    Keep model environment property in every file that still needs it for method calls.

    --- DeviceView.swift ---
    Migrated property reads found (audit):
      model.connectedDeviceGeneration       → bleState (add @Environment(BLEState.self))
      model.respiratoryPacketWatchStatus    → healthState (add @Environment(HealthState.self))
      model.respiratoryPacketWatchActive    → healthState

    In the DeviceContentView struct: add @Environment(BLEState.self) private var bleState.
    In the struct that reads respiratoryPacket* properties: add @Environment(HealthState.self) private var healthState.
    Replace reads accordingly. Method calls model.startRespiratoryPacketWatch() /
    model.stopRespiratoryPacketWatch() remain on model — do not change them.

    --- CoachRouteViews.swift ---
    Migrated property reads found (audit):
      model.alarmIsArmed        → bleState
      model.scheduledAlarmTime  → bleState
    Also has writes: model.alarmIsArmed = false / true, model.scheduledAlarmTime = ...
    These become bleState.alarmIsArmed = false / true, bleState.scheduledAlarmTime = ...

    Add @Environment(BLEState.self) private var bleState to the relevant struct.
    Replace all 6 model.alarmIsArmed and model.scheduledAlarmTime accesses with bleState.xxx.

    --- HomeDashboardView.swift ---
    Migrated property reads found (audit):
      model.homeActivityTimelineItems  → healthState (line ~56)

    Add @Environment(HealthState.self) private var healthState to the relevant struct.
    Replace model.homeActivityTimelineItems with healthState.homeActivityTimelineItems.
    All other model accesses (model.ble, model.activitySession, model.recordUIAction,
    model.refreshActivityTimeline) remain on model.

    --- FitnessLiveWorkoutViews.swift ---
    Migrated property reads found (audit):
      model.liveWorkoutStrain  → bleState (lines ~245-246)

    Add @Environment(BLEState.self) private var bleState to the relevant struct.
    Replace model.liveWorkoutStrain with bleState.liveWorkoutStrain.

    --- MoreDebugViews.swift ---
    Migrated property reads found (audit):
      model.onboardingComplete = false  → bleState.onboardingComplete = false (line ~616)

    Add @Environment(BLEState.self) private var bleState to the relevant struct.
    Replace model.onboardingComplete = false with bleState.onboardingComplete = false.

    --- CodexCoachSupport.swift ---
    Migrated property reads found (audit via appModel.xxx):
      appModel.activityDetectionStatus         → healthState
      appModel.activityPersistenceStatus       → healthState
      appModel.packetImportStatus              → healthState
      appModel.healthPacketCaptureSessionID    → healthState
      appModel.healthPacketCaptureStatus       → healthState
      appModel.healthPacketCaptureStartedAt    → healthState
      appModel.healthPacketCaptureFrameCount   → healthState
      appModel.healthPacketCaptureTargetSummary → healthState
      appModel.healthPacketCaptureLastPacketSummary → healthState
      appModel.healthPacketCaptureFamilyRows   → healthState

    This file accesses GooseAppModel via a stored appModel parameter/property (not
    @Environment). Read the file to confirm the access pattern. If appModel is passed
    as a parameter, add a healthState parameter alongside it. If it is an @Environment,
    add @Environment(HealthState.self) private var healthState.
    Replace all 10 appModel.xxx reads with healthState.xxx.

    --- LocalizedStatusStrings.swift ---
    Migrated property reads found (audit at lines ~166-198):
      GooseAppModel.healthPacketCaptureStatus (referenced in comment only)
      GooseAppModel.healthPacketCaptureTargetSummary (referenced in comment only)
      GooseAppModel.activityDetectionStatus (referenced in comment only)
      GooseAppModel.packetImportStatus (referenced in comment only)

    Read the file to confirm whether these are comment-only references or actual
    property reads. If they are only comments (MARK: - Capture Status (GooseAppModel.xxx)),
    no code change is needed — update the comments to reference healthState.xxx to stay
    accurate. If they are actual reads, apply the same healthState redirect as CodexCoachSupport.

    --- RootView.swift ---
    Migrated property reads found (audit):
      model.onboardingComplete != onboardingComplete  → bleState.onboardingComplete
      model.onboardingComplete = onboardingComplete   → bleState.onboardingComplete = onboardingComplete

    Add @Environment(BLEState.self) private var bleState to RootView.
    Replace both references with bleState.onboardingComplete.

    --- Build gate ---
    After all edits, run:
      xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift \
        -destination 'platform=iOS Simulator,name=iPhone 16' \
        -derivedDataPath /tmp/goose-90-build \
        CODE_SIGNING_ALLOWED=NO build 2>&1 | grep -E "error:|warning:" | grep -v "ld: warning"

    A clean build (zero error: lines) is required before writing the SUMMARY. If errors
    appear, fix them before considering this task done — do not leave compile errors for
    the next phase.
  </action>
  <verify>
    <automated>xcodebuild -project /Users/francisco/Documents/goose/GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -derivedDataPath /tmp/goose-90-build CODE_SIGNING_ALLOWED=NO build 2>&1 | grep "error:" | grep -v "ld: warning" | wc -l</automated>
  </verify>
  <done>xcodebuild returns 0 error lines. All view files compile against the new domain object properties. The three domain @Observable objects are injected at the scene root and observable isolation boundaries are active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view layer → domain objects | Views read domain object state; writes (alarmIsArmed, onboardingComplete) come from user interaction in views |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-04a | Tampering | bleState.alarmIsArmed write from CoachRouteViews | accept | View runs on @MainActor; write is user-initiated toggle; bleState is @MainActor — Swift 6 enforces isolation |
| T-90-04b | Tampering | bleState.onboardingComplete write from RootView/MoreDebugViews | accept | Debug-only reset in MoreDebugViews; RootView write is gated by equality check before assignment |
| T-90-SC | Tampering | No new package installs | accept | Pure Swift edits; no package manager changes |
</threat_model>

<verification>
Final build gate:
  xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift \
    -destination 'platform=iOS Simulator,name=iPhone 16' \
    -derivedDataPath /tmp/goose-90-build \
    CODE_SIGNING_ALLOWED=NO build 2>&1 | grep "error:" | grep -v "ld: warning"
→ must return empty output (zero error lines)

Property sweep confirmation:
  grep -rn "model\.\(serverReachable\|pendingBatchCount\|syncPendingRowCount\|liveWorkoutStrain\|alarmIsArmed\|homeActivityTimelineItems\|onboardingComplete\|respiratoryPacketWatchActive\|healthPacketCaptureStatus\)" GooseSwift/ --include="*.swift" | grep -v "GooseAppModel"
→ must return empty output (all view-layer reads now go through domain objects)

Environment injection:
  grep -c "environment(model.bleState)\|environment(model.syncState)\|environment(model.healthState)" GooseSwift/GooseSwiftApp.swift
→ 3
</verification>

<success_criteria>
1. BLEState, SyncState, HealthState injected via .environment() at GooseSwiftApp root.
2. All view files that previously read migrated properties via model.xxx now read from the
   appropriate domain object via @Environment.
3. iOS build (xcodebuild) produces zero errors and zero new warnings.
4. ARCH-06 observable isolation is in effect: SwiftUI only tracks access to the domain
   object a view actually reads — 1 Hz BLE HR updates do not invalidate sync or health views.
</success_criteria>

<output>
Create `.planning/phases/90-domain-viewmodels/90-04-SUMMARY.md` when done.
</output>
