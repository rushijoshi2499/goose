---
phase: 90-domain-viewmodels
plan: 02
type: execute
wave: 2
depends_on: [90-01]
files_modified:
  - GooseSwift/GooseAppModel.swift
autonomous: true
requirements: [ARCH-06]
must_haves:
  truths:
    - "GooseAppModel.swift declares `let bleState = BLEState()`, `let syncState = SyncState()`, `let healthState = HealthState()` as stored properties"
    - "All ~36 var properties that were migrated to domain objects are removed from GooseAppModel"
    - "rustStatus and helloSummary remain on GooseAppModel"
    - "init() callbacks that wrote to the old properties now write to bleState.xxx / syncState.xxx / healthState.xxx"
    - "deinit cancels the same DispatchWorkItems as before (no regression)"
  artifacts:
    - path: "GooseSwift/GooseAppModel.swift"
      provides: "Coordinator class with domain object composition"
      contains: "let bleState"
  key_links:
    - from: "GooseAppModel.init()"
      to: "bleState.bondingState"
      via: "ble.bondingManager.onBondingStateChange callback"
      pattern: "bleState\\.bondingState"
    - from: "GooseAppModel.init()"
      to: "bleState.heartRateHourlyRanges"
      via: "heartRateSamplePipeline.onHeartRateTimelineSnapshot callback"
      pattern: "bleState\\.heartRateHourlyRanges"
---

<objective>
Migrate GooseAppModel.swift: remove the ~36 var properties that belong to BLEState, SyncState,
and HealthState, add the three domain object `let` properties, and update all init() callbacks
to write through the domain objects instead of self.

Purpose: GooseAppModel becomes a thin coordinator. After this plan, the @Observable isolation
boundary is established — SwiftUI's observation system will only track access to the domain
object a view actually reads.
Output: Updated GooseAppModel.swift with domain composition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-domain-viewmodels/90-CONTEXT.md
@.planning/phases/90-domain-viewmodels/90-01-SUMMARY.md
@GooseSwift/GooseAppModel.swift
@GooseSwift/BLEState.swift
@GooseSwift/SyncState.swift
@GooseSwift/HealthState.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove migrated var properties from GooseAppModel and add domain object lets</name>
  <files>GooseSwift/GooseAppModel.swift</files>
  <action>
    Edit GooseSwift/GooseAppModel.swift. Make these changes in order:

    1. Add three new stored properties immediately after `let healthStore: HealthDataStore`
       (around line 52 of the current file):

         let bleState = BLEState()
         let syncState = SyncState()
         let healthState = HealthState()

    2. Remove ALL of the following var declarations from the top of the class body
       (they now live in the domain objects — do not leave duplicates):

       BLEState properties to remove:
       - var onboardingComplete = false
       - var heartRateHourlyRanges: [HeartRateHourlyRange] = []
       - var heartRateStorageStatus = "No HR samples stored"
       - var liveWorkoutStrain: Double = 0
       - var scheduledAlarmTime: Date? = nil
       - var alarmIsArmed: Bool = false
       - private(set) var hrSpikeCount: Int = 0
       - var bondingState: GooseBLEBondingState = .notStarted
       - var connectedDeviceGeneration: String? = nil

       SyncState properties to remove:
       - var serverReachable: Bool? = nil
       - private(set) var isNetworkReachable: Bool = true
       - var apnsDeviceToken: String? = nil
       - var uploadErrorState: String? = nil
       - var hasPendingUploadAfterReconnect: Bool = false
       - var lastUploadAt: Date? = nil
       - var pendingBatchCount: Int = 0
       - var lastSyncedCount: Int? = nil
       - var syncPendingRowCount: Int = 0
       - var serverImportInProgress: Bool = false
       - var serverImportLastFrameCount: Int? = nil
       - var connectionTestRunning: Bool = false
       - var connectionTestResult: String? = nil

       HealthState properties to remove:
       - var packetImportRevision = 0
       - var packetImportStatus = "No packet import"
       - var activityPersistenceStatus = "No activity stored"
       - var homeActivityTimelineItems: [ActivityTimelineItem] = []
       - var homeActivityTimelineStatus = "Activity timeline not loaded"
       - var activityDetectionStatus = "Watching for movement packets"
       - var movementPacketValidationStatus = "Not run"
       - var movementPacketValidationIsRunning = false
       - var healthPacketCaptureSessionID: String?
       - var healthPacketCaptureStatus = "No health packet capture"
       - var healthPacketCaptureStartedAt: Date?
       - var healthPacketCaptureFrameCount = 0
       - var healthPacketCaptureTargetSummary = "No health packet capture"
       - var healthPacketCaptureLastPacketSummary = "No packets captured"
       - var healthPacketCaptureFamilyRows: [HealthPacketCaptureFamily] = []
       - var respiratoryPacketWatchActive = false
       - var respiratoryPacketWatchStatus = "Not watching K18 respiratory history"

       Keep on GooseAppModel (do NOT remove):
       - var rustStatus = "Rust bridge not checked"
       - var helloSummary = "Client hello not prepared"
       - All DispatchWorkItems (activityDetectionIdleWorkItem, etc.)
       - All coordinator-level vars (activeActivityPersistence, activeHealthPacketCapture, etc.)
       - All performance-tracking vars (notificationIngestQueueDepth, etc.)
       - All nonisolated(unsafe) properties
       - All static let constants
       - All let constants (queues, pipelines, aggregators, etc.)
  </action>
  <verify>
    <automated>grep -c "var onboardingComplete\|var heartRateHourlyRanges\|var serverReachable\|var packetImportRevision\|var respiratoryPacketWatchActive" /Users/francisco/Documents/goose/GooseSwift/GooseAppModel.swift</automated>
  </verify>
  <done>grep returns 0 — none of the migrated var properties remain directly on GooseAppModel. `let bleState`, `let syncState`, `let healthState` are present.</done>
</task>

<task type="auto">
  <name>Task 2: Update init() callbacks to write through domain objects</name>
  <files>GooseSwift/GooseAppModel.swift</files>
  <action>
    In GooseAppModel.init(), update every callback closure that previously wrote to
    `self.xxx` for a migrated property. The domain object instances exist as stored
    properties so `self.bleState.xxx` is valid inside init() closures.

    Specific changes in init():

    heartRateSamplePipeline.onHeartRateTimelineSnapshot callback currently calls:
      self?.applyHeartRateTimelineSnapshot(snapshot)
    Leave this call as-is — applyHeartRateTimelineSnapshot is defined in the Lifecycle
    extension and will be updated in Plan 03 to write through bleState.

    ble.onHRSpike callback:
      Change: Task { @MainActor in self?.hrSpikeCount += 1 }
      To:     Task { @MainActor in self?.bleState.incrementHRSpikeCount() }

    ble.onLiveHeartRate callback — the liveWorkoutStrain write:
      Change: Task { @MainActor [weak self] in self?.liveWorkoutStrain = load }
      To:     Task { @MainActor [weak self] in self?.bleState.liveWorkoutStrain = load }

    ble.bondingManager.onBondingStateChange callback:
      Change: self.bondingState = newState
      To:     self.bleState.bondingState = newState

    networkMonitor.onReachabilityChange callback:
      Change: self?.isNetworkReachable = reachable
      To:     self?.syncState.applyNetworkReachability(reachable)

    All other callback calls that invoke extension methods (applyHeartRateTimelineSnapshot,
    applyPacketUIStateSnapshot, applyHealthPacketCaptureFamilySnapshot, etc.) remain
    unchanged — those extension methods will be updated in Plan 03.

    Do NOT change the configureUploadService() call or any other method calls in init() —
    only fix the three inline closures listed above that directly wrote to the removed
    self properties.
  </action>
  <verify>
    <automated>grep -c "self?\.hrSpikeCount\|self\.bondingState\|self?\.isNetworkReachable\s*=\|self?\.liveWorkoutStrain\s*=" /Users/francisco/Documents/goose/GooseSwift/GooseAppModel.swift</automated>
  </verify>
  <done>grep returns 0 — no direct self writes to the migrated properties remain in init(). The three domain object redirects (incrementHRSpikeCount, bleState.liveWorkoutStrain, bleState.bondingState, syncState.applyNetworkReachability) are in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init() closures → domain objects | Closures capture weak self; domain objects are owned by self so they live as long as GooseAppModel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-02a | Tampering | Domain object property writes off @MainActor | accept | All writes go through Task { @MainActor in ... } closures as before; Swift 6 strict concurrency enforces this at compile time |
| T-90-02b | Denial of Service | Premature dealloc of domain objects | accept | Domain objects are `let` stored properties on GooseAppModel — they are released only when GooseAppModel is released |
| T-90-SC | Tampering | No new package installs | accept | Pure Swift edits; no package manager changes |
</threat_model>

<verification>
- grep -c "var onboardingComplete\|var heartRateHourlyRanges\|var serverReachable" GooseSwift/GooseAppModel.swift → 0
- grep -c "let bleState" GooseSwift/GooseAppModel.swift → 1
- grep -c "let syncState" GooseSwift/GooseAppModel.swift → 1
- grep -c "let healthState" GooseSwift/GooseAppModel.swift → 1
- grep -c "hrSpikeCount +=" GooseSwift/GooseAppModel.swift → 0 (replaced by incrementHRSpikeCount())
Full build will be verified by Plan 04's build gate.
</verification>

<success_criteria>
GooseAppModel.swift is a coordinator: it holds the three domain `let` properties, all coordinator-level state (work items, pipelines, queues), and delegates domain-specific observable state to bleState/syncState/healthState. No duplicated var properties remain.
</success_criteria>

<output>
Create `.planning/phases/90-domain-viewmodels/90-02-SUMMARY.md` when done.
</output>
