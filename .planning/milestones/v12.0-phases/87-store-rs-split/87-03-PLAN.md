---
phase: 87-store-rs-split
plan: 03
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - Rust/core/src/store/capture.rs
  - Rust/core/src/store/mod.rs
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "store/capture.rs contains all 25 capture domain methods as impl GooseStore"
    - "Each method acquires self.conn.lock().map_err(...) at entry"
    - "Methods removed from store/mod.rs — no duplicates"
    - "cargo test --locked passes after capture move"
  artifacts:
    - path: "Rust/core/src/store/capture.rs"
      provides: "impl GooseStore — decoded frames, capture sessions, overnight, raw evidence, step counter, upload cursors, RR intervals"
      exports:
        - mirror_overnight_batch
        - overnight_mirror_counts
        - insert_raw_evidence
        - insert_decoded_frame
        - start_capture_session
        - set_capture_session_device_id
        - finish_capture_session
        - capture_session
        - capture_sessions_between
        - insert_step_counter_sample
        - step_counter_sample
        - step_counter_samples_between
        - raw_evidence
        - raw_evidence_between
        - raw_evidence_payload_bytes
        - compact_raw_evidence_payloads_to_limit
        - decoded_frames_between
        - decoded_frame
        - upsert_upload_cursor
        - get_upload_cursor
        - mark_synced_rows
        - rows_pending_upload
        - backfill_streams_from_decoded_frames
        - rr_intervals_between
        - prune_synced_stream_rows
  key_links:
    - from: "Rust/core/src/store/capture.rs"
      to: "Rust/core/src/store/mod.rs"
      via: "use super::GooseStore — submodule declared via mod capture in mod.rs"
      pattern: "use super::GooseStore"
---

<objective>
Move all 25 capture domain methods from store/mod.rs into store/capture.rs. This is the largest Wave 2 plan by line count due to backfill_streams_from_decoded_frames and overnight batch methods.

Purpose: Separates captured BLE frame storage, raw evidence, step counter, upload cursors, and RR interval queries into a coherent capture domain file.

Output: store/capture.rs with 25 methods, store/mod.rs with those methods removed.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-store-rs-split/87-CONTEXT.md
@.planning/phases/87-store-rs-split/87-RESEARCH.md
@.planning/phases/87-store-rs-split/87-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move 25 capture methods from store/mod.rs into store/capture.rs</name>
  <files>Rust/core/src/store/capture.rs, Rust/core/src/store/mod.rs</files>

  <read_first>
    - Rust/core/src/store/mod.rs — lines 1840–1930 (mirror_overnight_batch, overnight_mirror_counts)
    - Rust/core/src/store/mod.rs — lines 2259–2540 (insert_raw_evidence, insert_decoded_frame, start_capture_session, set_capture_session_device_id, finish_capture_session, capture_session, capture_sessions_between)
    - Rust/core/src/store/mod.rs — lines 4402–4500 (insert_step_counter_sample, step_counter_sample, step_counter_samples_between)
    - Rust/core/src/store/mod.rs — lines 5165–5400 (raw_evidence*, compact_raw_evidence_payloads_to_limit, decoded_frames_between, decoded_frame)
    - Rust/core/src/store/mod.rs — lines 7400–7650 (upsert_upload_cursor, get_upload_cursor, mark_synced_rows, rows_pending_upload, backfill_streams_from_decoded_frames, rr_intervals_between, prune_synced_stream_rows)
    - Rust/core/src/store/mod.rs — lines 1–80 (identify private helpers used: sha256_hex, validate_required, validate_allowed, is_allowed_sync_status, DEFAULT_RAW_EVIDENCE_PAYLOAD_RETENTION_LIMIT_BYTES, ALLOWED_* constants)
  </read_first>

  <action>
  Step 1 — Audit all 25 capture methods and list every private helper and constant they reference from store/mod.rs. Expected items include:
    - `sha256_hex` (used in raw evidence methods)
    - `validate_required`, `validate_allowed`
    - `is_allowed_sync_status`
    - `DEFAULT_RAW_EVIDENCE_PAYLOAD_RETENTION_LIMIT_BYTES`
    - Constants like `ALLOWED_SYNC_STATUS_VALUES`
  Also list all pub types used (CaptureSessionInput, RawEvidenceInput, RawEvidenceRow, DecodedFrameInput, StepCounterSampleInput, etc.).

  Step 2 — Write store/capture.rs:
    - `use super::GooseStore;`
    - `use super::{sha256_hex, validate_required, validate_allowed, is_allowed_sync_status, DEFAULT_RAW_EVIDENCE_PAYLOAD_RETENTION_LIMIT_BYTES};` (adjust to only what is actually used)
    - `use super::{CaptureSessionInput, RawEvidenceInput, RawEvidenceRow, DecodedFrameInput, ...};` for all pub types needed
    - `use crate::{GooseError, GooseResult};`
    - `use rusqlite::{params, named_params, Connection, ...};`
    - Any other imports (sha2, hex, etc.) that the methods currently have in store.rs — check the top of the current store/mod.rs for the full import list.

  Step 3 — Important: `backfill_streams_from_decoded_frames` (line ~7570 in mod.rs) uses `immediate_transaction` internally. After Plan 01 changed the closure signature to `|conn: &Connection|`, this method's closure should already be updated. Verify the closure in mod.rs uses `|conn|` and `conn.execute(...)`. If not, fix it before moving.

  Step 4 — Move all 25 method bodies into a single `impl GooseStore { ... }` block in store/capture.rs. Each method must acquire the lock at entry:
    `let conn = self.conn.lock().map_err(|_| GooseError::message("store mutex poisoned"))?;`

  Step 5 — Remove all 25 methods from store/mod.rs using Edit. Methods are scattered across several line ranges — remove each block precisely without disturbing surrounding code.

  Step 6 — Verify no duplicate method definitions remain: grep for a sample of moved method names across all store/ files.
  </action>

  <verify>
    <automated>cd Rust/core && cargo test --locked 2>&1 | grep -E '(^error|test result|FAILED)' | head -20</automated>
  </verify>

  <done>
  `cargo test --locked` shows no errors and no FAILED tests. `grep -c 'fn mirror_overnight_batch' Rust/core/src/store/capture.rs` returns 1. `grep -c 'fn mirror_overnight_batch' Rust/core/src/store/mod.rs` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store/ submodule | capture.rs is private submodule; raw evidence and upload cursor data remain inside store layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03 | Information Disclosure | sha256_hex helper | mitigate | Private function stays in store/mod.rs; capture.rs imports via super:: — not exposed outside store/ |
| T-87-SC | Tampering | cargo installs | accept | No new packages |
</threat_model>

<verification>
```bash
cd Rust/core && cargo test --locked 2>&1 | tail -5

# Spot-check method locations:
grep -c 'fn insert_raw_evidence' Rust/core/src/store/capture.rs   # Expected: 1
grep -c 'fn insert_raw_evidence' Rust/core/src/store/mod.rs        # Expected: 0

grep -c 'fn backfill_streams' Rust/core/src/store/capture.rs      # Expected: 1
grep -c 'fn backfill_streams' Rust/core/src/store/mod.rs           # Expected: 0

# Confirm lock pattern:
grep -c 'store mutex poisoned' Rust/core/src/store/capture.rs      # Expected: 25
```
</verification>

<success_criteria>
- All 25 capture methods in store/capture.rs, none remaining in store/mod.rs
- `backfill_streams_from_decoded_frames` closure correctly uses `|conn|` (not `|store|`)
- Each method acquires the mutex lock at entry with the error message "store mutex poisoned"
- `cargo test --locked` passes with zero failures
</success_criteria>

<output>
Create `.planning/phases/87-store-rs-split/87-03-SUMMARY.md` when done.
</output>
