---
phase: 87-store-rs-split
plan: 06
type: execute
wave: 3
depends_on:
  - 87-02
  - 87-03
  - 87-04
  - 87-05
files_modified: []
autonomous: false
requirements:
  - ARCH-02

must_haves:
  truths:
    - "cargo test --locked passes with all integration and unit tests green"
    - "cargo clippy --lib -D warnings passes with zero warnings"
    - "store/ directory has exactly 5 files: mod.rs, sleep.rs, capture.rs, metrics.rs, activity.rs"
    - "store.rs does not exist"
    - "GooseStore public API is identical to pre-split (no new or removed pub fn from Swift's perspective)"
    - "SC2 satisfied: open_existing_current() returns Err when schema version mismatches"
  artifacts:
    - path: "Rust/core/src/store/mod.rs"
      provides: "GooseStore struct + infrastructure only"
      contains: "pub(super) conn: Arc<Mutex<Connection>>"
    - path: "Rust/core/src/store/sleep.rs"
      provides: "13 sleep domain methods"
    - path: "Rust/core/src/store/capture.rs"
      provides: "25 capture domain methods"
    - path: "Rust/core/src/store/metrics.rs"
      provides: "~49 metrics domain methods"
    - path: "Rust/core/src/store/activity.rs"
      provides: "49 activity domain methods"
  key_links:
    - from: "Rust/core/tests/store_tests.rs"
      to: "goose_core::store::{GooseStore, ...}"
      via: "use statement — all types must resolve identically"
      pattern: "use goose_core::store"
---

<objective>
Phase gate: verify the complete store.rs split compiles, passes all tests, and is lint-clean. Confirm ARCH-02 success criteria are met. Human checkpoint to visually verify the directory structure.

Purpose: Final quality gate before committing Phase 87 complete. Catches any method that was missed, duplicated, or incorrectly moved by the Wave 2 plans.

Output: Confirmed green test suite + clean lint. Phase marked complete.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/87-store-rs-split/87-CONTEXT.md
@.planning/phases/87-store-rs-split/87-02-SUMMARY.md
@.planning/phases/87-store-rs-split/87-03-SUMMARY.md
@.planning/phases/87-store-rs-split/87-04-SUMMARY.md
@.planning/phases/87-store-rs-split/87-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + clippy gate</name>
  <files></files>

  <read_first>
    - Rust/core/tests/store_tests.rs — lines 1–30 (verify import list still resolves)
    - Rust/core/src/store/mod.rs — lines 1–50 (verify Arc<Mutex<Connection>> struct, mod declarations)
  </read_first>

  <action>
  Run the following checks in sequence. Fix any failures before proceeding to the checkpoint.

  Check 1 — Full test suite:
    `cd Rust/core && cargo test --locked`
  All tests must pass. If any test fails, read the error message, identify which store method is missing or miscompiled, and fix it.

  Check 2 — Clippy lint:
    `cd Rust/core && cargo clippy --lib -D warnings`
  Must exit 0 with no warnings. Common issues to watch for:
    - Unused imports in domain files (remove the unused `use` lines)
    - Dead code warnings for private helpers that are now only used in one domain file
    - `use super::SomeThing` that doesn't resolve (type moved to wrong file)

  Check 3 — Public API completeness. Confirm all types from store_tests.rs import still resolve:
    `cd Rust/core && cargo test --test store_tests 2>&1 | head -30`
  All 20+ types imported by store_tests.rs must resolve. If any don't, the type is missing from store/mod.rs re-exports.

  Check 4 — Directory structure:
    `ls -la Rust/core/src/store/`
  Must show: activity.rs, capture.rs, metrics.rs, mod.rs, sleep.rs — exactly 5 files.
    `ls Rust/core/src/store.rs 2>/dev/null && echo "FAIL: store.rs exists" || echo "OK: store.rs absent"`

  Check 5 — Method count sanity. Verify the total pub fn count matches original:
    `grep -c '^    pub fn' Rust/core/src/store/mod.rs Rust/core/src/store/sleep.rs Rust/core/src/store/capture.rs Rust/core/src/store/metrics.rs Rust/core/src/store/activity.rs`
  Sum across all files must be 139 (or 140 if one signature spans two lines and grep picks up both). If count is significantly different (e.g., 120 or 160), a domain file is missing methods.

  Check 6 — SC2 validation: confirm schema check preserved.
    `grep -A 8 'fn open_existing_current' Rust/core/src/store/mod.rs | grep 'schema_version\|CURRENT_SCHEMA_VERSION\|not current'`
  Expected: at least one match confirming the version check is still in place.

  Check 7 — immediate_transaction signature:
    `grep 'FnOnce(&Connection)' Rust/core/src/store/mod.rs`
  Expected: 1 match.

  Fix any issues found before the checkpoint. Apply `cargo fmt -- Rust/core/src/store/mod.rs Rust/core/src/store/sleep.rs Rust/core/src/store/capture.rs Rust/core/src/store/metrics.rs Rust/core/src/store/activity.rs` to format the new files.
  </action>

  <verify>
    <automated>cd Rust/core && cargo test --locked 2>&1 | tail -3 && cargo clippy --lib -D warnings 2>&1 | grep -E '^error' | head -5</automated>
  </verify>

  <done>
  `cargo test --locked` exits 0 with all tests passing. `cargo clippy --lib -D warnings` exits 0 with no errors. All 7 checks pass.
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
  Full store.rs split into 5 files under store/. The original 9,944-line monolith is now:
  - store/mod.rs — GooseStore struct (Arc&lt;Mutex&lt;Connection&gt;&gt;), all pub types, open/migrate/schema/transaction infrastructure
  - store/sleep.rs — 13 sleep domain methods
  - store/capture.rs — 25 capture domain methods
  - store/metrics.rs — ~49 metrics/calibration/algorithm domain methods
  - store/activity.rs — 49 activity/debug/exercise domain methods

  Automated verification (Task 1) confirmed: cargo test --locked green, cargo clippy --lib clean, store.rs absent, 5 domain files present.
  </what-built>

  <how-to-verify>
  1. Run in terminal:
     ```
     cd Rust/core
     cargo test --locked
     cargo clippy --lib -D warnings
     ls src/store/
     ```
     Expected: tests pass, no clippy warnings, 5 files listed.

  2. Open `Rust/core/src/store/mod.rs` and confirm:
     - `pub(super) conn: Arc<Mutex<Connection>>` in the GooseStore struct
     - 4 `mod` declarations near the top: `mod sleep; mod capture; mod metrics; mod activity;`
     - `FnOnce(&Connection)` in immediate_transaction signature
     - Only 7 `pub fn` methods (open*, immediate_transaction, migrate, schema_version)

  3. Spot-check one domain file (e.g., store/sleep.rs):
     - All methods start with `let conn = self.conn.lock()...`
     - No method still does `self.conn.execute(...)` directly

  4. Confirm store.rs is gone:
     ```
     ls Rust/core/src/store.rs 2>/dev/null && echo "FAIL" || echo "OK"
     ```
  </how-to-verify>

  <resume-signal>Type "approved" if all checks pass, or describe any issue found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test boundary | store_tests.rs imports via goose_core::store — public API must be unchanged |
| Bridge boundary | bridge/mod.rs calls GooseStore::open() — must still compile unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-06 | Repudiation | Phase gate skipped | mitigate | Checkpoint:human-verify ensures manual review before phase declared complete |
| T-87-SC | Tampering | cargo installs | accept | No packages installed in gate plan |
</threat_model>

<verification>
```bash
# SC1: Domain stores exist
ls Rust/core/src/store/
# Expected: activity.rs  capture.rs  metrics.rs  mod.rs  sleep.rs

# SC1: Arc<Mutex<Connection>> in struct
grep 'Arc<Mutex<Connection>>' Rust/core/src/store/mod.rs | grep 'pub(super)'
# Expected: 1 match

# SC2: Schema validation preserved
grep -c 'schema_version != CURRENT_SCHEMA_VERSION' Rust/core/src/store/mod.rs
# Expected: 1

# SC3: All tests pass
cd Rust/core && cargo test --locked
# Expected: all tests ok

# No regressions in bridge or integration tests
cd Rust/core && cargo test --test store_tests
# Expected: all tests ok
```
</verification>

<success_criteria>
ARCH-02 satisfied:
1. Domain stores exist as separate files (store/sleep.rs, store/capture.rs, store/metrics.rs, store/activity.rs) sharing Arc&lt;Mutex&lt;Connection&gt;&gt; — VERIFIED by ls and grep
2. Runtime schema version validation runs on SQLite open — VERIFIED by grep for version check in open_existing_current
3. All existing Rust integration tests pass (cargo test --locked) — VERIFIED by test run
</success_criteria>

<output>
Create `.planning/phases/87-store-rs-split/87-06-SUMMARY.md` when done.
</output>
