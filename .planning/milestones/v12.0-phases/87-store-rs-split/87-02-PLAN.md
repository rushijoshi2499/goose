---
phase: 87-store-rs-split
plan: 02
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - Rust/core/src/store/sleep.rs
  - Rust/core/src/store/mod.rs
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "store/sleep.rs contains all 9 sleep domain methods as impl GooseStore"
    - "Each method acquires self.conn.lock().map_err(...) at entry"
    - "Methods are removed from store/mod.rs (no duplicate definitions)"
    - "cargo test --locked passes for all sleep-related tests"
  artifacts:
    - path: "Rust/core/src/store/sleep.rs"
      provides: "impl GooseStore — sleep sessions, stages, external sleep, correction labels"
      exports:
        - insert_external_sleep_session
        - external_sleep_session
        - external_sleep_sessions_between
        - insert_external_sleep_stage
        - external_sleep_stage
        - external_sleep_stages_for_session
        - insert_sleep_correction_label
        - sleep_correction_label
        - sleep_correction_labels_between
  key_links:
    - from: "Rust/core/src/store/sleep.rs"
      to: "Rust/core/src/store/mod.rs"
      via: "use super::GooseStore — submodule declared via mod sleep in mod.rs"
      pattern: "use super::GooseStore"
---

<objective>
Move all 9 sleep domain methods from store/mod.rs into store/sleep.rs. Methods: insert_external_sleep_session, external_sleep_session, external_sleep_sessions_between, insert_external_sleep_stage, external_sleep_stage, external_sleep_stages_for_session, insert_sleep_correction_label, sleep_correction_label, sleep_correction_labels_between.

Purpose: First domain file populated after Wave 1 skeleton. Validates the multi-impl-GooseStore pattern and the lock acquisition idiom before the larger domain moves in Plans 03–05.

Output: store/sleep.rs with 9 methods, store/mod.rs with those methods removed.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-store-rs-split/87-CONTEXT.md
@.planning/phases/87-store-rs-split/87-RESEARCH.md
@.planning/phases/87-store-rs-split/87-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move 9 sleep methods from store/mod.rs into store/sleep.rs</name>
  <files>Rust/core/src/store/sleep.rs, Rust/core/src/store/mod.rs</files>

  <read_first>
    - Rust/core/src/store/mod.rs — lines 4735–5090 (all 9 sleep methods: insert_external_sleep_session at ~4735, external_sleep_session at ~4794, external_sleep_sessions_between at ~4826, insert_external_sleep_stage at ~4864, external_sleep_stage at ~4914, external_sleep_stages_for_session at ~4942, insert_sleep_correction_label at ~4969, sleep_correction_label at ~5021, sleep_correction_labels_between at ~5050)
    - Rust/core/src/store/sleep.rs — current stub file (to confirm imports)
    - Rust/core/src/store/mod.rs — lines 1–80 (to identify which private helpers are used by sleep methods: is_allowed_external_sleep_platform, is_allowed_external_sleep_stage_kind, is_allowed_sleep_correction_label_type, validate_required, validate_allowed)
  </read_first>

  <action>
  Step 1 — Identify all imports needed in store/sleep.rs by reading each of the 9 sleep methods and listing:
    - rusqlite types used (params, named_params, Connection, etc.)
    - store-internal types used (ExternalSleepSessionInput, ExternalSleepStageInput, SleepCorrectionLabelInput, etc.)
    - private helpers called (validate_required, validate_allowed, is_allowed_external_sleep_platform, is_allowed_external_sleep_stage_kind, is_allowed_sleep_correction_label_type)

  Step 2 — Write store/sleep.rs:
    - Header imports: `use super::GooseStore;`, `use super::{validate_required, validate_allowed, is_allowed_external_sleep_platform, is_allowed_external_sleep_stage_kind, is_allowed_sleep_correction_label_type};` (only the helpers actually used), plus any pub types from mod.rs needed (ExternalSleepSessionInput, ExternalSleepStageInput, ExternalSleepSessionRow, etc. — these are defined in mod.rs and accessible via `super::`).
    - `use crate::{GooseError, GooseResult};`
    - `use rusqlite::{params, ...};` — match exactly what the methods use.
    - A single `impl GooseStore { ... }` block containing all 9 methods verbatim from store/mod.rs.
    - Each method must start with: `let conn = self.conn.lock().map_err(|_| GooseError::message("store mutex poisoned"))?;`
    - Verify each method in store/mod.rs already follows this pattern (they should after Task 1 of Plan 01); if any still use `self.conn.X(...)` directly, convert them.

  Step 3 — Remove the 9 sleep methods from store/mod.rs. Delete the method bodies from the `impl GooseStore` block(s) in store/mod.rs. The methods span roughly lines 4735–5087 in the current file. Use Edit to remove each method block precisely — do not delete surrounding methods.

  Step 4 — Confirm `mod sleep;` declaration is present in store/mod.rs (added in Plan 01 Task 1). If absent, add it before the `impl GooseStore` blocks.
  </action>

  <verify>
    <automated>cd Rust/core && cargo test --locked 2>&1 | grep -E '(^error|test result|FAILED)' | head -20</automated>
  </verify>

  <done>
  `cargo test --locked` shows no `error` lines and no `FAILED` tests. store/sleep.rs contains the 9 sleep methods. The same methods are absent from store/mod.rs. Running `grep -c 'fn insert_external_sleep_session' Rust/core/src/store/sleep.rs` returns 1 and `grep -c 'fn insert_external_sleep_session' Rust/core/src/store/mod.rs` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store/ submodule | sleep.rs is a private submodule; pub(super) conn is accessible; external callers unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02 | Tampering | validate_allowed helper visibility | mitigate | Import via use super::validate_allowed; explicitly — not re-exported, keeps validation private to store/ family |
| T-87-SC | Tampering | cargo installs | accept | No new packages; pure move of existing code |
</threat_model>

<verification>
```bash
cd Rust/core && cargo test --locked 2>&1 | tail -5

# Confirm method location:
grep -c 'fn insert_external_sleep_session' Rust/core/src/store/sleep.rs
# Expected: 1

grep -c 'fn insert_external_sleep_session' Rust/core/src/store/mod.rs
# Expected: 0

# Confirm lock pattern used:
grep -c 'store mutex poisoned' Rust/core/src/store/sleep.rs
# Expected: 9 (one per method)
```
</verification>

<success_criteria>
- All 9 sleep methods live in store/sleep.rs and nowhere else in the store/ tree
- Each method acquires `self.conn.lock().map_err(|_| GooseError::message("store mutex poisoned"))?`
- `cargo test --locked` passes with zero failures
- No duplicate method definitions across store/ files
</success_criteria>

<output>
Create `.planning/phases/87-store-rs-split/87-02-SUMMARY.md` when done.
</output>
