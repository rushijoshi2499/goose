---
phase: 86-bridge-rs-split-protocol-comments
plan: 04
type: execute
wave: 3
depends_on:
  - 86-02
  - 86-03
files_modified:
  - Rust/core/src/bridge/mod.rs
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "bridge_methods_constant_matches_dispatcher test scans all 5 domain files by concatenating include_str! of each"
    - "The test passes — BRIDGE_METHODS count matches the dispatcher arm count across all domain files"
    - "bridge/mod.rs line count is ≤ 400 (pure router: BRIDGE_METHODS + shared utils + FFI + router + updated test)"
    - "cargo test --lib passes including bridge_methods_constant_matches_dispatcher"
  artifacts:
    - path: "Rust/core/src/bridge/mod.rs"
      provides: "Updated include_str! multi-file scanner in test module; lean router confirmed ≤ 400 lines"
      contains: "include_str!(\"metrics.rs\")"
  key_links:
    - from: "Rust/core/src/bridge/mod.rs (mod tests)"
      to: "Rust/core/src/bridge/metrics.rs"
      via: "include_str!(\"metrics.rs\") in bridge_methods_constant_matches_dispatcher"
      pattern: "include_str.*metrics"
---

<objective>
Update the bridge_methods_constant_matches_dispatcher test in bridge/mod.rs to scan all 5 domain files instead of the deleted bridge.rs. This is the highest-risk correctness task in Phase 86 — it ensures BRIDGE_METHODS stays in sync with the actual dispatcher arms across the split files.

Purpose: The consistency test was left as a placeholder in Plan 86-01. Now that all domain files are filled (Wave 2), the scanner can be updated to concatenate include_str! from all 5 domain files. Also confirms mod.rs meets the ≤ 100 line router target (ROADMAP SC1).

Output:
- bridge/mod.rs: updated test module with multi-file scanner; final line count verified ≤ 400 total (router + utilities), ≤ 100 lines for just the routing function itself
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-CONTEXT.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-RESEARCH.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-01-SUMMARY.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-02-SUMMARY.md
@.planning/phases/86-bridge-rs-split-protocol-comments/86-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update include_str! scanner to scan all 5 domain files (Option A)</name>
  <files>
    Rust/core/src/bridge/mod.rs
  </files>
  <read_first>
    - Rust/core/src/bridge/mod.rs — read the `mod tests` section completely; find the placeholder for bridge_methods_constant_matches_dispatcher left from Plan 86-01; also find bridge_methods_constant_is_sorted_and_unique which must remain untouched
    - Rust/core/src/bridge/metrics.rs — verify it contains real dispatch arms (needed to understand scanner output)
    - Rust/core/src/bridge/debug.rs — verify validation.* alias arms are present
  </read_first>
  <action>
    The bridge_methods_constant_matches_dispatcher test in bridge/mod.rs currently has a placeholder body. Replace it with Option A: concatenate include_str! from all 5 domain files.

    The updated test body must:

    1. Build a combined source string by concatenating:
       - `include_str!("metrics.rs")`
       - `include_str!("sleep.rs")`
       - `include_str!("capture.rs")`
       - `include_str!("activity.rs")`
       - `include_str!("debug.rs")`

       Note: include_str! paths are relative to the source file's location. Since bridge/mod.rs is at `src/bridge/mod.rs`, the domain files are siblings and include_str!("metrics.rs") resolves correctly.

    2. Use the SAME scanning logic as the original test:
       - Find the method name quoted strings in each arm by scanning for `"namespace.method" =>` patterns
       - The scanner should extract the first quoted string from each arm line (the existing logic already handles this; pipe-separated `"a" | "b" =>` arms extract only "a" — this is the pre-existing behaviour, acceptable per RESEARCH)
       - The combined source spans all domain dispatch functions; the scanner must NOT be restricted to a specific range (it was previously restricted to the range between the match start and the catch-all — adapt this if needed, or just scan all quoted strings followed by ` =>` in the concatenated source)

    3. Assert that the set of method strings found equals the set in BRIDGE_METHODS (same assertion logic as before).

    The scanner logic from the original test at line 9846 should be read from git history if needed:
    `git show HEAD~3:Rust/core/src/bridge.rs | sed -n '9830,9900p'`
    (adjust commit distance based on how many commits were made during Plan 86-01/02/03)

    After updating the test, also measure the routing function (handle_bridge_request_inner) line count to verify it meets the ROADMAP SC1 requirement:
    `grep -n "fn handle_bridge_request_inner" Rust/core/src/bridge/mod.rs` — find start/end lines.

    The ROADMAP says "bridge.rs reduced to ≤ 100 lines" — interpreted as the routing function body. Confirm the 5-arm prefix router is well within this limit. If the total mod.rs line count is > 400, check for any domain code not yet removed and clean up.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --lib bridge_methods_constant_matches_dispatcher -- --nocapture 2>&1 | tail -15</automated>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --lib bridge_methods_constant_is_sorted_and_unique 2>&1 | tail -5</automated>
    <automated>wc -l /Users/francisco/Documents/goose/Rust/core/src/bridge/mod.rs</automated>
    <automated>grep -c "include_str" /Users/francisco/Documents/goose/Rust/core/src/bridge/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib bridge_methods_constant_matches_dispatcher` exits 0 (PASS, not just compiles)
    - `cargo test --lib bridge_methods_constant_is_sorted_and_unique` exits 0
    - `grep -c "include_str" Rust/core/src/bridge/mod.rs` returns exactly 5 (one per domain file)
    - `wc -l Rust/core/src/bridge/mod.rs` shows ≤ 500 lines (the ≤ 100 constraint from ROADMAP SC1 applies to the routing function, not the entire mod.rs which also holds shared utilities, BRIDGE_METHODS, FFI, and tests)
    - `cargo test --lib` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>bridge_methods_constant_matches_dispatcher scans all 5 domain files and passes. BRIDGE_METHODS is consistent with the actual dispatcher arms. bridge/mod.rs is a lean router meeting the ARCH-01 intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| include_str! scanner test | Compile-time assertion that BRIDGE_METHODS matches actual arms; protects against silent method registration drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-07 | Tampering | BRIDGE_METHODS drift from domain files | mitigate | Option A multi-file scanner preserves the compile-time consistency guarantee established pre-split |
| T-86-08 | Elevation of Privilege | include_str! path injection | accept | include_str! paths are compile-time literals in the Rust source; no runtime input involved |
</threat_model>

<verification>
After Wave 3:
- `cargo test --lib bridge_methods_constant_matches_dispatcher` passes
- `grep -c "include_str" Rust/core/src/bridge/mod.rs` = 5
- All domain files compile and all 152 bridge methods are handled
</verification>

<success_criteria>
bridge_methods_constant_matches_dispatcher passes using the multi-file include_str! scanner. BRIDGE_METHODS is guaranteed to stay in sync with domain file dispatch arms at compile time.
</success_criteria>

<output>
Create `.planning/phases/86-bridge-rs-split-protocol-comments/86-04-SUMMARY.md` when done.
</output>
