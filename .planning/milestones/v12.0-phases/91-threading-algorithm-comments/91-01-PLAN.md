---
phase: 91-threading-algorithm-comments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseRustBridge.swift
  - GooseSwift/CaptureFrameWriteQueue.swift
  - GooseSwift/OvernightSQLiteMirrorQueue.swift
  - GooseSwift/GooseAppModel.swift
autonomous: true
requirements:
  - COMM-02
must_haves:
  truths:
    - "GooseRustBridge.swift explains that goose_bridge_handle_json blocks the calling thread and must not be called from @MainActor"
    - "GooseRustBridge.swift explains that each owner holds its own instance because the Rust side is stateless"
    - "CaptureFrameWriteQueue.swift explains why stateLock guards state shared between the BLE enqueue thread and the write queue flush thread"
    - "CaptureFrameWriteQueue.swift explains why @unchecked Sendable is safe (all mutation goes through stateLock)"
    - "OvernightSQLiteMirrorQueue.swift explains that all mutation is confined to the serial queue making @unchecked Sendable safe without NSLock"
    - "GooseAppModel.swift explains the BLE→parse→write queue pipeline and the @MainActor boundary rule for Rust bridge calls"
  artifacts:
    - path: "GooseSwift/GooseRustBridge.swift"
      provides: "Threading invariant comments at class declaration, instance pattern, and request() method"
    - path: "GooseSwift/CaptureFrameWriteQueue.swift"
      provides: "NSLock scope comment and @unchecked Sendable rationale"
    - path: "GooseSwift/OvernightSQLiteMirrorQueue.swift"
      provides: "Serial-queue confinement comment at class declaration"
    - path: "GooseSwift/GooseAppModel.swift"
      provides: "Queue pipeline comment and @MainActor boundary comment"
  key_links:
    - from: "GooseRustBridge.swift comment"
      to: "GooseAppModel+NotificationPipeline.swift usage pattern"
      via: "Documents why callers dispatch to background queue before calling request()"
---

<objective>
Add inline threading invariant comments to the four Swift files identified in COMM-02.

Purpose: Make the threading contract of the Rust bridge and queue subsystems discoverable at the declaration site, so future contributors do not accidentally call the synchronous FFI from the main actor or reason incorrectly about concurrent access safety.

Output: Four Swift files with targeted // THREADING: comments. No logic changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GooseSwift/GooseRustBridge.swift
@GooseSwift/CaptureFrameWriteQueue.swift
@GooseSwift/OvernightSQLiteMirrorQueue.swift
@GooseSwift/GooseAppModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Threading comments in GooseRustBridge.swift and CaptureFrameWriteQueue.swift</name>
  <files>GooseSwift/GooseRustBridge.swift, GooseSwift/CaptureFrameWriteQueue.swift</files>
  <action>
Add inline // THREADING: comments at the three identified sites in GooseRustBridge.swift (per D-COMM-02):

1. Above the `final class GooseRustBridge: @unchecked Sendable {` declaration (line 22), insert a comment block explaining the synchronous FFI contract and the multiple-instance pattern. The comment must say that goose_bridge_handle_json blocks the calling thread, that callers must never invoke request() from @MainActor, and that each owner (GooseAppModel, HealthDataStore, OvernightSQLiteMirrorQueue, CaptureFrameWriteQueue) holds its own instance because the Rust side is stateless across calls.

2. Above `func requestValue(method:args:)` (line 36), insert a comment that callers are responsible for dispatching to a background DispatchQueue before calling request() or requestValue(), and that the return always arrives on the calling thread.

3. The class already uses `private let lock = NSLock()` guarding `counter` and `_lastTiming`. Above the `private let lock = NSLock()` declaration (line 23), insert a comment explaining the lock guards the mutable counter and _lastTiming across concurrent callers — bridge instances may be shared between Task.detached and DispatchQueue contexts.

In CaptureFrameWriteQueue.swift, add two comments (per D-COMM-02):

4. Above `final class CaptureFrameWriteQueue: @unchecked Sendable {` (line 182), insert a comment explaining that @unchecked Sendable is safe because all reads and writes of mutable state go through stateLock (NSLock), and the write queue (com.goose.swift.capture-frame-writes) is the only thread that ever calls the Rust bridge on this instance.

5. Above `private let stateLock = NSLock()` (line 184), insert a comment explaining that stateLock guards the batch queue (pendingRows, queuedRowCount, isWriting, and related state) against concurrent access from the BLE notification ingest thread (enqueue callers) and the writeQueue flush path.

Comment style: `// THREADING: ...` prefix, single-line or multi-line with // continuation. No doc comments (///). No fenced code blocks. Keep each comment to 1-3 lines — explain WHY, not WHAT the code does.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "THREADING:" GooseSwift/GooseRustBridge.swift GooseSwift/CaptureFrameWriteQueue.swift</automated>
  </verify>
  <done>GooseRustBridge.swift has at least 3 THREADING: comments; CaptureFrameWriteQueue.swift has at least 2 THREADING: comments. Grep confirms counts.</done>
</task>

<task type="auto">
  <name>Task 2: Threading comments in OvernightSQLiteMirrorQueue.swift and GooseAppModel.swift</name>
  <files>GooseSwift/OvernightSQLiteMirrorQueue.swift, GooseSwift/GooseAppModel.swift</files>
  <action>
In OvernightSQLiteMirrorQueue.swift (per D-COMM-02):

1. Above `final class OvernightSQLiteMirrorQueue: @unchecked Sendable {` (line 25), insert a comment explaining that @unchecked Sendable is safe because all mutable state is confined to the serial queue `com.goose.swift.overnight-sqlite-mirror` — there is no NSLock because the serial queue provides the mutual exclusion. The Rust bridge instance (`rust`) is accessed only from within that serial queue.

In GooseAppModel.swift (per D-COMM-02):

2. Above the `notificationIngestQueue` declaration (line 27), insert a comment explaining the three-stage BLE→SQLite pipeline threading model: raw BLE bytes arrive on notificationIngestQueue (CoreBluetooth delegate callback), are reassembled into frames and forwarded to notificationParseQueue for Rust parsing, and completed rows are handed to CaptureFrameWriteQueue for batched SQLite writes. No stage returns to @MainActor until a UI update is needed.

3. Above the `@MainActor @Observable` class declaration (line 6), insert a comment explaining that Rust bridge methods (GooseRustBridge.request) must never be called directly from @MainActor — they block the thread for the duration of the SQLite + Rust computation. All bridge calls must be dispatched to a background DispatchQueue or wrapped in Task.detached / requestAsync before touching @MainActor state.

Comment style: `// THREADING: ...` prefix, single-line or multi-line with // continuation. No doc comments (///). Keep comments concise — 1-3 lines each.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "THREADING:" GooseSwift/OvernightSQLiteMirrorQueue.swift GooseSwift/GooseAppModel.swift</automated>
  </verify>
  <done>OvernightSQLiteMirrorQueue.swift has at least 1 THREADING: comment; GooseAppModel.swift has at least 2 THREADING: comments.</done>
</task>

<task type="auto">
  <name>Task 3: iOS build verification</name>
  <files></files>
  <action>
Run xcodebuild to confirm comment-only changes did not introduce any build error. Use the simulator SDK to avoid code-signing requirements. Build the GooseSwift scheme.

Command to run:
  xcodebuild build \
    -project /Users/francisco/Documents/goose/GooseSwift.xcodeproj \
    -scheme GooseSwift \
    -sdk iphonesimulator \
    -destination "generic/platform=iOS Simulator" \
    CODE_SIGNING_ALLOWED=NO \
    2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -5

A successful build shows "BUILD SUCCEEDED". Any "error:" line is a failure requiring investigation before marking done.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -sdk iphonesimulator -destination "generic/platform=iOS Simulator" CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -5</automated>
  </verify>
  <done>"BUILD SUCCEEDED" appears in xcodebuild output. No "error:" lines from Swift compilation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file edit | Comment-only edits; no executable logic crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering | Swift source files | accept | Comment-only changes; no logic modified; build verification gates the plan |
</threat_model>

<verification>
- grep -c "THREADING:" on all four Swift files returns >= 1 per file
- xcodebuild with iphonesimulator SDK returns BUILD SUCCEEDED
- git diff shows only comment additions (no lines removed from logic, no blank line reordering in non-comment areas)
</verification>

<success_criteria>
All four Swift files contain THREADING: comments at the locations specified in COMM-02. iOS build passes. No logic or whitespace changes outside comment insertions.
</success_criteria>

<output>
Create `.planning/phases/91-threading-algorithm-comments/91-01-SUMMARY.md` when done.
</output>
