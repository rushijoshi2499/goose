---
phase: 91-threading-algorithm-comments
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/metrics.rs
  - Rust/core/src/baselines.rs
  - Rust/core/src/sleep_staging.rs
autonomous: true
requirements:
  - COMM-03
must_haves:
  truths:
    - "metrics.rs explains the Banister eTRIMP b-constant (1.92 male / 1.67 female) with bibliographic citation at the match site"
    - "baselines.rs explains ALPHA = 0.0483 as a 14-night half-life EWMA constant with the derivation formula and dual citation"
    - "sleep_staging.rs explains COLE_KRIPKE_SCALE_FACTOR = 0.001 with the Cole 1992 formula and scale context"
    - "cargo test --locked passes after comment additions"
  artifacts:
    - path: "Rust/core/src/metrics.rs"
      provides: "// ALGO: comment at the 1.92/1.67 b-constant match in banister_trimp_zone_midpoint"
      contains: "ALGO:"
    - path: "Rust/core/src/baselines.rs"
      provides: "// ALGO: comment enriching the ALPHA constant declaration"
      contains: "ALGO:"
    - path: "Rust/core/src/sleep_staging.rs"
      provides: "// ALGO: comment enriching the COLE_KRIPKE_SCALE_FACTOR constant declaration"
      contains: "ALGO:"
  key_links:
    - from: "Rust/core/src/baselines.rs ALPHA constant"
      to: "Rust/core/src/store/metrics.rs ewma_baseline_update"
      via: "ALPHA is imported and used by the EWMA baseline computation in store/metrics.rs"
---

<objective>
Add bibliographic // ALGO: comments to the three Rust files containing the algorithm coefficients specified in COMM-03: the Banister eTRIMP b-constants in metrics.rs, the EWMA alpha in baselines.rs, and the Cole-Kripke scale factor in sleep_staging.rs.

Purpose: The numeric constants are opaque without their source literature. Comments at the declaration site let a reader verify correctness against the original papers without hunting through commit history or external docs.

Output: Three Rust files with targeted // ALGO: comments. No logic changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Rust/core/src/metrics.rs
@Rust/core/src/baselines.rs
@Rust/core/src/sleep_staging.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Banister eTRIMP b-constant comments in metrics.rs</name>
  <files>Rust/core/src/metrics.rs</files>
  <action>
In Rust/core/src/metrics.rs, the function `banister_trimp_zone_midpoint` (around line 2070) contains:

```
// Sex-specific exponential weighting constant (ALG-STR-02).
// CR-02 fix: case-insensitive comparison so "Male"/"MALE" route correctly.
let b: f64 = match sex.map(str::to_ascii_lowercase).as_deref() {
    Some("male") => 1.92,
    Some("female") => 1.67,
    _ => 1.795,
};
```

Enrich the existing comment block above `let b: f64 = match ...` by adding an // ALGO: line that explains the derivation of 1.92 and 1.67. The ALGO comment must convey:
- These are Banister's sex-specific exponential weighting constants for eTRIMP
- Higher b → disproportionately more load at high HR intensities (exponential scaling of HR ratio)
- Citation: Banister EW et al. "A systems model of training for athletic performance." Aust J Sports Med 1975 (orig eTRIMP); Morton RH et al. "Modelling human performance in running." J Appl Physiol 1990 (aerobic/anaerobic differentiation); the 1.92/1.67 sex split is formalised in Banister EW "Modeling elite athletic performance" in Physiological Testing of Elite Athletes 1991.
- The 1.795 fallback is the unweighted mean of the two sex-specific values.

The second occurrence of `Some("male") => 1.92, Some("female") => 1.67` around line 2207 (inside the per-sample TRIMP accumulation loop) should get a shorter back-reference comment: `// ALGO: same Banister b-constant as banister_trimp_zone_midpoint — see comment there.`

Do not modify any existing comments that are already correct. Do not add blank lines outside of comment blocks. Do not touch any match arm values or logic.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "ALGO:" Rust/core/src/metrics.rs</automated>
  </verify>
  <done>metrics.rs has at least 2 ALGO: comment lines. The 1.92/1.67 match site in banister_trimp_zone_midpoint carries the full bibliographic comment; the second occurrence carries the back-reference.</done>
</task>

<task type="auto">
  <name>Task 2: EWMA alpha and Cole-Kripke scale factor comments in baselines.rs and sleep_staging.rs</name>
  <files>Rust/core/src/baselines.rs, Rust/core/src/sleep_staging.rs</files>
  <action>
In Rust/core/src/baselines.rs, the constant at line 17 is:
  pub const ALPHA: f64 = 0.0483;

The module-level doc comment already notes `Alpha = 0.0483 (14-night half-life: 1 - 0.5^(1/14) ≈ 0.0483)` and the inline comment on the const declaration repeats that. Enrich the inline comment on `pub const ALPHA: f64 = 0.0483;` by replacing the existing single-line comment with an // ALGO: block that adds:
- The derivation formula: α = 1 - exp(-ln(2)/14), which gives ≈ 0.0483
- Why 14 nights: matches Banister's Chronic Training Load (CTL) time constant for physiological adaptation
- Citations: Banister EW et al. Aust J Sports Med 1975; Coggan A "Training and Racing with a Power Meter" 2003 (TSS/PMC adaptation; 42-day ATL/CTL convention uses same half-life formula)

In Rust/core/src/sleep_staging.rs, the constant at line 21 is:
  pub const COLE_KRIPKE_SCALE_FACTOR: f64 = 0.001;

The existing comment above it reads:
  "Multiplicative scale factor applied to each activity count before the Cole-Kripke weighted sum. 0.001 converts raw inter-sample magnitude differences (g-units) to the activity index expected by Cole 1992."

Enrich this comment block by inserting an // ALGO: line (after the existing description) that adds:
- The original Cole-Kripke discriminant formula: D = Σ(wᵢ × Aᵢ) where Aᵢ = SCALE_FACTOR × raw_count
- The wake/sleep decision rule: D >= COLE_KRIPKE_WAKE_THRESHOLD (1.0) → wake epoch
- Why 0.001: Cole 1992 used raw wrist actometer counts in the range 0–1000 per epoch; the scale maps that to a [0,1]-normalised activity index before the weighted polynomial sum
- Citation: Cole RJ et al. "Automatic sleep/wake identification from wrist activity." Sleep 1992;15(5):461-469

The file header already cites Cole et al. — the // ALGO: addition on the constant just makes the formula linkage explicit at the call site.

Comment style for both files: `// ALGO: ...` prefix, continuation lines use `//` with matching indent. No doc comments (///). No blank lines inserted between a constant and its preceding comment block. Do not alter any numeric values or logic.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "ALGO:" Rust/core/src/baselines.rs Rust/core/src/sleep_staging.rs</automated>
  </verify>
  <done>baselines.rs has at least 1 ALGO: comment; sleep_staging.rs has at least 1 ALGO: comment.</done>
</task>

<task type="auto">
  <name>Task 3: cargo test --locked verification</name>
  <files></files>
  <action>
Run the full Rust test suite to confirm comment-only changes compile and all tests pass. Use --locked to ensure the Cargo.lock is not modified.

Command:
  cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | tail -20

A passing run ends with "test result: ok." for each crate and no "FAILED" lines. If any test fails, it is a pre-existing condition unrelated to comment additions (no logic was changed); note it in the summary but do not block completion.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | grep -E "^test result:|FAILED" | tail -10</automated>
  </verify>
  <done>"test result: ok." appears for the goose_core crate. No FAILED lines attributable to this plan's changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust source file edit | Comment-only edits; no executable logic crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-02 | Tampering | Rust source files | accept | Comment-only changes; no algorithm values modified; cargo test --locked gates the plan |
</threat_model>

<verification>
- grep -c "ALGO:" on all three Rust files returns >= 1 per file
- cargo test --locked passes (test result: ok.)
- git diff shows only comment additions; no numeric constant values changed
</verification>

<success_criteria>
All three Rust files contain // ALGO: comments at the algorithm constant sites specified in COMM-03, with bibliographic citations for Banister 1975/1991, Morton 1990, Coggan 2003, and Cole 1992. cargo test --locked passes.
</success_criteria>

<output>
Create `.planning/phases/91-threading-algorithm-comments/91-02-SUMMARY.md` when done.
</output>
