---
phase: 85-rust-crash-safety
plan: 04
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - Rust/core/src/capabilities.rs
autonomous: true
requirements: [ARCH-03]
must_haves:
  truths:
    - "capabilities.rs has zero .unwrap() calls remaining (all 8 test-code unwraps became .expect())"
    - "The file-level #![allow(clippy::unwrap_used)] shield is removed from capabilities.rs"
    - "capabilities.rs compiles cleanly under deny(clippy::unwrap_used) with no shield"
  artifacts:
    - path: "Rust/core/src/capabilities.rs"
      provides: "Test code using .expect() instead of .unwrap(); no allow shield"
  key_links:
    - from: "Rust/core/src/capabilities.rs"
      to: "Rust/core/src/lib.rs deny lint"
      via: "shield removal exposes capabilities.rs to the deny lint"
      pattern: "capabilities\\.rs"
---

<objective>
Convert all 8 test-code `.unwrap()` calls in `capabilities.rs` to `.expect("descriptive message")`
(D-03), then remove the `#![allow(clippy::unwrap_used)]` shield Plan 1 added.

capabilities.rs has **zero** production unwrap violations (confirmed by clippy on HEAD) — all 8
`.unwrap()` are inside `#[cfg(test)]`. This is a pure D-03 test-quality pass plus shield removal.

Purpose: Shrink the allow list by one module and improve test-failure readability.
Output: capabilities.rs with `.expect()` test code and no allow shield.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-rust-crash-safety/85-CONTEXT.md
@.planning/phases/85-rust-crash-safety/85-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert capabilities.rs test .unwrap() to .expect() and remove shield</name>
  <read_first>
    - Rust/core/src/capabilities.rs line ~1 (the #![allow(clippy::unwrap_used)] shield from Plan 1)
    - Rust/core/src/capabilities.rs #[cfg(test)] module (all 8 .unwrap() live there per RESEARCH)
    - 85-RESEARCH.md "Pattern 5" and "Revised Plan Scope Summary" (capabilities.rs: 0 prod + 8 test)
  </read_first>
  <files>Rust/core/src/capabilities.rs</files>
  <action>
    Replace every `.unwrap()` in capabilities.rs (all 8 are inside `#[cfg(test)]`) with
    `.expect("descriptive message")` per D-03, where the message describes the call-site
    expectation (e.g., `.expect("device.capabilities should resolve for known kind")`).
    Then REMOVE the `#![allow(clippy::unwrap_used)]` inner attribute Plan 1 added at the top of
    capabilities.rs. No production code changes are needed (none have violations).
  </action>
  <verify>
    <automated>test "$(grep -c '\.unwrap()' Rust/core/src/capabilities.rs)" -eq 0 && ! grep -q '#!\[allow(clippy::unwrap_used)\]' Rust/core/src/capabilities.rs && cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.unwrap()' Rust/core/src/capabilities.rs` returns 0
    - `grep -c '#!\[allow(clippy::unwrap_used)\]' Rust/core/src/capabilities.rs` returns 0 (shield removed)
    - `cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used` reports no capabilities.rs violation
    - `cargo test --locked --manifest-path Rust/core/Cargo.toml --lib` exits 0
  </acceptance_criteria>
  <done>capabilities.rs has zero `.unwrap()`, no allow shield, passes the deny lint; lib tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device kind input → capability resolution | Unknown device kinds must resolve to a typed result, never panic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-06 | Tampering | Test-only unwrap masking capability-resolution failures | mitigate | D-03 conversion to `.expect("...")` surfaces descriptive panic context; shield removal proves no production unwrap remains |
| T-85-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no audit table required |
</threat_model>

<verification>
- `cargo clippy --locked --manifest-path Rust/core/Cargo.toml --lib -- -D clippy::unwrap_used` reports no capabilities.rs violation
- `cargo test --locked --manifest-path Rust/core/Cargo.toml --lib` exits 0
</verification>

<success_criteria>
- capabilities.rs has zero `.unwrap()` (8 converted to `.expect()`)
- capabilities.rs no longer carries the allow shield and passes the deny lint
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `Rust/core/src/capabilities.rs` — modified: 8 test `.unwrap()` → `.expect()`; `#![allow(clippy::unwrap_used)]` shield removed
</artifacts_produced>

<output>
Create `.planning/phases/85-rust-crash-safety/85-04-SUMMARY.md` when done
</output>
