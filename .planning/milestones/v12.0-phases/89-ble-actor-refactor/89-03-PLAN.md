---
phase: 89-ble-actor-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - 89-01
files_modified:
  - GooseSwift/DeviceCatalog.swift
  - GooseSwift/CoreBluetoothBLETransport+HistoricalCommands.swift
  - GooseSwift/CoreBluetoothBLETransport+DebugAndSync.swift
  - GooseSwift/CoreBluetoothBLETransport+Parsing.swift
  - GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
autonomous: true
requirements:
  - ARCH-05

must_haves:
  truths:
    - "DeviceCatalog struct exists at GooseSwift/DeviceCatalog.swift"
    - "DeviceCatalog exposes usesPageSequenceSync and isGen4 from a DeviceCapabilities value"
    - "All connectedCapabilities?.historicalSync == .pageSequence guards in extension files are replaced with DeviceCatalog queries"
    - "All connectedCapabilities?.wireProtocol == .gen4 / != .gen4 guards in extension files are replaced with DeviceCatalog queries"
    - "No scattered capability pattern guards remain in CoreBluetoothBLETransport+*.swift files"
  artifacts:
    - path: "GooseSwift/DeviceCatalog.swift"
      provides: "Centralised Gen4/Gen5 capability branching"
      contains: "struct DeviceCatalog"
    - path: "GooseSwift/CoreBluetoothBLETransport+HistoricalCommands.swift"
      provides: "Guard-free historical commands (uses DeviceCatalog)"
      contains: "DeviceCatalog"
  key_links:
    - from: "GooseSwift/CoreBluetoothBLETransport+HistoricalCommands.swift"
      to: "GooseSwift/DeviceCatalog.swift"
      via: "DeviceCatalog(capabilities:) init"
      pattern: "DeviceCatalog"
    - from: "GooseSwift/CoreBluetoothBLETransport+DebugAndSync.swift"
      to: "GooseSwift/DeviceCatalog.swift"
      via: "DeviceCatalog(capabilities:) init"
      pattern: "DeviceCatalog"
---

<objective>
Create DeviceCatalog struct that centralises all Gen4/Gen5 capability branching (per D3).
Replace all scattered connectedCapabilities?.historicalSync / wireProtocol guard patterns in
CoreBluetoothBLETransport extension files with DeviceCatalog queries.

Purpose: Eliminates the repeated connectedCapabilities?.historicalSync == .pageSequence
and connectedCapabilities?.wireProtocol != .gen4 patterns spread across 4 extension files.
All capability branching logic lives in one place — DeviceCatalog.

Output: DeviceCatalog.swift (new struct), 4 updated extension files.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-ble-actor-refactor/89-01-SUMMARY.md
@GooseSwift/GooseBLETypes.swift
@GooseSwift/CoreBluetoothBLETransport+HistoricalCommands.swift
@GooseSwift/CoreBluetoothBLETransport+DebugAndSync.swift
@GooseSwift/CoreBluetoothBLETransport+Parsing.swift
@GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeviceCatalog struct</name>
  <files>GooseSwift/DeviceCatalog.swift</files>
  <action>
    Create GooseSwift/DeviceCatalog.swift containing the DeviceCatalog struct (per D3).

    DeviceCatalog takes an optional DeviceCapabilities (matching the real call sites where
    connectedCapabilities may be nil) and exposes computed properties for every capability
    branch that appears in the extension files.

    Struct definition:

    struct DeviceCatalog {
      let capabilities: DeviceCapabilities?

      // Returns true when the device uses the Gen4 page-sequence historical sync protocol
      // (i.e. WHOOP 4 straps that require cmd 34 → 22 → 23 with page offsets).
      // Replaces all: connectedCapabilities?.historicalSync == .pageSequence
      var usesPageSequenceSync: Bool {
        capabilities?.historicalSync == .pageSequence
      }

      // Returns true when the device uses the Gen4 wire protocol.
      // Replaces all: connectedCapabilities?.wireProtocol == .gen4
      var isGen4: Bool {
        capabilities?.wireProtocol == .gen4
      }

      // Human-readable generation label for logging ("gen4" / "gen5" / "unknown").
      // Replaces: connectedCapabilities.map { $0.wireProtocol == .gen4 ? "gen4" : "gen5" } ?? "unknown"
      var generationLabel: String {
        guard let caps = capabilities else { return "unknown" }
        return caps.wireProtocol == .gen4 ? "gen4" : "gen5"
      }

      // Returns the retry label used in DebugAndSync for log messages.
      // Replaces: connectedCapabilities?.historicalSync == .pageSequence ? "gen4 cmd34→22→23" : "GET_DATA_RANGE then SEND_HISTORICAL_DATA"
      var historicalRetryLabel: String {
        usesPageSequenceSync ? "gen4 cmd34→22→23" : "GET_DATA_RANGE then SEND_HISTORICAL_DATA"
      }

      // Returns the device type string used in HistoricalHandlers for log messages.
      // Replaces: switch connectedCapabilities?.historicalSync { case .pageSequence: "GEN4" }
      var historicalDeviceType: String {
        usesPageSequenceSync ? "GEN4" : "GEN5"
      }
    }

    File header comment: "DeviceCatalog — centralises all Gen4/Gen5 capability branching.
    Construct from connectedCapabilities (which may be nil when disconnected).
    See D3 in 89-CONTEXT.md."

    No imports needed beyond the file's implicit access to GooseBLETypes in the same module.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "struct DeviceCatalog" GooseSwift/DeviceCatalog.swift</automated>
  </verify>
  <done>
    GooseSwift/DeviceCatalog.swift exists with struct DeviceCatalog containing
    usesPageSequenceSync, isGen4, generationLabel, historicalRetryLabel, historicalDeviceType.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace scattered capability guards with DeviceCatalog</name>
  <files>
    GooseSwift/CoreBluetoothBLETransport+HistoricalCommands.swift,
    GooseSwift/CoreBluetoothBLETransport+DebugAndSync.swift,
    GooseSwift/CoreBluetoothBLETransport+Parsing.swift,
    GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift
  </files>
  <action>
    Replace every scattered connectedCapabilities guard in the 4 extension files with a
    DeviceCatalog query. For each call site, construct DeviceCatalog(capabilities: connectedCapabilities)
    at the point of use (a local let binding), then use the appropriate computed property.

    Exact replacements by file:

    CoreBluetoothBLETransport+HistoricalCommands.swift:

    Line 68 area — was:
      if connectedCapabilities?.historicalSync == .pageSequence {
    Replace with:
      let catalog = DeviceCatalog(capabilities: connectedCapabilities)
      if catalog.usesPageSequenceSync {

    Line 108 area — was:
      } else if connectedCapabilities?.historicalSync == .pageSequence && (kind == .getDataRange || kind == .sendHistoricalData) {
    Replace with:
      } else if catalog.usesPageSequenceSync && (kind == .getDataRange || kind == .sendHistoricalData) {
      (reuse the catalog local let from line 68 if in scope; otherwise add a new binding)

    CoreBluetoothBLETransport+DebugAndSync.swift:

    Line 399 area — was:
      body: "history_complete=true packets=0 generation=\(connectedCapabilities.map { $0.wireProtocol == .gen4 ? "gen4" : "gen5" } ?? "unknown")"
    Replace with:
      let catalog = DeviceCatalog(capabilities: connectedCapabilities)
      body: "history_complete=true packets=0 generation=\(catalog.generationLabel)"

    Line 419 area — was:
      let retryLabel = connectedCapabilities?.historicalSync == .pageSequence ? "gen4 cmd34→22→23" : "GET_DATA_RANGE then SEND_HISTORICAL_DATA"
    Replace with:
      let catalog = DeviceCatalog(capabilities: connectedCapabilities)
      let retryLabel = catalog.historicalRetryLabel

    Line 440 area — was:
      if connectedCapabilities?.historicalSync == .pageSequence {
    Replace with (reuse catalog if already in scope at that point, otherwise new binding):
      let catalog = DeviceCatalog(capabilities: connectedCapabilities)
      if catalog.usesPageSequenceSync {

    CoreBluetoothBLETransport+Parsing.swift:

    Line 737 area — was:
      if status == "success", connectedCapabilities?.wireProtocol != .gen4 {
    Replace with:
      let catalog = DeviceCatalog(capabilities: connectedCapabilities)
      if status == "success", !catalog.isGen4 {

    Line 748 area — was:
      if status == "success", connectedCapabilities?.wireProtocol != .gen4,
    Replace with (reuse catalog if in scope):
      if status == "success", !catalog.isGen4,

    Lines 1015 and 1022 area — was:
      switch connectedCapabilities?.wireProtocol {
    Replace with:
      let catalog = DeviceCatalog(capabilities: connectedCapabilities)
      switch catalog.capabilities?.wireProtocol {
      (Note: these switch statements switch on the raw WireProtocol enum for detailed case
      branching, not just gen4/gen5 bool — keep the switch, but express it via catalog.capabilities
      to centralise the access path. If both switches are in the same function scope, a single
      catalog binding at function entry is sufficient.)

    CoreBluetoothBLETransport+HistoricalHandlers.swift:

    Line 80-81 area — was:
      switch connectedCapabilities?.historicalSync {
      case .pageSequence: deviceType = "GEN4"
    Replace with:
      let catalog = DeviceCatalog(capabilities: connectedCapabilities)
      deviceType = catalog.historicalDeviceType

    Scope guidance: Where multiple guard replacements occur within the same function, declare
    the catalog let once at the top of the function body rather than repeating it. Do not
    declare it at file scope or as a stored property.

    After replacing all guards, verify no raw connectedCapabilities?.historicalSync == .pageSequence
    or connectedCapabilities?.wireProtocol == .gen4 patterns remain in any of the 4 files.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -rn "connectedCapabilities?.historicalSync\|connectedCapabilities?.wireProtocol" GooseSwift/CoreBluetoothBLETransport+HistoricalCommands.swift GooseSwift/CoreBluetoothBLETransport+DebugAndSync.swift GooseSwift/CoreBluetoothBLETransport+Parsing.swift GooseSwift/CoreBluetoothBLETransport+HistoricalHandlers.swift | wc -l</automated>
  </verify>
  <done>
    Zero raw connectedCapabilities?.historicalSync or connectedCapabilities?.wireProtocol
    patterns remain in the 4 extension files. All replaced with DeviceCatalog queries.
    iOS build succeeds with no new errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DeviceCatalog(nil) | When disconnected, connectedCapabilities is nil; DeviceCatalog defaults to Gen5 behaviour (usesPageSequenceSync = false, isGen4 = false) — same as the original optional-chaining behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03 | Tampering | DeviceCatalog nil default | accept | nil capabilities → all properties return safe Gen5 defaults, matching previous optional-chain behaviour; no behaviour change |
| T-89-SC | Tampering | No new package installs | accept | No npm/pip/cargo installs; not applicable |
</threat_model>

<verification>
After both tasks complete, verify no scattered guards remain and build succeeds:

```
cd /Users/francisco/Documents/goose && grep -rn "connectedCapabilities?.historicalSync\|connectedCapabilities?.wireProtocol" GooseSwift/CoreBluetoothBLETransport+*.swift
```
Expected: zero matches.

```
cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "error:|Build succeeded|Build FAILED" | tail -20
```
Expected: Build succeeded.
</verification>

<success_criteria>
1. GooseSwift/DeviceCatalog.swift exists with struct DeviceCatalog
2. usesPageSequenceSync, isGen4, generationLabel, historicalRetryLabel, historicalDeviceType all present
3. Zero raw connectedCapabilities?.historicalSync / wireProtocol patterns in the 4 extension files
4. iOS build succeeds with no new warnings
</success_criteria>

<output>
Create `.planning/phases/89-ble-actor-refactor/89-03-SUMMARY.md` when done.
</output>
