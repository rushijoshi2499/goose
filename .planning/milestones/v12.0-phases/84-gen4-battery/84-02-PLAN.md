---
phase: 84-gen4-battery
plan: 02
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - GooseSwift/NotificationFrameParsing.swift
  - GooseSwift/GooseAppModel+NotificationPipeline.swift
autonomous: true
requirements: [BAT-01]

must_haves:
  truths:
    - "A Gen4 Event-48 frame carrying a battery value publishes that percentage to the battery UI"
    - "Event-48 battery dispatch only fires when batteryViaEvent48 is true AND wireProtocol is .gen4"
    - "Gen5 Event payloads do not trigger the Gen4 Event-48 battery path"
  artifacts:
    - path: "GooseSwift/NotificationFrameParsing.swift"
      provides: "event48BatteryPct field on NotificationFrameCompactSummary and NotificationFrameInterpretation"
      contains: "event48BatteryPct"
    - path: "GooseSwift/GooseAppModel+NotificationPipeline.swift"
      provides: "Event-48 battery dispatch branch gated on capabilities + Gen4"
      contains: "event48.battery"
  key_links:
    - from: "GooseAppModel+NotificationPipeline.swift handleParsedNotificationFrame"
      to: "GooseBLEClient.applyBatteryLevel"
      via: "interpretation.event48BatteryPct gated on batteryViaEvent48 && wireProtocol == .gen4"
      pattern: "applyBatteryLevel.*event48.battery"
    - from: "NotificationFrameInterpretation.event48BatteryPct"
      to: "NotificationFrameCompactSummary.event48BatteryPct"
      via: "interpretation built from compact summary (mirrors r22BatteryPct wiring)"
      pattern: "event48BatteryPct: compact"
---

<objective>
Wire the Event-48 battery value (BAT-01) from Rust into the Swift notification pipeline so it reaches the existing battery UI. Mirror the established `r22BatteryPct` path exactly: read the new `event48_battery_pct` compact-summary field (produced by plan 84-01), carry it through `NotificationFrameCompactSummary` and `NotificationFrameInterpretation`, and dispatch it via `applyBatteryLevel` — but gated on `connectedCapabilities?.batteryViaEvent48 == true && connectedCapabilities?.wireProtocol == .gen4` (D-03) so Gen5 event payloads never apply Gen4 offsets.

Purpose: Surfaces the live (passively-arriving) Gen4 battery reading. Together with plan 84-03 (the eager Cmd 26 reading), the natural update ordering gives Cmd 26 first then Event-48 overrides (D-04).
Output: Two struct fields, their construction wiring, and one dispatch branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-gen4-battery/84-CONTEXT.md
@.planning/phases/84-gen4-battery/84-RESEARCH.md
@.planning/phases/84-gen4-battery/84-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `NotificationFrameCompactSummary.event48BatteryPct: Int?` — parsed from compact JSON key `event48_battery_pct`.
- `NotificationFrameInterpretation.event48BatteryPct: Int?` — carried from the compact summary.
- A new dispatch branch in `handleParsedNotificationFrame` calling `ble.applyBatteryLevel(_:capturedAt:sourceTitle:)` with sourceTitle `"event48.battery"`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add event48BatteryPct to compact summary + interpretation structs</name>
  <files>GooseSwift/NotificationFrameParsing.swift</files>
  <read_first>
    - GooseSwift/NotificationFrameParsing.swift (lines 46-141: NotificationFrameCompactSummary struct + init reading r22BatteryPct at line 116; NotificationFrameInterpretation struct at 131-141 with r22BatteryPct at 137)
    - .planning/phases/84-gen4-battery/84-01-SUMMARY.md (confirms the compact JSON key name is exactly "event48_battery_pct")
    - .planning/phases/84-gen4-battery/84-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    In `NotificationFrameCompactSummary`, add a stored property `let event48BatteryPct: Int?` immediately after `let r22BatteryPct: Int?` (line 87). In the struct's `init(raw:)`, add `event48BatteryPct = NotificationFrameParser.intValue(raw["event48_battery_pct"])` immediately after the existing `r22BatteryPct = NotificationFrameParser.intValue(raw["r22_battery_pct"])` line (line 116) — use the exact JSON key `event48_battery_pct` matching plan 84-01.

    In `NotificationFrameInterpretation`, add `let event48BatteryPct: Int?` immediately after `let r22BatteryPct: Int?` (line 137). This struct has no custom init; it is constructed positionally/by member in GooseAppModel+NotificationPipeline.swift, which Task 2 updates.
  </action>
  <verify>
    <automated>grep -c "event48BatteryPct" GooseSwift/NotificationFrameParsing.swift</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'event48BatteryPct' GooseSwift/NotificationFrameParsing.swift` returns at least 3 (compact field + compact init + interpretation field).
    - Source assertion: `grep -c 'raw\["event48_battery_pct"\]' GooseSwift/NotificationFrameParsing.swift` returns 1 (exact JSON key matches Rust output).
  </acceptance_criteria>
  <done>Both structs carry `event48BatteryPct: Int?`; the compact init reads the `event48_battery_pct` JSON key.</done>
</task>

<task type="auto">
  <name>Task 2: Construct interpretation field + add gated Event-48 dispatch branch</name>
  <files>GooseSwift/GooseAppModel+NotificationPipeline.swift</files>
  <read_first>
    - GooseSwift/GooseAppModel+NotificationPipeline.swift (lines 540-590: where NotificationFrameInterpretation is constructed — note r22BatteryPct: nil at 547 and r22BatteryPct: compact?.r22BatteryPct at 565; lines 632-673: handleParsedNotificationFrame including the r22 dispatch at 661-663)
    - GooseSwift/NotificationFrameParsing.swift (the event48BatteryPct fields added in Task 1)
    - GooseSwift/GooseBLETypes.swift (lines 75, 316-328: DeviceCapabilities.wireProtocol, batteryViaEvent48; WireProtocol enum .gen4 case)
    - .planning/phases/84-gen4-battery/84-CONTEXT.md (D-03: gate on batteryViaEvent48 && wireProtocol == .gen4; D-04 update ordering)
  </read_first>
  <action>
    In the interpretation construction sites: at the early/empty construction (the one passing `r22BatteryPct: nil` near line 547) add `event48BatteryPct: nil` in the same position relative to r22BatteryPct. At the populated construction (the one passing `r22BatteryPct: compact?.r22BatteryPct` near line 565) add `event48BatteryPct: compact?.event48BatteryPct` in the matching position. Field order must match the struct declaration order from Task 1 (event48BatteryPct immediately after r22BatteryPct).

    In `handleParsedNotificationFrame` (line 632), immediately after the existing r22 battery dispatch block (lines 661-663) add the Event-48 branch implementing D-03:
      if let batteryPct = interpretation.event48BatteryPct,
         batteryPct <= 100,
         connectedCapabilities?.batteryViaEvent48 == true,
         connectedCapabilities?.wireProtocol == .gen4 {
        ble.applyBatteryLevel(batteryPct, capturedAt: event.capturedAt, sourceTitle: "event48.battery")
      }
    Use the source title exactly `"event48.battery"` (matches the OSLog naming convention of `"r22.battery"`). The `wireProtocol == .gen4` clause is mandatory and must not be removed — Gen5 also has batteryViaEvent48 == true, so without it Gen5 event payloads would be misparsed (D-03, RESEARCH Pitfall/anti-pattern "Applying Gen4 battery offsets on Gen5"). Reference the capability fields via `connectedCapabilities` which is the GooseAppModel/BLE-cached value used by the r22 path's surrounding context.
  </action>
  <verify>
    <automated>grep -c "event48.battery\|event48BatteryPct" GooseSwift/GooseAppModel+NotificationPipeline.swift</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'event48BatteryPct: compact?.event48BatteryPct' GooseSwift/GooseAppModel+NotificationPipeline.swift` returns 1.
    - Source assertion: `grep -c 'sourceTitle: "event48.battery"' GooseSwift/GooseAppModel+NotificationPipeline.swift` returns 1.
    - Source assertion: `grep -c 'wireProtocol == .gen4' GooseSwift/GooseAppModel+NotificationPipeline.swift` increased by at least 1 (Gen4 gate present in the new branch).
    - Source assertion: `grep -c 'batteryViaEvent48 == true' GooseSwift/GooseAppModel+NotificationPipeline.swift` returns at least 1.
  </acceptance_criteria>
  <done>The interpretation carries event48BatteryPct from the compact summary, and the gated Event-48 dispatch calls applyBatteryLevel only for Gen4 devices with batteryViaEvent48.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust compact summary -> Swift pipeline | Parsed battery value crosses into UI publishing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-04 | Tampering | Event-48 battery applied to wrong generation | mitigate | `wireProtocol == .gen4` gate (D-03) prevents Gen5 from using Gen4 offsets |
| T-84-05 | Tampering | battery pct > 100 reaching UI | mitigate | `batteryPct <= 100` guard in branch + applyBatteryLevel already clamps 0-100 |
| T-84-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- iOS build compiles with no new warnings (Xcode build of the GooseSwift scheme).
- The Event-48 branch is gated on both batteryViaEvent48 and wireProtocol == .gen4.
- sourceTitle string is exactly "event48.battery".
</verification>

<success_criteria>
- BAT-01 end-to-end: a Gen4 Event-48 frame with a battery value publishes the percentage via applyBatteryLevel.
- Gen5 event payloads never trigger the Gen4 Event-48 path.
</success_criteria>

<output>
Create `.planning/phases/84-gen4-battery/84-02-SUMMARY.md` when done.
</output>
