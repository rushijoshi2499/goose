---
phase: "97"
plan: "02"
type: execute
wave: 2
depends_on:
  - "97-01"
files_modified:
  - GooseSwift/GooseHealthKitExporter.swift
  - GooseSwift.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - HK-01
  - HK-02
  - HK-03
  - HK-04
  - HK-05

must_haves:
  truths:
    - "GooseHealthKitExporter.swift exists with #if canImport(HealthKit) guard wrapping all content"
    - "requestAuthorization() async throws requests share for heartRate, heartRateVariabilitySDNN, oxygenSaturation, sleepAnalysis (D-06)"
    - "exportAfterSleepSync() queries all four data types via bridge and saves to HKHealthStore"
    - "HRV write uses HKUnit.secondUnit(with: .milli) with rmssd_ms value directly — or divide by 1000 and use seconds (per verified Apple docs during implementation)"
    - "SpO2 HK write uses HKUnit.percent() with value as fraction 0.0–1.0 (spo2_percent / 100)"
    - "Sleep write uses HKCategoryValueSleepAnalysis.asleepUnspecified"
    - "All HKHealthStore.save() calls wrapped in do/catch per D-11"
    - "GooseHealthKitExporter.swift registered in project.pbxproj (PBXFileReference + PBXBuildFile)"
  artifacts:
    - path: "GooseSwift/GooseHealthKitExporter.swift"
      provides: "enum GooseHealthKitExporter with requestAuthorization() and exportAfterSleepSync() static methods"
    - path: "GooseSwift.xcodeproj/project.pbxproj"
      provides: "PBXFileReference and PBXBuildFile entries for GooseHealthKitExporter.swift"
  key_links:
    - from: "GooseSwift/GooseHealthKitExporter.swift"
      to: "GooseSwift/GooseRustBridge.swift"
      via: "exportAfterSleepSync receives bridge: GooseRustBridge parameter and calls requestAsync for store.hr_samples_between, metrics.daily_recovery_metrics, store.spo2_samples_between, store.external_sleep_sessions_between"
      pattern: "requestAsync.*store\\.hr_samples_between|requestAsync.*metrics\\.daily_recovery_metrics"
    - from: "GooseSwift/GooseHealthKitExporter.swift"
      to: "HKHealthStore"
      via: "HKHealthStore().save(_ objects: [HKObject]) called once per data type"
      pattern: "hkStore\\.save"
---

<objective>
Create GooseHealthKitExporter.swift — the single file responsible for all HealthKit write logic in this phase. It provides two public entry points: requestAuthorization() for the toggle handler, and exportAfterSleepSync() for the post-sync trigger.

This plan covers HK-01 through HK-05 at the infrastructure level: the exporter is the component that fulfils all four data type writes. Plan 97-03 will call it; Plan 97-04 added the toggle that gates it.

Purpose: Centralise all HKHealthStore write code in one file, following the HealthKitFullImporter.swift pattern already in the codebase. Keeps GooseAppModel+SleepSync.swift clean.

Output: GooseHealthKitExporter.swift (new) + project.pbxproj updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-healthkit-export-bevel-integration/97-CONTEXT.md
@.planning/phases/97-healthkit-export-bevel-integration/97-RESEARCH.md
@.planning/phases/97-healthkit-export-bevel-integration/97-01-SUMMARY.md

Key source references (read before touching):
- GooseSwift/HealthKitFullImporter.swift — requestAuthorization pattern, HKHealthStore creation, #if canImport(HealthKit) guard structure
- GooseSwift/GooseAppModel+Upload.swift — ble.record(level:.error, source:title:body:) exact call signature
- GooseSwift/GooseRustBridge.swift — requestAsync method signature and return type
- GooseSwift.xcodeproj/project.pbxproj — find PBXFileReference entry for HealthKitFullImporter.swift and copy UUID pattern for the new file
- GooseSwift/GooseAppModel+SleepSync.swift — overnightWindow() return type, dbPath pattern, deviceId pattern (for exportAfterSleepSync parameter shapes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GooseHealthKitExporter.swift with requestAuthorization and all four write helpers (D-05, D-06, D-10, D-11, HK-01..HK-05)</name>
  <files>GooseSwift/GooseHealthKitExporter.swift</files>
  <read_first>
    - GooseSwift/HealthKitFullImporter.swift — read entire file; extract: (a) exact requestAuthorization continuation pattern, (b) HKHealthStore creation pattern, (c) #if canImport(HealthKit) guard structure
    - GooseSwift/GooseRustBridge.swift — read requestAsync signature (return type is [String: Any] or similar — confirm exact return type)
    - GooseSwift/GooseAppModel+Upload.swift — read to find ble.record call; note exact parameter labels (level:, source:, title:, body:)
    - GooseSwift/HealthDataStore+Sleep.swift — read to see how existing HK sleep import uses HKCategoryValueSleepAnalysis (confirm enum case name for iOS 26)
  </read_first>
  <action>
    Create GooseSwift/GooseHealthKitExporter.swift. Entire file content wrapped in #if canImport(HealthKit) ... #endif at top level.

    Imports (inside the guard): Foundation, HealthKit.

    Declare as: enum GooseHealthKitExporter (caseless enum acts as namespace, per Swift convention — no instance state needed; all methods are static).

    Static property: static let exportEnabledKey = "goose.healthkit.export.enabled"

    Static computed property: static var isExportEnabled: Bool { UserDefaults.standard.bool(forKey: exportEnabledKey) }

    Method 1 — requestAuthorization() async throws:
    - Create HKHealthStore()
    - shareTypes: Set&lt;HKSampleType&gt; = [HKQuantityType(.heartRate), HKQuantityType(.heartRateVariabilitySDNN), HKQuantityType(.oxygenSaturation), HKCategoryType(.sleepAnalysis)]
    - Use withCheckedThrowingContinuation exactly as in HealthKitFullImporter.swift (copy the pattern; do not invent a different async wrapper)
    - No read types requested (read: [] — Goose already has read permission from existing import flows; sharing only)

    Method 2 — exportAfterSleepSync(dbPath: String, deviceId: String, startTs: Double, endTs: Double, bridge: GooseRustBridge, logError: @escaping (String, String) -> Void) async:
    - Guard: isExportEnabled else return
    - Guard: HKHealthStore.isHealthDataAvailable() else return
    - Let hkStore = HKHealthStore()
    - Run four export sub-calls in sequence (not parallel — simpler, and HK write latency is negligible):

      (a) writeHeartRate: call bridge requestAsync for "store.hr_samples_between" with args database_path, device_id, start_ts, end_ts. Parse result["rows"] as [[String:Any]]. Build [HKQuantitySample] where each row's "bpm" becomes HKQuantity(unit: HKUnit.count().unitDivided(by: .minute()), doubleValue: Double(bpm)), start = end = Date(timeIntervalSince1970: row["ts"] as? Double ?? 0). Call hkStore.save(_ hrSamples as [HKObject]) in a do/catch; on error call logError("heartRate", "\(error)").

      (b) writeHRV: call bridge requestAsync for "metrics.daily_recovery_metrics" with args database_path, device_id (check actual arg name for this method — read RESEARCH.md and existing metrics bridge calls in HealthDataStore+*.swift to confirm field names), start_time_unix_ms: Int64(startTs * 1000), end_time_unix_ms: Int64(endTs * 1000). Parse rows. For each row where hrv_rmssd_ms != nil/0: build HKQuantitySample with type .heartRateVariabilitySDNN. IMPORTANT — verify HK unit during implementation: use HKUnit.secondUnit(with: .milli) with rmssd_ms value directly (i.e. pass rmssdMs to HKQuantity without dividing by 1000, using the millisecond unit). If that unit is unavailable at compile time, fall back to seconds by dividing by 1000. Use start = end = Date(timeIntervalSince1970: startTs) or midnight of date_key if parseable. Save batch. do/catch with logError("hrv", "\(error)").

      (c) writeSpO2: call bridge for "store.spo2_samples_between" with database_path, device_id, start_ts, end_ts. Parse result["rows"]. Each row has "ts" and "spo2_percent". HK oxygenSaturation expects a fraction [0.0, 1.0] — divide spo2_percent by 100.0. HKUnit is HKUnit.percent(). Build HKQuantitySample array. Save batch. do/catch with logError("oxygenSaturation", "\(error)").

      (d) writeSleep: call bridge for "store.external_sleep_sessions_between" with database_path, start_time_unix_ms: Int64(startTs * 1000), end_time_unix_ms: Int64(endTs * 1000). Parse result["rows"]. Each row: HKCategorySample(type: HKCategoryType(.sleepAnalysis), value: HKCategoryValueSleepAnalysis.asleepUnspecified.rawValue, start: Date(timeIntervalSince1970: Double(startMs) / 1000.0), end: Date(timeIntervalSince1970: Double(endMs) / 1000.0)). Save batch. do/catch with logError("sleepAnalysis", "\(error)").

    Error convention (D-11): logError closure receives (typeLabel: String, errorDescription: String) and is called by the caller (GooseAppModel) which owns the ble.record reference.

    Do NOT call requestAsync on @MainActor — exportAfterSleepSync is called from GooseAppModel which already dispatches bridge calls on a background Task (see GooseAppModel+SleepSync.swift pattern with localRust.requestAsync).

    Keep each write helper as a private static func within the enum for readability (writeHeartRateSamples, writeHRVSamples, writeSpO2Samples, writeSleepSessions — each takes hkStore, bridge, dbPath, deviceId, startTs, endTs, logError).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination "platform=iOS Simulator,name=iPhone 16" build 2>&1 | grep -E "^.*error:.*GooseHealthKitExporter|BUILD SUCCEEDED|BUILD FAILED" | tail -5</automated>
  </verify>
  <done>
    - GooseHealthKitExporter.swift compiles without errors
    - enum GooseHealthKitExporter has requestAuthorization() and exportAfterSleepSync() as public static methods
    - All four data types have write paths (HR, HRV, SpO2, sleep)
    - Every HKHealthStore.save() is in a do/catch with logError on failure (D-11)
    - SpO2 is written as fraction (spo2_percent / 100.0), not raw percentage
    - Sleep uses HKCategoryValueSleepAnalysis.asleepUnspecified
    - HRV unit resolved and correct (no 1000x scaling error)
    - Entire file content inside #if canImport(HealthKit)
  </done>
</task>

<task type="auto">
  <name>Task 2: Register GooseHealthKitExporter.swift in project.pbxproj (Pitfall 7, HK-01..HK-05)</name>
  <files>GooseSwift.xcodeproj/project.pbxproj</files>
  <read_first>
    - GooseSwift.xcodeproj/project.pbxproj — grep for "HealthKitFullImporter.swift" to find the exact PBXFileReference and PBXBuildFile UUID lines; also find the GooseSwift group children list where the file reference is added
  </read_first>
  <action>
    Add GooseHealthKitExporter.swift to project.pbxproj in three places, following the exact format used for HealthKitFullImporter.swift (found via grep):

    1. PBXBuildFile section: add a new line with a new UUID (generate a valid 24-hex-char Xcode UUID — use the same UUID length/format as surrounding entries, e.g. A1B2C3D4E5F6A1B2C3D4E5F6) referencing the PBXFileReference UUID.
       Format: {BUILD_UUID} /* GooseHealthKitExporter.swift in Sources */ = {isa = PBXBuildFile; fileRef = {FILE_UUID} /* GooseHealthKitExporter.swift */; };

    2. PBXFileReference section: add a new entry.
       Format: {FILE_UUID} /* GooseHealthKitExporter.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = GooseHealthKitExporter.swift; sourceTree = "&lt;group&gt;"; };

    3. GooseSwift group children list: add {FILE_UUID} /* GooseHealthKitExporter.swift */ in alphabetical order among the other GooseHealth* file entries (GooseHealthKitExporter.swift sorts near GooseHealthDataStore-related files — find the right position alphabetically).

    Use the same UUID format length (24 hex chars) as existing entries in the file. Pick UUIDs that do not conflict with existing ones — increment from a known existing UUID or use a clearly distinct prefix.

    After editing, build must succeed (verified in the automated check).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination "platform=iOS Simulator,name=iPhone 16" build 2>&1 | grep -E "GooseHealthKitExporter|BUILD SUCCEEDED|BUILD FAILED" | tail -5</automated>
    <automated>grep -c "GooseHealthKitExporter.swift" /Users/francisco/Documents/goose/GooseSwift.xcodeproj/project.pbxproj</automated>
  </verify>
  <done>
    - GooseHealthKitExporter.swift appears 3 times in project.pbxproj (PBXBuildFile + PBXFileReference + group children)
    - xcodebuild succeeds — file is compiled as part of GooseSwift target
    - grep count == 3
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge→Swift | requestAsync returns [String:Any]; all numeric casts use optional binding (as? Double, as? Int) with defaults — no force-unwrap |
| Swift→HKHealthStore | Values written are computed/validated before HK save — SpO2 divided to fraction, HRV unit resolved, sleep times validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-06 | Tampering | HRV unit mismatch | mitigate | Verify HKUnit for heartRateVariabilitySDNN during implementation against Apple docs; add inline comment with confirmed unit |
| T-97-07 | Tampering | SpO2 range | mitigate | Divide spo2_percent by 100.0 before write; skip samples where result is outside [0.0, 1.0] |
| T-97-08 | DoS | HK write failure crashing app | mitigate | All save() calls in do/catch per D-11; errors logged, app continues |
| T-97-09 | Information Disclosure | Raw PPG counts in HK | mitigate | store.spo2_samples_between (97-01) returns computed spo2_percent only; no red/ir exposed to Swift or HK |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No new packages; Swift-only additions |
</threat_model>

<verification>
Build check: xcodebuild succeeds with GooseHealthKitExporter.swift compiled.

Manual simulator UAT (deferred to 97-03 plan, which wires the trigger):
- Enable toggle → authorize → complete sync → verify HR/HRV/SpO2/sleep appear in Health app
</verification>

<success_criteria>
- GooseHealthKitExporter.swift builds with no errors or warnings
- Registered in project.pbxproj (3 entries)
- requestAuthorization() requests share for all four HK types
- exportAfterSleepSync() calls all four write helpers and logs errors non-fatally
- No force-unwrap on bridge response values
- SpO2 written as fraction, not percentage
- Sleep uses non-deprecated asleepUnspecified value
</success_criteria>

<output>
Create .planning/phases/97-healthkit-export-bevel-integration/97-02-SUMMARY.md when done
</output>
