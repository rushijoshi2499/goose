---
phase: "97"
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/MoreView.swift
autonomous: true
requirements:
  - HK-05

must_haves:
  truths:
    - "MoreView Section('Apple Health') contains a Toggle labelled 'Export WHOOP data to Health' (default off)"
    - "Toggle uses @AppStorage('goose.healthkit.export.enabled') persisted to UserDefaults"
    - "Toggling on calls GooseHealthKitExporter.requestAuthorization(); toggling off does not"
    - "Permission denied turns the toggle back off and logs an error via ble.record"
    - "Toggle is only shown on devices where HKHealthStore.isHealthDataAvailable() == true"
  artifacts:
    - path: "GooseSwift/MoreView.swift"
      provides: "HK export toggle in Section Apple Health, @AppStorage key goose.healthkit.export.enabled"
  key_links:
    - from: "GooseSwift/MoreView.swift"
      to: "GooseSwift/GooseHealthKitExporter.swift"
      via: "onChange(of: exportEnabled) calls Task { await model.enableHealthKitExport() } when newValue == true (per D-09)"
      pattern: "enableHealthKitExport|exportEnabled"
---

<objective>
Add the HealthKit export toggle to MoreView Section("Apple Health") per D-08 and D-09. This plan is Wave 1 (no bridge dependency) — it only adds the SwiftUI toggle and wires it to a not-yet-existing method on GooseAppModel that Plan 97-03 will provide.

Purpose: HK-05 requires an opt-in toggle in More settings. Default off. Persist with @AppStorage key "goose.healthkit.export.enabled". Per D-09, toggling on requests HK authorization via GooseHealthKitExporter (the call site is defined here; the method body is in 97-02 and 97-03). Per D-07, permission denial turns the toggle back off with an error log.

Output: MoreView.swift modified to include the export toggle inside the existing Apple Health section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-healthkit-export-bevel-integration/97-CONTEXT.md
@.planning/phases/97-healthkit-export-bevel-integration/97-RESEARCH.md

Key source references (read before touching):
- GooseSwift/MoreView.swift — full file; Section("Apple Health") at lines 51-74; @AppStorage pattern at lines 15-19
- GooseSwift/GooseAppModel+Upload.swift — ble.record(level:.error, ...) pattern to reuse for permission denial logging
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HK export toggle to MoreView Section("Apple Health") (D-08, D-09, HK-05)</name>
  <files>GooseSwift/MoreView.swift</files>
  <read_first>
    - GooseSwift/MoreView.swift — read the full file; note Section("Apple Health") block (lines 51-74); note @AppStorage declarations (lines 15-19); note existing isImportingSleep @State pattern
    - GooseSwift/GooseAppModel+Upload.swift — find ble.record(level:.error, source:title:body:) call signature to confirm parameter labels
    - GooseSwift/HealthKitFullImporter.swift — read to understand requestAuthorization pattern and how permission denial is currently detected (HKHealthStore.authorizationStatus or the continuation error)
  </read_first>
  <action>
    Add to MoreView — three changes, each a targeted Edit:

    1. Add @AppStorage property declaration for the export toggle in the property block alongside the other @AppStorage declarations:

       @AppStorage("goose.healthkit.export.enabled") private var hkExportEnabled = false

    2. Inside Section("Apple Health"), after the existing Button and Text blocks, add a conditional block guarded by HKHealthStore.isHealthDataAvailable():

       The Toggle text is "Export WHOOP data to Health" with systemImage "heart.text.clipboard" (or "waveform.path.ecg" — pick whichever is available in SF Symbols; "heart.text.clipboard" is iOS 17+). Label uses Label("Export WHOOP data to Health", systemImage: ...).

       Toggle binding: isOn: $hkExportEnabled.

       onChange(of: hkExportEnabled) { _, newValue in
         if newValue {
           Task { await model.enableHealthKitExport() }
         }
       }

       The isHealthDataAvailable() guard: wrap the Toggle in an `if HKHealthStore.isHealthDataAvailable()` block so it does not appear on simulators or iPad without Health data. Keep this inside the `#if canImport(HealthKit)` conditional — Section("Apple Health") already has the canImport guard via the file-level HealthKit import (lines 6-8). If the section body is not already inside a canImport guard, add one around just the new Toggle (not the whole section).

    3. GooseAppModel.enableHealthKitExport() will be added in 97-03-PLAN. For this plan, the call site references a not-yet-existing method. To allow this plan to compile independently before 97-03 lands, add a stub extension in MoreView.swift's file scope (or in GooseAppModel) as follows: check if GooseAppModel already has enableHealthKitExport — if not, add a minimal stub in GooseAppModel that returns immediately (`func enableHealthKitExport() async {}`). This stub will be replaced by 97-03 with full implementation. Document it with a comment: "// Stub — replaced by 97-03".

    Do NOT add a duplicate HKHealthStore import — it is already guarded at file scope. Do NOT call requestAuthorization directly from MoreView — the call must go through model.enableHealthKitExport() to stay off @MainActor inline (per anti-pattern in RESEARCH.md).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination "platform=iOS Simulator,name=iPhone 16" build 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -5</automated>
  </verify>
  <done>
    - MoreView.swift compiles without errors
    - @AppStorage("goose.healthkit.export.enabled") property exists in MoreView
    - Section("Apple Health") contains a Toggle with onChange that calls model.enableHealthKitExport() when newValue == true (per D-09)
    - Toggle is guarded by HKHealthStore.isHealthDataAvailable() (per anti-pattern avoidance)
    - No requestAuthorization called directly from MoreView (per architectural constraint)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User→Toggle | User explicitly opts in; no HK data written without this toggle being on |
| Toggle→UserDefaults | @AppStorage persists the boolean; no server-side state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04 | DoS | requestAuthorization loop | mitigate | onChange only fires when newValue == true — turning off does not re-request auth (per D-09, Pitfall 6 in RESEARCH.md) |
| T-97-05 | Information Disclosure | HK permission on unsupported devices | mitigate | Guarded by HKHealthStore.isHealthDataAvailable() — toggle hidden on non-HealthKit targets |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No new packages; Swift-only change |
</threat_model>

<verification>
Build succeeds. Manually verify in simulator (Wave 3 UAT):
1. Navigate More → Apple Health section
2. Toggle "Export WHOOP data to Health" appears and is off by default
3. Toggling on shows system HealthKit authorization sheet
4. Toggling off does not show the sheet again
</verification>

<success_criteria>
- MoreView compiles clean
- @AppStorage("goose.healthkit.export.enabled") = false by default
- Toggle onChange calls model.enableHealthKitExport() only when newValue == true
- HKHealthStore.isHealthDataAvailable() guard prevents toggle on simulator/iPad without Health
</success_criteria>

<output>
Create .planning/phases/97-healthkit-export-bevel-integration/97-04-SUMMARY.md when done
</output>
