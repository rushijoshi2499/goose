---
phase: 93-hr-data-investigation-protocol-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 93-02
files_modified:
  - Rust/core/src/protocol.rs
  - Rust/core/src/capture_correlation.rs
  - Rust/core/src/export.rs
  - Rust/core/src/bridge/mod.rs
  - Rust/core/tests/protocol_tests.rs
  - Rust/core/tests/bridge_tests.rs
autonomous: true
requirements:
  - PROTO-09
  - PROTO-10
  - PROTO-11
must_haves:
  truths:
    - "parse_data_packet_body_summary has no bare _ => (None, Vec::new()) wildcard — PROTO-09"
    - "Unknown { packet_k } arm in parse_data_packet_body_summary produces a warning string — PROTO-09"
    - "DataPacketBodySummary::Unknown variant has arms in capture_correlation.rs and export.rs — compile gate"
    - "data_packet_domain values without explicit parse arms are covered by the Unknown catch-all — PROTO-10"
    - "commands_definitions_serialises_without_error test passes in bridge/mod.rs — PROTO-11"
  artifacts:
    - path: "Rust/core/src/protocol.rs"
      provides: "DataPacketBodySummary::Unknown variant; parse_data_packet_body_summary wildcard replaced"
      contains: "Unknown { packet_k"
    - path: "Rust/core/src/capture_correlation.rs"
      provides: "Unknown arm in body_summary_kind fn"
      contains: "Unknown"
    - path: "Rust/core/src/export.rs"
      provides: "Unknown arm (graceful skip)"
      contains: "Unknown"
    - path: "Rust/core/src/bridge/mod.rs"
      provides: "commands_definitions_serialises_without_error test"
      contains: "commands_definitions_serialises_without_error"
  key_links:
    - from: "parse_data_packet_body_summary (protocol.rs:641)"
      to: "DataPacketBodySummary::Unknown"
      via: "wildcard arm replacement producing warning string"
      pattern: "Unknown.*packet_k"
    - from: "bridge/mod.rs test"
      to: "commands.rs COMMAND_DEFINITIONS"
      via: "serde_json serialisation assertion"
      pattern: "commands_definitions_serialises_without_error"
---

<objective>
Three related protocol reliability improvements (PROTO-09, PROTO-10, PROTO-11):

PROTO-09: Replace the `_ => (None, Vec::new())` wildcard in parse_data_packet_body_summary with
`Unknown { packet_k }` — unhandled packet_k values produce a visible warning instead of a silent drop.

PROTO-10: Verify (and if needed confirm) that data_packet_domain() values lacking explicit parse arms
are automatically routed to the Unknown catch-all from PROTO-09. No explicit stub arms needed if
the Unknown wildcard already covers them (see RESEARCH.md Pitfall 5).

PROTO-11: Add a test in bridge/mod.rs asserting that COMMAND_DEFINITIONS serialises cleanly to JSON
and is non-empty — compile-time parity check matching the existing bridge_methods_constant_matches_dispatcher
pattern (per D-11).

Depends on 93-02 (PacketType enum) because DataPacketBodySummary::Unknown is a new variant requiring
exhaustive match updates in capture_correlation.rs and export.rs — those files already have exhaustive
matches on DataPacketBodySummary.

Output: protocol.rs with Unknown variant; capture_correlation.rs and export.rs with Unknown arms;
bridge/mod.rs with COMMAND_DEFINITIONS registry test; targeted tests for unknown_packet_k_warning and
commands_definitions_serialises_without_error.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-hr-data-investigation-protocol-cleanup/93-CONTEXT.md
@.planning/phases/93-hr-data-investigation-protocol-cleanup/93-RESEARCH.md
@.planning/phases/93-hr-data-investigation-protocol-cleanup/93-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DataPacketBodySummary::Unknown variant and replace wildcard in parse_data_packet_body_summary (PROTO-09 + PROTO-10)</name>
  <files>
    Rust/core/src/protocol.rs,
    Rust/core/src/capture_correlation.rs,
    Rust/core/src/export.rs
  </files>
  <read_first>
    - Rust/core/src/protocol.rs lines 183–253 (DataPacketBodySummary enum — read all current variants before adding Unknown)
    - Rust/core/src/protocol.rs lines 641–667 (parse_data_packet_body_summary — the wildcard to replace)
    - Rust/core/src/protocol.rs lines 1147–1170 (data_packet_domain — the 7 gap packet_k values: 11, 16, 19, 20, 22, 25, 26)
    - Rust/core/src/capture_correlation.rs lines 666–678 (body_summary_kind exhaustive match — must add Unknown arm)
    - Rust/core/src/export.rs lines 2310–2405 (DataPacketBodySummary exhaustive match — must add Unknown arm)
  </read_first>
  <behavior>
    - Test fn unknown_packet_k_produces_warning: calls parse_data_packet_body_summary with a payload and
      packet_k = 99 (unhandled), asserts result is (Some(DataPacketBodySummary::Unknown { packet_k: 99 }),
      warnings) where warnings contains "unhandled_packet_k_99".
    - Test fn unknown_packet_k_body_summary_kind: asserts body_summary_kind(&DataPacketBodySummary::Unknown { packet_k: 11 }) == "unknown".
    - Test fn domain_gap_packet_ks_produce_unknown: for each of packet_k 11, 16, 19, 20, 22, 25, 26 (domain-listed
      but no named parse arm), calls parse_data_packet_body_summary and asserts Unknown variant is returned
      (not None) — this validates PROTO-10 is satisfied by PROTO-09's catch-all.
  </behavior>
  <action>
    IMPORTANT — complete all three file edits before the first cargo build (per the all-in-one-pass rule
    for DataPacketBodySummary variants; see RESEARCH.md Pitfall 2).

    Step 1 — Add Unknown variant to DataPacketBodySummary enum in protocol.rs.
    Add as the LAST variant (after V18History, before the closing brace):
      Unknown {
          packet_k: u8,
      },
    The enum already derives Serialize, Deserialize with serde(tag = "kind", rename_all = "snake_case").
    Unknown will serialise as { "kind": "unknown", "packet_k": N } — correct.

    Step 2 — Replace the wildcard in parse_data_packet_body_summary (protocol.rs line 665):
      OLD: _ => (None, Vec::new()),
      NEW: _ => (
               Some(DataPacketBodySummary::Unknown { packet_k }),
               vec![format!("unhandled_packet_k_{packet_k}")],
           ),
    (packet_k is already in scope as a u8 from the outer Some binding at line 647.)

    Step 3 — PROTO-10 verification: after Step 2, the 7 gap packet_k values (11, 16, 19, 20, 22, 25, 26)
    that appear in data_packet_domain() are automatically handled by the Unknown catch-all. No explicit
    stub arms are needed. Confirm by adding the domain_gap_packet_ks_produce_unknown test (Task 2).
    Do NOT add unreachable stub arms — they would cause "unreachable pattern" compiler warnings.

    Step 4 — Add Unknown arm to body_summary_kind in capture_correlation.rs (line 677, after V18History arm):
      DataPacketBodySummary::Unknown { .. } => "unknown",
    This keeps the match exhaustive after the new variant is added.

    Step 5 — Add Unknown arm to the DataPacketBodySummary match in export.rs (after V18History arm at line 2402):
      DataPacketBodySummary::Unknown { .. } => {
          // Unknown packet_k — no sensor sample export path; skip gracefully.
      }
    This keeps the export match exhaustive.

    Step 6 — cargo build to confirm zero exhaustiveness errors. Then grep:
      grep -rn "DataPacketBodySummary::" Rust/core/src/ | grep -v "Unknown\|NormalHistory\|V18History\|R17\|RawMotion\|V24\|R22"
    Expect zero hits (confirms no other exhaustive match sites were missed). If any hits appear, add
    Unknown arms there too before proceeding.

    Do NOT modify metric_features.rs in this task — heart_rate_plan_from_row's existing _ => None wildcard
    correctly covers Unknown (it is not an HR source). The run_heart_rate_feature_report trusted_frames
    list already excludes "unknown" (no change needed).
  </action>
  <verify>
    <automated>cargo build --locked --manifest-path Rust/core/Cargo.toml 2>&1 | grep -E "^error" | head -10</automated>
  </verify>
  <done>
    - DataPacketBodySummary::Unknown { packet_k: u8 } exists in protocol.rs
    - parse_data_packet_body_summary wildcard arm returns Some(Unknown { packet_k }) with warning string
    - Unknown arm present in capture_correlation.rs body_summary_kind fn
    - Unknown arm present in export.rs DataPacketBodySummary match
    - cargo build --locked passes with zero errors
    - grep for DataPacketBodySummary match sites outside the known files shows zero unhandled matches
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unknown_packet_k_warning test (PROTO-09) and COMMAND_DEFINITIONS registry test (PROTO-11)</name>
  <files>
    Rust/core/tests/protocol_tests.rs,
    Rust/core/src/bridge/mod.rs
  </files>
  <read_first>
    - Rust/core/tests/protocol_tests.rs (first 40 lines — import style; last 40 lines — find insertion point)
    - Rust/core/src/bridge/mod.rs lines 955–1043 (bridge_methods_constant_matches_dispatcher — model for D-11 test)
    - Rust/core/src/bridge/debug.rs lines 1899–1910 (commands.definitions arm confirming COMMAND_DEFINITIONS is used)
    - Rust/core/src/commands.rs lines 25 and 534 (CommandDefinition struct and COMMAND_DEFINITIONS const)
  </read_first>
  <behavior>
    PROTO-09 tests (in protocol_tests.rs):
    - Test fn unknown_packet_k_warning: constructs a minimal payload, calls parse_data_packet_body_summary
      with packet_k = Some(99), asserts result.0 == Some(DataPacketBodySummary::Unknown { packet_k: 99 })
      and result.1 contains "unhandled_packet_k_99".
    - Test fn unknown_packet_k_body_summary_kind: constructs DataPacketBodySummary::Unknown { packet_k: 11 }
      and checks that the serde "kind" tag serialises as "unknown".

    PROTO-11 test (in bridge/mod.rs, inside the existing #[cfg(test)] mod tests block):
    - Test fn commands_definitions_serialises_without_error: calls serde_json::to_value(COMMAND_DEFINITIONS),
      asserts Ok result, asserts the JSON value is a non-empty array. This confirms COMMAND_DEFINITIONS
      is in sync with the "commands.definitions" dispatch arm (which directly serialises it).
  </behavior>
  <action>
    PROTO-09 — protocol_tests.rs:
    Add two test functions with the unknown_packet_k prefix. Note that parse_data_packet_body_summary is
    a private fn in protocol.rs — it cannot be called directly from an integration test. Instead, test
    via the public parse_frame or parse_hex_frame functions that exercise the same path. Alternatively,
    if the function is accessible via cfg(test) re-export, use that. If neither works, test the Observable
    behaviour via round-trip: build a synthetic frame hex with packet_k = 99 in the data packet body,
    call parse_hex_frame, and assert the resulting body_summary is Unknown { packet_k: 99 }.
    The serde serialisation test can use serde_json::to_value directly on a constructed Unknown struct.
    Name both functions with the unknown_packet_k prefix for the cargo test filter.

    PROTO-11 — bridge/mod.rs:
    Inside the existing #[cfg(test)] mod tests block (after the bridge_methods_constant_matches_dispatcher
    test, around line 1044), add:

      #[test]
      fn commands_definitions_serialises_without_error() {
          use crate::commands::COMMAND_DEFINITIONS;
          let value = serde_json::to_value(COMMAND_DEFINITIONS)
              .expect("COMMAND_DEFINITIONS must serialise to JSON without error");
          let arr = value.as_array()
              .expect("COMMAND_DEFINITIONS must serialise as a JSON array");
          assert!(
              !arr.is_empty(),
              "COMMAND_DEFINITIONS must not be empty"
          );
      }

    This test directly mirrors the "commands.definitions" bridge dispatch arm (debug.rs line 1904)
    that calls serde_json::to_value(COMMAND_DEFINITIONS). If that arm ever breaks (wrong type, missing
    Serialize impl), this test fails at compile time / test time — satisfying D-11 compile-time preference.
    The existing "commands.definitions" arm is already in BRIDGE_METHODS (verified by the
    bridge_methods_constant_matches_dispatcher test), so parity is already asserted at the routing level.
    This new test adds the serialisation-correctness assertion on top.
  </action>
  <verify>
    <automated>cargo test --locked --manifest-path Rust/core/Cargo.toml -- unknown_packet_k commands_definitions_serialises_without_error 2>&1 | tail -15</automated>
  </verify>
  <done>
    - unknown_packet_k_warning and unknown_packet_k_body_summary_kind tests pass in protocol_tests.rs
    - commands_definitions_serialises_without_error test passes in bridge/mod.rs
    - cargo test -- unknown_packet_k shows 2 passed, 0 failed
    - cargo test -- commands_definitions_serialises_without_error shows 1 passed, 0 failed
    - Full regression: cargo test --locked --manifest-path Rust/core/Cargo.toml passes (timeout 300000ms)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parse_data_packet_body_summary wildcard | BLE packet bytes with unhandled packet_k now produce Unknown instead of silent None |
| COMMAND_DEFINITIONS serialisation | Registry integrity asserted by test — mismatch caught at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03-01 | Information Disclosure | Unknown { packet_k } warning string logged | accept | Warning string contains only the packet_k byte value; no user PII in BLE packet type bytes |
| T-93-03-02 | Tampering | DataPacketBodySummary exhaustive match | mitigate | All known downstream match sites updated before first cargo build (cs rule s1-143 pattern) |
| T-93-03-03 | Denial of Service | COMMAND_DEFINITIONS serialisation failure at bridge dispatch | mitigate | commands_definitions_serialises_without_error test catches this at test time |
| T-93-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; source-only changes |
</threat_model>

<verification>
1. `grep -n "Unknown.*packet_k" Rust/core/src/protocol.rs` — shows Unknown variant and the wildcard replacement
2. `grep -n "Unknown" Rust/core/src/capture_correlation.rs` — shows Unknown arm in body_summary_kind
3. `grep -n "Unknown" Rust/core/src/export.rs` — shows Unknown arm in DataPacketBodySummary match
4. `cargo build --locked --manifest-path Rust/core/Cargo.toml` — passes
5. `cargo test --locked --manifest-path Rust/core/Cargo.toml -- unknown_packet_k` — 2 passed
6. `cargo test --locked --manifest-path Rust/core/Cargo.toml -- commands_definitions_serialises_without_error` — 1 passed
7. Full regression: `cargo test --locked --manifest-path Rust/core/Cargo.toml` — all pass (timeout 300000ms)
</verification>

<success_criteria>
- PROTO-09: No silent drop in parse_data_packet_body_summary; Unknown variant produces observable warning
- PROTO-10: data_packet_domain gap values (11, 16, 19, 20, 22, 25, 26) are covered by Unknown catch-all (confirmed by domain_gap_packet_ks_produce_unknown test)
- PROTO-11: COMMAND_DEFINITIONS registry sync test passes; serialisation correctness asserted
- All downstream exhaustive matches updated (capture_correlation.rs, export.rs)
- Full Rust test suite green
</success_criteria>

<output>
Create `.planning/phases/93-hr-data-investigation-protocol-cleanup/93-03-SUMMARY.md` when done.
</output>
