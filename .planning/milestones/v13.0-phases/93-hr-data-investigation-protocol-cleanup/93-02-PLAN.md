---
phase: 93-hr-data-investigation-protocol-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/protocol.rs
  - Rust/core/tests/protocol_tests.rs
autonomous: true
requirements:
  - PROTO-08
must_haves:
  truths:
    - "PacketType enum with 17 named variants + Unknown(u8) exists in protocol.rs"
    - "From<u8> for PacketType and From<PacketType> for u8 are implemented (infallible, no TryFrom)"
    - "All 17 PACKET_TYPE_* constants are deleted from protocol.rs — zero grep hits"
    - "All 5 match sites in protocol.rs use PacketType variants, not u8 literals"
    - "cargo build succeeds after deletion (compiler exhaustiveness check passes)"
    - "packet_type_from_u8 round-trip test passes"
  artifacts:
    - path: "Rust/core/src/protocol.rs"
      provides: "PacketType enum replacing 17 PACKET_TYPE_* constants; From<u8>/From<PacketType> for u8"
      contains: "enum PacketType"
    - path: "Rust/core/tests/protocol_tests.rs"
      provides: "packet_type_from_u8 round-trip test"
      contains: "packet_type_from_u8"
  key_links:
    - from: "protocol.rs build_v5_command_frame (line 452)"
      to: "PacketType::Command"
      via: "u8::from(PacketType::Command) replaces PACKET_TYPE_COMMAND literal"
      pattern: "PacketType::Command"
    - from: "protocol.rs packet_type_name (line 474)"
      to: "PacketType match"
      via: "fn signature changes to take PacketType, not u8"
      pattern: "fn packet_type_name"
---

<objective>
Introduce the PacketType enum in protocol.rs (per D-01..D-05), delete all 17 PACKET_TYPE_* u8
constants, and migrate the 5 match sites to use the enum. Compiler exhaustiveness replaces the old
wildcard fallbacks at every call site.

Purpose: Named enum variants give the compiler exhaustiveness at every packet-type dispatch point,
preventing future firmware packet types from being silently ignored without a deliberate Unknown arm.

Output: protocol.rs with PacketType enum + From impls; zero PACKET_TYPE_* references; 5 migrated
match sites; a round-trip unit test in protocol_tests.rs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-hr-data-investigation-protocol-cleanup/93-CONTEXT.md
@.planning/phases/93-hr-data-investigation-protocol-cleanup/93-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce PacketType enum + From impls; delete 17 PACKET_TYPE_* constants (D-01..D-05)</name>
  <files>Rust/core/src/protocol.rs</files>
  <read_first>
    - Rust/core/src/protocol.rs lines 1–25 (the 17 constants to delete; also FRAME_START and COMMAND_GET_HELLO which must NOT be deleted)
    - Rust/core/src/protocol.rs lines 451–455 (build_v5_command_frame — write path using PACKET_TYPE_COMMAND)
    - Rust/core/src/protocol.rs lines 474–495 (packet_type_name — 17-arm u8 match to migrate)
    - Rust/core/src/protocol.rs lines 514–536 (parse_payload — 6-group u8 match to migrate)
    - Rust/core/src/protocol.rs lines 538–547 (is_partial_data_packet_type_allowed — matches! macro to migrate)
    - Rust/core/src/protocol.rs lines 497–501 (packet_type_debug_name — calls packet_type_name(u8); must update call site)
  </read_first>
  <action>
    Step 1 — Add PacketType enum immediately after the `pub const FRAME_START: u8 = 0xaa;` line and before
    the existing constant block. The enum must have exactly these 17 named variants (per D-01 + RESEARCH.md
    Pattern 1) plus Unknown(u8):

      #[derive(Debug, Clone, Copy, PartialEq, Eq)]
      pub enum PacketType {
          Command,                        // 35
          CommandResponse,                // 36
          PuffinCommand,                  // 37
          PuffinCommandResponse,          // 38
          RealtimeData,                   // 40
          RealtimeRawData,                // 43
          HistoricalData,                 // 47
          Event,                          // 48
          Metadata,                       // 49
          ConsoleLogs,                    // 50
          RealtimeImuDataStream,          // 51
          HistoricalImuDataStream,        // 52
          RelativePuffinEvents,           // 53
          PuffinEventsFromStrap,          // 54
          RelativeBatteryPackConsoleLogs, // 55
          PuffinMetadata,                 // 56
          R22RealtimeData,               // 16 (0x10) — WHOOP 5.0 BLE handle 0x0022
          Unknown(u8),                    // catch-all: firmware-added or unrecognised values
      }

    Do NOT use #[repr(u8)] — Unknown(u8) is a tuple variant, incompatible with repr(u8) (per D-04 note in
    RESEARCH.md Anti-Patterns).

    Step 2 — Implement From<u8> for PacketType (infallible, per D-04):
      impl From<u8> for PacketType {
          fn from(byte: u8) -> Self {
              match byte {
                  35 => PacketType::Command,
                  36 => PacketType::CommandResponse,
                  37 => PacketType::PuffinCommand,
                  38 => PacketType::PuffinCommandResponse,
                  40 => PacketType::RealtimeData,
                  43 => PacketType::RealtimeRawData,
                  47 => PacketType::HistoricalData,
                  48 => PacketType::Event,
                  49 => PacketType::Metadata,
                  50 => PacketType::ConsoleLogs,
                  51 => PacketType::RealtimeImuDataStream,
                  52 => PacketType::HistoricalImuDataStream,
                  53 => PacketType::RelativePuffinEvents,
                  54 => PacketType::PuffinEventsFromStrap,
                  55 => PacketType::RelativeBatteryPackConsoleLogs,
                  56 => PacketType::PuffinMetadata,
                  0x10 => PacketType::R22RealtimeData,
                  other => PacketType::Unknown(other),
              }
          }
      }

    Step 3 — Implement From<PacketType> for u8 (round-trip for logging, per RESEARCH.md specifics):
      impl From<PacketType> for u8 {
          fn from(pt: PacketType) -> u8 {
              match pt {
                  PacketType::Command => 35,
                  PacketType::CommandResponse => 36,
                  PacketType::PuffinCommand => 37,
                  PacketType::PuffinCommandResponse => 38,
                  PacketType::RealtimeData => 40,
                  PacketType::RealtimeRawData => 43,
                  PacketType::HistoricalData => 47,
                  PacketType::Event => 48,
                  PacketType::Metadata => 49,
                  PacketType::ConsoleLogs => 50,
                  PacketType::RealtimeImuDataStream => 51,
                  PacketType::HistoricalImuDataStream => 52,
                  PacketType::RelativePuffinEvents => 53,
                  PacketType::PuffinEventsFromStrap => 54,
                  PacketType::RelativeBatteryPackConsoleLogs => 55,
                  PacketType::PuffinMetadata => 56,
                  PacketType::R22RealtimeData => 0x10,
                  PacketType::Unknown(b) => b,
              }
          }
      }

    Step 4 — Delete lines 7–23 (the 17 pub const PACKET_TYPE_* declarations). Do NOT delete FRAME_START
    (line 6) or COMMAND_GET_HELLO (line 24). After deletion, cargo build will fail until all 5 match sites
    are migrated — proceed immediately to Step 5.

    Step 5 — Migrate all 5 match sites (do all before first cargo build):

    5a. build_v5_command_frame (line 452):
        OLD: vec![PACKET_TYPE_COMMAND, sequence, command]
        NEW: vec![u8::from(PacketType::Command), sequence, command]

    5b. packet_type_name (line 474) — change signature and match to use PacketType:
        OLD: pub fn packet_type_name(packet_type: u8) -> Option<&'static str>
        NEW: pub fn packet_type_name(packet_type: PacketType) -> Option<&'static str>
        The match body replaces each PACKET_TYPE_* constant with the corresponding PacketType variant.
        Unknown(u8) maps to: _ => return None  (keep the existing None return for unknown types).
        Specifically:
          PacketType::Command => "COMMAND",
          PacketType::CommandResponse => "COMMAND_RESPONSE",
          ... (all 17 named variants with their existing string values)
          PacketType::Unknown(_) => return None,

    5c. packet_type_debug_name (line 497) — update call site since packet_type_name now takes PacketType:
        OLD: pub fn packet_type_debug_name(packet_type: u8) -> String {
             packet_type_name(packet_type)
        NEW: pub fn packet_type_debug_name(packet_type: u8) -> String {
             packet_type_name(PacketType::from(packet_type))
        (This function's public signature stays u8 — callers outside this file pass raw bytes.)

    5d. parse_payload (line 514) — change the local binding and match:
        OLD: let packet_type = *payload.first()?;
             match packet_type {
                 PACKET_TYPE_COMMAND | PACKET_TYPE_PUFFIN_COMMAND => ...
        NEW: let packet_type = PacketType::from(*payload.first()?);
             match packet_type {
                 PacketType::Command | PacketType::PuffinCommand => ...
             Map each group verbatim:
               Command | PuffinCommand => parse_command_payload
               CommandResponse | PuffinCommandResponse => parse_command_response_payload
               Event | RelativePuffinEvents | PuffinEventsFromStrap => parse_event_payload
               RealtimeData | RealtimeRawData | HistoricalData | RealtimeImuDataStream | HistoricalImuDataStream => parse_data_packet_payload
               R22RealtimeData => parse_r22_payload
               _ => ParsedPayload::Raw { ... }  (CommandResponse/PuffinCommandResponse,
               Metadata, ConsoleLogs, RelativeBatteryPackConsoleLogs, PuffinMetadata, Unknown(_))
        The wildcard arm in parse_payload returns ParsedPayload::Raw — keep that behaviour for all
        unhandled variants and Unknown.

    5e. is_partial_data_packet_type_allowed (line 538) — change the matches! macro:
        OLD: fn is_partial_data_packet_type_allowed(packet_type: u8) -> bool {
             matches!(packet_type, PACKET_TYPE_REALTIME_DATA | ...)
        NEW: fn is_partial_data_packet_type_allowed(packet_type: PacketType) -> bool {
             matches!(packet_type,
                 PacketType::RealtimeData
                 | PacketType::RealtimeRawData
                 | PacketType::HistoricalData
                 | PacketType::RealtimeImuDataStream
                 | PacketType::HistoricalImuDataStream
                 | PacketType::R22RealtimeData
             )
        Find the call site of is_partial_data_packet_type_allowed in parse_frame (around line 391) and
        update it: the caller passes `partial_packet_type: Option<u8>`, so map:
          partial_packet_type.is_some_and(|pt| is_partial_data_packet_type_allowed(PacketType::from(pt)))

    Also update the call to packet_type_name inside parse_frame (line 443):
        OLD: packet_type.and_then(packet_type_name).map(str::to_string)
        NEW: packet_type.map(|pt| PacketType::from(pt)).and_then(packet_type_name).map(str::to_string)
        (packet_type here is Option<u8> from the frame header)

    Step 6 — Run cargo build to confirm zero PACKET_TYPE_* references remain and all match arms are
    exhaustive. Expected: zero compile errors. If there are leftover references, grep for PACKET_TYPE_
    and fix each.

    FINAL CHECK: after build succeeds, run:
      grep -c "PACKET_TYPE_" Rust/core/src/protocol.rs
    Expected: 0 (zero — all constants deleted, per D-03).
  </action>
  <verify>
    <automated>grep -c "PACKET_TYPE_" Rust/core/src/protocol.rs; cargo build --locked --manifest-path Rust/core/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <done>
    - grep -c "PACKET_TYPE_" Rust/core/src/protocol.rs returns 0
    - PacketType enum with 17 named variants + Unknown(u8) exists
    - From<u8> for PacketType and From<PacketType> for u8 implemented
    - cargo build --locked passes with no errors
    - All 5 match sites use PacketType variants
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PacketType round-trip test to protocol_tests.rs</name>
  <files>Rust/core/tests/protocol_tests.rs</files>
  <read_first>
    - Rust/core/tests/protocol_tests.rs (read first 40 lines for import style and test module structure)
  </read_first>
  <behavior>
    - Test fn packet_type_from_u8_known_values: verifies that every known byte value round-trips through
      PacketType::from(byte) back to u8::from(PacketType::from(byte)) == byte for all 17 named values.
    - Test fn packet_type_from_u8_unknown_round_trips: verifies PacketType::from(99u8) == PacketType::Unknown(99)
      and u8::from(PacketType::Unknown(99)) == 99.
    - Test fn packet_type_from_u8_r22: verifies PacketType::from(0x10u8) == PacketType::R22RealtimeData.
  </behavior>
  <action>
    Add the three test functions to Rust/core/tests/protocol_tests.rs following the existing test style
    (use goose_core::protocol::PacketType or the appropriate crate path).
    Name all functions with the packet_type_from_u8 prefix so the cargo test filter targets them.
    No database or BLE fixtures needed — pure enum value tests.
    Keep the existing tests untouched.
  </action>
  <verify>
    <automated>cargo test --locked --manifest-path Rust/core/Cargo.toml -- packet_type_from_u8 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Three test functions named packet_type_from_u8_* exist in protocol_tests.rs
    - cargo test -- packet_type_from_u8 shows 3 passed, 0 failed
    - Full regression: cargo test --locked --manifest-path Rust/core/Cargo.toml passes (timeout 300000ms)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire byte → PacketType | Infallible From<u8>: unknown bytes map to Unknown(u8), never panic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02-01 | Tampering | PacketType::Unknown(u8) in match arms | accept | Unknown variant is explicit; callers handle it via wildcard or explicit Unknown arm |
| T-93-02-02 | Denial of Service | Panic on missing packet type | mitigate | From<u8> is infallible — no panic path; Unknown(u8) is the safe catch-all |
| T-93-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; source-only change |
</threat_model>

<verification>
1. `grep -c "PACKET_TYPE_" Rust/core/src/protocol.rs` returns 0 (D-03: constants fully deleted)
2. `grep -n "enum PacketType" Rust/core/src/protocol.rs` returns one hit
3. `cargo build --locked --manifest-path Rust/core/Cargo.toml` passes
4. `cargo test --locked --manifest-path Rust/core/Cargo.toml -- packet_type_from_u8` — 3 passed
5. Full regression: `cargo test --locked --manifest-path Rust/core/Cargo.toml` — all pass (timeout 300000ms)
</verification>

<success_criteria>
- PacketType enum fully replaces 17 u8 constants
- Zero PACKET_TYPE_* references remain in protocol.rs
- All 5 match sites migrated; compiler exhaustiveness enforced
- Round-trip tests cover named variants, Unknown, and R22
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/93-hr-data-investigation-protocol-cleanup/93-02-SUMMARY.md` when done.
</output>
