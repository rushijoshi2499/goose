---
phase: 95-whoop-mg-devicekind
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/capabilities.rs
  - Rust/core/src/protocol.rs
autonomous: true
requirements:
  - MG-01

must_haves:
  truths:
    - "DeviceKind::WhoopMg variant exists in capabilities.rs and serialises as \"WHOOP_MG\""
    - "DeviceType::Maverick.device_kind() returns DeviceKind::WhoopMg (not Whoop5)"
    - "DeviceCapabilities::for_kind(DeviceKind::WhoopMg) returns gen5 wire protocol with stream sync"
    - "cargo build succeeds with no non-exhaustive match errors on DeviceKind"
  artifacts:
    - path: "Rust/core/src/capabilities.rs"
      provides: "WhoopMg DeviceKind variant + for_kind arm + serde tests"
      contains: "WhoopMg"
    - path: "Rust/core/src/protocol.rs"
      provides: "Maverick => WhoopMg mapping in device_kind()"
      contains: "device_kind_maverick_is_whoop_mg"
  key_links:
    - from: "Rust/core/src/protocol.rs device_kind()"
      to: "Rust/core/src/capabilities.rs DeviceKind::WhoopMg"
      via: "DeviceType::Maverick => DeviceKind::WhoopMg match arm"
      pattern: "Maverick => DeviceKind::WhoopMg"
---

<objective>
Add DeviceKind::WhoopMg to the Rust capabilities layer so WHOOP MG (codename Maverick) is no longer misidentified as Whoop5.

Purpose: Closes MG-01 — gives MG hardware its own DeviceKind variant, enabling correct capability dispatch and downstream Swift identification.
Output: capabilities.rs gains WhoopMg variant + for_kind arm; protocol.rs Maverick mapping changed from Whoop5 to WhoopMg; all existing tests pass; two new tests added.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/95-whoop-mg-devicekind/95-CONTEXT.md
@.planning/phases/95-whoop-mg-devicekind/95-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DeviceKind::WhoopMg variant and for_kind arm to capabilities.rs</name>
  <files>Rust/core/src/capabilities.rs</files>

  <read_first>
    - Rust/core/src/capabilities.rs — full file (133 lines); read the DeviceKind enum (lines 5–9), for_kind match (lines 24–50), and the existing test module (lines 53–132) before making any change.
  </read_first>

  <behavior>
    - WhoopMg serialises as "WHOOP_MG" (SCREAMING_SNAKE_CASE rule already on enum)
    - "WHOOP_MG" deserialises back to DeviceKind::WhoopMg
    - for_kind(DeviceKind::WhoopMg) returns wire_protocol = "gen5", historical_sync = "stream", battery_via_r22 = true, battery_via_event48 = true, battery_via_cmd26 = true, r22_realtime = true
    - Existing "WHOOP4" / "WHOOP5" / "HR_MONITOR" serde tests still pass unchanged
  </behavior>

  <action>
    Add WhoopMg as the fourth variant of DeviceKind, immediately after HrMonitor. Annotate it with the comment: `// WHOOP MG (Maverick hardware codename) — candidate_MG_capabilities_unverified; identical to Whoop5 per APK Gen5 protocol analysis; pending hardware BLE capture.`

    In DeviceCapabilities::for_kind, add a new match arm for DeviceKind::WhoopMg that returns the same six fields as the Whoop5 arm (wire_protocol "gen5", historical_sync "stream", battery_via_r22 true, battery_via_event48 true, battery_via_cmd26 true, r22_realtime true).

    In the test module, add two new test functions:
    1. `whoop_mg_capabilities` — calls for_kind(DeviceKind::WhoopMg) and asserts wire_protocol == "gen5" and historical_sync == "stream".
    2. `device_kind_whoop_mg_serde` — serialises DeviceKind::WhoopMg and asserts the JSON string is `"WHOOP_MG"`; deserialises `"WHOOP_MG"` and asserts the result equals DeviceKind::WhoopMg.

    The existing `device_kind_unknown_variant_rejected` test requires no change.

    After editing, run `cargo fmt -- src/capabilities.rs` from Rust/core/ to normalise formatting.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test -p goose-core -- capabilities 2>&1 | tail -20</automated>
  </verify>

  <done>
    All capabilities tests pass (whoop4_capabilities, whoop5_capabilities, hr_monitor_capabilities, device_kind_screaming_snake_case_serde, device_kind_deserialise_from_screaming_snake_case, device_kind_unknown_variant_rejected, device_capabilities_serde_roundtrip, whoop_mg_capabilities, device_kind_whoop_mg_serde).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remap DeviceType::Maverick to WhoopMg in protocol.rs device_kind() + update test</name>
  <files>Rust/core/src/protocol.rs</files>

  <read_first>
    - Rust/core/src/protocol.rs lines 166–170 — device_kind() method (the only match on DeviceKind in this file).
    - Rust/core/src/protocol.rs lines 1330–1351 — existing device_kind_* tests (device_kind_gen4_is_whoop4, device_kind_goose_is_whoop5, device_kind_maverick_is_whoop5, device_kind_puffin_is_whoop5, device_kind_hr_monitor).
    - Run `grep -n "DeviceKind::" Rust/core/src/protocol.rs` to confirm there are no other match sites beyond device_kind().
  </read_first>

  <behavior>
    - DeviceType::Maverick.device_kind() returns DeviceKind::WhoopMg
    - DeviceType::Puffin.device_kind() still returns DeviceKind::Whoop5 (unchanged)
    - DeviceType::Goose.device_kind() still returns DeviceKind::Whoop5 (unchanged)
    - DeviceType::Gen4.device_kind() still returns DeviceKind::Whoop4 (unchanged)
    - DeviceType::HrMonitor.device_kind() still returns DeviceKind::HrMonitor (unchanged)
  </behavior>

  <action>
    In device_kind(), change the match arm that currently groups `DeviceType::Maverick | DeviceType::Puffin | DeviceType::Goose => DeviceKind::Whoop5` into two arms:
      - `DeviceType::Maverick => DeviceKind::WhoopMg`
      - `DeviceType::Puffin | DeviceType::Goose => DeviceKind::Whoop5`

    In the test module, rename the existing test `device_kind_maverick_is_whoop5` to `device_kind_maverick_is_whoop_mg` and change its assertion from `DeviceKind::Whoop5` to `DeviceKind::WhoopMg`.

    Do not change any other match sites in protocol.rs — the header_len(), expected_frame_len(), and parse_frame() matches use DeviceType variants, not DeviceKind variants, and do not need modification.

    After editing, run `cargo fmt -- src/protocol.rs` from Rust/core/ to normalise formatting.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test -p goose-core -- device_kind 2>&1 | tail -20</automated>
  </verify>

  <done>
    cargo test passes for device_kind_gen4_is_whoop4, device_kind_goose_is_whoop5, device_kind_maverick_is_whoop_mg, device_kind_puffin_is_whoop5, device_kind_hr_monitor. cargo build succeeds with no non-exhaustive pattern errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full test suite + fmt check</name>
  <files></files>

  <read_first>
    None — verification only.
  </read_first>

  <action>
    Run the full Rust test suite and fmt check to confirm no regressions across the codebase.

    Steps:
    1. `cargo fmt --check` from Rust/core/ — must exit 0 (no formatting diffs).
    2. `cargo test --locked` from Rust/core/ — all tests must pass.
    3. Run `grep -rn "DeviceKind::" Rust/core/src/ Rust/core/tests/` and verify every match site that switches on DeviceKind is exhaustive (compiler has already enforced this, but confirm visually that tests/ do not have stale Whoop5 assertions for Maverick).
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo fmt --check && cargo test --locked 2>&1 | tail -30</automated>
  </verify>

  <done>
    cargo fmt --check exits 0; cargo test --locked reports all tests passed with zero failures. No file contains an assertion that DeviceType::Maverick.device_kind() == DeviceKind::Whoop5.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift → Rust bridge | Swift passes "WHOOP_MG" string to device.capabilities; Rust deserialises via serde |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering | DeviceKind serde deserialisation | accept | Input comes from Swift side of the same app process — not an external attacker path; string is constructed by our own Swift code |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | No new crate dependencies introduced in this plan |
</threat_model>

<verification>
- `cargo test -p goose-core -- capabilities` passes (9 tests including 2 new WhoopMg tests)
- `cargo test -p goose-core -- device_kind` passes (5 tests; maverick now asserts WhoopMg)
- `cargo test --locked` passes (full suite, zero regressions)
- `cargo fmt --check` exits 0
- `grep -c 'WhoopMg' Rust/core/src/capabilities.rs` returns at least 4 (enum variant, for_kind arm, two test functions)
- `grep -c 'WhoopMg' Rust/core/src/protocol.rs` returns at least 2 (device_kind arm, test assertion)
</verification>

<success_criteria>
- DeviceKind::WhoopMg exists and serialises as "WHOOP_MG"
- DeviceType::Maverick maps to WhoopMg, not Whoop5
- All existing tests still pass
- Two new capability tests added for WhoopMg
- cargo build is clean (no non-exhaustive match warnings/errors)
</success_criteria>

## Artifacts this phase produces

| Artifact | Purpose |
|----------|---------|
| Rust/core/src/capabilities.rs | WhoopMg DeviceKind variant, for_kind arm, serde tests |
| Rust/core/src/protocol.rs | Maverick => WhoopMg mapping, updated test |

<output>
Create `.planning/phases/95-whoop-mg-devicekind/95-01-SUMMARY.md` when done.
</output>
