---
phase: 94-gen4-protocol-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/metric_features.rs
  - Rust/core/tests/v24_biometric_protocol_tests.rs
autonomous: true
requirements:
  - GEN4-06
must_haves:
  truths:
    - "respiratory_rate_plan_from_payload returns Some for a V24History frame with packet_k=24"
    - "resp_raw at absolute payload offset 76 (body offset 73) is read and stored as a candidate metric with quality flag v24_resp_raw_encoding_unverified"
    - "skin_temp_delta_c chain is traced end-to-end: skin_temperature_plan_from_payload pk=24 arm feeds VitalEventFeatureReport.skin_temperature_inputs"
    - "cargo test --locked --test v24_biometric_protocol_tests passes green with new respiratory_rate tests"
  artifacts:
    - path: Rust/core/src/metric_features.rs
      provides: "respiratory_rate_plan_from_payload with V24History guard and packet_k=24 arm"
      contains: "DataPacketBodySummary::V24History"
    - path: Rust/core/tests/v24_biometric_protocol_tests.rs
      provides: "v24_resp_raw_extraction and v24_respiratory_rate_plan_returns_some tests"
      contains: "respiratory_rate"
  key_links:
    - from: Rust/core/src/metric_features.rs
      to: Rust/core/tests/v24_biometric_protocol_tests.rs
      via: "respiratory_rate_plan_from_payload called via test fixture with V24History payload"
      pattern: "respiratory_rate_plan_from_payload"
---

<objective>
Fix GEN4-06: Gen4 users see None for respiratory_rate_rpm because respiratory_rate_plan_from_payload
only accepts NormalHistory and V18History body_summary variants — V24History (packet_k=24, the Gen4
format) is silently rejected by the outer guard before the packet_k match arm is reached.

Simultaneously verify the skin_temp_delta_c chain end-to-end for Gen4: confirm whether
skin_temperature_plan_from_payload pk=24 arm (body offset 3, scale 1000) feeds
MetricFeatures.skin_temp_delta_c, or whether a separate fix is needed for the NTC field at body offset 65.

Purpose: Gen4 users currently receive no respiratory rate data. Adding the V24History arm and the
packet_k=24 match unblocks metric extraction from existing stored frames without any new BLE capture.

Output: Modified metric_features.rs with V24History guard update and pk=24 resp_raw arm;
new tests in v24_biometric_protocol_tests.rs covering the extraction path.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/francisco/Documents/goose/.planning/PROJECT.md
@/Users/francisco/Documents/goose/.planning/ROADMAP.md
@/Users/francisco/Documents/goose/.planning/phases/94-gen4-protocol-completeness/94-CONTEXT.md
@/Users/francisco/Documents/goose/.planning/phases/94-gen4-protocol-completeness/94-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend respiratory_rate_plan_from_payload to accept V24History / packet_k=24 (per D-01, D-02)</name>

  <read_first>
    - Rust/core/src/metric_features.rs lines 4268–4301 — current respiratory_rate_plan_from_payload:
      outer guard at line 4278 matches only NormalHistory | V18History; inner match only has arm for 18.
    - Rust/core/src/metric_features.rs lines 4223–4265 — skin_temperature_plan_from_payload:
      shows the V24History pattern; pk=24 arm uses raw_body_offset:3, raw_absolute_offset:16, scale:1000.0.
    - Rust/core/src/protocol.rs lines 917–935 — V24 body layout comment:
      body offset 73 = resp_raw (u16 LE); body offset 65 = skin_temp_raw (u16 LE, NTC).
      Absolute payload offsets: payload[76..78] = resp_raw; payload[68..70] = skin_temp_raw.
    - Rust/core/src/protocol.rs lines 4–30 — DataPacketBodySummary enum variants (V24History definition).
    - Rust/core/tests/v24_biometric_protocol_tests.rs lines 1–80 — existing fixture helpers:
      make_82_byte_payload() builds a synthetic 82-byte payload (3-byte header + 79-byte body);
      body offset N corresponds to pkt[3 + N].
    - Rust/core/src/metric_features.rs — search for "respiratory_rate_semantics_unverified" to find
      how the quality-flag string is applied in respiratory_rate_feature_from_plan; preserve that
      pattern for V24 by tagging schema_field with the candidate suffix.
  </read_first>

  <behavior>
    - Test A: construct a ParsedPayload::DataPacket with body_summary=V24History and packet_k=24;
      call respiratory_rate_plan_from_payload — must return Some(RespiratoryRatePlan { packet_k: 24 }).
    - Test B: build an 82-byte synthetic payload (using make_82_byte_payload pattern) with
      pkt[76] and pkt[77] set to a known resp_raw value (e.g., 180u16 LE);
      construct the full ParsedPayload from that payload; verify raw_absolute_offset=76 reads the
      correct u16 LE value (180) from the payload bytes.
    - Test C: call respiratory_rate_plan_from_payload with body_summary=NormalHistory / pk=18 —
      still returns Some (regression guard).
    - Test D: call respiratory_rate_plan_from_payload with body_summary=V24History / pk=99 —
      returns None (no spurious arm).
  </behavior>

  <action>
    In Rust/core/src/metric_features.rs, function respiratory_rate_plan_from_payload (line 4268):

    Step 1 — Update the body_summary guard (line 4278–4280).
    The current guard is:
      Some(DataPacketBodySummary::NormalHistory { .. } | DataPacketBodySummary::V18History { .. })
    Add V24History alongside the existing variants:
      Some(DataPacketBodySummary::NormalHistory { .. }
           | DataPacketBodySummary::V18History { .. }
           | DataPacketBodySummary::V24History { .. })
    This is required: without it, packet_k=24 frames (which carry V24History body_summary) are
    rejected by the outer guard before the inner match is reached — the pk=24 arm would be dead code.

    Step 2 — Add packet_k=24 arm inside the match *packet_k block (after the existing arm for 18).
    Use these exact field values:
      packet_k: *packet_k,
      timestamp_seconds: *timestamp_seconds,
      timestamp_subseconds: *timestamp_subseconds,
      schema_field: "v24_history_k24_body_73_resp_raw_candidate",
      raw_body_offset: 73,
      raw_absolute_offset: 76,   // 3-byte data-packet header + body offset 73
      encoding: "u16_le_raw",    // scale unknown — tagged as unverified candidate (per D-02)
      scale: 1.0,
    The "u16_le_raw" encoding and scale:1.0 treats resp_raw as a raw ADC count. The existing
    respiratory_rate_feature_from_plan plausibility range gate (6–30 rpm) will reject implausible
    values. Tag the schema_field name with "_candidate" so downstream code (and queries) can
    distinguish this from the verified pk=18 field. Do NOT apply the NTC formula — that is for
    skin_temp, not resp_raw (per D-03 scope).

    Step 3 — Skin-temp chain trace (READ-ONLY, no edits).
    After the respiratory_rate fix, read the following to document the skin_temp_delta_c chain for
    the SUMMARY:
      - How skin_temperature_plan_from_payload (pk=24, body offset 3) feeds
        skin_temperature_feature_from_plan and ultimately VitalEventFeatureReport.skin_temperature_inputs.
      - Whether MetricFeatures.skin_temp_delta_c is populated from that path or from a different one
        (e.g., recovery_provided_vitals_feature from RecoveryFeatureScoreOptions).
    Record the finding in the plan SUMMARY under "skin_temp chain finding". No code edit required
    unless the chain is broken — if broken, add a note for a follow-up issue, do NOT expand scope.

    Do NOT modify body_hex suppression logic in protocol.rs — that is SYNC-07 scope (plan 94-02).
    Do NOT change the pk=18 arm or its scale (10.0 / u16_le_x10).
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked --test v24_biometric_protocol_tests -- respiratory_rate 2>&1 | tail -10</automated>
  </verify>

  <done>
    - respiratory_rate_plan_from_payload body_summary guard includes V24History at metric_features.rs line ~4278.
    - packet_k=24 arm present in the match block with raw_absolute_offset=76, encoding="u16_le_raw", scale=1.0.
    - Four test functions in v24_biometric_protocol_tests.rs: plan_returns_some_for_v24, resp_raw_offset_reads_correct_bytes, pk18_regression_still_returns_some, pk99_v24_returns_none.
    - cargo test --locked --test v24_biometric_protocol_tests passes with no failures.
    - cargo test --locked (full suite) passes with no regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add integration test for respiratory_rate extraction end-to-end via decoded_frames row (per D-01, D-02)</name>

  <read_first>
    - Rust/core/tests/store_tests.rs lines 1–50 — imports and GooseStore setup pattern; how
      DecodedFrameInput and GooseStore::open are used.
    - Rust/core/src/metric_features.rs — search for "respiratory_rate_inputs" and
      "respiratory_rate_feature_from_plan" to confirm the function signature and what it reads
      from a DecodedFrameRow (reads row.payload_hex via decode_hex_with_whitespace, then indexes
      at plan.raw_absolute_offset).
    - Rust/core/src/protocol.rs — parse_frame_hex or equivalent test-visible function used to
      produce a ParsedFrame from a hex string; look for #[cfg(test)] pub exports.
    - Rust/core/tests/v24_biometric_protocol_tests.rs — make_82_byte_payload() as the canonical
      byte builder for V24 payloads; re-use or adapt it.
  </read_first>

  <action>
    Add a new test function to Rust/core/tests/v24_biometric_protocol_tests.rs named
    v24_resp_raw_feature_extraction_from_decoded_row.

    The test must:
    1. Build a synthetic 82-byte V24 body payload using the make_82_byte_payload() helper (already
       in the file). Set pkt[76] and pkt[77] to the little-endian bytes of a known resp_raw value,
       e.g., 240u16 (0xF0, 0x00). This places resp_raw at body offset 73 (absolute offset 76).
    2. Wrap the payload in a Gen4 frame and produce a parsed_payload_json string that represents
       a ParsedPayload::DataPacket with packet_k=24, V24History body_summary, and the payload bytes.
       Use serde_json::to_string or hand-craft the JSON matching the schema that
       respiratory_rate_plan_from_payload expects. Look at how other tests in this file build
       ParsedPayload JSON — replicate the pattern exactly.
    3. Construct a DecodedFrameRow (or the minimal struct respiratory_rate_feature_from_plan expects)
       with payload_hex set to the hex encoding of the 82-byte payload, and parsed_payload_json
       set to the JSON from step 2.
    4. Call respiratory_rate_plan_from_payload with the deserialized ParsedPayload. Assert the result
       is Some and that raw_absolute_offset == 76 and encoding == "u16_le_raw".
    5. Assert that indexing payload_bytes[76..78] and reading as u16 LE equals 240 — confirming the
       byte offset arithmetic is correct.

    This test does NOT need to open a SQLite database. It validates the extraction logic in isolation.

    If respiratory_rate_feature_from_plan is not pub(crate) or pub enough to call from integration
    tests, test only respiratory_rate_plan_from_payload (step 4–5 above) and note the visibility
    constraint in a comment. Do NOT add pub(crate) solely for testability without understanding the
    existing visibility pattern — check how other plan functions are tested first.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked --test v24_biometric_protocol_tests -- v24_resp_raw_feature 2>&1 | tail -10</automated>
  </verify>

  <done>
    - v24_resp_raw_feature_extraction_from_decoded_row test exists in v24_biometric_protocol_tests.rs.
    - Test asserts: respiratory_rate_plan returns Some for pk=24 V24History, raw_absolute_offset=76, resp_raw bytes at payload[76..78] decode to the seeded u16 value.
    - cargo test --locked --test v24_biometric_protocol_tests passes (all tests, including new one).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE bytes → Rust parser | Raw packet bytes from WHOOP 4.0 device; parsed by parse_frame before reaching metric_features |
| metric_features → SQLite | Extracted float values written to daily_recovery_metrics table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | respiratory_rate_plan raw_absolute_offset | mitigate | Existing plausibility range gate (6–30 rpm) in respiratory_rate_feature_from_plan rejects out-of-range values; applies to pk=24 arm without modification |
| T-94-02 | Information Disclosure | v24_resp_raw_encoding_unverified schema_field | accept | schema_field name contains "_candidate" suffix; downstream query callers can filter by suffix; no PII involved |
| T-94-SC | Tampering | No new package installs this plan | accept | No npm/pip/cargo new packages; existing Cargo.lock unchanged |
</threat_model>

<verification>
Full phase gate (run after both plans complete):
  cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked 2>&1 | tail -20

Per-plan gate:
  cd /Users/francisco/Documents/goose/Rust/core && cargo test --locked --test v24_biometric_protocol_tests 2>&1 | tail -10

Confirm V24History appears in guard:
  grep -n "V24History" /Users/francisco/Documents/goose/Rust/core/src/metric_features.rs

Confirm pk=24 arm present in respiratory_rate_plan_from_payload:
  grep -n "v24_history_k24_body_73" /Users/francisco/Documents/goose/Rust/core/src/metric_features.rs
</verification>

<success_criteria>
- respiratory_rate_plan_from_payload returns Some for any V24History frame with packet_k=24
- resp_raw is read from absolute payload offset 76 (body offset 73) as u16 LE
- schema_field is "v24_history_k24_body_73_resp_raw_candidate" (tagged as candidate per D-02)
- encoding is "u16_le_raw", scale is 1.0 (unverified encoding flagged per D-02)
- Existing pk=18 behaviour is unchanged (regression test green)
- cargo test --locked (full suite) exits 0
- Skin-temp chain finding documented in SUMMARY (whether working or needs follow-up)
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Purpose |
|----------|------|---------|
| Modified metric extractor | Rust/core/src/metric_features.rs | V24History guard + pk=24 resp_raw arm |
| New extraction tests | Rust/core/tests/v24_biometric_protocol_tests.rs | Validate V24 respiratory rate extraction byte offsets |

<output>
Create /Users/francisco/Documents/goose/.planning/phases/94-gen4-protocol-completeness/94-01-SUMMARY.md when done.
</output>
