---
phase: 70-haptic-primitive-breathe-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseBLEClient+Haptics.swift
autonomous: true
requirements:
  - HAP-01

must_haves:
  truths:
    - "buzz(loops:) exists on GooseBLEClient and writes Data([0x13, N]) to commandCharacteristic"
    - "buzz is a silent no-op (OSLog only, no crash) when activePeripheral or commandCharacteristic is nil"
    - "buzz clamps the loops argument to UInt8 range 1–255 before encoding"
    - "OSLog records 'buzz.sent' with loops count and writeType on every successful write"
    - "Build succeeds with zero compiler errors and zero warnings on the new file"
  artifacts:
    - path: "GooseSwift/GooseBLEClient+Haptics.swift"
      provides: "buzz(loops:) BLE haptic command primitive"
      exports: ["buzz(loops:)"]
  key_links:
    - from: "GooseBLEClient+Haptics.swift"
      to: "GooseBLEClient.commandCharacteristic"
      via: "direct writeValue call"
      pattern: "activePeripheral\\.writeValue.*commandCharacteristic"
    - from: "GooseBLEClient+Haptics.swift"
      to: "GooseBLEClient.writeType(for:)"
      via: "helper call confirming .withResponse vs .withoutResponse"
      pattern: "writeType(for: commandCharacteristic)"
---

<objective>
Create GooseBLEClient+Haptics.swift — a new BLE command extension that adds buzz(loops:) to GooseBLEClient, implementing HAP-01.

Purpose: Provide the haptic primitive required by BreatheView (HAP-02, Plan 70-02), the future smart alarm (HAP-03, Phase 73), and the wake-window engine (HAP-04, Phase 73). This is a pure fire-and-forget BLE command — no sequence tracking, no response expected, no pending command state.

Output: One new Swift file (GooseBLEClient+Haptics.swift) with a single public method buzz(loops:) on GooseBLEClient. No other files are touched by this plan.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-haptic-primitive-breathe-screen/70-CONTEXT.md
@.planning/phases/70-haptic-primitive-breathe-screen/70-RESEARCH.md
@.planning/phases/70-haptic-primitive-breathe-screen/70-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GooseBLEClient+Haptics.swift with buzz(loops:)</name>
  <files>GooseSwift/GooseBLEClient+Haptics.swift</files>

  <read_first>
    - GooseSwift/GooseBLEClient+HistoricalCommands.swift — exact guard pattern (lines 89–126), writeType(for:) helper (lines 177–185), record(source:title:body:) calls (lines 15–16, 144–145)
    - GooseSwift/GooseBLEClient+Commands.swift — writeAlarmCommand pattern; confirms writeTypeName(_:) usage in record body strings
    - GooseSwift/GooseBLEClient+Parsing.swift — confirm writeTypeName(_:) is defined (line 788, per PATTERNS.md)
  </read_first>

  <action>
    Create GooseSwift/GooseBLEClient+Haptics.swift as a new BLE command extension following the exact structure of GooseBLEClient+HistoricalCommands.swift.

    File structure (3 imports, no class declaration, extension only):
      - import CoreBluetooth
      - import Foundation
      - import OSLog

    Extension declaration: extension GooseBLEClient { ... }

    Implement func buzz(loops: Int) with this exact logic (per D-01 locked decision in CONTEXT.md):

    Step 1 — guard activePeripheral + commandCharacteristic (shorthand let binding, same as +HistoricalCommands.swift lines 93–99):
      guard let activePeripheral, let commandCharacteristic else {
        record(source: "ble.haptic", title: "buzz.blocked", body: "no active peripheral or characteristic")
        return
      }

    Step 2 — guard writeType (same helper used in +HistoricalCommands.swift line 126):
      guard let writeType = writeType(for: commandCharacteristic) else {
        record(source: "ble.haptic", title: "buzz.blocked", body: "characteristic not writable")
        return
      }

    Step 3 — clamp, build payload, write, log:
      let clamped = UInt8(max(1, min(255, loops)))
      let payload = Data([0x13, clamped])
      activePeripheral.writeValue(payload, for: commandCharacteristic, type: writeType)
      record(source: "ble.haptic", title: "buzz.sent", body: "loops=\(clamped) \(writeTypeName(writeType))")

    Critical constraints (per CONTEXT.md locked decisions and RESEARCH.md anti-patterns):
    - Do NOT call buildCommandFrame(sequence:command:data:) — buzz is fire-and-forget, no sequence needed
    - Do NOT add pending command state or timeout DispatchWorkItem — not applicable to haptic commands
    - Do NOT add a separate method or helper beyond buzz(loops:) — keep the extension minimal
    - writeType(for:) and writeTypeName(_:) are existing helpers on GooseBLEClient — do not redefine them
    - record(source:title:body:) is existing on GooseBLEClient — call it directly, same as other extensions
    - If writeTypeName(_:) is confirmed absent from GooseBLEClient+Parsing.swift after reading, substitute inline: writeType == .withResponse ? "withResponse" : "withoutResponse"

    OSLog source string "ble.haptic" is consistent with existing "ble.sync", "ble.clock", "ble.alarm" naming convention.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E '(error:|BUILD SUCCEEDED|BUILD FAILED)' | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - GooseSwift/GooseBLEClient+Haptics.swift exists
    - grep -c "func buzz(loops: Int)" GooseSwift/GooseBLEClient+Haptics.swift returns 1
    - grep -c "Data(\[0x13" GooseSwift/GooseBLEClient+Haptics.swift returns 1
    - grep -c "buildCommandFrame" GooseSwift/GooseBLEClient+Haptics.swift returns 0 (no sequence wrapping)
    - xcodebuild produces BUILD SUCCEEDED with no compiler errors
    - grep -c "buzz.sent" GooseSwift/GooseBLEClient+Haptics.swift returns 1 (OSLog confirmation line exists)
  </acceptance_criteria>

  <done>GooseBLEClient+Haptics.swift exists with buzz(loops:) that writes Data([0x13, UInt8(clamped)]) to commandCharacteristic via writeValue, guarded against nil peripheral/characteristic, logged via OSLog. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift caller → BLE write | buzz(loops:) accepts caller-supplied Int; must not allow UInt8 overflow crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | buzz(loops:) input | mitigate | Clamp Int to 1–255 before UInt8 cast: max(1, min(255, loops)) — prevents overflow crash |
| T-70-02 | Denial of Service | commandCharacteristic write when disconnected | accept | BLE guard returns silently — no crash, no retry loop; fire-and-forget is safe when peripheral is nil |
</threat_model>

<verification>
1. xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build → BUILD SUCCEEDED, zero errors
2. grep "func buzz" GooseSwift/GooseBLEClient+Haptics.swift → exactly one match
3. grep "Data(\[0x13" GooseSwift/GooseBLEClient+Haptics.swift → exactly one match (cmd 0x13 payload)
4. grep "buildCommandFrame" GooseSwift/GooseBLEClient+Haptics.swift → zero matches (no frame wrapping)
5. grep "buzz.sent" GooseSwift/GooseBLEClient+Haptics.swift → one match (OSLog success log)
</verification>

<success_criteria>
- GooseBLEClient+Haptics.swift exists in GooseSwift/
- buzz(loops:) writes Data([0x13, N]) directly to commandCharacteristic — no sequence wrapping
- Guard pattern: silent no-op + OSLog when peripheral or characteristic is nil
- UInt8 clamping: max(1, min(255, loops)) prevents overflow crash
- Build clean: zero compiler errors, zero warnings on the new file
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Kind | Description |
|--------|------|------|-------------|
| `GooseBLEClient.buzz(loops:)` | `GooseSwift/GooseBLEClient+Haptics.swift` | method | Fire-and-forget BLE haptic command; writes Data([0x13, N]) to commandCharacteristic; HAP-01 primitive |

<output>
Create .planning/phases/70-haptic-primitive-breathe-screen/70-01-SUMMARY.md when done.
</output>
