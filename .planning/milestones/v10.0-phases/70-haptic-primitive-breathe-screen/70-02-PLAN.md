---
phase: 70-haptic-primitive-breathe-screen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/BreatheView.swift
  - GooseSwift/MoreRouteModels.swift
  - GooseSwift/MoreView.swift
  - GooseSwift/MoreDataStore.swift
autonomous: true
requirements:
  - HAP-02

must_haves:
  truths:
    - "More tab shows a 'Wellness' section with a Breathe row (title 'Breathe', subtitle 'Paced breathing with haptics', systemImage 'wind')"
    - "Tapping Breathe pushes BreatheView inside the More NavigationStack"
    - "BreatheView animates a Circle from scaleEffect 0.6 to 1.0 (inhale), holds at 1.0 (hold), and back to 0.6 (exhale) over 4s each phase with .easeInOut"
    - "Phase label ('INHALE' / 'HOLD' / 'EXHALE') updates below the circle at each phase transition"
    - "buzz(loops: 1) is called at the start of each phase transition (inhale, hold, exhale starts)"
    - "Stop button cancels the Task and resets state; back-navigation also cancels via .onDisappear"
    - "When connectionState != 'ready' and session not running, 'Connect WHOOP to enable haptics' banner appears"
    - "Build succeeds with zero compiler errors"
  artifacts:
    - path: "GooseSwift/BreatheView.swift"
      provides: "BreatheView SwiftUI screen with full box-breathing session loop"
      exports: ["BreatheView", "BreathePhase"]
    - path: "GooseSwift/MoreRouteModels.swift"
      provides: "MoreRoute.breathe case + wellnessRoutes group + MoreRouteStatus.breathe property"
      contains: "case breathe"
    - path: "GooseSwift/MoreView.swift"
      provides: "Wellness section + case .breathe destination arm"
      contains: "Section(\"Wellness\")"
    - path: "GooseSwift/MoreDataStore.swift"
      provides: "breathe: .ready in both MoreRouteStatus initializer sites"
      contains: "breathe: .ready"
  key_links:
    - from: "BreatheView.swift"
      to: "GooseBLEClient.buzz(loops:)"
      via: "model.ble.buzz(loops: 1)"
      pattern: "model\\.ble\\.buzz"
    - from: "BreatheView.swift"
      to: "GooseAppModel.ble.connectionState"
      via: "@Environment(GooseAppModel.self)"
      pattern: "connectionState.*ready"
    - from: "MoreView.swift"
      to: "BreatheView.swift"
      via: "navigationDestination case .breathe"
      pattern: "case .breathe"
    - from: "MoreRouteModels.swift"
      to: "MoreDataStore.swift"
      via: "MoreRouteStatus.breathe memberwise initializer"
      pattern: "breathe: .ready"
---

<objective>
Implement the Breathe screen (HAP-02): BreatheView.swift with the full 4s/4s/4s box-breathing session loop, plus all navigation wiring (MoreRoute.breathe, MoreView Wellness section, MoreDataStore struct updates).

Purpose: Deliver the user-facing paced-breathing feature with WHOOP strap haptic pacing. BreatheView calls buzz(loops: 1) — implemented in Plan 70-01 — at the start of each phase transition. Plans 70-01 and 70-02 are Wave 1 parallels; buzz(loops:) will exist on GooseBLEClient at build time regardless of plan execution order since both produce commits before the final xcodebuild verification.

Output: BreatheView.swift (new), plus targeted edits to MoreRouteModels.swift, MoreView.swift, and MoreDataStore.swift. AppShellView.swift requires no changes (navigationDestination(for: MoreRoute.self) already wired in MoreView.swift per PATTERNS.md).
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-haptic-primitive-breathe-screen/70-CONTEXT.md
@.planning/phases/70-haptic-primitive-breathe-screen/70-RESEARCH.md
@.planning/phases/70-haptic-primitive-breathe-screen/70-PATTERNS.md
@.planning/phases/70-haptic-primitive-breathe-screen/70-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MoreRouteModels.swift and MoreDataStore.swift for MoreRoute.breathe</name>
  <files>GooseSwift/MoreRouteModels.swift, GooseSwift/MoreDataStore.swift</files>

  <read_first>
    - GooseSwift/MoreRouteModels.swift — full file; read every switch arm (title, subtitle, systemImage, statusKeyPath), the static route group declarations, and the MoreRouteStatus struct definition to know exact insertion points
    - GooseSwift/MoreDataStore.swift — read lines around the two MoreRouteStatus construction sites (initial @Published var routeStatus = MoreRouteStatus(...) and the refreshRouteStatus function body); per PATTERNS.md lines 280–313 both use named argument labels
  </read_first>

  <action>
    Edit MoreRouteModels.swift — 6 changes, all mechanical:

    1. Add case to enum MoreRoute (before .privacy, per UI-SPEC §10):
       case breathe

    2. Add arm to the title computed var switch:
       case .breathe: String(localized: "Breathe")

    3. Add arm to the subtitle computed var switch:
       case .breathe: String(localized: "Paced breathing with haptics")

    4. Add arm to the systemImage computed var switch:
       case .breathe: "wind"

    5. Add arm to the statusKeyPath computed var switch:
       case .breathe: \.breathe

    6. Add new static route group (after existing group declarations):
       static let wellnessRoutes: [MoreRoute] = [.breathe]

    7. Add property to MoreRouteStatus struct (per UI-SPEC §10, PATTERNS.md lines 228–233):
       var breathe: MoreStatusKind

    Edit MoreDataStore.swift — 2 changes, both adding breathe: .ready to MoreRouteStatus construction sites (per PATTERNS.md lines 280–312):

    1. In the @Published var routeStatus = MoreRouteStatus(...) initializer — add:
       breathe: .ready

    2. In the refreshRouteStatus(ble:model:) function where MoreRouteStatus(...) is constructed — add:
       breathe: .ready

    Both construction sites must be updated — missing either causes a Swift memberwise initializer compile error (Pitfall 5 in RESEARCH.md).

    Breathe is always .ready (screen is always accessible regardless of BLE connection per CONTEXT.md).
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E '(error:|BUILD SUCCEEDED|BUILD FAILED)' | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "case breathe" GooseSwift/MoreRouteModels.swift returns at least 1
    - grep -c "wellnessRoutes" GooseSwift/MoreRouteModels.swift returns at least 1
    - grep -c "var breathe: MoreStatusKind" GooseSwift/MoreRouteModels.swift returns 1
    - grep -c "breathe: .ready" GooseSwift/MoreDataStore.swift returns 2 (both construction sites updated)
    - xcodebuild produces BUILD SUCCEEDED (no "type 'MoreRouteStatus' has no member 'breathe'" error)
  </acceptance_criteria>

  <done>MoreRoute.breathe exists with title/subtitle/systemImage/statusKeyPath. MoreRouteStatus has var breathe: MoreStatusKind. Both MoreDataStore construction sites include breathe: .ready. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create BreatheView.swift and add Wellness section to MoreView.swift</name>
  <files>GooseSwift/BreatheView.swift, GooseSwift/MoreView.swift</files>

  <read_first>
    - GooseSwift/MoreView.swift — full file; read the Section declarations (lines 45–89), routeRows helper (lines 120–127), destination(for:) switch (lines 130–165), navigationDestination wiring (lines 95–97), and @Environment access (line 11)
    - GooseSwift/MoreRemoteServerViews.swift — analog for BreatheView structure: @Environment(GooseAppModel.self) access pattern, navigation title modifiers
    - GooseSwift/FitnessFormatting.swift — FitnessColor static values (all 6 tokens) to use exact color references
  </read_first>

  <action>
    Create GooseSwift/BreatheView.swift. File layout follows MoreRemoteServerViews.swift structure (imports Foundation + SwiftUI; single file for all view structs in this feature).

    Imports:
      import Foundation
      import SwiftUI

    Define BreathePhase enum at file scope (per UI-SPEC §5):
      enum BreathePhase {
        case inhale, hold, exhale
        var label: String — "INHALE" / "HOLD" / "EXHALE" per switch
        static let duration: TimeInterval = 4.0
      }

    Define BreatheView: View with these @State properties (per UI-SPEC §5):
      @State private var isRunning = false
      @State private var currentPhase: BreathePhase = .inhale
      @State private var circleScale: CGFloat = 0.6
      @State private var phaseTask: Task<Void, Never>? = nil

    And these environment properties:
      @Environment(GooseAppModel.self) private var model
      @Environment(\.accessibilityReduceMotion) private var reduceMotion

    Body layout (ZStack, per UI-SPEC §5 component hierarchy):
    - ZStack background: FitnessColor.background (Color.black) with .ignoresSafeArea()
    - VStack(spacing: 0) centered:
      - Spacer()
      - BreatheCircle (ZStack of two Circle()s — per UI-SPEC §6):
          Outer circle: Circle().fill(FitnessColor.panel).frame(width:220, height:220).scaleEffect(circleScale)
          Inner ring: Circle().strokeBorder(FitnessColor.standCyan.opacity(0.72), lineWidth:3).frame(width:220, height:220).scaleEffect(circleScale)
          Accessibility: .accessibilityLabel("Breathing circle, \(currentPhase.label.lowercased()) phase")
      - Spacer().frame(height: 16)
      - Phase label (per UI-SPEC §7):
          Text(currentPhase.label)
            .font(.system(size: 20, weight: .semibold))
            .tracking(2.0)
            .foregroundStyle(isRunning ? FitnessColor.standCyan : FitnessColor.secondaryText)
            .animation(.easeInOut(duration: 0.25), value: currentPhase)
            .contentTransition(.opacity)
            .accessibilityAddTraits(.updatesFrequently)
      - Spacer()
      - Disconnected banner (per UI-SPEC §9) — rendered only when !isRunning && model.ble.connectionState != "ready":
          HStack(spacing: 8) with Image(systemName: "sensor.tag.radiowaves.forward") + Text("Connect WHOOP to enable haptics")
          .foregroundStyle(FitnessColor.secondaryText) on both
          .font(.system(size: 16, weight: .regular)) on Text
          .padding(.horizontal, 16).padding(.vertical, 10)
          .background(FitnessColor.panel, in: RoundedRectangle(cornerRadius:10, style:.continuous))
          .padding(.bottom, 16)
          .accessibilityElement(children: .combine)
          CRITICAL: connection check is model.ble.connectionState != "ready" (lowercase "ready" — NOT "Connected")
      - Button row (per UI-SPEC §8):
          if isRunning:
            Button("Stop") { stopSession() }
              .font(.body.weight(.semibold)).foregroundStyle(.white)
              .frame(width:160, height:48)
              .background(FitnessColor.panel, in:Capsule())
              .padding(.bottom, 32)
              .accessibilityLabel("Stop breathing session")
          else:
            Button("Start") { startSession() }
              .font(.body.weight(.semibold)).foregroundStyle(FitnessColor.standCyan)
              .frame(width:160, height:48)
              .background(FitnessColor.standCyan.opacity(0.14), in:Capsule())
              .padding(.bottom, 32)
              .accessibilityLabel("Start breathing session")

    Navigation chrome modifiers on BreatheView body (per UI-SPEC §11):
      .navigationTitle("Breathe")
      .navigationBarTitleDisplayMode(.inline)
      .toolbar(.hidden, for: .tabBar)
      .background(FitnessColor.background.ignoresSafeArea())
      .toolbarBackground(FitnessColor.background, for: .navigationBar)
      .toolbarColorScheme(.dark, for: .navigationBar)
      .onDisappear { stopSession() }

    startSession() func (per UI-SPEC §12 + RESEARCH.md Pattern 5):
      Sets isRunning = true
      Creates phaseTask = Task { @MainActor in ... } — @MainActor required for withAnimation on main thread
      repeat loop body (per UI-SPEC §12 session loop logic):
        1. currentPhase = .inhale; model.ble.buzz(loops: 1)
           if reduceMotion { circleScale = 1.0 } else { withAnimation(.easeInOut(duration: BreathePhase.duration)) { circleScale = 1.0 } }
           try? await Task.sleep(for: .seconds(BreathePhase.duration))
           guard !Task.isCancelled else { break }
        2. currentPhase = .hold; model.ble.buzz(loops: 1)
           (no circle animation — scale stays at 1.0)
           try? await Task.sleep(for: .seconds(BreathePhase.duration))
           guard !Task.isCancelled else { break }
        3. currentPhase = .exhale; model.ble.buzz(loops: 1)
           if reduceMotion { circleScale = 0.6 } else { withAnimation(.easeInOut(duration: BreathePhase.duration)) { circleScale = 0.6 } }
           try? await Task.sleep(for: .seconds(BreathePhase.duration))
      while !Task.isCancelled

    stopSession() func (per UI-SPEC §12):
      phaseTask?.cancel(); phaseTask = nil; isRunning = false; currentPhase = .inhale
      withAnimation(.easeInOut(duration: 0.4)) { circleScale = 0.6 }

    Edit MoreView.swift — 2 changes:

    1. Add Wellness section above the Settings section (per PATTERNS.md lines 240–244):
       Section("Wellness") {
         routeRows(MoreRoute.wellnessRoutes)
       }

    2. Add destination arm inside destination(for:) switch (per PATTERNS.md lines 249–252):
       case .breathe:
         BreatheView()

    No other MoreView.swift changes needed — routeRows helper and navigationDestination already exist.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E '(error:|BUILD SUCCEEDED|BUILD FAILED)' | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - GooseSwift/BreatheView.swift exists
    - grep -c "struct BreatheView" GooseSwift/BreatheView.swift returns 1
    - grep -c "BreathePhase" GooseSwift/BreatheView.swift returns at least 3 (enum def + usage in @State + session loop)
    - grep -c "model.ble.buzz" GooseSwift/BreatheView.swift returns 3 (one per phase: inhale, hold, exhale)
    - grep -c "Task.isCancelled" GooseSwift/BreatheView.swift returns at least 1
    - grep -c "onDisappear" GooseSwift/BreatheView.swift returns 1
    - grep -v '^//' GooseSwift/BreatheView.swift | grep -c '"Connected"' returns 0 (no wrong connection string)
    - grep -c '"ready"' GooseSwift/BreatheView.swift returns at least 1 (correct disconnected check)
    - grep -c "Section(\"Wellness\")" GooseSwift/MoreView.swift returns 1
    - grep -c "case .breathe:" GooseSwift/MoreView.swift returns 1
    - xcodebuild produces BUILD SUCCEEDED
  </acceptance_criteria>

  <done>BreatheView.swift exists with full box-breathing session loop (inhale/hold/exhale 4s each), circle animation, phase label, disconnected banner (connectionState != "ready"), start/stop buttons with correct styles, reduced-motion guard, .onDisappear cancellation. MoreView.swift has Wellness section and .breathe destination arm. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BreatheView @State → BLE write | UI layer drives buzz calls; BLE layer guards against nil peripheral |
| Task @MainActor → withAnimation | Animation calls must stay on main actor; @MainActor annotation on Task body enforces this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-03 | Denial of Service | phaseTask not cancelled on view disappear | mitigate | .onDisappear { stopSession() } mandatory — cancels Task, sets phaseTask = nil, prevents dual-Task double-buzz |
| T-70-04 | Tampering | connectionState string comparison | mitigate | Use model.ble.connectionState != "ready" (lowercase confirmed); never "Connected" — wrong string silently breaks banner |
| T-70-05 | Elevation of Privilege | withAnimation called off main actor | mitigate | Task { @MainActor in ... } annotation ensures animation runs on main thread; no explicit DispatchQueue.main needed |
</threat_model>

<verification>
1. xcodebuild BUILD SUCCEEDED — both new files and all edits compile clean
2. grep "struct BreatheView" GooseSwift/BreatheView.swift → 1 match
3. grep "model.ble.buzz" GooseSwift/BreatheView.swift → 3 matches (inhale + hold + exhale)
4. grep "onDisappear" GooseSwift/BreatheView.swift → 1 match (.onDisappear { stopSession() })
5. grep '"Connected"' GooseSwift/BreatheView.swift → 0 matches (correct: "ready" used)
6. grep "Section(\"Wellness\")" GooseSwift/MoreView.swift → 1 match
7. grep "case .breathe:" GooseSwift/MoreView.swift → 1 match
8. grep "breathe: .ready" GooseSwift/MoreDataStore.swift → 2 matches (both construction sites)
</verification>

<success_criteria>
- Breathe entry appears in More tab under "Wellness" section — title "Breathe", subtitle "Paced breathing with haptics", icon "wind"
- Tapping Breathe pushes BreatheView into the More NavigationStack (back button works)
- Start begins the session: circle animates inhale (0.6→1.0) / hold (static) / exhale (1.0→0.6), phase label updates, buzz called at each start
- Stop cancels the task and resets view state to idle
- Back navigation (.onDisappear) cancels any running session — no orphaned Tasks
- When WHOOP not connected and session not running: "Connect WHOOP to enable haptics" banner visible
- Reduced motion: circle scale sets directly without withAnimation when accessibilityReduceMotion is true
- Build succeeds with zero compiler errors
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Kind | Description |
|--------|------|------|-------------|
| `BreatheView` | `GooseSwift/BreatheView.swift` | struct (View) | Full Breathe session screen — HAP-02 |
| `BreathePhase` | `GooseSwift/BreatheView.swift` | enum | inhale/hold/exhale phase labels + 4s duration constant |
| `MoreRoute.breathe` | `GooseSwift/MoreRouteModels.swift` | enum case | Push navigation destination for Breathe screen |
| `MoreRoute.wellnessRoutes` | `GooseSwift/MoreRouteModels.swift` | static let | Route group for Wellness section in MoreView |
| `MoreRouteStatus.breathe` | `GooseSwift/MoreRouteModels.swift` | stored property | Status kind for Breathe route (always .ready) |

<output>
Create .planning/phases/70-haptic-primitive-breathe-screen/70-02-SUMMARY.md when done.
</output>
