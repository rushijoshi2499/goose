---
phase: 71-coach-vow-noopapp-notifications-hr-decimation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/NotificationScheduler.swift
  - GooseSwift/GooseAppModel+SleepSync.swift
  - GooseSwift/GooseAppModel+PacketPublishing.swift
  - GooseSwift/GooseBLEClient.swift
  - GooseSwift/GooseBLEClient+Parsing.swift
autonomous: true
requirements:
  - FEAT-03
must_haves:
  truths:
    - "Sleep processed notification fires once after syncBandSleepHistory() completes with bandSleepImportStatus = Sincronizado da pulseira"
    - "Workout detected notification fires once after .finished case in applyActivityDetectionEvents"
    - "Battery low notification fires at most once per BLE session when batteryLevel <= 20"
    - "batteryLowNotificationFired resets to false inside resetLiveDeviceFieldsIfNeeded — fires again on next BLE connect"
    - "All notification scheduling goes through NotificationScheduler actor — no inline UNUserNotificationCenter calls"
    - "Scheduler guards authorizationStatus == .authorized before adding requests"
  artifacts:
    - path: "GooseSwift/NotificationScheduler.swift"
      provides: "actor NotificationScheduler with scheduleSleepProcessed, scheduleWorkoutDetected, scheduleBatteryLow"
      contains: "actor NotificationScheduler"
    - path: "GooseSwift/GooseBLEClient.swift"
      provides: "batteryLowNotificationFired: Bool property"
      contains: "batteryLowNotificationFired"
    - path: "GooseSwift/GooseBLEClient+Parsing.swift"
      provides: "battery gate check in applyBatteryLevel"
      contains: "batteryLowNotificationFired"
    - path: "GooseSwift/GooseAppModel+SleepSync.swift"
      provides: "sleep notification dispatch after sync success"
      contains: "NotificationScheduler"
    - path: "GooseSwift/GooseAppModel+PacketPublishing.swift"
      provides: "workout notification dispatch in .finished case"
      contains: "NotificationScheduler"
  key_links:
    - from: "GooseAppModel+SleepSync.syncBandSleepHistory"
      to: "NotificationScheduler.shared.scheduleSleepProcessed"
      via: "Task { await NotificationScheduler.shared.scheduleSleepProcessed(...) }"
      pattern: "scheduleSleepProcessed"
    - from: "GooseAppModel+PacketPublishing.applyActivityDetectionEvents"
      to: "NotificationScheduler.shared.scheduleWorkoutDetected"
      via: "Task { await NotificationScheduler.shared.scheduleWorkoutDetected(...) }"
      pattern: "scheduleWorkoutDetected"
    - from: "GooseBLEClient+Parsing.applyBatteryLevel"
      to: "NotificationScheduler.shared.scheduleBatteryLow"
      via: "Task { await NotificationScheduler.shared.scheduleBatteryLow(percent:) }"
      pattern: "scheduleBatteryLow"
---

<objective>
Implement FEAT-03: Local notification scheduling via a NotificationScheduler actor for three event types — sleep sync completion, workout detection, and WHOOP battery below 20%.

UNUserNotificationCenter permission (.alert, .badge, .sound) was already granted in onboarding. No runtime permission request is needed; only a getNotificationSettings guard before scheduling.

Output: NotificationScheduler.swift (new actor) + surgical insertions in 4 existing files (GooseBLEClient.swift, GooseBLEClient+Parsing.swift, GooseAppModel+SleepSync.swift, GooseAppModel+PacketPublishing.swift).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-CONTEXT.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-RESEARCH.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-PATTERNS.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NotificationScheduler actor and add batteryLowNotificationFired to GooseBLEClient</name>
  <files>
    GooseSwift/NotificationScheduler.swift
    GooseSwift/GooseBLEClient.swift
  </files>
  <read_first>
    Read GooseSwift/GooseBLEClient.swift focusing on:
    - batteryLevelPercent property declaration — find the exact location among similar battery properties to know where to add batteryLowNotificationFired
    - resetLiveDeviceFieldsIfNeeded(for:) method body (confirmed at line ~459) — find the battery field resets (batteryLevelPercent = 0 or similar) to know exactly where to add batteryLowNotificationFired = false
    - Confirm no existing notification-related properties exist

    Read GooseSwift/OnboardingView.swift line 478 area to confirm the exact options set: [.alert, .badge, .sound] — this informs which content options are safe to use in notification content.
  </read_first>
  <action>
    Create GooseSwift/NotificationScheduler.swift:

    Imports: Foundation, UserNotifications

    actor NotificationScheduler {
      static let shared = NotificationScheduler()
      private init() {}

      // Generic internal dispatch — guards authorization before adding request
      private func schedule(title: String, body: String, identifier: String) {
        let center = UNUserNotificationCenter.current()
        center.getNotificationSettings { settings in
          guard settings.authorizationStatus == .authorized else { return }
          let content = UNMutableNotificationContent()
          content.title = title
          content.body = body
          content.sound = .default
          // No .badge (per Claude's discretion — avoids icon badge clutter)
          let trigger = UNTimeIntervalNotificationTrigger(timeInterval: 1, repeats: false)
          let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
          center.add(request)
        }
      }

      // FEAT-03 site 1: sleep sync completion
      func scheduleSleepProcessed(durationMinutes: Int, hrvMS: Double?, recoveryPercent: Double?) {
        let hours = durationMinutes / 60
        let minutes = durationMinutes % 60
        let dur = hours > 0 ? "\(hours)h \(minutes)m" : "\(minutes)m"
        var parts: [String] = [dur]
        if let hrv = hrvMS { parts.append(String(format: "HRV %dms", Int(hrv))) }
        if let rec = recoveryPercent { parts.append(String(format: "Recovery %d%%", Int(rec))) }
        schedule(
          title: "Sleep synced",
          body: parts.joined(separator: " · "),
          identifier: "goose.sleep.processed.\(Int(Date().timeIntervalSince1970))"
        )
      }

      // FEAT-03 site 2: passive workout detection
      func scheduleWorkoutDetected(activity: String, durationSeconds: Double, strain: Double?) {
        let minutes = Int(durationSeconds / 60)
        var body = "\(activity) · \(minutes)m"
        if let s = strain { body += String(format: " · Strain %.1f", s) }
        schedule(
          title: "Workout detected",
          body: body,
          identifier: "goose.workout.detected.\(Int(Date().timeIntervalSince1970))"
        )
      }

      // FEAT-03 site 3: battery low (static identifier — Bool gate prevents duplicates per session)
      func scheduleBatteryLow(percent: Int) {
        schedule(
          title: "WHOOP battery low",
          body: String(format: "Battery at %d%%. Charge your device.", percent),
          identifier: "goose.battery.low"
        )
      }
    }

    Note on String(format:) usage: per ASVS V5, all numeric values are formatted with explicit format specifiers (%d, %.1f, %.0f) — no raw string interpolation of sensor-derived values in notification body.

    ---

    In GooseSwift/GooseBLEClient.swift:
    1. Add var batteryLowNotificationFired = false alongside the existing battery property group (immediately after or before batteryLevelPercent — match the existing grouping style)
    2. Inside resetLiveDeviceFieldsIfNeeded(for:) method, add batteryLowNotificationFired = false immediately after the existing battery field reset(s) in that method
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "actor NotificationScheduler" GooseSwift/NotificationScheduler.swift && grep -c "batteryLowNotificationFired" GooseSwift/GooseBLEClient.swift && grep -c "batteryLowNotificationFired = false" GooseSwift/GooseBLEClient.swift</automated>
  </verify>
  <done>
    NotificationScheduler.swift exists with actor NotificationScheduler, static let shared, and all three schedule* methods.
    GooseBLEClient.swift has var batteryLowNotificationFired = false in the property list.
    GooseBLEClient.swift has batteryLowNotificationFired = false inside resetLiveDeviceFieldsIfNeeded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire notification dispatch at the 3 confirmed scheduling sites</name>
  <files>
    GooseSwift/GooseBLEClient+Parsing.swift
    GooseSwift/GooseAppModel+SleepSync.swift
    GooseSwift/GooseAppModel+PacketPublishing.swift
  </files>
  <read_first>
    Read GooseSwift/GooseBLEClient+Parsing.swift focusing on:
    - applyBatteryLevel(_:capturedAt:sourceTitle:) method — find the line that sets normalizedLevel / batteryLevelPercent, confirm it dispatches to main thread at entry
    - Confirm no existing notification call exists in this method
    - resetLiveDeviceFieldsIfNeeded reference — confirm Task 1 added the reset (do not add again here)

    Read GooseSwift/GooseAppModel+SleepSync.swift focusing on:
    - syncBandSleepHistory() async function — find the exact line setting store?.bandSleepImportStatus = "Sincronizado da pulseira" (confirmed at line ~174)
    - Find what stageSummary variable looks like — confirm how to compute total duration in minutes from it
    - Find how store: HealthDataStore? is referenced here for snapshot access
    - Confirm store?.liveHRVRMSSD existence or alternative HRV accessor on HealthDataStore

    Read GooseSwift/GooseAppModel+PacketPublishing.swift focusing on:
    - applyActivityDetectionEvents() method — find the .finished(let summary, let reason) case
    - Find the finishActivityRecording(...) call (confirmed at line ~743)
    - Confirm PassiveDetectedActivitySummary fields: activity.title (String), elapsed (Double, seconds), averageHeartRate (Int? or similar)
    - Confirm strain is not available at detection time (do not pass strain if not present)
  </read_first>
  <action>
    GooseBLEClient+Parsing.swift — in applyBatteryLevel(_:capturedAt:sourceTitle:), immediately after the line that sets batteryLevelPercent (the main battery property), add the battery gate:

    if normalizedLevel <= 20, !batteryLowNotificationFired {
      batteryLowNotificationFired = true
      Task {
        await NotificationScheduler.shared.scheduleBatteryLow(percent: normalizedLevel)
      }
    }

    The variable name may be percent or normalizedLevel — use whichever name the existing code uses for the Int battery value at that point. If the value is a different type (e.g. Double), cast to Int inline.

    ---

    GooseAppModel+SleepSync.swift — immediately after the line store?.bandSleepImportStatus = "Sincronizado da pulseira", add:

    Task {
      let durationMinutes: Int
      if let summary = stageSummary {
        durationMinutes = summary.values.reduce(0, +)
      } else {
        durationMinutes = 0
      }
      let hrv = store?.liveHRVRMSSD
      let recovery = Double(store?.snapshot(for: .recovery).value ?? "")
      await NotificationScheduler.shared.scheduleSleepProcessed(
        durationMinutes: durationMinutes,
        hrvMS: hrv,
        recoveryPercent: recovery
      )
    }

    Adjust the stageSummary access to match the actual variable name and type in syncBandSleepHistory(). If stageSummary is a [String: Int] dict, .values.reduce(0,+) gives total minutes. If the sleep result is a different struct, read the actual field from the codebase and compute duration in minutes accordingly. The goal is a reasonable integer minute count — do not force a value if no data is available (pass 0 in that case).

    ---

    GooseAppModel+PacketPublishing.swift — in the .finished(let summary, let reason) case, immediately after the finishActivityRecording(...) call, add:

    Task {
      await NotificationScheduler.shared.scheduleWorkoutDetected(
        activity: summary.activity.title,
        durationSeconds: summary.elapsed,
        strain: nil   // strain is not available at detection time
      )
    }

    If summary.activity does not have a .title property, use summary.activity.rawValue or the string representation that BreatheView or other workout views use for the activity name. Read the actual type from codebase if needed.

    All three insertions use Task { await ... } — they do not block the calling context.
    Do not import UserNotifications in any of the three files — it is encapsulated in NotificationScheduler.swift.
    Only import Foundation in GooseBLEClient+Parsing.swift if the import is not already present (check existing imports before adding).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "NotificationScheduler" GooseSwift/GooseBLEClient+Parsing.swift GooseSwift/GooseAppModel+SleepSync.swift GooseSwift/GooseAppModel+PacketPublishing.swift</automated>
  </verify>
  <done>
    NotificationScheduler appears in all three call-site files.
    batteryLowNotificationFired appears in GooseBLEClient+Parsing.swift.
    xcodebuild build succeeds: xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE battery value → notification body | normalizedLevel is a sensor-derived Int; formatted with %d format specifier — no raw interpolation |
| Sleep stage summary → notification body | durationMinutes computed from stageSummary dict values; formatted with %d |
| Workout summary → notification body | elapsed is Double from passive detector; formatted with %d (minutes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04-01 | Spoofing | NotificationScheduler.getNotificationSettings guard | mitigate | Authorization check ensures notifications are only sent when user has explicitly granted permission; no bypass |
| T-71-04-02 | Tampering | Notification body content injection | mitigate | All sensor-derived values formatted with String(format: "%d" / "%.1f") — no raw string interpolation of untrusted input |
| T-71-04-03 | Denial of Service | Battery notification repeat per session | mitigate | batteryLowNotificationFired Bool gate + reset in resetLiveDeviceFieldsIfNeeded prevents notification spam across reconnects |
| T-71-04-04 | Information Disclosure | HRV + recovery in sleep notification body | accept | Data is local (SQLite/HealthKit); notification delivered to device lock screen is visible to the device owner who is the patient |
| T-71-04-SC | Tampering | npm/pip/cargo installs | accept | Phase adds zero external packages — no package manager installs occur |
</threat_model>

<verification>
1. grep -c "actor NotificationScheduler" GooseSwift/NotificationScheduler.swift — must be 1
2. grep -c "scheduleSleepProcessed\|scheduleWorkoutDetected\|scheduleBatteryLow" GooseSwift/NotificationScheduler.swift — must be 3
3. grep "getNotificationSettings" GooseSwift/NotificationScheduler.swift — must appear (authorization guard)
4. grep "batteryLowNotificationFired" GooseSwift/GooseBLEClient.swift — must appear in both property declaration and resetLiveDeviceFieldsIfNeeded
5. grep "batteryLowNotificationFired" GooseSwift/GooseBLEClient+Parsing.swift — must appear in gate condition
6. grep -c "NotificationScheduler" GooseSwift/GooseAppModel+SleepSync.swift — must be >= 1
7. grep -c "NotificationScheduler" GooseSwift/GooseAppModel+PacketPublishing.swift — must be >= 1
8. xcodebuild build succeeds
</verification>

<success_criteria>
- NotificationScheduler.swift: actor with shared singleton, private schedule(), and 3 public schedule* methods, all using String(format:) for numeric values
- GooseBLEClient.swift: var batteryLowNotificationFired = false in property group + reset in resetLiveDeviceFieldsIfNeeded
- GooseBLEClient+Parsing.swift: battery gate (normalizedLevel <= 20, !batteryLowNotificationFired) → set flag → Task { await scheduleBatteryLow }
- GooseAppModel+SleepSync.swift: Task { await scheduleSleepProcessed(...) } after "Sincronizado da pulseira" status assignment
- GooseAppModel+PacketPublishing.swift: Task { await scheduleWorkoutDetected(...) } after finishActivityRecording in .finished case
- Project compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-04-SUMMARY.md` when done
</output>
