---
phase: 71-coach-vow-noopapp-notifications-hr-decimation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/IntervalTimerView.swift
  - GooseSwift/MetricExplorerView.swift
  - GooseSwift/MoreRouteModels.swift
  - GooseSwift/MoreDataStore.swift
  - GooseSwift/MoreView.swift
autonomous: true
requirements:
  - FEAT-02
must_haves:
  truths:
    - "Interval Timer appears in More tab Wellness section alongside Breathe"
    - "Metric Explorer appears in More tab in a new Data section between Wellness and Settings"
    - "IntervalTimerView session loop cancels correctly when Stop is tapped or view disappears"
    - "buzz(loops: 1) fires at each work→rest and rest→work transition"
    - "MetricExplorerView displays metric rows with name, value, and timestamp using healthStore.snapshot(for:)"
    - "MetricExplorerView receives healthStore as explicit parameter — no @EnvironmentObject"
    - "MoreRouteStatus struct has intervalTimer and metricExplorer fields (compile gate)"
  artifacts:
    - path: "GooseSwift/IntervalTimerView.swift"
      provides: "Full-screen session view with phase loop, countdown, stepper config"
      contains: "IntervalTimerView"
    - path: "GooseSwift/MetricExplorerView.swift"
      provides: "List view of current metric values from healthStore"
      contains: "MetricExplorerView"
    - path: "GooseSwift/MoreRouteModels.swift"
      provides: "intervalTimer + metricExplorer cases, MoreRouteStatus fields, dataRoutes array"
      contains: "intervalTimer"
    - path: "GooseSwift/MoreDataStore.swift"
      provides: "intervalTimer: .ready + metricExplorer: .ready in init and refreshRouteStatus"
      contains: "intervalTimer"
    - path: "GooseSwift/MoreView.swift"
      provides: "Data section + destination cases for both new routes"
      contains: "metricExplorer"
  key_links:
    - from: "MoreView.destination(for:)"
      to: "IntervalTimerView"
      via: "case .intervalTimer: IntervalTimerView()"
      pattern: "case \\.intervalTimer"
    - from: "MoreView.destination(for:)"
      to: "MetricExplorerView"
      via: "case .metricExplorer: MetricExplorerView(healthStore: healthStore)"
      pattern: "case \\.metricExplorer"
    - from: "IntervalTimerView.startSession"
      to: "model.ble.buzz"
      via: "model.ble.buzz(loops: 1)"
      pattern: "buzz\\(loops:"
---

<objective>
Implement FEAT-02: Interval Timer and Metric Explorer — two new More-tab destinations derived from the NoopApp feature set.

Breathe (FEAT-02 item 1) is already complete from Phase 70. This plan delivers the remaining two items: Interval Timer (work/rest loop with haptic transitions) and Metric Explorer (scrollable metric value browser).

Both follow the established MoreRoute 4-switch wiring pattern from Phase 70. IntervalTimerView is an exact adaptation of BreatheView. MetricExplorerView is a List-based data browser.

Output: 2 new Swift view files + 3 modified wiring files (MoreRouteModels, MoreDataStore, MoreView).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-CONTEXT.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-RESEARCH.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-PATTERNS.md
@.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MoreRouteModels, MoreDataStore, and MoreView for both new routes</name>
  <files>
    GooseSwift/MoreRouteModels.swift
    GooseSwift/MoreDataStore.swift
    GooseSwift/MoreView.swift
  </files>
  <read_first>
    Read GooseSwift/MoreRouteModels.swift in full. Identify:
    - enum MoreRoute: the exact case list, confirm .breathe is the last wellness case
    - var title: switch — all existing cases and their line numbers
    - var subtitle: switch — all existing cases
    - var systemImage: switch — confirm .breathe: "wind" spelling
    - var statusKeyPath: switch — confirm .breathe: \.breathe
    - static let wellnessRoutes: [MoreRoute] array definition
    - Whether dataRoutes already exists (it should not — new in this phase)
    - struct MoreRouteStatus — all existing var fields, confirm breathe is present

    Read GooseSwift/MoreDataStore.swift. Find:
    - MoreRouteStatus init site (where breathe: .ready is written)
    - refreshRouteStatus return site (where breathe: .ready appears again)
    - Confirm both sites need the new fields

    Read GooseSwift/MoreView.swift. Find:
    - Section("Wellness") and its routeRows call
    - Section("Settings") location — new Data section goes between Wellness and Settings
    - destination(for route:) switch — find case .breathe insertion point
    - private var healthStore: HealthDataStore property (line ~13) — confirm it exists for MetricExplorerView pass-through
  </read_first>
  <action>
    MoreRouteModels.swift — make 7 changes (per FEAT-02 decision D-01):

    1. Add to enum MoreRoute after .breathe:
       case intervalTimer
       case metricExplorer

    2. Add to var title switch (matching existing case format):
       case .intervalTimer: String(localized: "Interval Timer")
       case .metricExplorer: String(localized: "Metric Explorer")

    3. Add to var subtitle switch:
       case .intervalTimer: String(localized: "Work/rest intervals with haptic transitions")
       case .metricExplorer: String(localized: "Current metric values from your WHOOP data")

    4. Add to var systemImage switch:
       case .intervalTimer: "timer"
       case .metricExplorer: "chart.bar.doc.horizontal"

    5. Add to var statusKeyPath switch:
       case .intervalTimer: \.intervalTimer
       case .metricExplorer: \.metricExplorer

    6. Read the existing `static let wellnessRoutes` array first (must already contain `.breathe` from Phase 70). Append `.intervalTimer` surgically — do NOT replace the full array literal. The result must be `[.breathe, .intervalTimer]`, but verify `.breathe` is already present before writing to avoid dropping it.
       Add new: static let dataRoutes: [MoreRoute] = [.metricExplorer]

    7. Add to struct MoreRouteStatus after var breathe:
       var intervalTimer: MoreStatusKind
       var metricExplorer: MoreStatusKind

    MoreDataStore.swift — make 2 changes:
    1. In routeStatus init, after breathe: .ready, add:
       intervalTimer: .ready,
       metricExplorer: .ready
    2. In refreshRouteStatus return, after breathe: .ready, add:
       intervalTimer: .ready,
       metricExplorer: .ready

    MoreView.swift — make 2 changes:
    1. Add new Section("Data") between Section("Wellness") and Section("Settings"):
       Section("Data") {
         routeRows(MoreRoute.dataRoutes)
       }

    2. In destination(for route:) switch, after case .breathe: BreatheView(), add:
       case .intervalTimer:
         IntervalTimerView()
       case .metricExplorer:
         MetricExplorerView(healthStore: healthStore)

    All 7 MoreRouteModels edits must be complete before any MoreDataStore edit — the struct field additions must precede the init literal updates to compile.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "intervalTimer" GooseSwift/MoreRouteModels.swift GooseSwift/MoreDataStore.swift GooseSwift/MoreView.swift && grep -c "metricExplorer" GooseSwift/MoreRouteModels.swift GooseSwift/MoreDataStore.swift GooseSwift/MoreView.swift</automated>
  </verify>
  <done>
    intervalTimer appears in all three wiring files.
    metricExplorer appears in all three wiring files.
    dataRoutes static array exists in MoreRouteModels.swift.
    MoreRouteStatus struct has both new fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create IntervalTimerView.swift and MetricExplorerView.swift</name>
  <files>
    GooseSwift/IntervalTimerView.swift
    GooseSwift/MetricExplorerView.swift
  </files>
  <read_first>
    Read GooseSwift/BreatheView.swift in full. Extract:
    - Exact import statements (lines 1–2)
    - @Environment(GooseAppModel.self) injection spelling (line ~25)
    - FitnessColor token names used: .background, .panel, .standCyan, .workoutYellow, .endRed, .secondaryText
    - .toolbar(.hidden, for: .tabBar) modifier presence and exact form
    - .toolbarBackground and .toolbarColorScheme modifier forms
    - .onDisappear { stopSession() } placement
    - The exact pattern of the Task { @MainActor in repeat { } while !Task.isCancelled } loop — confirm try? await Task.sleep(for: .seconds(...)) and guard !Task.isCancelled else { break }
    - Button layout: exact frame(width: 160, height: 48) and padding(.bottom, 32)
    - BLE hint row structure (sensor icon + text when not connected)

    Read GooseSwift/MoreView.swift lines 140–175 to confirm MoreAlgorithmsView(store: store, healthStore: healthStore) pattern for explicit healthStore passing.

    Read GooseSwift/HealthModels.swift briefly to confirm snapshot(for:) returns HealthMetricSnapshot with .value (String), .unit (String), .title (String), .systemImage (String) fields.
  </read_first>
  <action>
    Create GooseSwift/IntervalTimerView.swift:

    Imports: Foundation, SwiftUI

    Private enum IntervalPhase: case work, rest

    Struct IntervalTimerView: View with:
    - @Environment(GooseAppModel.self) private var model
    - @State private var isRunning = false
    - @State private var phaseTask: Task<Void, Never>? = nil
    - @State private var currentPhase: IntervalPhase = .work
    - @State private var countdownSeconds: Int = 30
    - @State private var workSeconds: Int = 30   (default per CONTEXT.md discretion)
    - @State private var restSeconds: Int = 10   (default per CONTEXT.md discretion)

    Computed properties:
    - var currentPhaseLabel: String — "WORK" / "REST"
    - var currentPhaseColor: Color — FitnessColor.standCyan (.work) / FitnessColor.workoutYellow (.rest)
    - var countdownText: String — String(format: "%02d:%02d", countdownSeconds / 60, countdownSeconds % 60)

    body: ZStack with FitnessColor.background.ignoresSafeArea() then VStack(spacing: 0):
    - Spacer()
    - if !isRunning: config area — VStack(spacing: 24) with two IntervalStepperRow(label: "Work", seconds: $workSeconds, min: 5, max: 300, step: 5) and IntervalStepperRow(label: "Rest", seconds: $restSeconds, min: 5, max: 120, step: 5) — padded .horizontal 32
    - if isRunning: countdown area — VStack(spacing: 8) with Text(currentPhaseLabel) .font(.system(size: 20, weight: .semibold)) .tracking(2.0) .foregroundStyle(currentPhaseColor) .animation(.easeInOut(duration: 0.25), value: currentPhase) .contentTransition(.opacity), and Text(countdownText) .font(.system(size: 64, weight: .semibold, design: .rounded)) .foregroundStyle(currentPhaseColor) .monospacedDigit() .contentTransition(.numericText(countsDown: true))
    - BLE hint row (copy pattern from BreatheView — sensor.tag.radiowaves.forward icon + "Connect WHOOP to enable haptics" text — shown only when !isRunning)
    - Spacer()
    - CTA Button(isRunning ? "Stop" : "Start") frame(width: 160, height: 48) background Capsule padding(.bottom, 32) — Stop: foreground FitnessColor.endRed + background endRed.opacity(0.14); Start: foreground FitnessColor.standCyan + background standCyan.opacity(0.14)
    - Start button accessibility: .accessibilityLabel("Start interval timer")
    - Stop button accessibility: .accessibilityLabel("Stop interval timer")
    - Countdown: .accessibilityLabel("\(currentPhaseLabel) — \(countdownText) remaining") .accessibilityAddTraits(.updatesFrequently)

    View modifiers on body:
    - .navigationTitle("Interval Timer")
    - .navigationBarTitleDisplayMode(.inline)
    - .toolbar(.hidden, for: .tabBar)
    - .background(FitnessColor.background.ignoresSafeArea())
    - .toolbarBackground(FitnessColor.background, for: .navigationBar)
    - .toolbarColorScheme(.dark, for: .navigationBar)
    - .onDisappear { stopSession() }

    startSession() func:
    - isRunning = true
    - countdownSeconds = workSeconds
    - phaseTask = Task { @MainActor in
        repeat {
          currentPhase = .work
          model.ble.buzz(loops: 1)
          countdownSeconds = workSeconds
          for _ in 0..<workSeconds {
            try? await Task.sleep(for: .seconds(1))
            guard !Task.isCancelled else { break }
            countdownSeconds -= 1
          }
          guard !Task.isCancelled else { break }

          currentPhase = .rest
          model.ble.buzz(loops: 1)
          countdownSeconds = restSeconds
          for _ in 0..<restSeconds {
            try? await Task.sleep(for: .seconds(1))
            guard !Task.isCancelled else { break }
            countdownSeconds -= 1
          }
        } while !Task.isCancelled
      }

    stopSession() func:
    - phaseTask?.cancel()
    - phaseTask = nil
    - isRunning = false
    - currentPhase = .work

    Private struct IntervalStepperRow: View — params: label: String, seconds: Binding<Int>, min: Int, max: Int, step: Int:
    - HStack with Text(label) .subheadline.weight(.semibold), Spacer(), HStack(spacing: 16) with minus Button decrement (max(min, seconds - step)), Text("\(seconds)s") .subheadline.weight(.semibold) .monospacedDigit(), plus Button increment (Swift.min(max, seconds + step)) — .foregroundStyle(FitnessColor.standCyan)
    - .padding(14) .background(FitnessColor.panel, in: RoundedRectangle(cornerRadius: 10, style: .continuous))

    ---

    Create GooseSwift/MetricExplorerView.swift:

    Import: SwiftUI

    Private struct MetricRow: Identifiable — id: String, name: String, systemImage: String, tint: Color, displayValue: String, timestamp: String

    Struct MetricExplorerView: View with:
    - var healthStore: HealthDataStore (explicit parameter — NOT @EnvironmentObject, per FEAT-02 Pitfall 6)

    Private computed property var metricRows: [MetricRow] — build rows from healthStore snapshots grouped per UI-SPEC sections. Use the same snapshot routes as the Coach tab. RHR: use HeartRateSeriesStore.shared.restingEstimate() as Double? and format as Int (per research open question — no .rhr HealthRoute exists). Format displayValue: snapshot.value + " " + snapshot.unit if value is not "--"; otherwise "—" (em dash). Timestamp: format "Updated Xh ago" from snapshot's updatedAt if available, else "No data".

    body: List with sections per UI-SPEC layout:
    - Section("READINESS"): readiness and recovery rows
    - Section("STRAIN"): day strain and workout strain rows (use available HealthRoute cases)
    - Section("CARDIOVASCULAR"): HRV and RHR rows (RHR computed from HeartRateSeriesStore.shared.restingEstimate())
    - Section("SLEEP"): sleep duration and sleep efficiency rows
    - Section("STRESS"): stress score row

    Section headers: .font(.system(size: 11, weight: .black)) .foregroundStyle(.secondary) — matching DailyJournalSheet "TAGS" header pattern.

    Each row HStack(spacing: 12):
    - Image(systemName: row.systemImage) .font(.caption.weight(.bold)) .foregroundStyle(row.tint) .frame(28, 28) .background(row.tint.opacity(0.12), in: RoundedRectangle(cornerRadius: 6))
    - VStack(.leading, spacing: 2): Text(row.name) .subheadline, Text(row.timestamp) .caption2 .foregroundStyle(.tertiary)
    - Spacer()
    - Text(row.displayValue) .subheadline.weight(.semibold) .fontDesign(.rounded) — "—" when no data

    Row accessibility: .accessibilityElement(children: .combine) .accessibilityLabel("\(row.name), \(row.displayValue), \(row.timestamp)")
    .padding(.vertical, 4) on each row HStack

    If metricRows is empty: ContentUnavailableView("No Metrics Yet", systemImage: "chart.bar.xaxis", description: Text("Sync your WHOOP band to populate metrics."))

    View modifiers:
    - .navigationTitle("Metric Explorer")
    - .navigationBarTitleDisplayMode(.inline)
    - .listStyle(.insetGrouped)
    - .gooseListBackground()
    - .toolbarBackground(.hidden, for: .navigationBar)
    - .toolbar(.hidden, for: .tabBar)
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -c "IntervalTimerView" GooseSwift/IntervalTimerView.swift && grep -c "MetricExplorerView" GooseSwift/MetricExplorerView.swift</automated>
  </verify>
  <done>
    Both new files exist and contain their primary struct definitions.
    IntervalTimerView.swift: contains phaseTask, startSession(), stopSession(), buzz(loops: 1), onDisappear.
    MetricExplorerView.swift: var healthStore: HealthDataStore (explicit, not @EnvironmentObject), List with Section blocks.
    xcodebuild build succeeds: xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntervalTimerView → GooseBLEClient.buzz | Session loop fires buzz from @MainActor Task; GooseBLEClient must be reachable from model.ble |
| MetricExplorerView ← healthStore | healthStore passed as explicit parameter; no implicit environment lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03-01 | Denial of Service | IntervalTimerView Task leak | mitigate | stopSession() cancels phaseTask; .onDisappear { stopSession() } mandatory — verified by grep for onDisappear in IntervalTimerView.swift |
| T-71-03-02 | Tampering | MoreRouteStatus missing fields | mitigate | MoreRouteStatus is a plain struct with no defaults; missing intervalTimer or metricExplorer fields cause compile error — caught at build time |
| T-71-03-03 | Information Disclosure | MetricExplorerView healthStore injection | accept | healthStore is app-local data; explicit parameter prevents accidental environment injection from wrong scope |
</threat_model>

<verification>
1. grep -c "intervalTimer" GooseSwift/MoreRouteModels.swift — must be >= 5 (enum case, title, subtitle, systemImage, statusKeyPath)
2. grep -c "intervalTimer" GooseSwift/MoreDataStore.swift — must be >= 2 (init + refreshRouteStatus)
3. grep -c "intervalTimer" GooseSwift/MoreView.swift — must be >= 1 (destination case)
4. grep "dataRoutes" GooseSwift/MoreRouteModels.swift — must appear
5. grep "@EnvironmentObject" GooseSwift/MetricExplorerView.swift — must return empty (not injected via environment)
6. grep "onDisappear" GooseSwift/IntervalTimerView.swift — must appear (stop session on disappear)
7. xcodebuild build succeeds
</verification>

<success_criteria>
- Both new view files created; both structs referenced in MoreView.destination(for:)
- MoreRouteModels: 2 new enum cases + all 4 switch arms updated + wellnessRoutes updated + dataRoutes added + MoreRouteStatus has 2 new fields
- MoreDataStore: both new routes set to .ready in init and refreshRouteStatus
- MoreView: Section("Data") added between Wellness and Settings; both destination cases added
- IntervalTimerView: BreatheView pattern exact — Task loop, buzz, onDisappear
- MetricExplorerView: healthStore as explicit parameter, List with section groups, ContentUnavailableView empty state
- Project compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/71-coach-vow-noopapp-notifications-hr-decimation/71-03-SUMMARY.md` when done
</output>
