---
phase: 74-fork-pr-integration-ux-i18n-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/Localizable.xcstrings
autonomous: true
requirements:
  - PR-INT-04

must_haves:
  truths:
    - "All 58 genuine UI strings display translated pt-PT text — no bare English key strings visible in the pt-PT locale"
    - "5 format-specifier/accessibility strings are marked shouldTranslate: false — Xcode will not flag them as missing translations"
    - "Localizable.xcstrings remains valid JSON — the entire localisation system does not silently break"
  artifacts:
    - path: GooseSwift/Localizable.xcstrings
      provides: "pt-PT translations for 58 UI strings + shouldTranslate:false for 5 technical strings"
      contains: '"pt-PT"'
  key_links:
    - from: GooseSwift/Localizable.xcstrings
      to: SwiftUI views
      via: "String(localized:) / NSLocalizedString"
      pattern: '"pt-PT"'
---

<objective>
Add pt-PT translations for the 63 strings currently missing any pt-PT localization entry in Localizable.xcstrings, and mark 5 format/accessibility strings as shouldTranslate: false.

Purpose: PR-INT-04 — eliminate visible key-string fallback for pt-PT locale; English is already set as sourceLanguage and requires no change.
Output: Updated GooseSwift/Localizable.xcstrings with 58 new pt-PT entries and 5 shouldTranslate annotations.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-fork-pr-integration-ux-i18n-auth/74-CONTEXT.md
@.planning/phases/74-fork-pr-integration-ux-i18n-auth/74-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark 5 technical strings as shouldTranslate: false (per D-03 / PR-INT-04)</name>
  <read_first>
    GooseSwift/Localizable.xcstrings — read the full JSON structure around these 5 keys before editing:
    - "%@ — %@ remaining"
    - "%@, %@, %@"
    - "%@. %@. Double-tap to dismiss."
    - "Breathing circle, %@ phase"
    - "Include Database%@"
  </read_first>
  <action>
    For each of the 5 keys listed above, locate their entry in Localizable.xcstrings (JSON file) and replace the entry with the shouldTranslate form. The correct structure is:

    "KEY_STRING" : {
      "shouldTranslate" : false
    }

    If the key currently has a "localizations" dict, replace the entire entry. If it currently has no content at all (bare key with empty dict), replace with the shouldTranslate form.

    Work one key at a time using Edit. Do not touch any other keys. Preserve all surrounding JSON (commas, brackets).

    After all 5 edits, run the JSON validation gate:
    python3 -c "import json; json.load(open('GooseSwift/Localizable.xcstrings')); print('JSON valid')"

    If the validation fails, revert the last edit that broke JSON before proceeding.
  </action>
  <verify>
    <automated>python3 -c "import json; data=json.load(open('GooseSwift/Localizable.xcstrings')); keys=['%@ — %@ remaining','%@, %@, %@','%@. %@. Double-tap to dismiss.','Breathing circle, %@ phase','Include Database%@']; errors=[k for k in keys if data.get('strings',{}).get(k,{}).get('shouldTranslate') is not False]; print('PASS' if not errors else f'FAIL: {errors}')"</automated>
  </verify>
  <done>All 5 technical strings have shouldTranslate: false in Localizable.xcstrings; JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add pt-PT translations for 58 genuine UI strings (per PR-INT-04)</name>
  <read_first>
    GooseSwift/Localizable.xcstrings — read each key's current entry before adding pt-PT to it. The file is ~8934 lines; use grep to locate each key's line number before reading that section.
  </read_first>
  <action>
    For each of the 58 strings below, add a "pt-PT" localization entry inside the string's "localizations" dict. If the string entry has no "localizations" key yet, add it. The JSON structure for each addition is:

    "pt-PT" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "TRANSLATION"
      }
    }

    Translations by category:

    PORTUGUESE-KEY STRINGS (pt-PT value = key verbatim):
    - "A importar…" → "A importar…"
    - "A iniciar..." → "A iniciar..."
    - "Armar Alarme" → "Armar Alarme"
    - "Armar alarme de despertar" → "Armar alarme de despertar"
    - "Cancelado" → "Cancelado"
    - "Cancelar" → "Cancelar"
    - "Cancelar Alarme" → "Cancelar Alarme"
    - "Cancelar alarme armado" → "Cancelar alarme armado"
    - "Conecta o WHOOP para ativar" → "Conecta o WHOOP para ativar"
    - "Conecta o WHOOP para usar o alarme" → "Conecta o WHOOP para usar o alarme"
    - "Diário" → "Diário"
    - "Diário de Hoje" → "Diário de Hoje"
    - "Eliminar" → "Eliminar"
    - "Eliminar todos os dados locais?" → "Eliminar todos os dados locais?"
    - "Escrever nota do dia" → "Escrever nota do dia"
    - "Esta acção remove…" → "Esta acção remove…"
    - "Guardar" → "Guardar"
    - "Hora de acordar" → "Hora de acordar"
    - "Import do servidor" → "Import do servidor"
    - "Não executado" → "Não executado"
    - "ROTAS COACH" → "ROTAS COACH"

    ENGLISH-KEY STRINGS (pt-PT translations):
    - "7-Day Trends" → "Tendências 7 Dias"
    - "Breathe" → "Respirar"
    - "Cancel workout entry" → "Cancelar registo de treino"
    - "Choose Export Preset" → "Escolher predefinição de exportação"
    - "Connect WHOOP to enable haptics" → "Liga o WHOOP para ativar hápticos"
    - "Current metric values from your WHOOP data" → "Valores actuais das métricas dos teus dados WHOOP"
    - "Custom (current settings)" → "Personalizado (definições actuais)"
    - "Data" → "Dados"
    - "Dismiss nudge" → "Dispensar aviso"
    - "Effort" → "Esforço"
    - "Effort %lld of 10" → "Esforço %lld de 10"
    - "Frames & Metrics" → "Frames & Métricas"
    - "Full Diagnostic" → "Diagnóstico Completo"
    - "HR Sanitizer" → "HR Sanitizer"
    - "Interval Timer" → "Temporizador de Intervalos"
    - "License" → "Licença"
    - "Log" → "Registo"
    - "Log Workout" → "Registar Treino"
    - "Log workout" → "Registar treino"
    - "Metric Explorer" → "Explorador de Métricas"
    - "No Metrics Yet" → "Ainda sem Métricas"
    - "No data for the last 7 days" → "Sem dados nos últimos 7 dias"
    - "Paced breathing with haptics" → "Respiração compassada com hápticos"
    - "Perceived Effort" → "Esforço Percebido"
    - "Sport" → "Desporto"
    - "Start breathing session" → "Iniciar sessão de respiração"
    - "Start interval timer" → "Iniciar temporizador de intervalos"
    - "Stop" → "Parar"
    - "Stop breathing session" → "Parar sessão de respiração"
    - "Stop interval timer" → "Parar temporizador de intervalos"
    - "Sync your WHOOP band to populate metrics." → "Sincroniza a tua banda WHOOP para preencher as métricas."
    - "Wellness" → "Bem-estar"
    - "Work/rest intervals with haptic transitions" → "Intervalos trabalho/descanso com transições hápticas"
    - "%lld / 10" → "%lld / 10"
    - "%lld min" → "%lld min"
    - "%llds" → "%llds"
    - "Frames & Metrics: decoded data only\n…" — if this key exists, add pt-PT matching the English value verbatim (technical export description, keep English)

    After completing all edits, validate JSON:
    python3 -c "import json; json.load(open('GooseSwift/Localizable.xcstrings')); print('JSON valid')"

    If any key cannot be found in the file, note it in the SUMMARY but do not halt execution — proceed to remaining keys.
  </action>
  <verify>
    <automated>python3 -c "
import json
data = json.load(open('GooseSwift/Localizable.xcstrings'))
strings = data.get('strings', {})
sample_keys = ['Breathe', '7-Day Trends', 'Cancelar', 'Stop', 'Wellness', 'Effort']
missing = []
for k in sample_keys:
    entry = strings.get(k, {})
    locs = entry.get('localizations', {})
    if 'pt-PT' not in locs:
        missing.append(k)
print('PASS' if not missing else f'FAIL missing pt-PT: {missing}')
"</automated>
  </verify>
  <done>
    - 58 genuine UI strings have pt-PT translation entries in Localizable.xcstrings
    - Localizable.xcstrings passes JSON validation (python3 json.load returns without error)
    - pt-PT entries use state: "translated" (not "new")
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| xcstrings → UI | Localised strings are rendered directly into SwiftUI Text() views |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering | Localizable.xcstrings JSON edit | mitigate | Validate JSON after every edit batch with python3 json.load; invalid JSON silently breaks all localisation |
| T-74-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
1. python3 -c "import json; json.load(open('GooseSwift/Localizable.xcstrings')); print('JSON valid')" — must print "JSON valid"
2. grep -c '"pt-PT"' GooseSwift/Localizable.xcstrings — must be substantially higher than pre-task count (was 757; expect 815+ after adding 58 entries)
3. Build check: xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -derivedDataPath /tmp/goose-74-01 CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5
</verification>

<success_criteria>
- Localizable.xcstrings is valid JSON
- 5 technical strings carry shouldTranslate: false
- 58 UI strings carry pt-PT "translated" entries
- xcodebuild build succeeds (no localisation-related errors)
</success_criteria>

<output>
Create `.planning/phases/74-fork-pr-integration-ux-i18n-auth/74-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| File | Change |
|------|--------|
| GooseSwift/Localizable.xcstrings | +58 pt-PT translation entries; 5 shouldTranslate:false annotations |
