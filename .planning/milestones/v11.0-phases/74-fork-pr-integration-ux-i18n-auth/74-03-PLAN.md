---
phase: 74-fork-pr-integration-ux-i18n-auth
plan: 03
type: execute
wave: 2
depends_on:
  - "74-01"
  - "74-02"
files_modified:
  - GooseSwift/FitnessFormatting.swift
  - GooseSwift/FitnessLiveWorkoutViews.swift
  - GooseSwift/HomeTimelineViews.swift
  - GooseSwift/HealthDataStore+V24Biometrics.swift
  - GooseSwift/HealthRecoveryWidgets.swift
autonomous: true
requirements:
  - PR-INT-03

must_haves:
  truths:
    - "Skin temperature displays in °F when unitSystem is imperial, °C when metric — stored skinTempCelsius value is never mutated"
    - "GPS distance displays in MI/FT when imperial, KM/M when metric — all 3 call sites in FitnessLiveWorkoutViews updated"
    - "GPS elevation displays in FT when imperial, M when metric — FitnessElevationPage updated"
    - "Pace displays as min/mile when imperial, min/km when metric — formatFitnessPace updated"
    - "Home timeline distance (formatDistance) displays in MI/km depending on unit preference"
    - "Switching unitSystem in device settings re-renders all affected views without app restart (AppStorage reactive)"
  artifacts:
    - path: GooseSwift/FitnessFormatting.swift
      provides: "Unit-aware fitnessDistanceParts, fitnessPaceParts, fitnessElevationParts free functions"
      contains: "unitSystemRaw"
    - path: GooseSwift/HealthDataStore+V24Biometrics.swift
      provides: "skinTempText removed from V24BiometricsResult extension; unit-aware helper in FitnessFormatting"
      contains: "skinTempText"
    - path: GooseSwift/FitnessLiveWorkoutViews.swift
      provides: "@AppStorage unitSystemRaw wired into distance/pace/elevation call sites"
      contains: "OnboardingStorage.unitSystem"
    - path: GooseSwift/HomeTimelineViews.swift
      provides: "formatDistance reads @AppStorage unit preference"
      contains: "OnboardingStorage.unitSystem"
    - path: GooseSwift/HealthRecoveryWidgets.swift
      provides: "V24BiometricsCard passes unitSystemRaw to skin temp helper"
      contains: "unitSystemRaw"
  key_links:
    - from: GooseSwift/FitnessLiveWorkoutViews.swift
      to: GooseSwift/FitnessFormatting.swift
      via: "fitnessDistanceParts(_:unitSystemRaw:) + fitnessElevationParts(_:unitSystemRaw:) + formatFitnessPace(_:unitSystemRaw:)"
      pattern: "unitSystemRaw"
    - from: GooseSwift/HealthRecoveryWidgets.swift
      to: GooseSwift/HealthDataStore+V24Biometrics.swift
      via: "V24BiometricsResult.skinTempCelsius passed to FitnessFormatting.skinTempText(celsius:unitSystemRaw:)"
      pattern: "skinTempText"
---

<objective>
Extend the existing imperial/metric unit system to temperature (skin temp) and GPS fitness metrics (distance, pace, elevation) — the four concrete hardcoded sites found during research. All conversion happens at display time only; stored values (skinTempCelsius, CLLocationDistance metres) are never mutated.

Purpose: PR-INT-03 — users whose device is set to imperial see °F, MI/FT, min/mile instead of hardcoded metric values. The @AppStorage key (OnboardingStorage.unitSystem) is already reactive: changing the setting in More > Profile causes immediate view re-render with no app restart.
Output: FitnessFormatting.swift gains unit-aware helper functions; 4 call sites in FitnessLiveWorkoutViews, HomeTimelineViews, and HealthRecoveryWidgets are updated.
</objective>

<execution_context>
@/Users/francisco/.claude/gsd-core/workflows/execute-plan.md
@/Users/francisco/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/74-fork-pr-integration-ux-i18n-auth/74-CONTEXT.md
@.planning/phases/74-fork-pr-integration-ux-i18n-auth/74-RESEARCH.md
@GooseSwift/FitnessFormatting.swift
@GooseSwift/MoreProfileViews.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit-aware formatting helpers to FitnessFormatting.swift and patch skinTempText (per PR-INT-03)</name>
  <read_first>
    - GooseSwift/FitnessFormatting.swift — read entire file; understand current fitnessDistanceParts and formatFitnessPace signatures
    - GooseSwift/MoreProfileViews.swift lines 464–580 — read MoreProfileUnitSystem enum and MoreProfileFormatting pattern (the exact model to replicate)
    - GooseSwift/HealthDataStore+V24Biometrics.swift lines 1–35 — read current skinTempText computed var
  </read_first>
  <behavior>
    - FitnessFormatting.skinTempText(celsius: 37.0, unitSystemRaw: "imperial") == "98.6 °F"
    - FitnessFormatting.skinTempText(celsius: 37.0, unitSystemRaw: "metric") == "37.0 °C"
    - FitnessFormatting.skinTempText(celsius: nil_sentinel_use_guard, unitSystemRaw: any) == "--" (guard let v = celsius)
    - fitnessDistanceParts(1609.344, unitSystemRaw: "imperial").unit == "MI"
    - fitnessDistanceParts(500, unitSystemRaw: "imperial").unit == "FT"
    - fitnessDistanceParts(1500, unitSystemRaw: "metric").unit == "KM"
    - fitnessDistanceParts(500, unitSystemRaw: "metric").unit == "M"
    - fitnessElevationParts(100, unitSystemRaw: "imperial").unit == "FT"
    - fitnessElevationParts(100, unitSystemRaw: "metric").unit == "M"
    - formatFitnessPace(300, unitSystemRaw: "imperial") contains "'" (min/mile format)
    - formatFitnessPace(300, unitSystemRaw: "metric") == "5'00\"" (min/km, same as before)
    - formatFitnessPace(nil, unitSystemRaw: any) == "--'--\""
  </behavior>
  <action>
    In GooseSwift/FitnessFormatting.swift, make these changes:

    1. REPLACE the existing `fitnessDistanceParts(_ meters: CLLocationDistance)` free function (currently returns hardcoded KM/M) with a two-parameter version:

       func fitnessDistanceParts(_ meters: CLLocationDistance, unitSystemRaw: String = MoreProfileUnitSystem.metric.rawValue) -> (value: String, unit: String) {
         if MoreProfileUnitSystem(rawValue: unitSystemRaw) == .imperial {
           let miles = meters / 1609.344
           if miles >= 0.1 {
             return (String(format: "%.2f", miles), "MI")
           }
           let feet = meters * 3.28084
           return ("\(Int(max(feet, 0).rounded()))", "FT")
         }
         if meters >= 1000 {
           return (String(format: "%.2f", meters / 1000), "KM")
         }
         return ("\(Int(max(meters, 0).rounded()))", "M")
       }

       Default parameter preserves metric behaviour for any call sites not yet updated.

    2. REPLACE the existing `formatFitnessPace(_ secondsPerKilometer: TimeInterval?)` with a two-parameter version:

       func formatFitnessPace(_ secondsPerKilometer: TimeInterval?, unitSystemRaw: String = MoreProfileUnitSystem.metric.rawValue) -> String {
         guard let secondsPerKilometer, secondsPerKilometer.isFinite else {
           return "--'--\""
         }
         let secondsPerUnit: TimeInterval
         if MoreProfileUnitSystem(rawValue: unitSystemRaw) == .imperial {
           secondsPerUnit = secondsPerKilometer * 1.60934  // convert s/km to s/mile
         } else {
           secondsPerUnit = secondsPerKilometer
         }
         let totalSeconds = max(Int(secondsPerUnit.rounded()), 0)
         return String(format: "%d'%02d\"", totalSeconds / 60, totalSeconds % 60)
       }

    3. ADD a new free function after formatFitnessPace:

       func fitnessElevationParts(_ meters: CLLocationDistance, unitSystemRaw: String = MoreProfileUnitSystem.metric.rawValue) -> (value: String, unit: String) {
         if MoreProfileUnitSystem(rawValue: unitSystemRaw) == .imperial {
           let feet = meters * 3.28084
           return ("\(Int(max(feet, 0).rounded()))", "FT")
         }
         return ("\(Int(max(meters, 0).rounded()))", "M")
       }

    4. ADD a new static method to the FitnessFormatting-equivalent scope. Since FitnessFormatting.swift has no enclosing enum, add this as a free function at the end of the file:

       func skinTempText(celsius: Double?, unitSystemRaw: String = MoreProfileUnitSystem.metric.rawValue) -> String {
         guard let v = celsius else { return "--" }
         if MoreProfileUnitSystem(rawValue: unitSystemRaw) == .imperial {
           return String(format: "%.1f °F", v * 9.0 / 5.0 + 32.0)
         }
         return String(format: "%.1f °C", v)
       }

    5. In GooseSwift/HealthDataStore+V24Biometrics.swift, REMOVE the hardcoded `skinTempText` computed var from the `extension V24BiometricsResult` block (lines ~20–23). The view will call the free function `skinTempText(celsius:unitSystemRaw:)` from FitnessFormatting.swift instead.
       Keep `spo2Text` and `respRateText` unchanged.

    IMPORTANT: Do not mutate `skinTempCelsius`. The stored Double remains in Celsius always.
  </action>
  <verify>
    <automated>xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -derivedDataPath /tmp/goose-74-03a CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -5</automated>
  </verify>
  <done>
    - FitnessFormatting.swift contains fitnessDistanceParts(_:unitSystemRaw:), fitnessElevationParts(_:unitSystemRaw:), formatFitnessPace(_:unitSystemRaw:), skinTempText(celsius:unitSystemRaw:) — all with default parameter preserving metric behaviour
    - V24BiometricsResult.skinTempText computed var is removed from HealthDataStore+V24Biometrics.swift
    - Build succeeds (no type errors from removed computed var or signature change)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire @AppStorage unitSystemRaw into all 4 display call sites (per PR-INT-03)</name>
  <read_first>
    - GooseSwift/FitnessLiveWorkoutViews.swift — read lines 220–250, 320–345, 385–405 (distance call sites) and lines 425–470 (FitnessElevationPage, pace display)
    - GooseSwift/HomeTimelineViews.swift — read lines 140–160 (formatDistance private method)
    - GooseSwift/HealthRecoveryWidgets.swift — read lines 390–415 (V24BiometricsCard, skinTempText call site)
  </read_first>
  <action>
    SITE 1 — FitnessLiveWorkoutViews.swift: distance + pace call sites (lines ~227, ~233, ~330, ~334, ~392, ~396).

    Each View struct that calls fitnessDistanceParts or formatFitnessPace must gain:
      @AppStorage(OnboardingStorage.unitSystem) private var unitSystemRaw = MoreProfileUnitSystem.imperial.rawValue

    Then update each call:
      fitnessDistanceParts(distanceMeters)  →  fitnessDistanceParts(distanceMeters, unitSystemRaw: unitSystemRaw)
      formatFitnessPace(currentPace)        →  formatFitnessPace(currentPace, unitSystemRaw: unitSystemRaw)
      formatFitnessPace(averagePace)        →  formatFitnessPace(averagePace, unitSystemRaw: unitSystemRaw)

    Also update the "ROLLING\nKM" and "AVERAGE\nPACE" label strings to be dynamic:
      label: MoreProfileUnitSystem(rawValue: unitSystemRaw) == .imperial ? "ROLLING\nMI" : "ROLLING\nKM"
      label: "AVERAGE\nPACE"  (pace label needs no unit suffix change — min/km vs min/mile is conveyed by the value format)

    SITE 2 — FitnessElevationPage struct in FitnessLiveWorkoutViews.swift (lines ~425–460).

    Add @AppStorage to FitnessElevationPage:
      @AppStorage(OnboardingStorage.unitSystem) private var unitSystemRaw = MoreProfileUnitSystem.imperial.rawValue

    Replace the two hardcoded elevation displays:
      value: "\(Int(max(elevationGainMeters, 0).rounded()))", unit: "M"
      →  let gainParts = fitnessElevationParts(elevationGainMeters, unitSystemRaw: unitSystemRaw)
         value: gainParts.value, unit: gainParts.unit

      value: "\(Int(max(elevationMeters, 0).rounded()))", unit: "M"
      →  let elevParts = fitnessElevationParts(elevationMeters, unitSystemRaw: unitSystemRaw)
         value: elevParts.value, unit: elevParts.unit

    SITE 3 — HomeTimelineViews.swift: formatDistance private method (lines ~148–152).

    The containing View struct must gain @AppStorage. Locate the struct that owns `formatDistance` and add:
      @AppStorage(OnboardingStorage.unitSystem) private var unitSystemRaw = MoreProfileUnitSystem.imperial.rawValue

    Replace the method body:
      private func formatDistance(_ meters: Double) -> String {
        let parts = fitnessDistanceParts(meters, unitSystemRaw: unitSystemRaw)
        return "\(parts.value) \(parts.unit.lowercased())"
      }

    SITE 4 — HealthRecoveryWidgets.swift: V24BiometricsCard (lines ~390–415).

    Add @AppStorage to V24BiometricsCard:
      @AppStorage(OnboardingStorage.unitSystem) private var unitSystemRaw = MoreProfileUnitSystem.imperial.rawValue

    Replace call from:
      result.skinTempText
    to:
      skinTempText(celsius: result.skinTempCelsius, unitSystemRaw: unitSystemRaw)

    NOTE: If `result.skinTempText` is referenced elsewhere in the codebase, update those sites too. Run:
      grep -rn "\.skinTempText" GooseSwift/ to find all remaining references before completing.

    After all edits, do a final build.
  </action>
  <verify>
    <automated>xcodebuild build -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -derivedDataPath /tmp/goose-74-03b CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD SUCCEEDED" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "OnboardingStorage.unitSystem" GooseSwift/FitnessLiveWorkoutViews.swift — returns at least 1 hit (AppStorage declaration present)
    - grep -n "OnboardingStorage.unitSystem" GooseSwift/HomeTimelineViews.swift — returns at least 1 hit
    - grep -n "OnboardingStorage.unitSystem" GooseSwift/HealthRecoveryWidgets.swift — returns at least 1 hit
    - grep -rn "\.skinTempText" GooseSwift/ — returns 0 hits (computed var removed and all call sites updated)
    - grep -n "unitSystemRaw" GooseSwift/FitnessFormatting.swift — returns multiple hits (all helper functions have the parameter)
    - xcodebuild build succeeds with BUILD SUCCEEDED
  </acceptance_criteria>
  <done>
    All 4 display sites pass unitSystemRaw to the unit-aware helpers. @AppStorage is declared on each containing View struct. Build succeeds. skinTempText computed var reference count is zero across the codebase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust bridge → Swift display | skinTempCelsius stored value must not be mutated during display conversion |
| UserDefaults → View | @AppStorage reads unitSystem preference; malformed raw value falls back to .metric via ?? .metric |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03-01 | Tampering | skinTempCelsius stored value | mitigate | Conversion function takes celsius: Double? parameter — stored property is read-only; only the display string changes |
| T-74-03-02 | Information Disclosure | Unit preference default | accept | Default parameter is .metric (not .imperial) for all helpers — safe fallback if AppStorage key is absent |
| T-74-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. grep -n "unitSystemRaw" GooseSwift/FitnessFormatting.swift — 4+ hits across 4 helper functions
2. grep -rn "\.skinTempText" GooseSwift/ — 0 hits
3. grep -n "OnboardingStorage.unitSystem" GooseSwift/FitnessLiveWorkoutViews.swift GooseSwift/HomeTimelineViews.swift GooseSwift/HealthRecoveryWidgets.swift — at least 1 hit per file
4. xcodebuild build — BUILD SUCCEEDED
5. Manual spot-check (simulator): change More > Profile unit system to Imperial; open a live workout view; confirm distance shows MI and elevation shows FT
</verification>

<success_criteria>
- 4 helper functions in FitnessFormatting.swift are unit-aware with default parameters
- V24BiometricsResult.skinTempText computed var is removed; all references updated
- 3 view files have @AppStorage(OnboardingStorage.unitSystem) wired to all display helpers
- Build succeeds
</success_criteria>

<output>
Create `.planning/phases/74-fork-pr-integration-ux-i18n-auth/74-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| File | Change |
|------|--------|
| GooseSwift/FitnessFormatting.swift | fitnessDistanceParts + fitnessElevationParts + formatFitnessPace + skinTempText gain unitSystemRaw parameter |
| GooseSwift/HealthDataStore+V24Biometrics.swift | skinTempText computed var removed from V24BiometricsResult extension |
| GooseSwift/FitnessLiveWorkoutViews.swift | @AppStorage unitSystemRaw added; 6 call sites updated |
| GooseSwift/HomeTimelineViews.swift | @AppStorage unitSystemRaw added; formatDistance updated |
| GooseSwift/HealthRecoveryWidgets.swift | @AppStorage unitSystemRaw added; skinTempText call site updated |
